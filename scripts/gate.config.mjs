// 회귀 게이트 설정 — **수치는 전부 여기 한 곳에만 있다.**
//
// 근거 문서: docs/archive/design/20260727-improvement-roadmap.md §9.1(번들)·§9.6(수용 게이트)
//           docs/archive/design/20260727-execution-plan.md §2(08-02 기준선 / 08-04 통과)·§6(일일 체크포인트)
//           ※ 위 둘은 2026-08-25 archive/로 이관됐다. 현행 우선순위는 docs/design/20260825-roadmap-1y.md
//           docs/design/20260720-ai-tech-doc.md §3(환각 검증)
//
// ⚠ 이 파일은 **판정 기준**만 담는다. "지금 값이 얼마인가"(기준선)는
//    `scripts/gate.baseline.json`에 따로 기록한다 — 기준선은 코드 변경과 함께
//    **사람이 사유를 적고** 갱신하는 값이고, 기준은 로드맵이 바뀔 때만 움직이기 때문이다.

/** 게이트가 검사하는 항목. `quick`은 커밋 전 모드(`--quick`)에 포함되는가. */
export const ITEMS = {
  typecheck: {
    id: "typecheck",
    label: "타입 (pnpm -r typecheck)",
    quick: true,
    // 근거: 8~15초. 컴파일이 깨진 채 커밋되면 다른 작업자 전원이 즉시 막힌다.
    //       마감 4일 전 최다빈도 사고이므로 quick의 1순위.
    why: "가장 싸고(≈10s) 가장 자주 깨진다. 깨진 채 push하면 동시 작업자 전원이 멈춘다.",
  },
  build: {
    id: "build",
    label: "빌드 (client vite build)",
    quick: false,
    // 근거: 25~60초. typecheck가 잡지 못하는 것(자산 경로·순환 의존·번들러 해석)만
    //       추가로 잡는다. 커밋마다 물리기에는 비싸고, 번들 계측의 **선행 조건**이라
    //       전체 모드에서만 돈다.
    why: "느리다(≈30s+). typecheck 이후 남는 위험은 번들러 해석뿐 — 그건 push/촬영 전 전체 모드에서 잡는다. 번들 게이트의 선행 조건.",
  },
  bundle: {
    id: "bundle",
    label: "번들 예산 (roadmap §9.1)",
    quick: false,
    why: "빌드 산출물이 있어야 잰다. 빌드가 전체 모드 전용이므로 함께 전체 모드.",
  },
  sim: {
    id: "sim",
    label: "규칙 밸런스 회귀 (sim-balance)",
    quick: true,
    // 근거: 시드 고정 순수 시뮬. 1~2초. 규칙을 건드린 커밋이 밸런스를 뒤집었는지
    //       그 자리에서 알려준다 — 나중에 알면 되돌릴 시간이 없다.
    why: "시드 고정 순수 시뮬이라 ≈2초. 규칙 커밋이 밸런스를 뒤집은 것을 **그 커밋에서** 잡는 유일한 수단.",
  },
  narrator: {
    id: "narrator",
    label: "LLM 환각 검증 (eval-narrator · 오프라인)",
    quick: true,
    // quick에서는 `--quick`(36케이스), 전체에서는 전수 스윕(≈11만 문장).
    why: "quick은 36케이스(≈3s)로 규약 위반의 존재만 본다. 전수(≈11만 문장)는 전체 모드. **어느 모드에서도 Gemini 실호출 0.**",
  },
  gpu: {
    id: "gpu",
    label: "GPU 자원 정적 계측 (Phaser 텍스처·dispose)",
    quick: true,
    why: "정적 스캔이라 0.05초. 공짜인 검사를 빼면 quick의 신뢰도만 떨어진다.",
  },
  docs: {
    id: "docs",
    label: "문서·코드 상수 정합",
    quick: true,
    why: "정적 스캔이라 0.05초. 문서/코드 갈라짐이 반복 발생한 저장소라 상시 감시가 싸다.",
  },
  screen: {
    id: "screen",
    label: "화면 게이트 (겹침·스크롤·터치·글자크기·대비·목록배지·컬럼배분)",
    quick: false,
    // 근거(실측 ≈62s · `--full` ≈77s · S9 신설로 +3s): Colyseus 서버 + Vite + Chrome 기동은 한 번뿐이고
    //       (셋 다 끝까지 재사용한다) 나머지는 전부 화면 비용이다.
    //         · 정적 화면 1.6~1.9s × 12회
    //         · 결과 화면 6인 실판 세션 17s 1회 → 종료 오버레이 **4행 6장**
    //         · S7 공개방 목록 배지 +8s — 앞 두 시점(대기 중·정원 초과)은 위 세션에
    //           얹혀 돌아 비용이 0에 가깝고, «종료됨» 한 행만 3막(사람 5 + NPC 1)이
    //           필요하다. 6인 방은 좌석이 차는 순간 잠겨(`locked`) 목록에서 사라지고
    //           그 잠금이 120초 재접속 창 뒤에야 풀리기 때문이다(SCREEN.roomList 참고).
    //         · S8 판 중반 우측 컬럼 배분 +6s — **같은 6탭 세션**에 [다시 하기] 한 번을 더해
    //           3판을 열고, 소환 사슬로 제안 기록표를 6행까지 키운 시점에서 잰다.
    //           서버·클라·Chrome·탭을 새로 띄우지 않는다(계측 구간 실측 4.3~4.5s + 리매치).
    //       quick 전체가 ≈3초이므로 넣는 순간 커밋 게이트가 **16배 이상 무거워진다.**
    //       그보다 큰 이유는 **포트를 두 개 열고 Chrome을 띄운다**는 것이다 —
    //       커밋마다 물리면 동시에 dev를 켜 둔 작업자와 자원을 다툰다.
    //       이 게이트가 잡는 사고(가림·밀림·작은 버튼)는 커밋 단위가 아니라
    //       **화면 커밋이 쌓인 뒤 push·촬영 전에** 확인하면 충분하다.
    why: "브라우저·서버·클라를 실제로 띄운다(실측 ≈62s, `--full` ≈77s, 포트 2개 + Chrome). quick(≈3s)에 넣으면 16배 무거워지고 남의 dev와 자원을 다툰다 — push·촬영·제출 전 전체 모드 전용.",
  },
};

/**
 * 런타임 자원 게이트 — **이 러너로는 측정할 수 없다.**
 * 자원 카운터(±5)는 살아 있는 WebGL 컨텍스트가 필요하다.
 * 따라서 항상 `SKIP`으로 **명시 출력**한다(은폐된 미측정 금지).
 *
 * 📍 2026-08-25 — 원래 왕복 축이 «뷰1→2→3→4→1 10회 순회»였다. 뷰가 하나가 된 지금
 *    자원이 쌓일 수 있는 유일한 왕복은 **리매치**다(절차는 `--print-runtime-gate`).
 */
export const RUNTIME_GATE = {
  id: "runtime",
  label: "런타임 자원 수용 게이트 (리매치 10회 ±5)",
  reason:
    "브라우저 WebGL 컨텍스트가 필요해 CLI로 측정 불가. " +
    "절차: node scripts/gate-gpu-baseline.mjs --print-runtime-gate",
};

// ── 번들 예산 (roadmap §9.1) ────────────────────────────────────────
export const BUNDLE = {
  distDir: "apps/client/dist",
  /**
   * 로드맵 §9.1이 명시한 **목표**: 크리티컬 JS 2,130 → 약 120 kB.
   * 목표는 "달성하면 좋은 값"이라 **FAIL 기준으로 쓰지 않는다**(오늘 돌리면 전부 FAIL이라
   * 아무도 안 돌리게 된다). 대신 목표 대비 진척을 항상 인쇄한다.
   */
  targetInitialJsKb: 120,
  targetSource: "roadmap §9.1 «크리티컬 JS 2,130 → 약 120 kB (−94%)»",
  /**
   * 실제 FAIL 기준 = **래칫**. 기준선(gate.baseline.json)보다 이만큼 넘게 커지면 실패.
   * 3%를 고른 이유: 현 기준선 2,175 kB에서 +65 kB. 기능 추가 한 건이 우발적으로 삼키는
   * 크기(라이브러리 하나 = 수백 kB)보다는 작고, 코드 몇 줄 추가로는 절대 안 닿는 폭이다.
   * 스플리팅이 들어와 기준선이 100 kB대로 내려가면 3% = 3 kB로 자동으로 촘촘해진다.
   */
  ratchetSlackPct: 3,
  /** 기준선보다 이만큼(%) 이상 **줄었으면** 기준선을 조여 달라고 안내한다(FAIL 아님). */
  ratchetTightenPct: 10,
  /**
   * 단일 청크 상한 — 스플리팅의 성공 여부를 크기 하나로 재는 보조 지표.
   * 지금은 청크가 1개라 이 값이 초기 로드와 같다. 스플리팅 후에는
   * "가장 큰 지연 청크"(phaser 1,479 kB)가 여기 걸린다 → 경고만 한다.
   */
  warnChunkKb: 1600,
};

// ── 규칙 밸런스 회귀 (sim-balance) ──────────────────────────────────
export const SIM = {
  /**
   * 게이트가 재는 변형 = **현재 규칙**(⑥ revealed 분리 + 즉시고발 + 재진입 + 상한 75).
   * 나머지 변형은 "왜 지금 규칙인가"의 근거라 회귀 대상이 아니다.
   */
  variant: "current",
  seed: 20260728,
  /**
   * 판 수. 시드가 고정이라 **같은 코드면 결과가 비트 단위로 같다**(RNG는 mulberry32 단일 스트림).
   * 그런데 밸런스와 무관한 리팩터가 RNG 소비 순서만 바꿔도 표본이 통째로 새로 뽑힌다.
   * 그 경우의 표본오차를 무시할 수 있는 크기로 20,000판을 쓴다(≈2초).
   *   승률 p≈0.20에서 SE = √(p(1-p)/N) = 0.28 %p → 두 독립표본 차의 SE = 0.40 %p.
   *   따라서 아래 밴드(±2.0 %p)는 **표본잡음의 5σ**이고, 실질적으로 "설계상 의미 있는
   *   밸런스 변화"만 걸린다.
   */
  games: 20000,
  /**
   * 허용 밴드. 시드 고정이므로 이 폭은 **잡음 흡수용이 아니라 "이 정도 움직이면
   * 의도된 밸런스 변경이다"의 선**이다. 넘으면 FAIL → 사람이 기준선을 사유와 함께 갱신한다.
   */
  bands: {
    // 사람 승률: 공정 몫 16.7%. ±2.0 %p면 "16.7 근처"라는 설계 의도를 깨지 않는 폭이고,
    // 봇 확률적 오답 고발 같은 규칙 변경은 이보다 크게 움직인다(로드맵 실측 2.3%→22.3%).
    humanWinPct: { band: 2.0, unit: "%p" },
    // 판 길이: 7.3라운드. ±0.8은 "체감 페이싱이 달라졌다"의 하한(약 11%).
    avgRounds: { band: 0.8, unit: "라운드" },
    // 제안 p95: 67. ±8은 상한 75와의 여유(8)와 같다 — 이보다 움직이면 상한 근거가 무너진다.
    p95Suggestions: { band: 8, unit: "회" },
    // 보드가 장식인지 재는 지표(9개 방 중 몇 개를 밟는가). 5.1 → ±0.6.
    avgHumanRooms: { band: 0.6, unit: "개" },
  },
  /**
   * 밴드가 아니라 **절대 상한**으로 잠그는 값 — 설계 의도가 "0에 가깝다"인 지표.
   */
  ceilings: {
    // 상한은 밸런스 손잡이가 아니라 무한지연 백스톱(sim-balance 주석). 목표 도달률 ≤1%.
    // 2%를 넘으면 백스톱이 밸런스에 개입하기 시작한 것이다.
    capHitPct: { max: 2.0, unit: "%" },
    // 무승부는 공통단서 추가 공개로 수렴하므로 설계상 0%여야 한다.
    drawPct: { max: 0.5, unit: "%" },
  },
};

// ── 문서·코드 상수 정합 ────────────────────────────────────────────
// 원칙: **오탐이 없는 것만.** 앵커(문서 쪽 문구)를 못 찾으면 FAIL이 아니라 SKIP이다
//       (문서가 리라이트된 것과 값이 갈라진 것은 다른 사건이다).
export const DOC_CHECKS = [
  {
    id: "D1",
    label: "SUGGEST_CAP — 서버 규칙 ↔ 시뮬 미러",
    kind: "code-vs-code",
    why:
      "sim-balance.mjs는 clue-room.ts 규칙의 **이식본**이다. 한쪽만 바뀌면 " +
      "시뮬 기준선이 배포되는 규칙과 다른 것을 재게 된다(회귀 게이트가 조용히 거짓말을 한다).",
    a: { file: "apps/server/src/rooms/clue-room.ts", re: /^const SUGGEST_CAP = (\d+);/m },
    b: { file: "scripts/sim-balance.mjs", re: /^const SUGGEST_CAP = (\d+);/m },
  },
  {
    id: "D2",
    label: "MAX_PLAYERS — shared ↔ 시뮬 SEATS",
    kind: "code-vs-code",
    why: "좌석 수가 갈라지면 시뮬의 승률·공정몫(16.7%)이 전부 무의미해진다.",
    // shared가 engine/ ↔ content/clue/ 로 갈리면서 보드 상수가 배럴 루트에서 나왔다
    // (docs/design/20260729-mafia-content-design.md §1.2-③). 앵커는 **값이 사는 파일**을 따라간다 —
    // 경로를 안 고치면 이 검사가 FAIL이 아니라 조용한 SKIP으로 죽는다.
    a: { file: "packages/shared/src/content/clue/board.ts", re: /^export const MAX_PLAYERS = (\d+);/m },
    b: { file: "scripts/sim-balance.mjs", re: /^const SEATS = (\d+);/m },
  },
  {
    id: "D3",
    label: "LINE_BUDGET_SUGGEST / LINE_MAX — 코드 ↔ ai-tech-doc §3 C2",
    kind: "code-vs-doc",
    why:
      "④ 제출물이 «제안 25자 / 그 외 40자»를 **규약으로 명시**한다. " +
      "07-28에 한 번 갈라진 전력이 있다(문서 40자 vs 코드 80자).",
    code: [
      { name: "LINE_BUDGET_SUGGEST", file: "apps/server/src/ai/narrator.ts", re: /^export const LINE_BUDGET_SUGGEST = (\d+);/m },
      { name: "LINE_MAX", file: "apps/server/src/ai/narrator.ts", re: /^export const LINE_MAX = (\d+);/m },
    ],
    doc: {
      file: "docs/design/20260720-ai-tech-doc.md",
      // 앵커: "제안 **25자** / 그 외 **40자**" 형태(강조 기호 유무 무관).
      re: /제안\s*\*{0,2}(\d+)자\*{0,2}\s*\/\s*그\s*외\s*\*{0,2}(\d+)자/,
      names: ["LINE_BUDGET_SUGGEST", "LINE_MAX"],
    },
  },
  {
    id: "D4",
    label: "CELL_PX — 코드 ↔ (아카이브) view-contract-spec §1.1",
    kind: "code-vs-doc",
    why:
      "칸 크기의 유일한 상수. 문서가 «1칸 = CELL_PX 80»으로 값을 박아뒀다. " +
      "짝이던 PX_PER_UNIT(월드 유닛 환산)은 뷰2·3 제거와 함께 2026-08-25에 사라졌다.",
    // 좌표계 상수는 클루 보드에서 나온 값이라 `content/clue/view-board.ts`로 옮겨졌다(같은 문서 §1.2-⑤).
    code: [{ name: "CELL_PX", file: "packages/shared/src/content/clue/view-board.ts", re: /^export const CELL_PX = (\d+);/m }],
    doc: {
      file: "docs/archive/design/20260727-view-contract-spec.md",
      re: /1칸\s*=\s*`?CELL_PX`?\s*\*{0,2}(\d+)/,
      names: ["CELL_PX"],
    },
  },
];

// ── 화면 게이트 (gate-screen.mjs) ───────────────────────────────────
//
// 왜 여기 있는가: 07-28 실기 스크린샷이 잡은 세 가지를 **자동 검증이 전부 놓쳤다.**
//   ① 턴 배너가 상단 액션 바 6개 중 5개를 덮었다.
//      → 그때 게이트가 물은 것은 "버튼이 DOM에 있고 뷰포트 안인가"였고 답은 **참**이었다.
//        가려진 요소도 `getBoundingClientRect()`는 정상 크기를, `visibility`는 `visible`을
//        그대로 보고한다. **가림은 요소의 속성이 아니라 요소 쌍의 관계**다.
//   ② 문서가 세로로 스크롤돼 화면이 통째로 밀려 올라갔다.
//      → 그때 잰 것은 `scrollWidth vs innerWidth`(**가로**)뿐이었다. 세로는 잰 적이 없다.
//        게다가 전체 페이지 캡처는 밀린 화면을 **정상으로 렌더**한다(스크린샷도 못 잡는다).
//   ③ 터치 타깃이 전반적으로 44px 미달이었다.
//
// 기대값을 **화면별로** 둔다. 예: 세로 스크롤은 게임 화면에서 금지지만
// 랜딩·대기실에서는 정상이다(카드가 뷰포트보다 길 수 있다). 한 기준을 전 화면에
// 밀어붙이면 곧 아무도 못 믿는 게이트가 된다.

/**
 * «전면 모달 위를 바깥 컨트롤이 덮는가» — 배경음 컨트롤 전용 쌍. 배경은 플랜 39.
 *
 * 왜 `protect`로는 안 되는가: `protect`는 «모달 **안의** 요소가 가려졌는가»만 보고,
 * 그것도 요소마다 **중심 + 20% 인셋 5점**만 히트 테스트한다(`gate-screen.mjs` `sampleInsetPct`).
 * `#bgmCtrl`은 우상단이고 보호 대상은 카드 가운데라 데스크톱에선 아예 교차가 없고,
 * 폰에서 `.end-card`와 세로로 겹치더라도 **5점이 그 모서리를 안 찍는다** → 원리적으로 침묵.
 * (결과 화면 `pairs` 위 주석이 「오버레이 위를 아무도 덮지 않는다는 protect가 이미 한다」고
 *  적어 뒀는데 **거짓이다.** 겹치는 보호 대상이 있고 그 5점이 찍힐 때만 참이다.)
 *
 * 왜 이 쌍이 작동하는가: 모달은 `inset: 0`이라 교차 = 배경음 상자 전체이고, 그 중심을
 * 히트 테스트해 `#bgmCtrl`이 잡히면 `a-covers-b` → **FAIL**(음성 테스트로 확인).
 *
 * ⚠️ **끄는 구현과 이 검사는 한 벌이다.** 정상 상태에서는 컨트롤이 `display:none`이라
 * `shown` 필터에 걸러져 쌍이 조용히 넘어간다. **z-index를 낮추는 방식으로 고치면**
 * 모달이 컨트롤을 덮는 것이 `b-covers-a`로 잡혀 오탐이 된다 — 실제로 폰에서 그렇게 났다.
 * `visibility:hidden`·`opacity:0`도 `shown`이 걸러 통과는 하지만, `opacity:0`은
 * **히트 타깃이 남아** 「안 보이는데 눌린다」가 되므로 `display:none`이어야 한다.
 *
 * ⚠️ **이 쌍만으로는 반쪽이다** — «숨겨져 있으면 통과»이므로 `modal-on`이 고착돼
 * 컨트롤이 영영 사라져도 전부 PASS다. 반대쪽은 `GAME_PROTECT`의 `#bgmToggle`이 잰다.
 */
const BGM_OVER_MODAL = (modalSel) => [
  "#bgmCtrl",
  modalSel,
  "전면 모달 위에 배경음 컨트롤이 뜨는가 — 스크림이 안 먹은 밝은 버튼 + 눌리기까지 한다",
];

// ── 게임 화면(HUD) 기대값 ────────────────────────────────────────
//
// HUD는 캔버스 위에 얹힌 **DOM 한 벌**이다(index.html §③ — `.hud-*`는 `#gameScreen`
// 직속이고 Phaser 캔버스는 그 아래에 깔린다). 겹침·스크롤·터치 타깃은 전부 이 DOM 층의
// 성질이라 캔버스가 무엇을 그리든 기대값은 하나다.
const GAME_PROTECT = [
  {
    // 🔴 **«끄는 검사»만 있으면 한 벌이 아니다.** `BGM_OVER_MODAL`은 «숨겨져 있으면 통과»라,
    //    `body.modal-on`이 고착돼 배경음이 **영영 사라져도** 모든 쌍이 PASS다(검수 지적).
    //    이 항목이 그 반대쪽을 잰다 — 모달이 없는 화면에서는 **보여야 한다**.
    //    `protect`는 `visible < min`이면 FAIL이므로 고착이 곧바로 드러난다.
    sel: "#bgmToggle",
    why: "모달이 없는 화면에서 배경음 컨트롤이 보이는가 — `modal-on` 고착으로 영구히 사라지는 회귀를 잡는다",
  },
  {
    sel: ".hud-ctrl button",
    min: 6,
    why:
      "액션 바 6개. **07-28 실기에서 [제안] 말고 5개가 턴 배너에 가려졌다.** " +
      "존재·크기·visibility는 그때도 전부 정상이었다 — 여기서는 실제 히트 테스트로 본다",
  },
  { sel: "#turnInfo", why: "지금 누구 턴인가 — 가려지면 판 진행을 못 읽는다" },
  { sel: "#rpToggle", optional: true, why: "우측 패널 토글(폰에서만 뜬다)" },
  { sel: ".hud-dpad .dp", min: 4, optional: true, why: "터치 이동 패드(coarse에서만 렌더)" },
];
const GAME_PAIRS = [
  /* 🔴 **46회차 초안이 이 겹침을 «만들었고» 게이트는 못 봤다.** 하단 데크를 3칸 두 줄로
     눕히며 D-패드를 39px 올렸더니 `#rpToggle`(세로 중앙)과 **119px² 겹쳤다** —
     `z-index` 6 > 5라 «지금은» 오조작이 아니지만 여유가 0이었다(검수 실측).
     저장소는 이 사고를 이미 겪고 처방까지 적어 뒀는데(`index.html` — 「짧은 화면에서는
     세로 중앙이 D-패드의 자리다」) 그 처방이 `max-height: 500` 안에 있어 **폭 축으로
     패드를 올릴 때는 안 걸렸다.** 처방이 조건부면 계약으로 못 박아야 한다. */
  ["#rpToggle", "#dpad", "패널 토글과 D-패드가 겹치면 컬럼을 열다가 말이 움직인다"],
  /* 🔴 47회차가 배너를 한 줄 내리면서 **이 두 축을 새로 만들었는데 재는 검사가 없었다**(검수 지적).
     배너 바닥이 `.hud-turnface`·`#bgmCtrl`의 띠로 내려왔고, `.ti-sub`(즉시고발 카운트다운)가
     뜨면 배너가 한 줄 더 자란다 — 그때 겹치는지는 아무도 안 봤다.
     「폭 임계값을 안 만든 이유가 문안 의존이라서」인데 그 처방이 **다른 축에 같은 문안 의존을
     새로 만든** 셈이다. 만들었으면 재야 한다. */
  ["#turnInfo", ".hud-turnface", "턴 배너가 «지금 차례» 명패를 덮는가 — 47회차에 같은 띠로 내려왔다"],
  ["#turnInfo", "#bgmCtrl", "턴 배너가 배경음 컨트롤을 덮는가 — 배너가 자라면 닿는다"],
  [
    "#bgmCtrl",
    "#rightPanel",
    "배경음 컨트롤이 우측 컬럼을 덮는가 — z-index로는 못 피한다(`#gameScreen`이 스택 컨텍스트). " +
      "컬럼 폭은 사용자가 **220~680px**로 드래그한다. 왼쪽 배치가 «유일한» 답은 아니지만" +
      "(리사이저가 폭을 CSS 변수로 내보내는 안 · 컨트롤을 `#gameScreen` 안으로 옮기는 안) " +
      "23회차가 고른 답이고, 이 검사는 그 답이 되돌려지는 것을 막는다",
  ],
  // 23회차가 배경음 컨트롤을 **왼쪽 세로 줄**로 옮겼다(우측은 컬럼의 자리) →
  // 바로 위 이웃과의 관계를 검사로 못 박는다. 데스크톱 y120 · 폰 y150.
  // ⚠️ 「지금 차례」 높이는 **내용에 따라 안 변한다**(고정 44px 원이 지배 · 40자 이름으로
  //    실측 확인). 이 쌍이 실제로 잡을 조건은 `.tf-tx`가 **3줄**이 될 때다.
  ["#bgmCtrl", ".hud-turnface", "배경음 컨트롤이 「지금 차례」를 덮는가 — 같은 왼쪽 줄의 바로 위 이웃"],
  // ⚠️ ~~`["#bgmCtrl", ".hud-hand"]`~~ 는 **넣었다가 뺐다.** 어느 뷰포트에서도 발화할 수 없다 —
  //    폰은 `.hud-hand{display:none}`이라 `shown()`이 거르고, 데스크톱은 손패가 좌하단이라
  //    배경음(b160)과 **682px** 떨어져 있다. 발화 불가능한 검사는 **커버리지 착시**를 만든다.
  //    폰에서 재야 할 대상은 `.rp-hand`인데 그건 **컬럼을 열어야** 보이고, 게이트는 게임 화면에서
  //    컬럼을 한 번도 열지 않는다 → 지금은 잴 수단이 없다(플랜 43 큐).
  ["#turnInfo", ".hud-ctrl", "**07-28 사고 그 자체.** 턴 배너가 액션 바를 덮었다"],
  ["#turnInfo", ".hud-hand", "배너가 손패를 덮으면 «내 3장»을 못 본다"],
  [".hud-ctrl", ".hud-dpad", "액션 바와 D-패드가 겹치면 이동 중 오조작"],
  [".hud-hand", ".hud-dpad", "손패와 D-패드"],
  ["#rightPanel", ".hud-ctrl", "우측 컬럼이 액션 바를 덮으면 아무 행동도 못 한다"],
];

export const SCREEN = {
  /** 손가락 타깃 하한(px). 근거: Apple HIG 44pt · index.html §터치 T1이 이미 쓰는 값. */
  minTouchPx: 44,

  /**
   * 🔴 **마우스 타깃 하한(px).** WCAG 2.2 **2.5.8 Target Size (Minimum)**은 **AA**이고
   * **포인터 종류를 가리지 않는다** — 24×24다. 44(2.5.5 AAA)를 마우스에 요구하지 않는 것은
   * 옳지만, 그렇다고 **아무것도 안 재는 것**이 옳지는 않다.
   *
   * 27회차까지 S3는 `coarse` 전용이었고, 그래서 데스크톱 타깃은 **한 번도 계측된 적이 없다.**
   * 실측으로 드러난 것: `.evi-hit` **83×20** · `.evi-memo-btn` **11.5×12**(게이트 출력).
   * ⚠️ **간격 예외를 못 쓰는 이유를 정확히 적는다.** 규범문은 «원이 **다른 타깃**(또는 다른
   *    미달 타깃의 원)과 교차하지 않을 것»인데, 두 **중심**은 약 51px 떨어져 있어
   *    **원끼리는 안 겹친다.** 못 쓰는 진짜 이유는 메모 버튼의 24px 원이 `.evi-hit`의
   *    **바운딩 박스**를 약 2px 파고들기 때문이다. (초안이 «원이 겹친다»로 적었다 —
   *    그대로 두면 다음 사람이 원↔원 거리로 계산해 **틀린 면제를 발급한다**.)
   */
  minPointerPx: 24,
  /**
   * S10 — 추적 대상이 «보이는 영역» 중앙에서 벗어나도 되는 한계(화면 px).
   *
   * lerp가 매 프레임 수렴하는 값이라 정확히 0이 되지는 않는다 — 대상이 걷는 동안은
   * 항상 조금 뒤처진다(실측: 폰 **0.0px**, 데스크톱 **5.0px**).
   * 그래서 한계를 **24px**로 둔다. 근거는 양쪽이다:
   *   · 아래로는 실측 수렴 오차(5px)의 **4.8배** — 플레이크가 안 난다.
   *   · 위로는 이 검사가 반드시 잡아야 할 회귀가 폰에서 **253px**이었다(34회차) — 10배 여유.
   * 24px은 데스크톱 줌 0.662에서 **반 칸도 안 되는**(칸 53px) 거리라 «중앙이다»가 깨지지 않는다.
   */
  camCenterTolPx: 24,

  /**
   * S11 — 선택 컨트롤이 최장 옵션보다 좁아도 되는 한계(px).
   *
   * **1px이다.** 여유를 주는 축이 아니다 — 잘림은 정도 문제가 아니라 «읽히느냐»다.
   * 1px은 서브픽셀 몫(사본과 원본은 같은 스타일이라 이론상 0)과 렌더 반올림 몫이다.
   * 🔴 초안 문안은 「오직 서브픽셀 반올림 몫」이라 적었는데 **그건 ≤0.05px이라 tol 1과 어긋났다**
   *    (검수 지적). 비교는 이제 **원값**으로 하고 표시만 0.1px로 반올림한다 —
   *    그전에는 양쪽을 반올림한 뒤 비교해 근거와 코드가 따로 놀았다.
   *
   * ⚠️ 이 값을 올려서 FAIL을 없애지 마라 — 그건 잘린 화면을 초록으로 칠하는 것이다.
   *    폭을 넓히거나 문안을 줄이는 것이 고치는 방법이다.
   */
  clipTolPx: 1,

  /**
   * S11 — 폭 계약을 **이번 판이 아니라 최악의 판**에 대해 세운다.
   *
   * 🔴 초안은 «지금 화면에 있는 옵션»을 쟀는데 그것이 두 가지로 틀렸다(검수 지적, 코드로 확인):
   *   ① **딜마다 값이 달라진다.** `— 내 패 (정답 아님)`는 그 판 손패에만 붙어서
   *      같은 결함의 필요폭이 실행마다 **209~219px**로 흔들렸다 — 아슬아슬한 회귀가
   *      운 좋은 배분에서 통과한다.
   *   ② **신고 모달에서 그 옵션은 전부 `disabled`다.** `openPicker({lockKnown:true})`가
   *      `myCards`를 잠그는데 접미가 붙는 것이 정확히 그 `myCards`다(`selectFrom`).
   *      disabled는 선택될 수 없어 닫힌 컨트롤에 **표시되지 않는다** —
   *      즉 그 폭으로 낸 FAIL은 **사용자가 볼 수 없는 잘림**이었다.
   *
   * 그래서 옵션마다 **기본 라벨 + 이 접미**를 붙여서 잰다. 근거: 어떤 카드든 **어떤 판에서는**
   * 내 손에 들어오고, 제안 모달(`lockKnown` 없음)에서는 그 라벨이 **실제로 선택된다.**
   * `.modal select` CSS는 두 모달이 공유하므로 예산은 그 최악을 견뎌야 한다.
   * → 판정이 **결정론적**이 되고 시드 고정이 필요 없어진다.
   *
   * ⚠️ 이 문자열은 `main.ts` `selectFrom`의 접미와 **같아야 한다**(단일 출처는 ui-copy §1.5.1).
   *    갈라지면 게이트가 실제보다 좁은 예산을 재게 된다.
   */
  clipWorstSuffix: " — 내 패 (정답 아님)",

  /**
   * ── S12 조판 분기 규칙 `[2026-08-31 · 48회차]` ────────────────────────────────
   * 「이 뷰포트 조건에서 이 선택자의 이 속성은 이 값이어야 한다.」
   *
   * 🔴 **왜 필요한가.** S1~S11은 전부 「들어가는가 · 가려졌는가 · 읽히는가」를 잰다.
   *    **«의도한 조판이 섰는가»는 아무도 안 쟀다** — 47회차가 실증했다:
   *    `.card` 데스크톱 가드를 600 → 660으로 되돌려도 카드가 «더 작은» 폰 조판으로
   *    떨어질 뿐 **여전히 들어가서** 전건 PASS였다.
   *    그런데 39~47회차가 내내 다툰 것이 정확히 그 경계값들(가드·tier·폭 하한)이고,
   *    그 값들은 **회귀해도 화면이 «깨지지» 않는다 — 그냥 나빠진다.**
   *    이 검사가 그 축을 잡는다.
   *
   * 🔴 **다만 «경계값 자체»는 아직 못 잡는다**(48회차 검수 실측). 조건이 CSS 미디어 조건과
   *    같으므로 규칙은 «경계 안쪽의 값»만 지킨다 — 가드를 600 → 629, tier를 719 → 684로
   *    (47회차가 얻은 여유를 거의 다 먹는 크기) 동시에 옮겨도 **전건 PASS**였다.
   *    회귀가 **고정 뷰포트를 건너뛸 만큼 커야** 잡힌다.
   *    → 그래서 **경계 그 자체를 뷰포트로** 둔다(`cardGuard 1024×600` · `tierTop 1024×719`).
   *      43회차가 「계약이 가장 얇은 곳은 경계」라며 `edge`를 만든 것과 같은 논리다.
   *
   * ⚠️ **규칙은 «의도»를 적는 것이지 «현재 값»을 베끼는 것이 아니다.** 지금 코드가 통과하니까
   *    라고 값을 적으면 회귀 방지 장치가 못 된다(`minFontPx` 주석이 같은 말을 한다).
   *    각 규칙의 `why`에 **그 값이 어디서 나왔는지**를 적는다.
   */
  layoutRules: [
    {
      when: "(min-width: 761px) and (min-height: 600px)", screen: ["landing", "lobby"],
      sel: ".card", prop: "width", expect: "520px",
      why: "데스크톱 카드 조판(가드 600 — 47회차 실측 절벽 567 위). 되돌리면 380으로 떨어진다",
    },
    {
      when: "(max-height: 719px)", screen: ["landing", "lobby"],
      sel: ".card", prop: "paddingTop", expect: "6px",
      why:
        "세로가 모자란 화면의 조이기 tier. **경계 719**는 47회차 실측 683(조이기 없이 카드가 " +
        "서는 최소 높이) 위 여유이고, 값 **6**은 그 tier가 만들어 낸 여백이다",
    },
    {
      /* 🔴 **관계로 적는다.** 초안은 `top: 62px`이라는 결과값을 박았는데, 액션 바 높이가
         바뀌면 **규칙은 통과하면서 겹침이 자란다**(검수 지적). 62는 `6 + 44 + 12`의 결과다. */
      when: "(min-width: 761px) and (min-height: 501px)", screen: ["game", "game-column"],
      sel: ".hud-turn", expect: "below", after: ".hud-ctrl", gap: 12,
      why:
        "배너는 액션 바 **아래 줄**이어야 한다 — 같은 줄에 서면 폭 761~1286에서 덮는다(47회차 실측). " +
        "S1 쌍은 «이미 겹쳤는가»를 재고 이 규칙은 «겹치기 전 여유»를 잰다",
    },
    {
      when: "(max-width: 380px)", screen: ["game", "game-column", "goal-modal", "accuse-modal"],
      sel: ".hud-ctrl", prop: "gridTemplateColumns",
      expect: "equal-tracks", tracks: 6,
      why:
        "좁은 폭에서는 **균등분할**이어야 한다 — `1fr`(= `minmax(auto, 1fr)`)이면 " +
        "긴 라벨이 짧은 칸의 폭을 깎아 `[?]`가 13.4px이 된다(46회차 실측). " +
        "⚠️ 지금 균등함은 **두 장치가 함께** 만든다(`minmax(0,…)` + `white-space: normal`) — " +
        "하나만 되돌리면 다른 하나가 메워서 이 규칙도 S3도 안 운다(48회차 되주입 확인). " +
        "둘 다 되돌리면 **S12가 S3보다 먼저 운다**(트랙 차 10.9px) — 증상이 아니라 **원인**을 재기 때문이다",
    },
  ],

  /**
   * ── S5 글자 크기 하한(px) ────────────────────────────────────────
   * §사람 확인 «읽히는가»에서 **기계가 잴 수 있는 조각**만 떼어낸 것이다.
   * 「읽힌다」 전부를 재는 것이 아니다 — 줄바꿈·서체·행간은 여전히 사람 몫이고,
   * 이 검사가 말하는 것은 딱 하나: **하한보다 작은 글자는 폰에서 읽히지 않는다.**
   *
   * 10px = 저장소가 실제로 쓰는 최소값(`.sg-n` 턴 순서 스트립의 순번 숫자).
   * 이 값을 «지금 코드가 통과하니까»로 정하면 회귀 방지 장치가 못 된다 —
   * 근거는 **더 작아지면 안 된다**는 상한선 고정이다. 지금보다 작은 글자가
   * 새로 들어오면 잡히고, 10px짜리를 더 줄이려 해도 잡힌다.
   * (11px로 올리려면 `.sg-n` 10px을 먼저 고쳐야 한다 — 그건 화면 변경이라 별건이다.)
   *
   * ⚠️ 손가락 뷰포트에서만 잰다(S3와 같은 규약). 데스크톱은 사용자가 확대할 수 있고
   *    시거리도 다르다 — 같은 하한을 들이대면 근거 없는 실패가 된다.
   */
  minFontPx: 10,

  /**
   * ── S6 명암비 하한(WCAG 2.1 AA) ─────────────────────────────────
   * roadmap §7.13이 `#6b6355`(2.9:1)를 `#8f8574`(4.6:1)로 올린 근거가 이 기준이다.
   * 그 수정은 07-28에 코드에 들어갔지만 **다시 낮추는 것을 막는 장치는 없었다.**
   * 지금 이 검사가 그 장치다.
   *
   * 큰 글자 예외는 WCAG 원문 그대로: 24px 이상, 또는 굵은(700+) 18.66px 이상.
   */
  minContrast: 4.5,
  minContrastLarge: 3.0,
  largeFontPx: 24,
  largeBoldFontPx: 18.66,
  /**
   * S5·S6에서 제외할 선택자(전 화면 공통). **사유 없이는 추가하지 마라.**
   * 화면별 예외는 각 화면 정의의 `textExempt`에 적는다.
   *
   * `[aria-disabled="true"]` · `:disabled` — WCAG 2.1 §1.4.3의 명시적 예외
   * («inactive user interface component … have no contrast requirement»).
   * 이 저장소의 `.is-off`(제안·통로·계략)와 «만석» 버튼이 정확히 그것이다:
   * 지금 누를 수 없다는 것을 **색이 빠진 것으로** 알리는 게 그 색의 일이라,
   * 대비를 올리면 «눌러도 된다»로 읽힌다 — 고치는 쪽이 오히려 결함이다.
   * ⚠️ S3(터치 44px)는 이 예외를 쓰지 않는다. `.is-off`는 클릭이 살아 있어
   *    (누르면 사유를 안내한다) **손가락이 닿아야 하는 것**은 맞기 때문이다.
   *    두 검사의 대상이 다른 것은 의도다.
   */
  textExempt: ['[aria-disabled="true"]', ":disabled"],
  /**
   * 대비를 **잴 수 없는** 경우 — 조용히 통과시키지 않고 «판정 불가»로 세어 인쇄한다.
   *   bgImage : 조상 어딘가에 배경 이미지/그라디언트가 있어 뒤 색이 한 값이 아니다
   *   bgAlpha : 뿌리까지 올라가도 불투명 배경을 못 찾았다(캔버스 위 글자 등)
   * 이 둘은 사람 몫으로 남는다 — §사람 확인 «읽히는가»가 지워지지 않는 이유다.
   */

  /**
   * 겹침 표본점. 가시 사각형의 중심 + 네 모서리(20%/80% 안쪽).
   * FAIL 조건 = **중심이 막혔거나** 5점 중 3점 이상이 막혔을 때.
   * 한 점만 막힌 것으로 실패시키면 이웃 요소의 그림자·둥근 모서리가 오탐을 만든다.
   */
  sampleInsetPct: 0.2,
  blockedFailCount: 3,
  /**
   * 쌍 겹침을 "겹쳤다"고 볼 최소 교차 면적(px²)과 **최소 교차 변**(px).
   *
   * 면적만 보면 오탐이 난다 — 실측: 턴 배너 하단(y=72.6)과 손패 상단(y=72.0)이
   * **0.6px 접선**으로 만나 폭 163px과 곱해져 98px²가 나왔다. 이것은 «덮음»이 아니라
   * 서브픽셀 반올림이다. 가로·세로 **양쪽 모두** 이 변을 넘어야 겹침으로 센다.
   * 4px = 글자 한 획도 가리지 못하는 폭이라, 진짜 가림(수십 px)은 그대로 걸린다.
   */
  minOverlapPx2: 16,
  minOverlapEdgePx: 4,
  /** 세로 스크롤 허용 오차(px). 서브픽셀 반올림. */
  scrollTolPx: 1,
  /** 화면 하나가 준비 상태에 도달할 때까지 기다리는 상한(ms). 넘으면 SKIP(판정 불가). */
  readyTimeoutMs: 25_000,
  /** 준비 후 레이아웃이 멎기를 기다리는 시간(ms). */
  settleMs: 700,

  /**
   * 뷰포트. 폰에서는 **터치 에뮬레이션 + `pointer: coarse`** 를 켠다 —
   * 이 저장소의 D-패드·44px 하한은 전부 `@media (pointer: coarse)` 안에 있어서
   * 켜지 않으면 **실제로 심사자가 보는 화면과 다른 화면을 재게 된다.**
   */
  viewports: [
    { id: "phone", label: "폰 390×844", width: 390, height: 844, dsf: 3, mobile: true, coarse: true },
    { id: "desktop", label: "데스크톱 1440×900", width: 1440, height: 900, dsf: 1, mobile: false, coarse: false },
    /**
     * 🔴 **뷰포트가 둘뿐인 것이 35~39회차의 근본 원인이었다.**
     * 390과 1440 사이에는 «어느 미디어 분기도 안 걸리는 구간»이 있는데
     * 그 구간을 **아무도 열어 보지 않았다.** 39회차 결함(모달 기본 폭이
     * 좁아 최장 옵션이 잘림)이 세 회차를 살아남은 자리가 정확히 거기다.
     *
     * **왜 720×620인가** — `.modal`의 두 분기 사이는 이렇게 정의된다:
     *   · 데스크톱 분기 `(min-width:761) and (min-height:660)` — 높이 620이라 **안 걸린다**
     *   · 폰/가로 분기 `(max-width:680), (max-height:500)` — 둘 다 **안 걸린다**
     * 그 구간은 **폭 681~760 전 높이** ∪ **폭 ≥761 · 높이 501~659** 두 반쪽인데,
     * **720×620은 둘 다에 들어간다**(681≤720≤760 **그리고** 501≤620≤659).
     *
     * 🔴 초안은 `1280×620`이었고 「두 반쪽은 같은 한 상태라 대표 하나면 덮인다」고 적었다 —
     *    **`.modal` 규칙에 한해서만 참이고 스타일시트 전체로는 거짓이다**(검수 지적).
     *    `index.html`에는 **폭 760/761 경계**가 따로 있다 — `.hud-turn`(:740),
     *    `#lobby-char`(:185), `.cx-stage-info`(:276). 1280은 그 경계에서 기존 1440과
     *    **같은 쪽**이라, 새 뷰포트가 더하는 것이 **높이 축뿐**이었다.
     *    720은 같은 비용으로 **폭 축까지** 연다 — 한 번도 계측된 적 없는 `.hud-turn`
     *    비-데스크톱 조판이 여기서 처음 열린다.
     *
     * ⚠️ **뷰포트를 늘리는 것만으로는 결함이 안 잡힌다** — 40회차가 결함을 되주입해
     *    확인했다(이 뷰포트를 넣고도 S1~S10 전건 PASS). 재는 검사(S11)가 있어야 한다.
     *    **상태를 만드는 일과 재는 일은 다르다.**
     */
    { id: "gap", label: "빈 구간 720×620", width: 720, height: 620, dsf: 1, mobile: false, coarse: false },
    { id: "short", label: "넓고 짧은 1280×450", width: 1280, height: 450, dsf: 1, mobile: false, coarse: false },
    { id: "land", label: "폰 가로 844×390", width: 844, height: 390, dsf: 3, mobile: true, coarse: true },
    /* **2단 조판의 하한 그 자체**를 잰다. 계약이 가장 얇은 곳은 경계인데, 43회차 초안은
       경계(600)에서 67px 떨어진 667만 재고 있었다 — 그래서 「600은 절벽 가장자리」를 못 봤다
       (검수 지적: 「상태를 만드는 일과 재는 일은 다르다」의 재발).
       여기서 재는 것은 **손가락 하한**이다: 폭이 더 줄면 `[참가]`가 44px 아래로 내려간다
       (실측 585에서 42.5px). 하한이 다시 내려가거나 열 폭이 깎이면 여기서 곧바로 FAIL이 난다.
       ✅ **높이도 하한으로 내렸다** `[2026-08-31 · 44회차]`. 375·390은 **기기 포인트 크기**이지
          브라우저가 주는 높이가 아니다 — 실기는 툴바만큼 덜 준다. 그래서 375만 재는 동안
          `667×320`에서 랜딩 **26px** · 대기실 **15.5px**이 넘치고 있었다.
          🔴 **초안이 적은 「320 FAIL(24.5px) · 340 PASS」는 «고치기 전» 형상의 값이었다**
             (검수 실측 — 고친 뒤 620×320은 **통과**한다). 게다가 26/15.5px은 폭 **667**에서
             잰 값을 폭 620 블록에 옮겨 적은 것이다. **한 형상의 값을 다른 형상에 쓴 것**으로
             43회차가 지적받은 실수와 같은 종류다.
          하한 340의 근거는 **여유**다 — 실측 절벽은 랜딩 **322** · 대기실 **302**이고
          340은 그 위 18/38px이다. 그리고 `#hostHint` 문안을 2행 늘려도 견딘다(검수 실측).
       **이 뷰포트는 «가로 조판»의 모서리다** — 폭 하한 620 × 높이 하한 340.
          ⚠️ 「어느 쪽이 밀려도 여기서 먼저 FAIL이 난다」고 적었던 것은 **거짓이었다** —
             세로 조판은 다른 함수라, `edge`가 초록인 채로 `375×553`이 깨져 있었다.
             그래서 세로 뷰포트를 따로 둔다(아래). */
    { id: "edge", label: "지원 하한 620×340", width: 620, height: 340, dsf: 2, mobile: true, coarse: true },
    /* **가장 좁은 폰**(iPhone SE 1세대 세로 · 고전적 최소 폭 320).
       S11(선택 컨트롤 잘림)은 40회차부터 있었지만 **이 폭의 뷰포트가 없어 잠들어 있었다** —
       넣자마자 select 셋이 19~30px 잘린 것이 나왔다(46회차).
       그리고 6칸 하단 데크가 `[?]`를 **13.4px**으로 짜부라뜨리고 있었고,
       대기실 카드는 22px 넘쳐 `[잔치 시작]`이 잘리고 안내가 화면 밖이었다.
       ⚠️ 이 폭은 «조판이 바뀌는» 구간이다(모달 라벨 스택 · 데크 균등분할) — 경계는 **380** 하나다.
       🔴 **높이 568은 «기기 포인트 크기»다** — 44회차가 「무효」라 선언한 바로 그것이다.
          `p553`과 같은 뺄셈(−114)을 적용하면 실기는 **320×454**이고, 그 높이에서는
          `[잔치 시작]`이 93px · `#hostHint`가 111px 밖이다(검수 실측).
          **여백으로 될 크기가 아니다** — 12칸 격자를 가진 카드가 454px에 안 들어간다.
          조판을 다시 짜야 하는 일이라 **큐로 넘긴다.** 지금 이 뷰포트는 «폭»을 재는 것이지
          «SE1 실기»를 재는 것이 아니다. */
    { id: "n320", label: "좁은 폰 320×568", width: 320, height: 568, dsf: 2, mobile: true, coarse: true },
    /* 🔴 **`main`에 있던 선재 결함 띠**(47회차가 찾아 고쳤다). 카드 데스크톱 가드가 660인데
       조이기 tier가 659에서 끝나서, **높이 660~682**에서 데스크톱 카드가 조이기 없이 서다가
       `#hostHint`가 화면 밖으로 나갔다(실측 660: 6px · 680: 3px).
       44회차가 `#hostHint`를 계약에 넣고도 **이 띠의 뷰포트가 없어** 한 번도 안 울렸다.
       이제 가드 600 · 조이기 719라 그 띠는 「데스크톱 조판 + 조인 여백」으로 선다.
       🔴 **초안은 「둘 중 하나라도 되돌아가면 여기서 FAIL」이라 적었는데 절반이 거짓이다**
          (검수 실측): 670 ≥ 660이라 **가드를 660으로 되돌려도 계산된 스타일이 완전히 같다.**
          이 뷰포트가 지키는 것은 **조이기 tier 하나**다 — 가드는 아래 `guard`가 지킨다. */
    { id: "band", label: "조이기 띠 1024×670", width: 1024, height: 670, dsf: 1, mobile: false, coarse: false },
    /* 가드 600이 여는 구간(600~659)에 뷰포트가 없었다(검수 지적) — 그 구간의 **들어감**을 잰다.
       🔴 **다만 가드 «값» 자체는 이 게이트가 래칫하지 못한다**(실측 확인).
          660으로 되돌리면 카드가 **더 작은** 폰 조판으로 떨어질 뿐이라 여전히 들어간다 —
          이 게이트는 «들어가는가»를 재지 «어느 조판으로 섰는가»를 재지 않는다.
          초안이 「되돌리면 곧바로 FAIL」이라 적었는데 **거짓이었다**(`band`도 같은 이유로 절반 거짓).
          그 축을 재려면 «조판이 어느 분기로 섰는가»를 보는 검사가 따로 있어야 한다 → 큐. */
    { id: "guard", label: "가드 구간 1024×630", width: 1024, height: 630, dsf: 1, mobile: false, coarse: false },
    /* 🔴 **경계 그 자체.** S12의 규칙은 CSS 미디어 조건을 그대로 쓰므로 «경계 안쪽»만 지킨다 —
       경계가 옮겨 가면 규칙째로 따라가서 침묵한다(48회차 검수 실측: 가드 600→629 · tier 719→684를
       동시에 넣어도 전건 PASS). 경계에 뷰포트를 두면 **한 칸만 밀려도** 규칙이 안 걸려 FAIL이 난다. */
    { id: "cardGuard", label: "카드 가드 경계 1024×600", width: 1024, height: 600, dsf: 1, mobile: false, coarse: false },
    { id: "tierTop", label: "조이기 tier 상한 1024×719", width: 1024, height: 719, dsf: 1, mobile: false, coarse: false },
    /* 🔴 **세로 축에는 계측 뷰포트가 하나도 없었다**(검수 지적). `phone 390×844`는 이 회차가
       「무효」라고 선언한 바로 그 **기기 포인트 크기**다 — 실기 브라우저는 툴바만큼 덜 준다.
       375×553은 iPhone SE2/SE3/8 세로(375×667pt)에서 크롬을 뺀 값이고, **여기서 대기실
       `[잔치 시작]`이 26px 잘려 있었다**(선재 결함 · 44회차가 고쳤다).
       가로만 재는 동안 «가장 흔한 실기 세로»가 무계측이었다. */
    { id: "p553", label: "SE2 세로 375×553", width: 375, height: 553, dsf: 2, mobile: true, coarse: true },
  ],

  /**
   * 화면별 기대값.
   *   url/steps/ready — 어떻게 그 화면에 도달하는가(파라미터 없는 진입을 기본으로 둔다).
   *   vscroll        — "locked": 문서가 세로로 스크롤되면 FAIL / "allow": 스크롤이 정상
   *   protect        — **무엇이 가려도 안 되는가.** 심사자가 눌러야 하는 것들.
   *                    ⚠️ 2026-08-31(45회차)부터 **«그리고 조상 스크롤 상자의 접힘 «위»에
   *                    있어야 한다»**도 계약한다 — 모달·우측 컬럼처럼 `overflow: auto`인 상자
   *                    «안»에서 접힘 아래로 내려간 것은 뷰포트로는 멀쩡해 보인다.
   *                    ⚠️ 2026-08-30(41회차)부터 **«그리고 뷰포트 안에 통째로 들어와야 한다»**
   *                    도 함께 계약한다(S1 「뷰포트에 잘림」). `vscroll: "allow"` 화면에서도
   *                    그렇다 — 스크롤하면 닿는다는 것과 «첫 화면에 있다»는 다른 말이고,
   *                    1차 행동이 접힘 아래면 «있는데 못 찾는» 것과 같다.
   *   pairs          — 서로 가리면 안 되는 **요소 쌍**(관계로만 드러나는 사고).
   *   touchExempt    — 타깃 하한에서 제외할 선택자. **사유 없이는 추가하지 마라.**
   *                    ⚠️ 하한이 **둘**이다(손가락 44 · 마우스 24) — 폰 사유로 넣은 면제가
   *                    데스크톱 24까지 **조용히** 면제한다.
   */
  screens: [
    {
      id: "landing",
      label: "랜딩(파라미터 없는 진입)",
      url: "/",
      steps: [],
      ready: "!document.getElementById('landing').classList.contains('hidden') && !!document.getElementById('createBtn')",
      // 랜딩은 카드가 길어질 수 있다. 세로 스크롤은 **정상**이다.
      vscroll: "allow",
      protect: [
        { sel: "#createBtn", why: "심사자의 첫 행동. 이게 가려지면 45초가 통째로 죽는다" },
        { sel: "#joinBtn", why: "초대 코드 참가 경로" },
        { sel: "#codeInput", why: "코드 입력칸" },
        { sel: "#refreshRooms", why: "공개방 새로고침" },
        { sel: "#visSeg .seg-btn", min: 2, why: "공개/비공개 선택" },
        // 📍 2026-08-25 제거 — `.ai-links`(랜딩 → 과제 제출물 문서 링크)는 **DOM에 이미 없다.**
        //    과제 종료로 «심사자 동선»이라는 근거도 사라졌다. 대상 없는 보호 항목은
        //    매 실행 FAIL을 찍어 게이트 전체의 신뢰도를 갉아먹는다.
      ],
      pairs: [
        /* 🔴 **42회차가 이 겹침을 캡처로 발견하고서야 여기 넣었다.**
           `.game-meta`는 `margin-top: -14px`으로 **부제 밑에 붙는 줄**이라, 41회차가
           짧은 화면에서 부제를 감추자 **제목을 파고들었다** — 글자끼리 겹쳐 그려졌는데
           S1~S11 어느 것도 안 잡았다(그때 게이트는 «글자 겹침»을 재는 축이 없었다).
           이 파일이 스스로 적어 둔 규약이 「상태를 만드는 일과 재는 일은 다르다」인데
           42회차가 상태만 고치고 재는 일을 안 할 뻔했다(검수 지적). */
        ["#gameMeta", "#landing h1", "소개 줄이 제목을 파고드는가 — 음수 마진의 전제가 사라지면 겹친다"],
        /* 🔴 **42회차 쌍은 «제목»만 봤다.** `.game-meta`의 `margin-top: -14px`이 실제로 붙는 것은
           **부제**이고, 44회차가 부제 마진을 8px으로 내리자 상쇄로 **−6px**이 되어 소개 줄이
           부제를 파고들었다(실측 4뷰포트 교차 235~379×6) — 제목 쌍은 그때 침묵했다.
           불변량: `.subtitle`의 `margin-bottom` ≥ **14**(음수 마진의 절댓값). */
        ["#gameMeta", "#landing .subtitle", "소개 줄이 부제를 파고드는가 — margin-bottom < 14면 상쇄로 겹친다"],
        ["#createBtn", ".join-row", "«방 만들기»와 «초대 코드» 줄이 겹치면 둘 다 오조작"],
        ["#landingMsg", "#roomList", "상태 문구가 공개방 목록을 덮으면 참여 경로가 사라진다"],
        /* 🔴 **초안은 `.join-row`를 걸었는데 그건 원리적으로 못 잡는다** — `#bgmCtrl` y 12..62,
           `.join-row` y 63..107로 **1px 차이로 안 겹친다**(검수 실측). 주석에는 「오른쪽 열의
           머리가 `.divider`」라고 **정답을 적어 놓고 다른 선택자를 코딩했다.**
           열 전체를 걸어야 머리가 무엇으로 바뀌든 잡힌다 — 거터를 지우면 31×31로 FAIL 난다. */
        ["#bgmCtrl", "#landing .card-col + .card-col", "배경음 버튼이 오른쪽 열을 덮는가 — §1.7이 세 번 겪은 충돌"],
      ],
      touchExempt: [],
    },
    {
      id: "lobby",
      label: "대기실(방 만들기 직후)",
      url: "/",
      steps: [{ click: "#createBtn" }],
      ready: "!document.getElementById('lobby').classList.contains('hidden')",
      vscroll: "allow",
      protect: [
        { sel: "#startBtn", why: "방장이 잔치를 시작하는 단 하나의 버튼" },
        { sel: "#copyBtn", why: "초대 링크 복사" },
        { sel: "#inviteLink", why: "초대 링크(선택·복사 대상)" },
        { sel: "#lobbyChars .char", min: 12, why: "캐릭터 12칸 — 하나라도 가려지면 못 고른다" },
        /* 🔴 **41회차가 「비방장이 이유를 아는 유일한 자리」라고 못 박고도 계약에 안 넣었다.**
           `#startBtn`은 비방장에게 `disabled`이고, 왜 못 누르는지 말하는 것은 이 줄뿐이다.
           계약 밖이면 잘려도 게이트가 초록을 준다 — 44회차 실측 `620×320`이 정확히 그 상태였다. */
        { sel: "#hostHint", why: "비방장이 «왜 못 누르는지»를 아는 유일한 자리" },
      ],
      pairs: [
        /* 🔴 **§1.7이 「두 번 났다」고 기록한 충돌의 세 번째를 43회차가 여기서 잡았다.**
           `#bgmCtrl`은 이 분기에서 **`right: 8` · 폭 60**이다(초안이 «12 · 44»라 적었는데
           12는 **기본 규칙**이라 이 뷰포트엔 안 걸리고 44는 **`#bgmToggle`**의 폭이다 — 쌍이
           재는 것은 `#bgmCtrl`이다 · 검수 지적). 짧은 화면에서 카드가 사실상 전폭이라
           **버튼이 카드 안으로 들어온다** — 실측 대기실 격자 **31×11**, 랜딩 오른쪽 열 **31×16**.
           42회차가 그대로 내보냈고 **재는 쌍이 없어 아무도 못 봤다.**
           §1.7의 처방은 「내용을 줄여 피하지 마라 · 배치로 막아라」이고, 그 배치가
           지켜지는지는 **이 쌍만 말할 수 있다.** */
        ["#bgmCtrl", "#lobbyChars", "배경음 버튼이 캐릭터 격자를 덮는가 — §1.7이 세 번 겪은 충돌"],
        ["#lobbyPersona", "#startBtn", "직업 설명이 [잔치 시작]을 덮으면 시작을 못 한다"],
        ["#lobbyChars", "#startBtn", "캐릭터 격자와 시작 버튼"],
      ],
      touchExempt: [],
    },
    {
      id: "game",
      label: "게임 뷰1(솔로 즉시 진입 · 안내 카드 없음)",
      /**
       * S10(카메라 프레이밍)을 여기서만 판정한다 — **«평상 판»의 계약**이기 때문이다.
       * 모달이 열렸거나 판이 끝난 화면, 턴 전환 `pan` 도중은 대상이 중앙에 있을 이유가 없다.
       * 뺀 화면들은 SKIP + 사유 + 참고 실측이 매 실행 인쇄된다(조용히 빼지 않는다).
       */
      camFrame: true,
      // ?solo=1 = 원클릭 솔로(create→start). demo=1 = 60초 안내 카드 생략.
      url: "/?solo=1&demo=1",
      // 🔴 **주사위 오버레이가 사라진 뒤에 잰다.** 바로 아래 `game-column`이 같은 사유를
      //    적어 뒀는데(「타이머로 사라져 … 기동 속도에 따라 달라진다 — 결정론이 아니면
      //    회귀를 판정할 수 없다」) **정작 가장 많이 재는 이 화면에는 안 걸려 있었다.**
      //    실제로 33회차에 캡처를 세 번 찍는 동안 세 번 다 주사위 카드가 보드를 덮어
      //    «눈으로 본다»가 불가능했다.
      steps: [
        // ⚠️ **순서가 보증이다.** `#diceOverlay`는 `class="dice-overlay hidden"`으로 **시작**하므로
        //    「숨겨져 있다」만 보면 **판이 시작되기도 전에 통과**한다 — 지금 통과하는 유일한
        //    이유가 «에셋 로딩이 판 시작보다 느려서»라면 캐시가 데워지는 순간 깨진다(검수 지적).
        //    `game-column`이 이 함정을 피한 방법도 순서였다. 먼저 판이 섰는지부터 본다.
        { waitFor: "!document.getElementById('turnInfo').classList.contains('hidden')" },
        { waitFor: "document.getElementById('diceOverlay').classList.contains('hidden')" },
      ],
      ready:
        "!document.getElementById('gameScreen').classList.contains('hidden')" +
        " && !document.getElementById('turnInfo').classList.contains('hidden')",
      // 게임 화면은 **문서 스크롤 자체가 없어야 한다**(index.html `body.no-scroll`).
      // 밀려 올라가면 보드가 위로 사라지고 아래에 검은 띠가 남는다 — 실기에서 실제로 났다.
      vscroll: "locked",
      protect: GAME_PROTECT,
      pairs: GAME_PAIRS,
      touchExempt: [],
    },
    // 📍 2026-08-25 — 여기 있던 뷰2·3·4 HUD 화면 3종(`viewScreen(…)`)이 사라졌다.
    //    렌더러가 하나면 «뷰를 바꿔도 HUD가 그대로인가»라는 질문 자체가 성립하지 않는다.
    {
      id: "goal-modal",
      label: "게임 + 60초 안내 카드(모달)",
      url: "/?solo=1",
      steps: [],
      ready:
        "!document.getElementById('gameScreen').classList.contains('hidden')" +
        " && !document.getElementById('goalCard').classList.contains('hidden')",
      vscroll: "locked",
      // 모달이 뜬 동안 **뒤가 가려지는 것은 정상**이다. 그래서 보호 대상은 모달 자신뿐이다.
      protect: [
        { sel: "#goalOk", why: "안내를 닫는 유일한 버튼. 가려지면 게임에 못 들어간다" },
        { sel: "#goalCard .goal-lines div", min: 5, why: "규칙 5줄 — 온보딩 본문. min이라 4로 두면 ⑤(탈락 대가)가 가려져도 통과한다" },
      ],
      pairs: [
        ["#goalOk", ".goal-lines", "확인 버튼이 본문을 덮으면 규칙을 못 읽는다"],
        BGM_OVER_MODAL("#goalCard"),
      ],
      touchExempt: [],
    },
    {
      // 🔴 **폰의 «우측 컬럼 펼침»은 23회차까지 통째로 무계측이었다.**
      // 그 상태에서만 존재하는 것이 셋이다 — ① `.rp-hand`(폰에서 「내 손패」를 보여 주는
      // **유일한** 줄 · 플로팅 `.hud-hand`는 폰에서 `display:none`) ② 컬럼을 닫는 유일한 길
      // `#rpToggle.rp-on` ③ 컬럼이 화면의 77%를 덮는 배치. 23회차가 «배경음이 그 줄을 88px
      // 덮는다»를 검수 실측으로 알아냈는데, **게이트로는 재현할 수단이 없었다.**
      id: "game-column",
      label: "게임 + 우측 컬럼 펼침(폰 전용)",
      url: "/?solo=1&demo=1",
      /* 🔴 **하한(`edge`)을 넣고서야 컬럼이 가장 빡빡한 지점이 계측됐다**(45회차 검수 지적).
         새 «접힘 아래» 검사가 가장 값진 자리가 바로 `.rp-col { overflow-y: auto }`인데
         그 뷰포트가 빠져 있었다 — 넣자마자 `#handPanel`이 2px 접혀 있는 것이 나왔다. */
      onlyViewports: ["phone", "land", "edge"],
      onlyViewportsWhy:
        "데스크톱은 컬럼이 늘 펼쳐져 있어 `game` 화면이 곧 그 상태다 — 토글 버튼(`#rpToggle`) 자체가 `display:none`이라 도달할 수 없다",
      steps: [
        { waitFor: "!document.getElementById('turnInfo').classList.contains('hidden')" },
        // ⚠️ **«닫힘에서 시작해 열었다»를 못 박는다.** `ready`는 «열려 있다»만 보므로,
        //    기본값이 열림으로 바뀌면 클릭이 오히려 **닫고** 도달 실패가 되고, 반대로
        //    클릭이 no-op가 돼도 ready가 즉시 참이라 **`game`과 같은 것을 두 번 재고 통과**한다.
        { waitFor: "document.getElementById('rightPanel').classList.contains('rp-off')" },
        { click: "#rpToggle" },
        // ⚠️ **주사위 오버레이가 사라진 뒤에 잰다.** `.dice-overlay`는 `pointer-events: none`이라
        //    S1은 원리적으로 침묵하지만, 그 카드가 증거 노트 위 **55,914px²**를 덮어 캡처를
        //    가리고 무엇보다 **타이머로 사라져** S5·S6가 세는 줄과 S3의 «문서 밖» 개수가
        //    기동 속도에 따라 달라진다 — 결정론이 아니면 회귀를 판정할 수 없다.
        { waitFor: "document.getElementById('diceOverlay').classList.contains('hidden')" },
      ],
      ready: "!document.getElementById('rightPanel').classList.contains('rp-off')",
      vscroll: "locked",
      // 🔴 **컬럼이 게임 HUD를 덮는 것은 «의도»다 — 그 사실을 여기 적는다.**
      //    컬럼이 열리면 `.hud-ctrl`(액션 바)은 **완전히** 덮이고(교차 300×52 = 15,600px²)
      //    `#turnInfo`도 5/5점이 `summary`에 맞는다. `main.ts`가 컬럼을 열 때 D-패드·AI HUD를
      //    **일부러** 끄는 것과 같은 설계다(폰은 화면이 하나뿐이다).
      //    그래서 `GAME_PROTECT`를 쓰지 않는다 — 쓰면 «의도된 가림»이 매 실행 오탐이 된다.
      //    (`goal-modal`이 「모달이 뜬 동안 뒤가 가려지는 것은 정상」이라 적은 것과 같은 처리.)
      //    ⚠️ 초안은 이 사유를 «배경음 하나» 때문이라고만 적었다 — **틀렸다.** 실제로 같이
      //       빠진 것은 `.hud-ctrl button` 6개 · `#turnInfo` · `GAME_PAIRS` 7종 전부다.
      protect: [
        { sel: "#rpToggle", why: "컬럼을 닫는 **유일한** 길. 가려지면 보드로 못 돌아간다" },
        { sel: ".rp-hand", why: "폰에서 「내 손패」를 보여 주는 **유일한** 줄(`.hud-hand`는 폰에서 `display:none`)" },
        { sel: "#eviPanel .rp-head", why: "증거 노트 머리글 — 무엇을 보고 있는지" },
      ],
      pairs: [
        // ⚠️ 대상은 `.rp-head`가 **아니다** — 컬럼 맨 위 패널(`#aiPanel`)의 머리글은
        //    `<summary>`다. 초안이 `.rp-head`로 걸어 **기하 교차 0으로 영원히 침묵**했다.
        //    그리고 올바른 선택자로 바꾸면 **상자끼리는 실제로 겹친다**(2032px²) —
        //    `padding-right: 58px`이 **글자만** 비켰고 상자는 그대로이기 때문이다.
        //    그래서 대상을 «글자»로 잡는다. 사람이 읽어야 하는 것은 제목이지 상자가 아니다.
        ["#rpToggle", "#aiPanel .ai-title", "닫기 버튼이 맨 위 패널 **제목 글자**를 덮는가"],
        // ⚠️ 이 쌍은 **지금 침묵한다** — 플랜 43이 「컬럼이 열리면 배경음을 감춘다」로 고쳤다.
        //    `[미확인]` 감추기를 되돌려도 **평상 상태로는 안 걸린다**(복원 시 오른끝 x=70 ·
        //    컬럼은 x82부터 → 교차 0). 23회차가 실측한 88px 겹침은 **슬라이더가 펼쳐진**
        //    상태에서만 나는데, 게이트는 `hover: none`이고 포커스를 주는 step도 없어
        //    **그 상태에 도달할 수 없다.** 즉 이 쌍은 «되돌림 래칫»으로만 쓸모가 있고
        //    원래 겨냥한 사고는 **여전히 무계측**이다 — 정직하게 적어 둔다(플랜 44 큐).
        BGM_OVER_MODAL("#rightPanel"),
      ],
      touchExempt: [],
    },
    {
      id: "accuse-modal",
      label: "게임 + 신고 모달(select 3개)",
      url: "/?solo=1&demo=1",
      steps: [
        { waitFor: "!document.getElementById('turnInfo').classList.contains('hidden')" },
        { click: "#accuse" },
      ],
      ready: "!!document.querySelector('.overlay .modal select')",
      vscroll: "locked",
      protect: [
        { sel: ".modal select", min: 3, why: "용의자·훔친 것·장소 — 신고의 전부" },
        { sel: ".modal .actions button", min: 2, why: "[취소]/[신고한다]" },
      ],
      pairs: [
        [".modal .actions", ".modal-note", "버튼 줄이 안내 문구를 덮는가"],
        // ⚠️ 여기선 **데스크톱이 사고 현장**이다. `.overlay`는 z:10인데 `.bgm-ctrl` 기본값이
        //    z:40이라(폰·coarse 블록에서만 4로 내린다) **스택 컨텍스트와 무관하게** 40 > 10이다.
        //    「폰에서 안 났다」가 「없다」가 아니다 — 이 쌍이 그 차이를 잡는다.
        BGM_OVER_MODAL(".overlay"),
      ],
      touchExempt: [],
    },
  ],

  /**
   * ── 결과 화면(종료 오버레이) — **6인 실판을 정상 조작해서 도달한다** ──────────
   *
   * 예전 사유("정답 봉투가 동기화 상태에 없어 결정론적으로 못 만든다")는 **한 명일 때만**
   * 참이다. 좌석 6개를 전부 사람(브라우저 탭)으로 채우면 다음 두 성질이 생긴다.
   *
   *  ① **손패의 합집합 = 정답 아닌 카드 전부.**
   *     덱 = (참가자 용의자 6 + 장물 6 + 장소 9) − 정답 3 = 18장, 6인 × 3장으로 **딱** 나뉜다
   *     (clue-room.ts `deal` — 봇이 없으면 공통 단서도 나가지 않는다).
   *     각 탭은 자기 손패를 **신고 모달의 `disabled` 옵션**으로 이미 알고 있다
   *     (main.ts `selectFrom(..., lockMine=true)`). 6탭이 서로 알려주면
   *     각 카테고리에서 **아무의 손에도 없는 값이 정확히 하나** 남는다 = 정답 봉투.
   *     → 서버에 묻지 않고, 동기화 상태를 읽지도 않고, **콜루전 6인이 실제로 할 수 있는
   *       추론만으로** 정답이 나온다. 진실값은 여전히 서버가 만든 것이고 클라는 소거만 한다.
   *  ② **틀린 고발은 그보다 더 싸다.** 남의 손패에 있는 카드 = 정답일 수 없는 카드이고,
   *     그 옵션은 내 모달에서 `disabled`가 아니다(잠기는 것은 **내** 패뿐).
   *     → UI 잠금을 우회하지 않고 «반드시 오답»인 조합을 고른다. **실패 확률 0.**
   *
   * 그리고 좌석이 전부 사람이면 **탈락이 곧 종료로 이어진다** — 봇은 자기 판단으로만
   * 탈락하므로(도박 고발) 솔로 판에서는 종료 시점이 시드마다 달랐다. 5명이 차례로
   * 오답 고발하면 `advanceTurn`이 «남은 1명»을 보고 그 자리에서 판을 끝낸다.
   *
   * 도달 순서(전부 기존 UI 조작뿐 — 서버에 새 경로를 만들지 않는다):
   *   방 만들기(비공개) → 5탭이 초대 코드로 참가 → [잔치 시작]
   *   → 한 바퀴: 각 탭이 자기 차례에 신고 모달을 열어 **손패만 읽고 [취소]** → [턴 종료]
   *   → 두 바퀴째: 생존 지정 탭은 [턴 종료], 나머지 5탭은 «남의 패» 조합으로 고발(반드시 오답)
   *   → 5번째 오답에서 종료(`survivor`) → 탈락 탭 = §7.1 4번 / 생존 탭 = §7.1 3번
   *   → [다시 하기] → 손패 한 바퀴 다시 읽기 → 생존 탭이 **정답 봉투**로 고발
   *   → 종료(`accuse`) → 승자 탭 = §7.1 1번 / 나머지 = §7.1 2번
   */
  result: {
    label: "결과 화면(6인 실판 · 종료 오버레이)",
    /**
     * 기본 대상 포함 여부. 실측 소요는 아래 «시간» 주석 참고.
     * (`--only=result`로는 tier와 무관하게 언제든 돌릴 수 있다.)
     */
    tier: "default",
    /** 좌석 수 = MAX_PLAYERS. 이보다 적으면 서버가 NPC로 채워 «전원 사람»이 깨진다. */
    seats: 6,
    /**
     * 탭 역할. `vp`가 있으면 그 뷰포트로 계측하고, 없으면 계측하지 않는 조력 탭이다
     * (조력 탭은 뷰포트 에뮬레이션도 걸지 않는다 — 6탭을 전부 에뮬레이션하면 그만큼 느려진다).
     */
    tabs: [
      { id: "A", vp: "phone", role: "loser" },
      { id: "B", vp: "desktop", role: "loser" },
      { id: "C", vp: null, role: "loser" },
      { id: "D", vp: null, role: "loser" },
      { id: "E", vp: null, role: "loser" },
      { id: "F", vp: "phone", role: "keeper" }, // 1판 생존자 · 2판 승자
    ],
    /** 턴 왕복 상한 — 무한 루프 방지. 한 바퀴 6턴 × (읽기 + 고발) + 여유. */
    maxTurns: 40,
    /**
     * 계측할 결과 화면. `title`은 **도달한 행이 맞는지** 확인하는 지문이다 —
     * 다른 행에 도달했는데 조용히 재는 것이 가장 위험하다(그때는 SKIP + 사유).
     */
    screens: [
      {
        id: "result-lose",
        game: 1,
        tab: ["A", "B"],
        label: "결과 ❌ 신고 실패(§7.1 4번 · 내 지목 ↔ 정답 봉투 대조)",
        title: "신고 실패",
      },
      {
        id: "result-survivor",
        game: 1,
        tab: ["F"],
        label: "결과 🏅 최후의 1인(§7.1 3번)",
        title: "최후의 1인",
      },
      {
        id: "result-win",
        game: 2,
        tab: ["F"],
        label: "결과 🎉 사건 해결(§7.1 1번 · 신고 성공)",
        title: "사건 해결",
      },
      {
        id: "result-defeat",
        game: 2,
        tab: ["A", "B"],
        label: "결과 🔍 사건 종결(§7.1 2번 · 남이 먼저 맞힘)",
        title: "사건 종결",
      },
    ],
    /** 결과 오버레이의 기대값(4종 공통 — 같은 DOM이다). */
    vscroll: "locked",
    protect: [
      { sel: "#endRematch", why: "[다시 하기] — 심사자가 두 번째 판을 여는 유일한 경로" },
      { sel: "#endHome", why: "[메인으로] — 여기서 못 나가면 판에 갇힌다" },
      { sel: "#endTitle", why: "결과 한 줄. 이게 가려지면 이겼는지 졌는지를 모른다" },
      {
        // `[정정 2026-08-26 · 플랜 31]` 이 가드는 원래 `#endSub`를 지켰다 — 그때는 봉투·내 지목이
        // **부제 한 줄 산문**이었기 때문이다. §7.1.1이 그것을 3장 조판으로 옮기면서
        // 부제는 «산문만» 남게 됐고(1번 «사건 해결!»에선 아예 빈다) 가드의 **대상이 이동**했다.
        // 완화가 아니라 **이전**이다 — 게다가 `min`으로 «3장이 다 있는가»까지 잰다(전엔 못 쟀다).
        sel: "#endCards .ec-card",
        min: 3,
        why:
          "«내 지목 ↔ 정답 봉투» 3장 조판. ui-copy §7.1이 «패배 화면의 유일한 정보 가치… " +
          "생략하지 말 것»으로 못박은 자리다. 3장이 다 보여야 대조가 성립한다",
      },
      {
        sel: "#endCards .ec-label",
        why: "«내 지목» / «정답 봉투» — 어느 줄이 무엇인지. 라벨이 가려지면 3장이 무엇인지 모른다",
      },
      {
        // 조판이 생기면서 부제는 «산문»만 남았고 1·4번에선 아예 빈다 → `optional`.
        // **그래도 지운 게 아니다** — 2·3·3′·5번의 산문이 통째로 사라지거나 누가 덮는 회귀는
        // 새 가드(`.ec-card`)가 **못 잡는다**. 있으면 지키고 없으면 넘어간다.
        sel: "#endSub",
        optional: true,
        why: "결과 산문(«○○ 님이 먼저 맞혔어요» 등). 2·3·3′·5번에만 있고 1·4번은 비어 있다",
      },
    ],
    // ⚠️ `#endOverlay` ↔ `.hud-ctrl` 쌍은 **일부러 넣지 않는다.** 결과 오버레이가 액션 바를
    //    덮는 것은 설계된 동작인데(전면 오버레이), `pairs`는 «덮었다/안 덮었다»만 말할 뿐
    //    «누가 덮어야 하는가»를 표현하지 못한다 → 넣으면 매 실행 오탐이다.
    //    정작 확인해야 하는 «오버레이 위를 아무도 덮지 않는다»는 위 `protect`가 이미 한다.
    // `pairs`는 양쪽을 `shown`으로 거른 뒤 교차를 보므로, 한쪽이 없는 화면(1·4번의 부제)에서는
    // **조용히 넘어간다** — 그래서 조판·부제 쌍을 둘 다 둬도 오탐이 나지 않는다(실측 확인).
    pairs: [
      BGM_OVER_MODAL("#endOverlay"),
      [".end-actions", "#endCards", "버튼 줄이 3장 조판을 덮는가"],
      ["#endTitle", "#endCards", "제목이 조판을 덮는가(구성이 무너졌는가)"],
      [".end-actions", "#endSub", "버튼 줄이 결과 산문을 덮는가"],
      ["#endTitle", "#endSub", "제목이 산문을 덮는가"],
    ],
    touchExempt: [],
  },

  /**
   * ── S7 공개방 목록 상태 배지(ui-copy §8.5) ──────────────────────────
   *
   * **무엇을 재는가.** 랜딩 `#roomList`의 방 한 줄이 «지금 그 방이 어떤 상태인지»를
   * 옳게 적는가. 문안은 ui-copy §8.5 확정본이고, 이 표는 그 표의 **기계 판정용 사본**이다.
   * (문서를 고치면 여기도 고쳐야 한다 — 갈라지면 문서가 아니라 이 표가 틀린 것이다.)
   *
   * **왜 필요한가.** 판이 끝난 방은 목록으로 되돌아오는데(`setListed(true)`), 거기 들어가면
   * 리매치 전까지 **관전**이다. 버튼이 `참가`라고만 적혀 있으면 «참가한다»고 읽고 들어가
   * 관전 화면을 본다. 이 회귀는 **DOM에 요소가 있는가**로는 절대 잡히지 않는다 —
   * 요소는 늘 있고, 틀리는 것은 그 안의 **글자**다.
   *
   * **어떻게 도달하는가 — 확률이 아니라 규칙이다.** `SCREEN.result`의 6탭 세션에 얹혀 돈다.
   * 관측 탭 1개(방에 들어가지 않는다)를 더 열어 같은 세션의 세 시점에서 랜딩 목록을 읽는다:
   *   ① `lobby` — 방장이 **공개방**을 만든 직후(1/6). 사람 1명 · 판 시작 전. 브라우저·서버·
   *               탭을 새로 띄우지 않으므로 추가 비용이 0에 가깝다.
   *   ② 정원 초과 — 6탭이 다 들어온 직후. **아래 `full.unreachableWhy` 참고**(도달 불가 → SKIP).
   *   ③ `ended`  — 결과 화면 4행을 다 계측한 **뒤에** 3막을 하나 더 돈다(아래 `endedRoom`).
   *
   * ⚠️ 목록에 «진행 중»은 원리적으로 나타나지 않는다 — 판이 도는 동안 `setListed(false)`다.
   *    그래서 배지는 2종뿐이고, 이 표에도 2종만 있다.
   */
  roomList: {
    label: "공개방 목록 상태 배지(ui-copy §8.5)",
    /** 목록이 기대 개수로 갱신되기를 기다리는 상한(ms). 넘으면 FAIL이 아니라 SKIP. */
    waitMs: 8000,
    /**
     * ⚠️ **우리 방을 지목하는 방법** — 「목록에 방이 정확히 1개」다.
     * `li`에 `roomId`가 박혀 있지 않으므로 «몇 번째가 우리 방인가»를 말할 수 없다.
     * 그래서 지목이 불가능한 상황(0개·2개 이상)은 **조용히 통과시키지 않고 SKIP**한다.
     * 게이트가 띄우는 서버는 자기 전용이고, 이 시점에 살아 있는 공개방은 우리 방뿐이다.
     */
    expectRooms: 1,
    /**
     * ── 3막: «종료됨»에 도달하는 유일한 구성 ────────────────────────────
     *
     * **6탭 방을 그대로 못 쓰는 이유(실측).** 판이 끝나면 방은 `setListed(true)`로 공개로
     * 되돌아오지만 **목록에는 안 뜬다.** `getAvailableRooms()`가 `locked:false`만 돌려주는데
     * 좌석이 6/6이 되는 순간 Colyseus가 방을 잠그고(`Room._incrementClientCount` → `lock()`),
     * 그 잠금은 접속 수가 줄어야 풀린다. 그런데 접속 수를 줄이는 `_decrementClientCount()`는
     * `Room._onLeave`에서 **`await this.onLeave()` 뒤**에 있고, 이 저장소의 `onLeave`는
     * `phase !== "lobby"`면 `allowReconnection(120초)`를 await 한다(결과 화면 새로고침 복구용).
     * → 탭을 닫아도 **120초 동안** `clients`는 6, `locked`는 true다. 게이트 예산 안에서
     *   6인 방이 목록에 되돌아오는 일은 **일어나지 않는다.**
     *
     * **그래서 좌석 하나를 NPC에게 준다 — 사람 5 + NPC 1.**
     * NPC는 접속(client)이 아니라 좌석이므로 접속 수는 5에서 멈춘다 → 방은 **한 번도 잠기지
     * 않는다** → 판이 끝나면 그대로 목록에 뜬다. 끝내는 방법은 결과 흐름과 **같은 장치**다:
     * 각 탭이 **남의 손패 카드**로 고발하면 정의상 오답이라 반드시 탈락하고,
     * 사람 5명이 전부 탈락하면 남는 것은 NPC 하나뿐이라 서버가 그 자리에서 `survivor`로
     * 판을 끝낸다. **고발 5회 상한이 규칙으로 확정**돼 있다(확률이 아니다).
     * 정답 봉투는 모르고, 알 필요도 없다 — 이 막이 필요한 것은 «판이 끝났다»뿐이다.
     *
     * 비용: 탭·브라우저·서버를 새로 띄우지 않고 기존 5탭을 재사용하지만, **판은 한 번 더
     * 돈다**(실측 ≈+11초). 그 값으로 사는 것은 «심사자가 실제로 밟는 동선인데 화면으로
     * 확인된 적이 없던» §8.5 2행 하나다.
     */
    endedRoom: {
      /** 사람 탭 수. **6이면 안 된다** — 위 잠금 설명 그대로다. */
      humans: 5,
    },
    cases: {
      lobby: {
        id: "roomlist-lobby",
        label: "공개방 목록 · 대기 중(§8.5 1행 — 배지 «대기 중» · 버튼 «참가»)",
        badge: "대기 중",
        button: "참가",
        disabled: false,
        /** `{clients}`·`{maxClients}`는 그 시점의 실측값으로 치환된다. */
        sub: "{clients}/{maxClients}인",
      },
      full: {
        id: "roomlist-full",
        label: "공개방 목록 · 정원 초과(§8.5 3행 — 버튼 «만석»(비활성))",
        /**
         * §8.5 3행은 **버튼만** 규정한다(배지·부가 칸이 `—`). 그래서 여기서도 버튼만 잰다 —
         * 규정하지 않은 칸에 기대값을 지어내면 문서에 없는 것을 게이트가 단언하게 된다.
         */
        badge: null,
        button: "만석",
        disabled: true,
        sub: null,
        /**
         * **이 행은 목록에 나타날 수 없다**(실측 · Colyseus 0.15).
         * `matchmaker/controller.js`의 `getAvailableRooms()`가 `{ locked: false, private: false }`로
         * 거르는데, 좌석이 다 차면 `Room._incrementClientCount()`가 `lock()`을 걸어 `locked: true`가
         * 된다 → **정원이 찬 방은 목록에서 통째로 빠진다.** 즉 클라의 `full` 분기
         * (`r.clients >= r.maxClients` → «만석»)는 현재 구성에서 **도달 불가 코드**다.
         * 그러니 목록이 비어 있는 것은 **정상**이고, 그때는 PASS가 아니라 **SKIP + 이 사유**다.
         * (만약 언젠가 나타나면 그때는 실제로 판정한다 — 아래 기대값이 그때 도는 몫이다.)
         */
        unreachableWhy:
          "Colyseus `getAvailableRooms()`는 `locked:false`인 방만 돌려주는데, 좌석이 차면 " +
          "`Room._incrementClientCount()`가 방을 잠근다 → **정원이 찬 방은 목록에서 사라진다.** " +
          "따라서 «만석» 버튼은 목록에 원리적으로 나타나지 않는다(클라 `full` 분기는 도달 불가). " +
          "목록이 비어 있는 것이 곧 이 사실의 실측이다 — 통과로 세지 않는다.",
      },
      ended: {
        id: "roomlist-ended",
        label: "공개방 목록 · 종료됨(§8.5 2행 — 배지 «종료됨» · 버튼 «관전»)",
        badge: "종료됨",
        button: "관전",
        disabled: false,
        /** R2 — 변수 없는 **고정 문장**이다. 여기 변수를 끼우면 문안이 조사에 걸린다. */
        sub: "다시 하기를 기다리는 중",
      },
    },
  },

  /**
   * ── S8 판 중반 우측 컬럼 배분 ────────────────────────────────────────
   *
   * **무엇을 재는가.** 우측 컬럼(`.rp-col`)은 `AI 대사 → 증거 노트 → 제안 기록표 →
   * 기록/알림` 세로 스택이고, 넷이 **같은 높이 하나**를 나눠 쓴다. 이 검사는 그 나눔의
   * 결과 중 **맨 아래 `#logPanel`이 실제로 몇 줄을 보여줄 수 있는가**를 잰다.
   *
   * **왜 S1(겹침·가림)으로는 안 잡히는가.** S1이 묻는 것은 «이 요소가 **남에게** 덮였는가»다.
   * 그런데 여기서 일어나는 일은 덮임이 아니라 **자기 몫이 0으로 수렴하는 것**이다 —
   * 로그 패널은 온전히 보이고, 아무도 그 위를 덮지 않으며, `getBoundingClientRect()`도
   * 정상 크기(72px)를 돌려준다. 가림 검사는 원리적으로 침묵한다.
   * S2(스크롤)도 마찬가지다: 컬럼은 문서를 밀지 않고 **자기 안에서** 줄어들 뿐이다.
   *
   * **왜 «줄 수»가 기능의 하한인가.** 로그(`📜 기록/알림`)는 **반증·계략·소환**이 사람에게
   * 도달하는 **유일한 통로**다. 반증 결과는 개인 메시지로, 소환은 `kind:"move"` 브로드캐스트로
   * 오직 이 패널에만 찍힌다(제안 기록표는 «누가 무엇을 제안했고 누가 반증했나»만 적고,
   * 소환·계략·개인 반증 카드는 적지 않는다). 그래서:
   *   **온전히 보이는 줄이 0이면 그 통로의 대역폭이 0이다.**
   * «스크롤하면 보인다»는 반례가 아니다 — 알림은 찾지 않아도 눈에 들어와야 성립하고,
   * 사용자는 자기가 모르는 사건을 찾으러 스크롤하지 않는다. 하한을 1줄로 두는 근거가 이것이다:
   * 1줄이면 «가장 최근 사건 하나»는 전달되고, 0줄이면 아무것도 전달되지 않는다.
   * (하한을 «보기 좋은 5줄» 같은 값으로 두지 않은 이유도 같다 — 그건 취향이라 근거가 없다.)
   *
   * **⚠️ 지금 이 값은 결함 상태다.** 그래서 이 검사는 **하한으로 FAIL을 내지 않는다** —
   * 상시 빨간불이 된 게이트는 아무도 안 돌리고, 그러면 게이트 전체가 무의미해진다.
   * 대신 번들 예산(`gate-bundle-budget.mjs`)과 **같은 래칫**을 쓴다: 현재 실측을
   * `gate.baseline.json`에 박고 **더 나빠지면 FAIL**, 좋아지면 기준선을 조인다.
   * 출력은 하한 미달을 매번 «결함 확인»으로 인쇄한다 — 기준선 통과가 «정상»으로 읽히면 안 된다.
   *
   * **어떻게 판 중반에 도달하는가 — 확률이 아니라 규칙이다.**
   * `SCREEN.result`의 6탭 실판(1판)에 **얹혀** 돈다. 새 판·새 서버·새 브라우저가 없다.
   *   ① 걷기 — 방장 탭(A)은 판 시작 시 잔치상 `(11,11)`에 선다(`spawnPoint(0)`, 사람은
   *      입장 순서대로 배치되고 방장이 첫 번째다). 거기서 **↑만 눌러** `(11,8)`까지는
   *      잔치상 발이라 한도 없이 가고, 남은 3칸 `(11,7)(11,6)(11,5)`만 이동 한도를 쓴다.
   *      `(11,5)`는 대청의 **문**이라 복도에서 들어갈 수 있는 유일한 칸이다(`canCross`).
   *      한 턴 이동 한도는 주사위 2개(2~12)라 3칸에 못 미치는 경우는 «둘 다 1»뿐이고,
   *      그때는 **다음 자기 턴에 이어 걷는다**(위치는 턴을 넘어 유지된다) — 도달은 확정이다.
   *   ② 소환 사슬 — 제안은 지목한 용의자를 **그 방으로 소환**하고(`doSuggestion`),
   *      끌려온 좌석의 재진입 잠금은 그때 풀린다(`clearSuggestedIn`). 그래서 제안자가
   *      **턴 순서상 바로 다음 좌석**의 용의자를 지목하면, 다음 좌석은 자기 턴이 왔을 때
   *      이미 같은 방에 있고 잠금도 없다 → **매 턴 정확히 1건씩** 제안이 쌓인다.
   *      «다음 좌석»은 턴 배너의 순서 스트립(`.ti-chip` 2번째 = «(다음)»)이 화면에 이미
   *      적어 둔 값이라 내부 상태를 훔쳐보지 않는다. 6좌석이 한 바퀴 돌면 사슬은 끝난다
   *      (마지막 좌석이 지목할 다음 좌석은 이미 그 방에 있어 «이동»이 아니다).
   *   ③ 계측 — 표가 정확히 `suggestRows`행이 된 시점에 잰다. **행 수를 고정**해야
   *      실행마다 같은 값이 나온다(래칫은 재현 가능한 수치 위에서만 성립한다).
   *
   * **서버에 아무것도 추가하지 않았다.** ①은 D-패드 버튼의 `pointerdown`(손가락이 하는
   * 그것), ②는 [제안] 버튼과 모달 `select`다 — 전부 사람이 밟는 경로 그대로다.
   */
  rightColumn: {
    id: "right-column",
    label: "판 중반 우측 컬럼 — 📜 기록/알림 몫(S8) + 이 상태의 S1~S9",
    /**
     * 계측 뷰포트. **데스크톱만** 잰다.
     * 폰(≤680px)에서는 컬럼이 기본 접힘(`display:none`)이고, 펴도 `.rp-col`이
     * `overflow-y:auto`라 패널들이 **컬럼 안에서 스크롤**된다 → «높이를 나눠 갖는다»는
     * 전제 자체가 없어 같은 수치를 정의할 수 없다. 폰은 SKIP + 이 사유로 인쇄한다.
     */
    viewport: "desktop",
    /** 폰을 재지 않는 사유(출력에 그대로 실린다). */
    phoneSkipWhy:
      "폰(≤680px)에서 `.rp-col`은 기본 접힘이고 펴도 컬럼 자체가 `overflow-y:auto`로 " +
      "스크롤된다 — 패널이 높이를 **나눠 갖지 않으므로** «몫»이라는 수치가 정의되지 않는다. " +
      "이 결함은 데스크톱(고정 높이 배분)에서만 성립한다.",
    /** 계측 시점 = 제안 기록표 행 수. 고정하지 않으면 실행마다 값이 흔들려 래칫이 못 선다. */
    suggestRows: 6,
    /** 사슬을 도는 턴 예산(무한 루프 방지). 6좌석 × 한 바퀴 + 걷기 여유. */
    maxTurns: 26,
    /** 한 턴에 누르는 ↑ 횟수. `(11,11)→(11,5)`는 6칸이라 6이면 되고, 2를 여유로 둔다. */
    walkClicks: 8,
    /** D-패드 연타 간격(ms). `MOVE_COOLDOWN_MS`(110)보다 커야 눌림이 삼켜지지 않는다. */
    walkGapMs: 130,
    /**
     * 기능 하한 — «이 값 아래면 기록/알림이 아무 사건도 전달하지 못한다».
     * 위 주석의 근거대로 **1줄**이다. 미달은 FAIL이 아니라 **«결함 확인»으로 인쇄**된다.
     *
     * **이 하한은 «지금 받은 몫»이 아니라 «보장하는 몫»에 건다**(`floorLines`).
     * `#logPanel`은 `flex: 1 1 0` — 남는 것을 받는 쪽이라, 지금 넉넉해 보이는 것은
     * 위 패널들이 아직 안 자란 덕이지 약속이 아니다. 이 화면이 실제로 약속하는 값은
     * CSS의 `min-height` 하나뿐이다. 위 패널 합이 컬럼을 넘는 순간(증거 노트 260px 고정 +
     * 제안 기록표 `#sugBody{max-height:26vh}` + AI 대사 패널이 폴백 사유만큼 자람)
     * 로그는 **그 바닥으로 떨어진다** — 그 바닥이 이 검사가 재는 값이다.
     *
     * `[해소 2026-08-26 · 플랜 28]` 오래 «아니오»였다. `min-height: 72px`에서 머리글·패딩
     * 55px를 빼면 본문 **17px = 0줄**이라, 통로의 보장 대역폭이 0이었다.
     * → AI 대사 패널을 기본 접힘으로 돌려 122px를 되찾고 `min-height`를 **195px**로 올렸다
     *   (줄 최소 높이 35px × **4줄** + 55px). 보장 0줄 → **4줄**, 받은 몫 68px/1줄 → 151px/4줄.
     * 4줄인 이유: 제안과 반증은 **한 쌍**으로 오므로 2줄이면 «무슨 일이 있었는지»가
     * 한 화면에 안 남는다. 아래 `minVisibleLines`(기능 하한 1줄)는 **그것과 별개인 최저선**이라
     * 그대로 둔다 — 하한을 올리면 «하한»이 아니라 목표가 된다.
     *
     * 줄 수를 세는 방식은 `⌊본문 픽셀 / 한 줄 최소 높이⌋`다. 「지금 화면에 온전히 보이는
     * 줄」(`visibleLines`)을 기준선으로 쓰지 않는 이유: 그 값은 맨 위 줄이 두 줄로 접혔는지
     * 같은 **문장 길이**에 흔들려 실행마다 달라진다. 래칫은 흔들리지 않는 수치 위에서만 선다.
     * (`visibleLines`도 관측치로 함께 인쇄한다 — 둘이 갈라지면 그것도 정보다.)
     */
    minVisibleLines: 1,
    /**
     * **이 검사가 도달시킨 화면에서 «봤지만 이번엔 판정하지 않는» 것.** 매 실행 인쇄된다.
     * 조용히 빼면 그 순간 은폐가 된다 — 이 저장소가 가장 경계하는 실패 방식이다.
     */
    /**
     * ⚠️ **주사위 카드(`#diceOverlay`)를 재는 화면이 저장소에 하나도 없다**(2026-08-29 · 33회차).
     * `game`과 `game-column`이 **둘 다** 그것이 사라진 뒤에 재기 때문이다. 사라지기를 기다리는
     * 것은 결정론 때문에 옳지만(같은 사유를 두 화면이 적어 뒀다), 그 대가로 `.dice-label`의
     * 글자 크기·대비는 **사람 몫**이 됐다. 조용히 빠지지 않게 여기 적는다.
     */
    knownUnmeasured:
      "**제안 기록표(`.sg-*`)의 글자는 어떤 검사도 대비를 재지 않는다.** S6는 `screens` 목록의 " +
      "`game` 화면에서 도는데, 그 시점엔 제안이 0건이라 표가 **존재하지 않는다**. 표가 뜨는 것은 " +
      "판 중반(이 화면)뿐이고 여기서는 S8만 돈다 — 즉 이 표는 **계측 사각지대**에 있다. " +
      "『2026-08-26 (플랜 30) — 여기 적혀 있던 실제 결함은 **고쳤다**. `.sg-mine`이 " +
      "`rgba(180,98,43,0.14)`라는 **반투명 패널 위 알파 합성**이라 캔버스 색에 따라 `.sg-n` 대비가 " +
      "4.10~4.49:1로 흔들렸다(AA 미달) → 불투명 `--wds-tint-cautionary`로 고정해 **5.31:1**. " +
      "일반 행(`.sg-row` `--wds-tint-primary`)은 **4.96:1**로 통과하나 여유가 0.46뿐이다. " +
      "**사각지대 자체는 그대로다** — 이 값들은 손으로 계산한 것이고, 다음 회귀는 아무도 못 잡는다.』 " +
      "『2026-08-29 (플랜 50) — **절반 해소.** 아래 `probe`가 이 탭에서 `pageProbe`+hover를 함께 " +
      "돌린다. 실제로 S6가 재는 것은 **직계 텍스트가 있는** `.sg-n`·`.sg-combo`·`.sg-by`·`.sg-res`·" +
      "`.sg-mine-tag` 다섯이다(`.sg-head`/`.sg-meta`는 직계 텍스트가 없어 수집 대상이 아니다). " +
      "⚠️ **S9는 `.sg-*`를 하나도 안 잰다** — 조작 요소가 아니라 후보에 안 든다. 초안이 " +
      "「S6·S9가 잰다」고 적었는데 **거짓이었고**, 새로 만든 계측을 실제보다 넓게 주장한 것이다. " +
      "S5는 손가락 뷰포트 전용이라 여기서는 SKIP으로 인쇄된다. " +
      "⚠️ **그래도 `.sg-res.none`(❗ 미반증)은 여전히 무계측이다.** 이 흐름은 좌석 사슬을 돌아 " +
      "제안을 만드는데 **그 6건이 전부 반증된다**(실측 — 미반증 0행). 즉 판에서 가장 값진 사건의 " +
      "표기가 게이트에 한 번도 안 뜬다. 30회차가 그 자리에서 실제 결함을 찾았다 — primary-normal " +
      "on tint-primary **4.08:1로 AA 미달**이었고 세는 검사가 없었다(→ 채운 배지 **7.32:1**로 고침). " +
      "**무계측이 무결함이 아니다.** 남은 사각지대 둘: **미반증 행** · **폰의 제안 기록표**" +
      "(폰은 컬럼 배분이 정의되지 않아 이 흐름이 데스크톱만 돈다).』 " +
      "⚠️ **이 행의 S1~S9는 비결정 DOM 위에서 잰다** — `screens` 흐름과 달리 6탭 실판 중간이라 " +
      "배너·턴 상태가 실행마다 다르다. 여기서 FAIL이 나면 **재현 여부부터 확인하라.**",
    /**
     * 🔴 **이 화면은 29회차까지 S8 하나만 재고 있었다.** `gate-screen.mjs`가
     *    「캡처만 남긴다 — S8이 재는 것은 배분 하나다」라고 적고 `pageProbe`를 건너뛰었고,
     *    바로 위 `knownUnmeasured`가 그 사실을 **매 실행 인쇄하면서도 세 회차를 넘겼다.**
     *    결과: 제안 기록표(`.sg-*`)가 뜨는 화면은 여기 하나뿐인데(`game`·`game-column`은
     *    제안 **0행**) 그 화면에서만 대비를 안 쟀다. `index.html` 주석의 5.31·9.79·4.26은
     *    전부 **사람이 손으로 계산한 값**이고, 그 뒤 회귀를 잡는 검사는 없었다.
     *
     * 🔴 **초안은 `GAME_PROTECT`/`GAME_PAIRS`를 «중복 계측은 커버리지 착시»라며 뺐다 —
     *    자기모순이었다**(검수 지적). 그 사유는 **S1에만** 적용된다. S3·S5·S6·S9는 선택자
     *    스코프가 없어 `pageProbe`가 **페이지 전체**를 훑는다 — 즉 이 화면은 그 사유를 대면서
     *    이미 같은 DOM을 통째로 다시 재고 있었다(S6 143줄).
     *    그리고 **여기가 컬럼이 가장 큰 유일한 지점**이다: `game`·`game-column`은 제안 0행 +
     *    AI 패널 접힘이라 컬럼이 가장 홀쭉하다. `["#rightPanel", ".hud-ctrl"]`·
     *    `["#bgmCtrl", "#rightPanel"]`의 **최악 조건이 바로 이 상태**인데 그 지점에서만 안 걸었다.
     */
    probe: {
      id: "right-column",
      // `game` 화면과 같은 약속 — 게임 화면은 문서 스크롤이 없어야 한다.
      // 빠뜨리면 `undefined !== "locked"`라 S2가 **조용히 아무것도 안 잰다.**
      vscroll: "locked",
      protect: [
        ...GAME_PROTECT,
        {
          // 🔴 초안은 `#sugPanel .rp-head`로 걸어 **DOM 0개 → 즉시 FAIL**이었다.
          //    이 패널의 머리글은 `.rp-head`가 아니라 `<summary>`다 — 바로 위 `game-column`
          //    설정이 「대상은 `.rp-head`가 **아니다**」라고 적어 둔 그 함정에 한 화면 옆에서
          //    똑같이 빠졌다. 상자가 아니라 **사람이 읽는 글자**를 건다(그쪽 판단과 같다).
          sel: "#sugPanel summary .ai-title",
          why: "제안 기록표 제목 글자 — 무엇을 보고 있는지",
        },
        {
          // 행 전체(`.sg-row`)로 걸지 않는다 — `#sugBody{max-height}` 안에서 아래 행은
          // **정상적으로** 잘리므로 «뷰포트 밖» 판정이 매 실행 오탐이 된다.
          // 가장 최근 제안 = 첫 행 하나가 안 보이면 그것은 진짜 결함이다.
          sel: ".sg-row:first-child",
          why: "가장 최근 제안 1행. 이것이 안 보이면 표가 있어도 «방금 무슨 일이 있었나»를 못 읽는다",
        },
      ],
      pairs: GAME_PAIRS,
      touchExempt: [],
      textExempt: [],
    },
    /** 래칫 여유(%) — 서브픽셀·폰트 로딩 편차 흡수용. 번들 게이트의 3%와 같은 성격. */
    ratchetSlackPct: 4,
    /** 이만큼 좋아지면 «기준선을 조여라»를 인쇄한다(조이지 않으면 되돌림이 통과한다). */
    ratchetTightenPct: 10,
  },

  /**
   * **측정하지 못하는 화면** — 조용히 빼지 않는다. 실행할 때마다 사유와 함께 인쇄한다.
   * (은폐된 미측정이 회귀보다 위험하다 — roadmap §9.6의 취지)
   *
   * ⚠️ 뭉뚱그리지 마라. 결과 화면 6종(ui-copy §7.1) 중 **어느 행이 왜 안 되는지**를
   *    행 단위로 적는다. "결과 화면을 못 잰다"는 한 줄은 4행을 잴 수 있게 된 지금 거짓이다.
   */
  unreachable: [
    {
      id: "result-draw",
      label: "결과 🏳 무승부(§7.1 5번)",
      why:
        "`endInDraw()`는 **제안 SUGGEST_CAP(75)회 도달 + 더 공개할 공통 단서 없음**에서만 " +
        "열린다. 제안은 «방 안에서 · 자기 차례에 1회»뿐이라 최소 75턴이고, 각 턴마다 " +
        "주사위 이동으로 방에 들어가야 한다 — 왕복 수백 회(수 분). 도달은 가능하지만 " +
        "게이트 예산(전체 1분 이하)을 통째로 먹는다. 실측 발생률 0.0%(sim-balance)라 " +
        "우선순위도 가장 낮다. → 사람이 §사람 확인에서 본다.",
    },
    {
      id: "result-spectator",
      label: "결과 🏅 판 종료(§7.1 3′번 · **좌석 없는 관전자**가 보는 화면)",
      why:
        "좌석 없는 관전자는 «진행 중인 방에 7번째로 입장»해야 생긴다. 그런데 " +
        "`maxClients = MAX_PLAYERS = 6`이라 6인 실판에는 7번째가 들어갈 수 없고, " +
        "좌석을 비우려 한 명을 내보내면 `onLeave`가 그 자리를 **대리 NPC로 인계**해 " +
        "(clue-room.ts §8.1) 좌석 수는 그대로 6이다. 즉 «전원 사람 + 관전자 1»이 " +
        "성립하지 않는다. → 사람이 실기 2대로 확인한다.",
    },
    {
      id: "result-win-desktop",
      label: "결과 1번(사건 해결)·3번(최후의 1인)의 **데스크톱 뷰포트**",
      why:
        "한 판의 승자·생존자는 정의상 **1명**이고, 뷰포트는 탭 로드 시점에 정해진다 — " +
        "같은 판에서 두 뷰포트로 동시에 잴 수 없다. 지금은 폰(390×844)만 잰다: " +
        "44px 터치 하한이 걸리는 쪽이 폰이라 정보량이 크다. 데스크톱까지 보려면 " +
        "역할을 맞바꾼 3·4판이 필요하고, 그 비용(≈+15초)만큼의 새 정보가 없다 " +
        "(2번·4번은 이미 폰·데스크톱 양쪽을 잰다 — 같은 `.end-card` DOM이다).",
    },
  ],

  /** 기계가 **판정할 수 없는** 것. 실행할 때마다 인쇄한다. 지우지 마라. */
  human: [
    ["미(美)·균형", "여백·정렬·색의 조화는 기계가 못 잰다. 캡처 PNG를 열어 봐라."],
    [
      "읽히는가(남은 몫)",
      "**글자 크기 하한은 S5, 명암비는 S6이 이제 기계로 잰다.** 여기 남은 것은 그 둘로 " +
        "환원되지 않는 것들: 줄바꿈이 낱말을 쪼개는가 · 서체·행간이 답답한가 · " +
        "«잴 수 있었지만 판정 불가»로 빠진 글자(배경 이미지·캔버스 위 — S6이 건수를 인쇄한다) · " +
        "`:active`/`:focus` 상태의 색 — **`:hover`는 27회차부터 S9가 기계로 잰다**(마우스 뷰포트 한정). "
          + "손가락 화면은 `(hover: hover)`가 거짓이라 S9가 SKIP한다 — 실기에도 hover가 없으므로 그게 맞다.",
    ],
    ["실기 1회", "헤드리스는 실제 폰이 아니다. 주소창 접힘·노치·홈 인디케이터는 실기에서만 나온다."],
    [
      "스크롤바",
      "🔴 **이 게이트의 «폰»은 스크롤바를 실기와 다르게 그릴 수 있다.** 헤드리스 모바일 " +
        "에뮬레이션은 클래식 스크롤바를 그리고 **9px을 레이아웃에서 가져간다**(실측). " +
        "`[미확인]` 실기가 그러한지는 엔진마다 다르다 — **Android Chrome은 같을 가능성이 높고** " +
        "(Blink 문서: `::-webkit-scrollbar`에 `width/height`를 주면 오버레이가 클래식이 된다), " +
        "**iOS Safari는 `::-webkit-scrollbar` 자체를 안 그려 신호가 0일 가능성이 높다.** " +
        "둘 다 문헌 근거이고 **실기 측정이 아니다** — 초안이 «실기는 전부 오버레이»라고 " +
        "단정했다가 검수에서 뒤집혔다. 폭 방향은 안전하다(캡처가 실기보다 9px 좁게 잡는다). " +
        "«더 있다»는 신호가 실기 폰에 닿는지는 **사람이 두 기기로** 봐야 한다(플랜 41).",
    ],
    ["의도된 겹침", "겹쳐도 되는 것(모달·오버레이)은 화면별 기대값에 적어 뒀다. 그 목록이 지금도 맞는지 사람이 판단하라."],
    ["45초 동선", "가려지지 않았다는 것과 «심사자가 다음에 무엇을 눌러야 할지 안다»는 것은 다르다."],
    ["CSS로 넓힌 히트 영역", "이 게이트는 요소의 상자만 잰다. `::after`로 넓힌 탭 영역은 못 본다 — 반대로 상자가 작으면 실패로 나온다."],
    ["뷰2·3·4의 **캔버스**", "게이트는 뷰를 실제로 전환한 뒤 **HUD만** 잰다. 헤드리스 WebGL(SwiftShader)은 실기 GPU가 아니다 — 그림자·톤·아이소 정렬은 사람이 봐야 한다."],
    ["결과 화면 6종 중 남은 2행", "무승부(5번)·관전자(3′번)는 §측정 못 한 화면의 사유대로 여전히 사람 몫이다."],
  ],
};
