// 뷰 표기 강도 — **엔진 층.** "정보가 얼마나 죽어 보이는가"는 표현이 아니라 정보다.
// 4뷰가 같은 값을 쓴다(모션 프로파일 무관 · docs/design/20260727-view-contract-spec.md §1.2).
//
// 원래 `packages/shared/src/view-consts.ts`(미분류 층)에 있었다. **값은 한 글자도 바꾸지 않았고**
// 루트 배럴이 `engine/`을 그대로 재수출하므로 import 경로는 변하지 않는다.
//
// ── 이 파일에 무엇을 넣는가 — 판정 기준 4개 ──────────────────────
//   A 어휘 : 상수의 뜻을 **주제 명사 0개**로 진술할 수 있는가(계략·보드·장물·십이지가 필요하면 콘텐츠).
//   B 근거 : 두 번째 주제 문서(20260729-mafia-content-design)가 그 표기를 **명시적으로 재사용**한다고
//            적었는가. 그 문서는 재사용 자산을 §4.6 표와 §8.5 표에 **열거**했다 —
//            열거된 목록에서 빠진 것은 침묵이 아니라 **반대 증거**로 읽는다.
//   C 값   : 값이 «지각·기하 상수»(알파·px·불리언·무채색)인가, 아니면 «세계관 팔레트의 유채색»인가.
//            유채색은 주제가 바뀌면 바뀐다 — 개념이 엔진이어도 **색값은 콘텐츠**다.
//            그래서 `TOKEN_OUTLINE_PX`(두께)는 여기 있고 `BOARD.helperEdge`(색)는 콘텐츠에 있다.
//   D 방향 : 옮긴 뒤 `engine/`이 `content/`를 참조하게 되지 않는가(이 파일의 import 0).
//
// 판정이 애매하면 **엔진으로 올리지 않는다.** 콘텐츠에 잘못 둔 값은 두 번째 주제가 제 값을
// 정의하면 그만이지만, 엔진에 잘못 올린 값은 «엔진은 주제를 모른다»는 주장 자체를 거짓으로 만든다.

/**
 * 행동권을 잃은(= 제거된) 토큰의 불투명도. 토큰 **전체**(링·원·얼굴·이름표)에 적용한다.
 * 클루에서는 「고발 실패 탈락」, 마피아에서는 「밤 지목·처형 제거」 — 규칙은 다르지만
 * **표기는 같다.** mafia-content-design §4.6: 「제거 = `ELIM_ALPHA` + 2중 표기 계약 그대로」.
 */
export const ELIM_ALPHA = 0.35;

/**
 * 제거 표기는 알파 단독 금지 — 회색조·저대비에서 사라진다.
 * 알파 + (파선 테두리 또는 ✕ 오버레이) 2중 표기가 계약이다.
 *
 * 두 번째 주제가 **그대로 쓴다고 명시**한 유일한 표기 규약이다 —
 * mafia-content-design §8.5 자산표가 이 줄을 「🟢 엔진」으로 직접 판정했고(판정 배지 3상태에 적용),
 * §8.7-5(「색 단독 표기 포기」)가 같은 논리를 다시 인용한다.
 */
export const ELIM_NEEDS_SECOND_CUE = true;

/**
 * 모든 토큰 색면의 최소 아웃라인 **두께**(px) — view-contract-spec §4.1.
 * 팔레트가 밝은 배경과 대비 1.0~1.3까지 떨어지는 것을 **설계상 허용**하는 대신,
 * 배경 분리를 아웃라인이 전담한다. 「색이 못 하는 분리를 형태가 한다」는 점에서
 * `ELIM_NEEDS_SECOND_CUE`와 같은 규약이고, 팔레트가 바뀌어도 이 정책은 남는다.
 *
 * ⚠ 두께만 엔진이다. 무엇을 그 두께로 그리는지의 **색**은 팔레트(콘텐츠)가 준다
 *    — 예: `BOARD.helperEdge`.
 */
export const TOKEN_OUTLINE_PX = 2;
/**
 * 아웃라인 기본색 — **무채 최대명도**. 팔레트의 정체성 색이 아니라 대비 장치라서 엔진에 둔다.
 * (좌석 12색·보드 금색 같은 유채색은 콘텐츠 팔레트에 있다.)
 */
export const TOKEN_OUTLINE_COLOR = 0xffffff;

// ── 말풍선 가독 ──────────────────────────────────────────────
// 말풍선은 LLM 산출물이 **인월드에서 읽히는 유일한 지점**이다(improvement-roadmap §1.4).
// 잘리거나 배경에 녹으면 «AI를 어떻게 썼는가»의 증거가 화면에서 사라진다.
//
// 근거: mafia-content-design §4.6의 재사용 표가 계약 메서드 `bubble`을 **좌표 무관 재사용**으로
// 열거하고, §1.1이 뷰 크롬(HUD 인셋 등)을 「🟢 엔진 — 좌표 없이 그대로 산다」로 판정한다.
// ⚠ 두 상수의 원래 주석은 근거를 `view-contract-spec §1.6`으로 적었으나 **그 문서의 §1은 §1.5에서
//   끝난다**(§1.6은 존재하지 않는 앵커다). 위 두 근거로 대체해 판정했다 — 문서는 고치지 않았다.

/** 말풍선 테두리 **두께**(화면 px). 색은 팔레트가 준다(`BOARD.bubbleEdge`와 짝). */
export const BUBBLE_BORDER_PX = 2;
/** 말풍선이 화면 가장자리·HUD에서 떨어져 있어야 하는 최소 여백(화면 px). */
export const BUBBLE_SAFE_PAD_PX = 6;
