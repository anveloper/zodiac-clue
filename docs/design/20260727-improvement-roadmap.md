# 기획 개선 로드맵 — 마감 08-10 역산

> 작성 2026-07-27 · 사람용 `.html` 동봉 · 근거: 심사축·플레이경험·4뷰디자인 3개 관점의 실코드 검수
> 원칙: **진실값=규칙엔진 / LLM=표현 전용** 불변, **모든 디자인 개선은 뷰1~4 전부 대응**.

## 0. 결론 한 줄
남은 리스크는 기능 부족이 아니라 **① 미제작 필수 제출물(영상) ② 봇의 정보 우위 ③ 심사축 1위(AI)가 화면에 안 보임** 세 가지다.

---

## 1. 확정된 결함 (실코드 검수로 확인)

### 1.1 봇이 사람보다 더 안다 — **공정성·문서 신뢰도 동시 리스크**
- `clue-room.ts`의 `revealed`는 **룸 전역 Set**. 반증 카드가 여기 쌓이고 **모든 봇**이 후보 소거에 쓴다. 사람은 자기에게 보인 카드만 안다.
- 덱 18장·비정답 15장 기준 라운드당 봇 5명이 각 1장 노출 → **3~4라운드면 봇 지식 수렴**. 사람의 실제 행동 턴은 3~4번뿐.
- **AI 기술문서(제출물 ④)에 "공유 공개카드(revealed)"로 명시**돼 있어 심사자가 발견하기 쉽다 → "AI가 치트한다"로 읽힐 유일한 지점.
- 조치: `revealed`를 **공통 단서 전용**으로 축소, 봇별 `seen`(자기 손패+자기에게 보인 카드+공통단서)로 분리. 수렴 보전은 **공개 정보만 쓰는 추론**으로 대체 — "패스한 사람은 그 3장을 안 가짐"(`notHolds`)은 사람도 로그로 도달 가능하므로 비대칭이 아니다.
- 무한 지연 방지: 총 제안 상한(예 40) 초과 시 결정론적으로 공통 단서 1장 추가 공개.

### 1.2 "패스"가 로그에 없다 — 사람 쪽 정보 결손
- `doSuggestion`은 첫 매치에서 `🛡 X 반증`만 브로드캐스트하고 return. **반증 못 한 앞 순서 플레이어들이 로그에 남지 않는다.**
- 정통 클루에서 "누가 못 냈는가"는 카드 내용보다 중요한 공개 정보. 지금은 턴 스트립과 대조해 머릿속 역산이 필요.
- 조치: `suggestLog { sid, by, suspect, weapon, room, passed[], disprovedBy }` 브로드캐스트 + **제안 기록표 패널**(행=제안, 열=6인, 셀=`·`패스/`🛡`반증/나에게 보인 카드). 전부 공개 데이터만 사용 → 진실값 누출 0.
- `onMessage("disprove")`에 증거노트 자동 `cleared` 기록 추가 — 현재 **부수 기능인 `peek`(계략)만 자동 기록되고 주 정보원인 반증은 로그 한 줄로 끝난다**(역전 상태).

### 1.3 심사축 1위(AI)가 화면에 없다 — ✅ **07-28 해소**(§7.7 · ④ §4)
- LLM 기여는 `broadcast("say", {id, from, text})` 한 줄. **LLM인지 캐시인지 규칙 폴백인지, 몇 ms 걸렸는지 아무도 모른다.**
- 조치: `narrate()` 반환을 `{ text, source: "llm"|"cache"|"fallback", ms, model }`로 확장 → `speak()`가 메타 동봉 브로드캐스트 → **AI 디렉터 패널**(우측, 기본 접힘, `?ai=1`이면 펼침)에 `LLM n · 캐시 n · 폴백 n · 평균 Xs`와 최근 발화 로그. 로그 배지 `✨`(llm)/`♻`(cache)/`⚙`(fallback).
- **경계 확인**: 추가 필드는 전부 "문장이 어느 경로로 왔는가" 메타데이터이며 게임 상태가 아니다. 진실값 경계 불변.
- 말풍선은 3개 렌더러가 **이미 동일한 `showBubble(id, text)` 계약** → 배지를 문자열 접두로 넣으면 4뷰 자동 대응.
- 📌 **07-28 구현 시 정정 2건**: ① `?ai=1` 게이팅은 폐기됐다(§7.7). ② **말풍선에는 배지를 붙이지 않았다** — 서버가 `bubbleLifeMs(line)`으로 턴을 붙들고 클라가 같은 함수로 말풍선을 재는데 접두를 붙이면 두 계산이 다른 문자열을 보게 되어 4뷰 계약 §1.3 정합이 다시 깨진다. 배지는 **로그 줄과 AI 패널에만** 붙는다.

### 1.4 NPC 페이싱이 필수 제출물(영상)을 막는다
- 봇 1턴 = `npcDelay()` + `BOT_ACT_GAP`(1300) + `SPEAK_HOLD`(2400). 기본값 기준 **봇 5명 = 내 턴 사이 33.5초**, 사람이 20초 숙고하면 상한에 걸려 **53.5초**.
- EMA 결합이 역방향: **오래 고민하는 플레이어를 가장 심하게 벌준다.**
- 조치: 규약("사용자 평균의 절반")은 유지하되 **라운드 예산 상한** `min(현재값, 12000 / 남은 봇 수)` 도입. `SPEAK_HOLD` 2400→1400, 직전 타겟과 같은 방이면 카메라 팬 생략·봇→봇이면 `CAM_SWITCH_DELAY` 900→400. `[⏩ 빠르게]` 토글(촬영용).
- **`TYPE_MS`(55ms 타자기)는 건드리지 않는다** — NPC 대사가 AI 활용의 유일한 가시적 증거다.
- ⚠️ 이 변경은 `CLAUDE.md`·AI 기술문서의 "NPC 딜레이 = 평균의 절반" 규약 문구를 바꾼다 → **코드와 문서를 같은 커밋 묶음으로** 처리해야 심사 시 불일치가 없다.

### 1.5 첫 60초에 규칙을 이해할 방법이 없다
- 튜토리얼·도움말 오버레이 **0건**. 제공되는 것은 로그 1줄과 하단 키 안내뿐.
- `내 단서 패: 잡채, 서재, 뱀 무녀` — **"내 카드는 정답이 아니다"**라는 추리의 출발점이 화면 어디에도 없다.
- 제안 모달이 `needRoom=false`라 **3요소 중 장소를 안 보여준 채 확정**시킨다(서버가 `player.room`에서 조용히 가져감).
- 비활성 사유 안내(`addLog("방 안에서만…")`)는 **disabled 버튼이라 클릭 이벤트가 안 떠 영원히 실행되지 않는 죽은 코드**다.
- 조치: 목표 카드 3줄 오버레이(1회, `?` 재열람, `?demo=1` 스킵) · 손패 라벨에 "이 3장은 정답이 아님" 명시 · 제안 모달에 `장소: <현재 방> (고정)` 읽기전용 행 · 버튼 4종 동적 `title` + 턴 배너 부제 · (P1) 도달 가능 칸 하이라이트(`reachable()`를 `packages/shared`에 1개만 두고 4뷰 공유, **방은 통째로 칠할 것** — 방 안 이동은 무료라 부분 채색은 오해를 부른다).

### 1.6 심사자 진입 장벽
- 랜딩 → 방 만들기 → 대기실 → 캐릭터 선택 → 시작, **4단계**. 계정도 친구도 없는 심사자에게 요건 ①의 "플레이 가능"은 사실상 이 통과율이다.
- 조치: `[혼자 바로 시작 (NPC 5명)]` 버튼 + `?solo=1` 딥링크. **서버 변경 0** — 기존 `create`→`character`→`start` 메시지 3개 조합.

---

## 2. 4뷰 대응 디자인 (제약 1 준수)

### 2.1 뷰 전환이 "개발 진화"를 전달하지 못한다
- 전달 경로가 좌하단 버튼 라벨 하나뿐 → 심사자에겐 **그래픽 품질 옵션**으로 읽힌다. 왜 항상 뷰1에서 시작하는지도 설명이 없다.
- 조치: `Stage` 타입에 `date·oneLine·tech·commit` 추가(**실제 커밋 해시**가 "개발 과정"이라는 주장의 증거성) → 전환 시 1.1초 인터스티셜(`#diceOverlay` 패턴 복제) + 드롭다운을 **연표**로(날짜·한 줄 설명·`에셋 0`/`에셋 44` 배지).
- 인터스티셜 문구는 자랑이 아니라 **기술 선택의 이유**로("에셋이 0인 상태로 규칙부터 검증했다") — "게임성 부족의 우회"로 읽히는 것을 방어.
- **뷰별 틴트만 다르게**: 뷰1 `#2a2118`(복도색·각진 테두리) / 뷰2 `#1c1712`(CSS perspective 기울임) / 뷰3 동일 틴트+금색 광채 / 뷰4 `#5b8c4a`(grass1·3px 계단 픽셀 보더).

### 2.2 소환이 시각적으로 존재하지 않는다
- 서버는 `target.x/y`만 바꾼다 → 클라는 일반 이동과 구분 못 해 **110ms tween**. 뷰2·3은 장물이 **보간 없이 매 프레임 스냅**(사실상 버그).
- 조치: `fx/fx-bus.ts` — 상태 델타로 판정(체비셰프 2칸 이상 점프 = `warp`), `FxRenderer` 계약(`warp/lootWarp/focusRoom/pulseCell`)을 3개 렌더러가 구현. 카메라 포커스는 뷰1에 구현하면 **뷰4는 카메라 미러링이라 자동 상속**, 뷰2·3만 별도.
- 게이팅: 카메라 포커스는 **내 턴이거나 내가 지목당했을 때만**, 나머지는 배너+펄스만(NPC 6인이 매 턴 제안하므로 과하면 화면이 계속 흔들린다).

| FX | 뷰1 | 뷰2 | 뷰3 | 뷰4 |
|---|---|---|---|---|
| 소환 | `Arc` 잔상 링 3겹 + 금색 사각 펄스, tween 110→420 `Back.easeOut` | `RingGeometry` 펄스 + y축 포물선 호 이동 | 뷰2 + 도착 시 `PointLight` 0.4초 | **사각 프레임 3겹** 확대(도트 문법) + 4방향 화살표 도트 |
| 공통 | 중앙 상단 `#fxBanner` "🐍 뱀이 서재로 불려왔다" 1.2초 — diceOverlay와 동일 레이어 패턴이라 4뷰 공통 보장 | | | |

### 2.3 뷰4가 뷰1보다 정보량이 적다 (진화 서사에 정면 위배)
- 십이지 12종을 **색 6개 순환**으로만 구분, 장물 6종은 **전부 동일한 상자**, 계략 NPC는 **회색 크리터·마크 없음**.
- 조치(전부 코드 생성, 외부 에셋 0 = 뷰4 정체성 유지): `pixel-glyphs.ts` — 귀 4형 × 볏 2형 × 꼬리 3형 × 색 6종으로 12종 구분 / `LOOT_DOTS` 8×8 문자열 6벌을 `generateTexture` / 계략 NPC는 **실루엣은 십이지대로, 색만 회색** + 머리 위 3×3 카드 마크.
- 축소판(1~2h): 머리 위 3×3 도트 문장 배지 12종 + 장물 스탬프 6종만 → 정보 결손의 8할 해소.
- **근거**: 제약 1을 "동일 정보가 4뷰에서 동등하게 읽히는가"로 해석하면 뷰1·2·3은 이미 충족이고 뷰4만 미달이다. 뷰4를 끌어올리는 것이 곧 4뷰 대응이며, 뷰1~3에 손대면 오히려 균일성이 깨진다.

### 2.4 뷰3가 "에셋 대기"가 아니라 역효과 상태
- `public/assets/**` **44/44 전부가 우상단에 `PLACEHOLDER` 배지를 그려 넣은 SVG**이고 내용물도 결국 같은 이모지. → 뷰2→뷰3 전환이 **"이모지가 원판에 갇히고 도장이 찍히는" 역방향 연출**이다.
- 조치 1 (**10분·최고 ROI**): `scripts/gen-placeholder-assets.mjs`의 배지 블록 제거 후 44개 재생성.
- 조치 2 (P1): 뷰3의 차별점을 **텍스처 → 렌더 품질**로 재정의. 현재 조명은 Ambient 0.85 + Directional 0.6뿐(그림자·포그 없음). `setAssets(true)`일 때만 **발밑 접지 데칼**(빌보드라 실그림자가 안 맞음 → 비용·품질 모두 유리) + `THREE.Fog`(42° 원근의 깊이가 처음 읽힌다) + 문기둥 `emissive` 등불 + `CanvasTexture` 야간 배경.
- PNG 아트 도착 시 같은 경로 덮어쓰기만으로 자동 승격(기존 `assetSprite` 폴백 구조 유지).
- ⚠️ 뷰3 정의 변경은 `docs/plans/active/12-view-evolution-stages.md`의 단계 표와 어긋난다 → 문서 동기화 필수.

### 2.5 뷰 간 표기 계약 부재 (뷰5 추가 시 어긋남이 5개로 늘어남)
- 실측된 불일치: **뷰2·3 탈락 표현이 face 스프라이트 알파만 바꾸고 disc·ring·이름표는 그대로** → 탈락자가 살아있는 것처럼 보인다(명백한 버그). 뷰4 말풍선은 **타자기 없음·3200ms**(다른 뷰는 55ms·2600ms). `CAM_SWITCH_DELAY 900`이 두 파일에 중복 정의.
- 조치: `view-consts.ts`(TYPE_MS·BUBBLE_HOLD_MS·WARP_MS·ELIM_ALPHA·CAM_SWITCH_DELAY·MOVE_TWEEN_MS) + `view-contract.ts`(`setCurrent/setElim/warp/lootWarp/bubble/identity`)를 3개 렌더러가 `implements` → **타입 시스템이 4칸 채우기를 강제**.
- **계약은 *정보*만 강제하고 *표현*은 자유로 둔다** — 과하게 조이면 "진화"가 아니라 "같은 게임의 스킨 4개"가 된다.
- ✅ **07-28 완료** — `packages/shared/src/view-consts.ts` + `apps/client/src/scenes/view-contract.ts`, 렌더러 3종 전부 `implements ViewContract`(15메서드). 뷰2·3 탈락 알파 버그와 뷰4 `3200` 인라인은 이때 함께 해소됐다.

#### ✅ 07-28 추가 — 비밀 통로 표기를 상수로 (커밋 `41c7a52`)
통로가 켜지자 새 결함이 생겼다: **보드를 가로지르는 대각선이 방·토큰보다 먼저 눈에 들어와 판을 읽는 것을 방해한다**(사용자 피드백). 선을 지우면 정보가 사라지고 켜두면 판을 가린다 → **평소엔 잠그고 필요할 때만 연다.**

| 상수 | 값 | 역할 |
|---|---|---|
| `PASSAGE_ALPHA_IDLE` | **0.09** | 기본 밝기. "있다"만 알리고 시선을 뺏지 않는 하한 |
| `PASSAGE_ALPHA_HOVER` | **0.75** | 커서가 근처일 때 드러나는 밝기 |
| `PASSAGE_HOVER_PX` | **34** | 커서–선 **화면 거리** 임계 |
| `PASSAGE_FADE_MS` | **160** | 전환 시간. 툭 켜지면 그것대로 시선을 뺏는다 |

- 4상수 전부 `packages/shared/src/view-consts.ts` 단일 소스 — 4뷰가 같은 값을 본다(§2.5의 취지 그대로).
- **임계를 뷰마다 다르게 재는 것이 핵심**이다. 뷰1·4는 임계를 **줌으로 나눈다**(`PASSAGE_HOVER_PX / zoom`) — 안 그러면 축소했을 때 화면상 먼 선이 켜진다. 뷰2·3은 42° 피치라 월드 거리가 화면 거리와 무관하므로 **선의 양 끝점을 화면에 투영해** 잰다.
- 정보량은 그대로다(선은 항상 있다). 바뀐 것은 **주목의 우선순위**뿐이라 §5 매트릭스의 "통로 표기 있음"은 유지된다.

---

## 3. 제출물 ② 영상 — 촬영이 아니라 설계
5종 중 **유일한 미제작 필수 항목**(누락 시 심사 제외)이고 심사자가 **끝까지 보는 유일한 산출물**이다. 현재 페이싱으로는 45초 창에 "복도 걷는 장면"만 담기기 쉽고, **"실제 플레이 화면만·AI합성 금지"** 조항 때문에 편집으로 메울 수 없다.

콘티(55초 목표) → `docs/submission/20260728-video-storyboard.{md,html}`로 별도 작성:

| 구간 | 내용 |
|---|---|
| 0-4s | 랜딩 → `[혼자 바로 시작]` → 즉시 진입. 자막 "설치·로그인 없이 즉시 플레이" |
| 4-10s | 2d6 → 방 진입 → `[제안]` → 반증 로그 |
| 10-22s | NPC 대사 말풍선 2개(성격 다른 캐릭터) + AI 디렉터 패널에 `[LLM 0.9s]` 배지가 찍히는 순간. 자막 "결정=규칙엔진 / 대사=LLM" |
| 22-30s | `[계략]` → 귓속말 + `peek` 로그 + 증거노트 자동 제외 체크 |
| 30-42s | **뷰1→2→3→4 4연속 전환.** 자막 "개발 진화 과정을 게임 기능으로" |
| 42-55s | 고발 성공 → 결과 오버레이 → AI 누적 카운터 클로즈업 |

- 촬영 보조는 **표시 계층만**(`?ai=1` 패널 펼침, `?rec=1` 폰트·패널 폭 확대). **주사위·정답·NPC 판정은 절대 손대지 않는다** — 요건 위반이며, 이 경계를 콘티에 명시하는 것 자체가 성실성 증거다.
- **안전 테이크 우선**: 08-03까지 선행 개선이 안 끝나면 현 상태로 먼저 1테이크 녹화·업로드해 요건 충족을 확보하고 이후 재촬영한다.

## 4. 제출물 ④ 승격 — "구현 설명" → "에이전트 설계·디렉팅 명세"
현재 ④는 66줄이고 **환각 검증이 예시 2줄, AI 도구 사용내역이 2줄**이다. 이 프로젝트는 실제로 `docs/plans/` 기반 에이전트 워크플로로 만들어졌는데 그 증거가 제출물에 없다 — 심사축 1위에 대한 **최대 미회수 자산**.
- `scripts/eval-narrator.mjs`: 12캐릭터 × 3액션 = **36케이스** 결정론 검사 — ①1줄·12~40자 ②따옴표/마크다운/머리말 없음 ③프롬프트로 준 결정값이 문장에 보존(표현이 진실값을 바꾸지 않았는지) ④**금지어 누출**(정답·봉투·손패 + 프롬프트에 없던 카드 라벨 등장) ⑤`source`·`ms` 분포 → `docs/design/eval-narrator-report.md` 자동 생성.
- ④에 3개 절 추가: **디렉팅 명세**(입출력 계약·페르소나 12종 표·폴백 결정 트리) / **환각 검증 프로토콜**(위 5개 검사 + 실측 수치) / **AI 도구 사용내역(실증)** — 도구·용도·산출물 경로·**사람 검토 지점** 표. 근거: 커밋 100+, `docs/plans/` active→done 이관 규칙, 서브에이전트 병렬 검수로 스코프 정정한 사례, 15분 세그먼트 스프린트.
- 프레이밍은 "AI가 짠 코드"가 아니라 **"AI를 어떻게 지휘·검수했는가"**.
- 디렉팅 명세에 **"4뷰 전부가 동일한 `showBubble(id, text)` 계약으로 표현층이 렌더러에 독립"**을 1줄 넣으면 표현/결정 분리가 렌더 계층까지 일관된다는 추가 논거가 된다.
- eval은 무료 쿼터 36콜을 먹으므로 캐시 무시로 1회만 돌리고 리포트를 커밋.

---

## 5. 실행 순서 (D-14)

| 일자 | 항목 | 근거 |
|---|---|---|
| 07-27 | PLACEHOLDER 배지 제거(10분) · 뷰2·3 탈락 알파 버그(30분) | 영상에 그대로 찍힌다 / 명백한 오작동 |
| 07-27~28 | 원클릭 솔로 진입 + 온보딩 1~4항 + **영상 콘티** | 최소 비용으로 "60초 이해" 확보 |
| 07-29~30 | NPC 페이싱(라운드 예산) + 뷰 인터스티셜·연표 | 영상 촬영 가능성 + 서사 축 |
| 07-31~08-01 | FX 레이어(warp·배너·포커스) → **안전 테이크 녹화·업로드** | ②요건 확보 |
| 08-02~04 | 정보 대칭성(`revealed` 분리) + 제안 기록표 + 솔로 10판 밸런스 실측 | 가장 깊은 개선, 여유 필요 |
| 08-05~07 | AI 디렉터 패널 + eval 스크립트 + ④ 문서 승격 | 심사축 1위 회수 |
| 08-08 | 본 테이크 재촬영, 뷰4 도트 아이덴티티, `docs/index.html`·체크리스트 갱신, `.html`→PDF | |
| 08-09 | 예비일 | |

**밸런스 주의**: ~~정보 대칭성과 계략 강화를 동시에 넣으면 반전될 수 있다.~~ → **틀린 쌍을 지목했음(§7.5).**
계략은 밸런스 레버가 아니다(시뮬 측정 **+1.8%p**, 노이즈 수준). 진짜 레버는 **공통단서 장수(+21%p) · 즉시고발권(+10%p) · 재진입 규칙(+14%p)**이다.
그리고 **1.1과 1.2를 같은 스프린트에 넣지 말 것** — 동시 투입 시 승률이 68~76%로 반대편 극단이 된다.

---

## 6. 지금 하지 않는다 (시간 대비 효용 낮음)
1. **뷰3 실아트 PNG 90장 생성** — placeholder가 이미 로드되고 실패 시 이모지 폴백이라 뷰3은 깨지지 않는다. 수일이 드는데 심사축 3개 어디에도 직접 기여하지 않으며, "그래픽 퀄리티"는 채점 항목이 아니다. 오히려 **절차 생성 렌더(뷰1·뷰4, 외부 에셋 0)가 "AI 시대의 에셋 전략" 서사로 더 설득력 있다.**
2. **계략 종류 다양화(소환술·추궁·발빠르게) 및 뷰5 3D** — 서버·클라·4뷰 렌더·문서 4곳을 전부 건드려 회귀 위험이 크고, 심사자는 계략이 1종인지 4종인지 45초 영상에서 구분하지 못한다. 본행사용 백로그로.
3. ~~**⑤ 팀원 롤 기술서** — 개인 참여면 불필요하니 "해당 없음"으로 확정 표기.~~ → **반증됨(§7.2 참조).** 상류 회의록에 **3인 팀**이 명시돼 있다. 이 결정은 AI가 임의로 내릴 수 없다.

---

# 7. 정정·추가 (2차 검수)

> 라이브 서버 실접속 · 규칙 시뮬레이션 1만 판 · 렌더 코드 재검수로 얻은 결과.
> **§1~6의 판단 2건이 반증됐다(7.2 · 7.5).**

## 7.1 🔴 배포된 빌드에서 LLM이 한 줄도 동작하지 않는다 — 제출물 ④ 전체가 검증 불가
라이브 서버(`wss://141-147-157-219.sslip.io`)에 직접 접속해 솔로 1판을 돌린 결과, **NPC 대사 5/5가 전부 `fallbackLine`** 이었다.
- `narrator.ts`의 접미 `…아니라니 하나 지웠군`이 **5건 바이트 동일**(temperature 0.95 LLM에서 불가능), base 템플릿 4종이 그대로 재현.
- 지연 **0.2~0.6초**(LLM 경로 실측 ~1s, 타임아웃 4s) → 로컬 문자열 결합.
- 문법 오류 `떡시루**이** 사라졌던걸` — 조사 미처리. LLM이 낼 수 없는 결함.
게다가 폴백은 "캐릭터색 유지"를 목표로 설계돼 **의도적으로 구분 불가능**하다.

### ✅ 원인 규명·해결 (2026-07-28)
초판은 원인을 "`GEMINI_API_KEY` 미설정 또는 무료 쿼터 소진"으로 **추정했으나 둘 다 아니었다.**

| 검증 | 결과 |
|---|---|
| 모델 목록 조회(`GET /v1beta/models`) | `200` · 56개 → **키 정상** |
| `gemini-flash-lite-latest` + `thinkingConfig.thinkingBudget: 0` | **`400` INVALID_ARGUMENT** ← 당시 코드 |
| 동일 모델, `thinkingConfig` 미지정 | `200` · 정상 대사 (실프롬프트 5/5) |
| `thinkingBudget: -1`(동적) | `200`이나 생각 토큰이 64 예산을 먹어 **문장 잘림** |
| `gemini-2.5-flash-lite`로 고정 | `404` 제공 종료 |

**진짜 원인**: `-latest` 별칭이 **`thinkingBudget: 0`을 거부하는 세대로 이동**했다. 그 400이 `if (!res.ok) return null`로 흡수돼 전 대사가 폴백으로 나갔다.
코드 주석("flash 계열은 thinking 모델이라 꺼야 빈 응답을 막는다")은 **이전 모델에선 옳았지만 지금은 파라미터 자체가 무효**다.
**발견이 늦은 이유**: 실패 로그가 `[narrate] HTTP 400`으로 상태코드만 남겨 **사유가 어디에도 없었다.**

- **조치 완료**: `thinkingConfig` 제거 + 실패 로그에 응답 본문 포함(`477c88b`). 배포 후 라이브 재검증 **12/12 LLM · 폴백 0**.
- ~~남은 조치~~ → **07-28 전건 완료**: ① `GET /health` → `{ai, model, llmCalls, fallbackCalls, quotaErrors}` + `cache{size,max,lookups,keyFields}`·`normalizeTiers` ② §1.3 계측 P0 승격·`?ai=1` 게이팅 제거(7.7) ③ 429 수신 시 `quotaErrors` + `reason: "http"` 표기.
- **교훈**: 폴백이 조용하면 장애도 조용하다. 관측 가능성(§4)은 편의 기능이 아니라 **이 장애를 두 달 놓치지 않기 위한 조건**이다.

### 🔴 7.1.1 07-28 2차 — 폴백이 **22%로 되살아났다** (해소) — 커밋 `6d3a8a9`
§7.1을 고친 뒤 계측을 붙이고 라이브 1판을 완주시켰더니 **NPC 대사 5개 중 1개가 조용히 규칙 템플릿으로 대체**되고 있었다: `fallback 9/41` · **사유 전부 `toolong`**. §7.1과 같은 함정이다 — 폴백이 캐릭터색을 유지하도록 잘 만들어져 **육안으로는 구분되지 않는다.** 계측이 없었으면 이번에도 몰랐다.

**원인은 계측이 이미 갖고 있었다.** `ops {drop: 9}`인데 `truncate`가 **0건**. 프롬프트가 "한 문장만"을 요구하니 종결부호는 문장 맨 끝에 하나뿐이고, 원문이 상한을 넘겼다는 말은 곧 **그 유일한 경계가 상한 밖**이라는 뜻이다 → **1순위 절단은 원리적으로 발동할 수 없었고** 초과분 100%가 폐기로 떨어졌다.

| 조치 | 내용 |
|---|---|
| ① **목표 길이 인하** (상한 40 불변) | 관측 초과율 1.25배에서 역산해 `LINE_MAX/1.25 = 32`자, 제안은 페이싱 예산 때문에 25자. **모델은 글자를 못 세지만 어절은 세므로** "어절 6개 이하" 앵커 + 실제 길이 예시 3개를 함께 준다. **규약을 느슨하게 한 것이 아니라 지키기 쉬운 목표를 준 것** |
| ② **3단 절단** (문장 → 절 → 어절) | 미완결은 `…`로 닫는다. 어절 중간은 어느 단계에서도 안 자르고, 공백 없는 덩어리는 **여전히 폐기**. 결과에 `truncate`가 남아 준수율 분자에서 빠진다 — **통과시킨 게 아니라 못 지킨 사실을 기록한 채 살린 것** |
| ✗ 상한 인상(40→50) | **기각.** 봇 1턴 +550ms → **판당 +15.4초**. §7.5.4가 어렵게 회수한 페이싱을 도로 뱉는다 |

- **실호출 16콜 검증**: 폐기 **22% → 0%** · 최대 원문 **50 → 34자** · 준수율 **0.78 → 1.00**. ⚠️ **표본 8건이라 "한 자릿수 확정"은 아니다** — 한 판 완주 후 `/health` 재확인이 필요하다(체크리스트 §3).
- ⚠️ **1차 시도가 C4를 깼고 실측이 그것을 잡았다.** 짧게 쓰라는 압력으로 "세 요소를 다 읊지 말라"를 넣었더니 라벨을 **하나도 안 넣는** 응답이 나와 ④ §3 **C4(진실값 유지)가 6/8로 떨어졌다.** "최소 하나는 준 글자 그대로"로 못 박아 **8/8 복구**. 프롬프트 한 줄이 진실값 계약을 무너뜨릴 수 있다는 것을 문서가 아니라 계측이 보여줬다.
- **캐시 0%는 고장이 아니다.** 키가 진실값 조합이라 키 공간 **15,552** 대 판당 발화 **30** → 기대 충돌 **0.03건**. 적중률을 올리려면 3요소를 키에서 빼야 하는데 그러면 캐시된 문장이 **다른 제안의 라벨을 말한다**(C4 위반) → **의도적으로 올리지 않았다.** `/health`에 `cache{size,max,lookups,keyFields}`·`normalizeTiers`를 추가 — `hits:0`만으로는 "고장"과 "칠 일이 없었다"가 구분되지 않는다.
- **회귀 방어**: eval에 전제조건 **P6**(절단 구제 불변식) 신설. 합성 코퍼스 16종으로 **이전 규칙 생존 0/16 → 현재 16/16.** 🔴 **이 코퍼스는 실측 표본이 아니다**(폐기된 원문은 저장하지 않는다) — 재는 것은 "관측된 *형태*에 대한 구제율"이다.
- 근거: ④ §2.3 · §2.5 · §3

## 7.2 🔴 ⑤ 팀원 롤 기술서 — "개인 참여" 확정은 **심사 제외 위험**
상류 문서가 정반대를 말한다.
- `planning/meetings/20260714-first-meeting-agenda.md` — "대상: **팀 전원(anveloper / 2jonghun / hwi2924)**", "**3인 팀이므로 겸임 전제**", R&R 안건("제출물 5종 담당자 지정")이 통째로 존재.
- `20260718-meeting-notes.md`도 3인 명시. `20260715-*.md` 2건은 "요청: **2jonghun**", `20260714-game-ideas.md`는 "제안: 2jonghun".
- 상금이 **팀 단위**(`20260714-nhn-analysis.md`).
- 솔로의 유일한 근거는 커밋 로그(102/102 anveloper)뿐인데 **단독 커미터 ≠ 개인 참여**다.
→ 신청서를 팀으로 내고 ⑤를 생략하면 나머지 4종과 무관하게 **탈락**한다.
- 조치: 체크리스트 ⑤를 "해당 없음"으로 지우지 말고 **`[!] 신청서 제출 형태(개인/팀) 확정 필요 — 담당: 사람, 기한 08-03`**로 남긴다. 그리고 **⑤ 초안을 미리 써둔다(30분)** — 개인 확정 시 지우면 그만. **비대칭 리스크(쓰는 비용 30분 vs 안 쓰는 비용 탈락)** 앞에서 "판단 비용 제거" 논리는 성립하지 않는다.
- 부수: `planning/README.md`가 폐기된 `20260716-onepager-autoplay-director.md`(07-18 보류 확정)를 보류 표시 없이 나열 → 색인만 읽으면 살아있는 스코프로 오독된다.

## 7.3 🔴 PDF 인쇄 파이프라인 전면 실패 — 11개 HTML 전부 `@media print` 0건
`grep -rln "media print|print-color-adjust|@page" docs/` → **0건**. 전 문서가 다크 테마 고정이다.

| 요소 | 인쇄 결과 |
|---|---|
| 본문 `#f0e9dc` on `#16130f` | 배경 그래픽 끄면 **크림색 글자 / 흰 종이 = 판독 불가** |
| ③의 CTA 3개 `color:#fff` | **흰 글자/흰 배경 = 완전 소실**(플레이·GitHub·문서 버튼이 백지) |
| h1·h2 `--gold` | 판독 불가 |
| `<code>`·표 헤더 배경 | 배경 소실 → 글자 소실 (④의 표 2개 헤더 포함) |
| 링크 | 앵커 텍스트만 남고 **URL 소실**. ③의 ④ 문서 링크는 상대경로라 PDF에서 죽은 링크 |
| `docs/index.html` | `overflow:hidden` + iframe → **인쇄 시 백지 한 장** |

- 조치: 공통 `@media print` 블록(라이트 팔레트 강제 + `a[href^="http"]::after{content:" (" attr(href) ")"}` + `break-inside:avoid` + `@page{margin:14mm}`)을 11개 HTML에 삽입, ③의 상대링크를 절대 URL로, 대시보드는 인쇄 시 안내 문구만.
- **인쇄 검증을 실제로 1회 수행할 것** — 헤드리스 `--print-to-pdf`로 뽑아 눈으로 확인. 체크리스트의 "PDF 인쇄만 남음"이 **가장 위험한 낙관**이다.

## 7.4 🟠 공개 문서 사이트가 심사자에게 자기 결함 목록을 배포 중
`curl .../docs/manifest.json` 실측 — `📤 제출물` 그룹에 **`submission-checklist.md`가 노출**된다(`gen-docs-manifest.mjs`가 짝 없는 `.md`를 포함). 그리고 ③의 CTA `문서`가 심사자를 **정확히 그 트리로 안내**한다.
심사자가 보게 되는 것: "**미제작**(직접 녹화 필요)", "**팀 구성·역할 정보 필요**", "**남은 액션(사용자)**", 심사계정 이메일.
- `plans/active/*.md`도 **HTTP 200** 실측. 이 로드맵도 커밋·푸시하는 순간 `design` 그룹에 자동 편입되어 "AI가 치트한다로 읽힐 지점" 같은 문장이 공개 게시된다.
- 조치: `gen-docs-manifest.mjs`에 `EXCLUDE=[/submission-checklist/, /improvement-roadmap/]`, `vercel.json`의 `cp -r docs`를 화이트리스트로 교체(`plans/`·`planning/` 제외).
- 단 **`logs/`와 `plans/done/`은 남긴다** — 이건 결함이 아니라 "AI 에이전트 워크플로로 개발했다"(§4)의 **증거물**이다. 성실성은 "무엇을 개선하겠다"(로드맵)가 아니라 "무엇이 개선됐다"(일지·커밋)로 보여준다.

## 7.5 🔴 밸런스 실측 — 사람 승률 **3.9%** (공정 몫 16.7%)
규칙을 그대로 포팅해 판당 1만 회 시뮬레이션한 결과.

| 설정 | 사람 승률 | 라운드 | 사람 행동/판 | 방문 방(9개 중) |
|---|---|---|---|---|
| **현행** | **3.9%** | 3.1 | 3.0 | 2.1 |
| 현행 + 완전논리 사람(상한) | 11.8% | 3.0 | 2.8 | 2.2 |
| + `revealed` 분리(§1.1) | 28.4% | 5.5 | 5.3 | 3.3 |
| + 즉시고발 + 재진입 | **31.0%** | 6.8 | 6.0 | 4.4 |

- **실력 구간 전체(3.9~11.8%)가 패배**다. 봇 좌석은 각 15~22%.
- **사람 승리의 100%가 "미반증 제안"(운)** 이고, 정보수집 정석 플레이는 승률을 32.7%→19.1%로 **떨어뜨린다**. 지금은 추리 게임이 아니라 **제안 복권**이다.
- **§1.1 수치 정정**: "총 제안 상한 40"은 틀렸다 — 분리 후 총 제안 p95 = **53~60**이라 상한 40은 정상 게임의 25%를 강제 종료시킨다 → **60**으로. 그리고 판이 3.1→5.5라운드로 길어지는 것은 부작용이 아니라 **이 개선의 핵심 효과**다("수렴 보전"이라 쓰면 나중에 되돌리고 싶어진다).
- **§1.2 재분류**: 제안 기록표는 **새 정보를 주지 않는다**(반증자·턴순서가 이미 브로드캐스트라 사람도 역산 가능) → 정보 대칭성이 아니라 **인지부하** 항목이다.

#### ✅ 07-28 구현 완료 — 실측 재현 (`scripts/sim-balance.mjs` · 3,000판 · 시드 20260728)

| 변형 | 사람 승률 | 평균 라운드 | 평균 제안 | 제안 p95 | 상한 도달 |
|---|---|---|---|---|---|
| ① 변경 전 | **2.3%** | 3.04 | 15.9 | 24 | 0.0% |
| ② 즉시고발권만(§7.5.1) | 4.0% | 3.00 | 15.7 | 23 | 0.0% |
| ③ `revealed` 분리+상한 60만(§1.1) | 15.4% | 5.72 | 31.5 | 53 | 1.1% |
| ④ **전부** | **22.3%** | 5.63 | 30.9 | 51 | 0.8% |

- 위 1만 판 표(3.9% → 31.0%)와 **방향·규모가 일치**한다. 이 시뮬레이터는 보드 이동·주사위·비밀통로·계략을 모델에 넣지 않아(모든 좌석이 매 턴 원하는 방에서 제안) 절대값이 다르다 — **비교용이지 절대 지표가 아니다.**
- **22.3%가 공정 몫 16.7%를 넘는 것은 과튜닝이 아니다.** 좌석별 승률이 `22.3 / 19.0 / 17.9 / 16.6 / 12.3 / 11.9`로 **턴 순서에 따라 단조 감소**한다 — 사람이 좌석 0(선공)이라 얻는 자리값이고 알고리즘·시야는 6좌석이 동일하다. §8.5 선공 셔플이 들어가면 균등해진다(현재 컷 항목).
- **상한 60이 실측으로 확인됐다** — p95가 51~53이라 60은 1% 미만에서만 작동한다. 40이었으면 정상 게임의 상당수를 잘랐다.
- 🔴 **부작용 — 페이싱이 지금 필요해졌다(§7.5.4).** 판이 3.0 → 5.6라운드로 길어져 현행 상수(`BOT_ACT_GAP 1300`)로는 **솔로 1판이 3.8분을 넘긴다.** 심사자 45초·영상 55초와 정면 충돌한다.
- 🔴 **새로 발견 — 서버·클라 말풍선 정합 붕괴.** 서버 `SPEAK_HOLD = 2400`인데 클라 `bubbleLifeMs()`의 최솟값이 **2600**이다. 40자 대사면 4800 vs 2400으로 2배 어긋나 **모든 대사에서 서버가 먼저 턴을 넘긴다** — 4뷰 계약 §1.3이 경고한 "말하는 도중 턴이 넘어가 카메라가 튄다"가 100% 재현 조건이다. `SPEAK_HOLD`를 공용 `bubbleLifeMs(text)`로 대체하는 것이 해법(shared 함수는 DOM 의존 없이 작성돼 서버가 import 가능).
- 🚧 **문안 미확정** — 상한 도달 무승부 종료 로그가 `20260727-ui-copy.md`에 없다. 임시 문구 + 코드 주석으로 표시해 뒀다.

#### 🔴 07-28 2차 실측 — 재진입 규칙(§7.5.2) 투입 후 · **판 길이가 목표를 넘었다**

| 변형 | 사람 승률 | 평균 라운드 | 제안 p95 | 방문 방/9 | 상한 도달 |
|---|---|---|---|---|---|
| ④ 즉시고발+분리+상한 | 22.3% | 5.63 | 51 | 2.41 | 0.8% |
| **⑤ ④ + 재진입 규칙** | **23.0%** | **7.22** | **65** | **5.11** | **16.1%** |

- ✅ **"보드가 장식" 결함은 해소됐다** — 방문 방 2.41 → 5.11/9(로드맵 예측 3.3→4.7보다 큰 개선). 승률은 +0.7%p로 사실상 불변이라 밸런스를 흔들지 않았다.
- 🔴 **제안 상한 60의 근거가 무너졌다.** p95가 51 → **65**가 되어 상한이 0.8%가 아니라 **16.1%의 판에서 작동**한다. 무승부는 여전히 0%(공통 단서 추가 공개로 수렴)라 판이 깨지지는 않지만, "60"을 고른 근거(p95 51~53)는 더 이상 성립하지 않는다. **재산정 필요 — 임의로 올리지 않았다.**
- 🔴 **판 길이 목표 미달(§7.5.4).** 페이싱 값을 로드맵대로 넣었는데도 **솔로 1판 4.4~5.7분**이다.
  - 봇 1턴 = `npcDelay 1600` + `BOT_ACT_GAP 600` + 대사 홀드 ≈ 5,190 → **약 7.6초**(실측 7.6~8.0)
  - 1라운드 ≈ 5봇 × 7.6 + 사람 8 ≈ **47초** × 7.22라운드 ≈ **5.7분**
  - **페이싱으로 번 3.1초를 말풍선 정합 비용 3.0초가 정확히 상쇄**했다. 딜레이를 전부 0으로 해도 대사 홀드만으로 하한이 3.1분이다.
  - §7.5.4의 "권장 3.6초/봇턴"은 `SPEAK_HOLD 1400` 전제인데, 그 값은 **4뷰 계약 §1.3(서버 홀드 ≥ `bubbleLifeMs` 최솟값 2600)과 양립 불가능**하다 → 두 문서가 충돌한다.
  - **남은 레버는 대사 길이 자체다.** 폴백 대사가 평균 **47자**로 `narrator.ts`의 시스템 프롬프트가 스스로 명시한 **"12~40자"를 넘는다**(④ §2 디렉팅 명세 위반). 대사를 규약 안으로 줄이면 `bubbleLifeMs`가 함께 내려간다. `BUBBLE_HOLD_MS 2600`·`TYPE_MS 55` 재검토도 같은 축.

#### ✅ 07-28 3차 — 대사 길이·상한 재산정으로 해소

| 항목 | 전 | 후 | 근거 |
|---|---|---|---|
| 폴백 제안 대사 | 47.1자(최대 71) | **21.3자**(최대 25) | ④ §2.3 규약 "12~40자". 홀드를 거는 것은 제안뿐이라 제안만 조이고 고발·계략은 40자까지 열었다 |
| `BUBBLE_HOLD_MS` | 2600 | **1200** | 2600은 뷰별 인라인 리터럴을 옮겨 적은 값이라 유도 근거가 없었다. 읽기 속도 12자/초에서 `(83−55)×40 = 1120` → 1200 |
| **봇 1턴** | **7.6초** | **3.8초** | 실측 80표본 평균 3,784ms(계산식과 11ms 오차) |
| 제안 상한 | 60 (도달 17.0%) | **75** (도달 0.1%) | 자연 분포 p99.5=74·최대 76. 80 이상은 자연 최대를 넘어 백스톱 구실을 못 한다 |

- 승률 23.0% → **21.5%**. 상한 60이 공통 단서를 뿌려 **사람을 돕고 있었다**는 뜻이다 — 여전히 공정 몫 16.7%를 넘는다.
- ⚠️ **남은 간극**: LLM이 켜져 있으면 Gemini 왕복(~1.2초)이 홀드 앞에 붙어 봇 1턴이 5.7초가 된다. 대사 길이가 아니라 `narrate()`를 await한 뒤 방송하는 **구조 비용**이며 캐시 히트 시엔 사라진다(④ §6.2).
- ⚠️ **`20260727-ui-copy.md` §8.2의 폴백 템플릿 확정 문안이 코드와 갈라졌다.** 같은 문서 §1.4가 "폴백 템플릿은 이 문서가 확정한다"고 못박고 있는데, 그 확정 문안이 ④ §2.3의 길이 규약을 71~79% 위반하고 있었다. **규약 쪽을 살렸다** — ui-copy §8.2는 코드 기준으로 갱신이 필요하다.

### 7.5.1 🔴 사람만 "제안한 턴에 고발할 수 없다" (P0 · 0.5h)
`handleSuggest`는 반증 직후 **무조건 `advanceTurn()`** → 사람은 봇 5턴 뒤에야 고발 가능. 반면 봇은 `SPEAK_HOLD` 타이머 안에서 **같은 턴에 고발**한다.
이 비대칭만으로 승률 7.3%→3.9%(사람 승리의 47% 강탈). **판의 10.3%에서 사람이 정답을 확정하고도 자기 턴이 오기 전에 진다.**
- 조치: `advanceTurn()` 제거 → `stepsLeft=0` + `suggestedTurn` 잠금 + `client.send("canAccuse")` → `[고발]`/`[턴 종료]` 선택. 30초 자동 종료 타이머. 서버 ~15줄 + 클라 ~10줄.

### 7.5.2 🟠 같은 방에서 무한 제안 가능 — 보드가 장식이다 (P0- · 2h)
재진입 추적 변수가 코드에 **없다**(정통 클루의 "방에 들어온 턴에 1회"가 통째로 누락). 최적 플레이 = 방 하나에 눌러앉아 매 턴 같은 방 제안.
실측 **사람이 판당 방문하는 방 2.1개 / 9개**, 제안의 27~35%가 직전 턴과 같은 방 → **2d6·9개 방·비밀통로 3쌍이 사실상 무의미**. 통로는 더 심각(`handlePassage`가 턴을 유지해 매 턴 제안 루프 가능).
- 조치: `suggestedIn: Map<playerId, room>`으로 같은 방 재제안 차단(봇 경로에도 동일 적용), 방을 벗어나면 해제. 도입 시 방문 방 3.3→**4.7개**.
- **함께**: 제안 모달이 `selectFrom(..., myCards)`로 **내 손패를 disabled 처리**하는데 서버는 허용한다 → 클루 최강 전술("내 카드를 지목해 반증자를 통제")이 **클라 1줄 때문에 봉인**돼 있다. 제안 모달만 `myCards` 인자 제거(고발 모달은 유지).
- 4뷰 표시: 뷰1 방 바닥 45° 해칭(alpha .18) / 뷰2·3 방 명패에 반투명 슬래시(기존 스프라이트 재사용) / 뷰4 방 외곽 3px 점선 도트 / 폴백은 `#suggest` `title` + 턴 배너 부제.

### 7.5.3 ✅ 봇은 수학적으로 오답 고발이 불가능하다 (P2 · 1.5h) — **07-28 구현 완료 (`1f55b28`)**
`eff()`가 빼는 것은 전부 비정답이라 **정답은 항상 후보에 남는다** → 봇 고발은 100% 정답, **봇 탈락 판 0.0%**. 탈락 위험은 사람만 진다.
- 조치: `BOT_NERVE[suspect]` 페르소나 상수 + 시드 RNG로 `combos<=6`일 때 확률적 도박 고발(결정론 유지 → 진실값 규약 불변). 승률 영향 ±1%p, **봇 탈락 판 0%→4~7%**. 호랑이는 성급히 고발하고 양 목동은 안 한다 → **"페르소나가 대사뿐 아니라 의사결정에도 반영된다"**가 제출물 ④의 강한 근거가 된다.
- ~~→ §2.5의 "뷰2·3 탈락 알파 버그"는 **솔로에서 절대 발현되지 않는다**(봇이 안 죽으니까).~~ → 🔴 **정정(07-28): 이제 발현된다.** 도박 고발이 들어가 **봇이 7.0%의 판에서 죽는다** → 솔로 심사에서도 탈락 표기가 화면에 뜬다. 그 버그가 §2.5 계약 도입 때 이미 고쳐져 있었던 것이 다행이지, 미수정 상태로 이 규칙을 넣었다면 "죽은 봇이 살아 있는 것처럼 보이는" 화면이 심사자에게 재현됐다.

#### ✅ 구현 (`packages/shared/src/cards.ts` `BOT_NERVE` · `apps/server/src/rules/bot-accuse.ts`)
- **발동 조건**: 미반증 고발·확정 고발이 불발했고 남은 후보 조합이 **6 이하**일 때만.
- **확률** `p = nerve × (1 / combos)`. `1/combos`는 **무작위로 하나를 골랐을 때 실제로 맞을 확률**(= 봇이 가진 확신도)이고 `nerve`는 그 확신도를 행동으로 옮기는 **페르소나 계수**다. 곱으로 두면 후보가 넓을수록 급격히 줄고, 성급한 캐릭터일수록 커지며, **임의 마법상수가 0개**다 — 두 항 모두 의미가 있는 값이다.
- **결정론 유지(④ §0.2-2)**: `Math.random()`은 **판 시드 발행 1회**에만 쓰고, 이후는 `(seed, 좌석, 제안순번, 용도)`를 **FNV-1a로 해시한 순수 함수**다. **스트림이 아니라 해시**를 쓴 이유는 이 서버가 타이머·LLM 왕복·재접속으로 호출 순서가 흔들려 **스트림 RNG로는 같은 시드로도 재생이 안 되기 때문**이다. 시드는 로그에 남고 `ZODIAC_SEED`로 되먹이면 같은 판이 재생된다.

**시뮬 5,000판**(`node scripts/sim-balance.mjs 5000` · 시드 20260728)

| 변형 | 사람 승률 | 봇 탈락 판 | 평균 라운드 | 제안 p95 | 상한 도달 |
|---|---|---|---|---|---|
| ⑥ 변경 전(상한 75) | 22.7% | 0.2% | 7.28 | 67 | 0.1% |
| **⑦ + 봇 도박 고발** | **21.6%** | **7.0%** | 7.20 | 67 | 0.1% |

- **승률이 −1.1%p로 내려간 것은 예측(±1%p)의 방향과 다르다.** 봇이 죽어 자리가 비는 이득보다 **봇의 조기 도박 승리가 사람 승리를 빼앗는 쪽이 컸다**(도박 고발 적중률 **23.7%** — 570회 중). 공정 몫 16.7%는 여전히 넘는다.
- **페르소나가 의사결정에 반영됐다는 실측** — 십이지별 도박 고발(봇 출전 100판당):

| 캐릭터 | 호랑이 | 게코 | 토끼 | 잔나비 | 말 | 황소 | 돼지 | 개 | 생쥐 | 뱀 | 닭 | 양 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `nerve` | 0.90 | 0.80 | 0.55 | 0.75 | 0.65 | 0.45 | 0.40 | 0.25 | 0.30 | 0.20 | 0.15 | 0.05 |
| 100판당 | **4.3** | 4.2 | 3.5 | 3.5 | 2.7 | 2.4 | 2.1 | 1.2 | 1.2 | 1.1 | 0.9 | **0.3** |

  순위가 `nerve` 순서를 따르며(토끼·잔나비, 개·생쥐 두 쌍이 소수점 반올림에서 동률) **최상 호랑이 4.3 vs 최하 양 0.3 = 14배**다. "호랑이 대감은 성급히 고발하고 양 목동은 안 한다"가 문서 주장이 아니라 **재현 가능한 측정값**이 됐다 → 제출물 ④ §1.2·§2.4의 근거.

### 7.5.4 페이싱 재계산 — §1.4로는 부족하다
| | 라운드 | 현행 6.7s/봇턴 | §1.4 적용 5.1s | 권장 3.6s |
|---|---|---|---|---|
| 현행 | 3.1 | 104s | 79s | 56s |
| 7.5.1~2 적용 후 | 6.8 | **228s** | 173s | **122s** |

판이 2배로 길어지므로 `BOT_ACT_GAP` **1300→600**, `npcDelay` 상한 **1600**을 추가해야 판당 총 3.8분에 들어온다. `TYPE_MS 55`는 그대로.
- **영상 영향**: 개선 후 고발 성공은 평균 **7라운드째** → §3 콘티의 `42-55s 고발 성공`을 1테이크로 담을 수 없다.

## 7.6 🟠 영상 콘티가 실행 불가능하다 — "편집 금지"는 과잉 해석
§3은 "실제 플레이 화면만·AI합성 금지"를 **컷 편집 금지**로 읽었으나, 조항이 금지하는 것은 **합성·타인 영상**이다.
55초 안에 랜딩→진입→제안→반증→계략→뷰 4연속 전환→고발 성공을 **원테이크로 담는 것은 불가능**하다(로드맵 자신이 "봇 5명 = 33.5초"라고 계산했다).
- 조치: 콘티를 **실플레이 클립 5~6개의 컷 편집**으로 재설계하고 각 클립 원본을 보관. 자막은 후처리 텍스트 오버레이임을 ③에 1줄 명시.

## 7.7 🟠 `?ai=1` 게이팅은 문제를 재생산한다 — ✅ **07-28 해소**
§1.3은 문제를 "AI가 화면에 안 보임"이라 진단해놓고 해결책을 **"쿼리 파라미터를 붙여야 보이는 접힌 패널"**로 설계했다. 심사자는 파라미터 없는 URL로 들어온다.
- 조치: **AI 카운터 칩(`✨LLM 12 · ⚙폴백 0`)은 기본 노출**, 상세 로그만 접힘.

#### ✅ 07-28 완료 — 그리고 "기본 노출"만으로는 부족했다 (커밋 `55297c0`)
게이팅은 제거했는데 **첫 LLM 호출까지 27초**가 걸린다. 45초 안에 판단하는 심사자가 계측을 **아예 못 보고 나갈 수 있다** — 게이팅을 지운 자리에 "늦게 뜸"이라는 같은 문제가 남아 있었다.
- **패널을 판 시작과 동시에 노출**한다. 다만 **`0·0·0`은 그리지 않는다** — 그건 서버가 하지 않은 주장("대사가 없었다")을 화면이 대신하는 것이다. **자리는 띄우되 값은 확정 문안의 `—` 관례로만** 말한다. 서버 빈 스냅샷의 `avgMs: 0`이 `평균 0.0s`로 새던 경로도 함께 막았다.
- **`✨`/`♻`/`⚙` 범례를 넣었다** — 기호만 있고 뜻이 없으면 계측이 아니라 장식이다.
- 턴 HUD가 보드 방 이름을 가리던 것을 좌표로 잡아 해소.

#### 🔴 07-28 후속 — 계측을 만들어도 **랜딩에서는 신호가 0이었다** (커밋 `c90c723`)
심사자 시선으로 라이브를 열어 본 결과 **첫 45초의 AI 신호가 정확히 0**이었다 — 랜딩 DOM에 `<a>` **0개**, "AI"·"LLM"·"Gemini" 문자열 **0개**. 심사 1순위 축이 AI인데 **잘 만든 계측을 아무도 못 보는 자리에 둔 셈**이었다(§7.4가 "무엇을 감출까"를 다뤘다면 이건 "무엇을 반드시 보여야 하는가"다).
- 랜딩 첫 화면에 경계를 밝힌다 — "NPC 대사는 Gemini가 실시간 생성 · **판정과 정답은 규칙 엔진만**" + 게임 화면에서 호출 수가 실시간으로 보인다는 안내.
- **링크는 배포본에 실제로 있는 경로만 건다.** 없는 링크를 거는 것은 그것대로 감점이다.
- 함께 잡힌 것: ④ §3·§10이 "생성한다"고 적은 `docs/design/eval-narrator-report.md`가 **실제로는 리포에 없었다.** 가장 강한 정량 주장(전수 112,752문장)의 근거 파일이 없으면 확인하러 온 사람이 못 찾고, 그 순간 **나머지 참인 주장까지 함께 의심받는다.** 이제 eval이 매 실행 갱신한다(`--no-report`로 끌 수 있다).

## 7.8 🟠 `rightInset()` null 가드 부재 — 패널을 숨기면 카메라가 붕괴한다 (P0 · 0.5h)
`game-scene.ts`/`iso-view.ts`의 inset 계산이 `window.innerWidth - rect.left`인데, 패널이 `display:none`이거나 하단 시트가 되면 `rect.left===0` → **inset = 화면 전체 폭** → 카메라가 절반만큼 어긋나 내 캐릭터가 화면 밖으로 밀려난다.
- 조치: zero-rect·화면 밖·비표시를 인셋 0으로 취급 + `hudInset(): {right, bottom}`로 승격. **§1.2·§1.3 착수 전 선행 조건.**
- 📌 **조치 문구 정정(07-28 구현 시점)**: 원래 적었던 `el.offsetParent === null` 가드는 **이 DOM에서 틀렸다.** 우측 컬럼 `.rp-col`이 `position: fixed`이고 **fixed 요소의 `offsetParent`는 항상 `null`**이라, 이 가드를 넣으면 인셋이 영구 0이 된다. §9.2의 zero-rect 진단(`rect.width/height === 0`) + 화면 밖·`display/visibility/opacity` 비표시 판정으로 대체했다.
- ✅ **07-28 구현 완료** — `apps/client/src/scenes/hud-inset.ts`(뷰1·2·3 공용, 화면 절반 클램프) + ResizeObserver 캐시(§9.2 동시 해소) + refcount `disconnect()`. 뷰4는 `mirrorCamera` 상속으로 코드 변경 0.
- 부수: 반응형 블록이 `.hud-evi`/`.hud-log`를 타깃하는데 **DOM에 그 클래스가 없다**(우측 컬럼이 `.rp-col/.rp-panel`로 재구현됨) → **현재 우측 패널 반응형 CSS는 100% 죽은 코드**. 375px 폰에서 260px 컬럼이 화면의 69%를 덮는다.
  - 📌 **07-28 실측 확인**(헤드리스 390×844): 터치 T0 적용 후에도 `.rp-col`이 화면 폭의 약 2/3를 덮어 보드·턴 배너를 가린다. T0(뷰포트·터치·오버스크롤) 범위 밖의 **레이아웃 결함으로 분리** — 우측 컬럼 반응형 재작성이 필요하다(§7.9 탭 도크와 같은 파일).

## 7.9 🟠 우측 컬럼 260px에 패널 4개는 물리적으로 안 들어간다
제안 기록표는 6열 × 40px + 라벨 60px = **300px 폭**이 필요하다.
- 조치: `.rp-col`을 **세그먼트 탭 도크**(🔍노트 / 📜기록 / 🧾제안표 / 🤖AI, 비활성 탭에 미읽음 배지)로 재설계 + 3단 브레이크포인트(≥1440 상하 2분할 / 900~1440 탭 1개 / <900 또는 portrait는 **하단 시트 38dvh**).
- 최소안(0.5h): 기존에 **정의만 돼 있고 아무도 안 쓰는 `.hud summary` 스타일**로 `<details>` 아코디언 4개.

## 7.10 🟠 상단 중앙은 이미 3중 점유 · 인터스티셜이 첫 진입을 방해한다 (§2.1·§2.2 반증)
- `.hud-turn`(top:14, 실측 높이 56~62px)과 `.hud-spectate`(top:56)가 **이미 겹친다**(현존 버그). 여기에 `#fxBanner`를 "중앙 상단"에 넣으면 3중.
- 뷰2·3은 말풍선이 DOM z3이고 화면 좌표 투영이라 **상단 중앙에 온다** → 배너가 말풍선을 가린다.
- `setStage(0)`이 `enterGame()` 말미에 **무조건 호출**되므로 인터스티셜이 **게임 시작 즉시** 뜬다. 같은 사이클에서 `showDiceRoll()`도 호출되어 **둘이 동시에 전면을 덮는다**. 게다가 §1.6(원클릭 진입)과 콘티 0-4초를 정면으로 방해한다.
- `showDiceRoll()`은 이전 타이머를 취소하지 않아 리매치·연속 턴에서 조기 종료된다.
- 조치: `overlay-bus.ts` 단일 큐(`end 100 > dice 80 > interstitial 60 > fxBanner 40`, 동시 표시 금지·타이머 소유권 단일화) + **첫 `setStage(0)`은 `{silent:true}`**(뷰 서사는 사용자가 직접 고를 때만) + `#fxBanner`를 **하단 중앙**(`.hud-help` 자리)으로 + 틴트는 `body.dataset.stage`로 노출해 CSS만 접근.
- 최소안(30분): 큐 없이 `#fxBanner` 하단 배치 + 인터스티셜에 `if (diceOverlay 표시중) return;` 한 줄 → 실제 충돌 2건이 모두 막힌다.

## 7.11 🟡 캐릭터 색이 비결정론적이고 HUD와 끊겨 있다
`PLAYER_COLORS`가 **3파일에 6색씩 중복** 정의되고 배정이 `ids.indexOf(id) % 6`(= **접속 순서**)이다 → 같은 캐릭터가 판마다 다른 색, 봇 충원·재접속으로 **판 도중에도 색이 바뀔 수 있다**. 그런데 **HUD는 색을 전혀 쓰지 않는다**(턴 스트립·증거노트·제안 모달 전부 이모지/텍스트) → 색이 아무 정보도 전달하지 않는다.
- 뷰4에서 치명적: `makeCritter`가 12캐릭터 **동일 실루엣**이라 유일한 식별자가 색인데, 그 색이 판마다 바뀐다.
- 색맹: 6색 중 라임·그린·레드가 2형 색각에서 수렴 → 최대 3명 구분 불가.
- 조치: `packages/shared/src/cards.ts`에 **`ZODIAC_COLOR` 12색**(색상 4계열 × 명도 3단, 회색조 명도차 ≥18% 검증 스크립트) → 3개 렌더러 로컬 배열 삭제. 이름표 좌측 3px 색 스트라이프로 **색과 이름을 같은 픽셀에**. `.ti-chip`에 `inset 0 -3px 0 <색>` + 15px→18px + 이름 첫 글자 병기. `?cvd=1`은 토큰 하단 1~12 도트.
- **§2.3(뷰4 실루엣)보다 먼저** 넣어야 `makeCritter` 시그니처를 두 번 고치지 않는다.

## 7.12 🔴 터치 기기에서는 이동이 아예 불가능하다 — ✅ **T0·T1 07-28 완료**
이동은 **키보드 전용**, 팬은 `rightButtonDown||middleButtonDown` 게이트(터치는 `button===0`), 줌은 `wheel`만. `touch-action` 선언 **0건**이라 폰에서 핀치가 **페이지 전체를 확대**해 HUD가 잘린다. 조작 안내는 680px 미만에서 `display:none`.
→ 심사자가 폰으로 열면 **보드는 보이는데 캐릭터를 1칸도 못 움직인다**(방에 못 들어가니 `[제안]`도 영구 비활성).
- **T0(30분·협상 불가)**: `viewport-fit=cover`, `100vh→100dvh`, `overscroll-behavior:none`, 캔버스 `touch-action:none`, `pointer:coarse`에서 조작 안내를 터치용으로 교체. → ✅ **07-27~28 완료**
- **T1(2h·권장)**: 우하단 D-패드(48px 타깃, 기존 `move` 메시지 재사용 → **서버 변경 0**). → ✅ **07-28 완료** (아래)
- T2(0.5d): 탭-투-무브(그리드 역변환 → `move` 큐). → **폐기**(아래).

#### 🔴 07-28 — **T1을 컷했던 결정을 뒤집었다** (커밋 `55297c0`)
실행계획 §5.1 컷라인은 T1 D-패드를 "**T0로 화면이 깨지는 것은 막힌다. 폰 실플레이는 본행사**"라며 잘랐다. **그 판단이 틀렸다.**
심사자 검수에서 iPhone 에뮬레이션으로 캔버스를 탭해도 말이 움직이지 않았다 — 이동이 `keydown` 전용이라 **키보드가 없으면 게임 진행 자체가 불가능**했고, 이는 제출물 ①("웹에서 즉시 플레이") 요건의 **절반이 성립하지 않는다**는 뜻이다.
> **화면이 안 깨지는 것과 플레이가 되는 것은 다르다.** T0의 성공(레이아웃)이 T1의 필요(조작)를 가린 것이 컷 판단의 오류다. **실행계획 §5.1 컷 목록에서 빼고 완료로 옮긴다.**

- **DOM D-패드**(`pointer: coarse`에서만 노출 — 마우스 데스크톱에는 뜨지 않는다). 우측 패널이 열리면 `dp-off`로 숨긴다.
- **탭-투-무브(T2)는 버렸다** — 클라가 경로를 계산해 `move`를 여러 번 큐잉해야 하고, 그 순간 벽·입구·충돌·한도를 **클라가 판정**하게 된다. D-패드는 **방향 1칸만** 보낸다. 진실값 경계를 지키는 쪽이 코드도 작다.
- **실측**: 패드가 보낸 프레임이 키보드 `ArrowLeft`와 **바이트 단위로 동일**하다 → 서버 변경 0이 말이 아니라 측정으로 확인됐다. 390×844 터치 에뮬레이션으로 **4뷰 전부에서** 실제로 말을 움직여 확인했다.
- 조작 안내가 키보드 전용(`휠`·`우클릭`)이라 터치에서는 **거짓 안내**였다 → 확정 문안에서 **존재하지 않는 조작 절만 덜어내는** 방식으로 처리했다(새 낱말 0 = ui-copy 재확정 불필요).

#### 🔴 07-28 저녁 — 이동은 되는데 **화면이 조작을 가리고 있었다** (커밋 `1e23362`)
T1로 말은 움직였다. 그런데 사용자가 보낸 **실기 iPhone 스크린샷**에 3건이 찍혀 있었고, **셋 다 우리 헤드리스 390×844 검증이 통과시킨 것**이다(왜 놓쳤는지는 §9.7.1).

| | 증상 | 원인 | 조치 |
|---|---|---|---|
| 🔴 | **`[제안]` 말고 버튼이 전부 가려졌다** | 좌측 상단 액션 바 6개가 폭을 거의 다 먹어 **상단 중앙 턴 배너와 겹쳤다** | 액션 바를 **하단으로**(엄지가 닿는 자리) + 우측 D-패드 자리를 비움 · 손패는 턴 배너 아래 · 뷰 선택은 액션 바 위. 실측 390×844 **6개 전부 화면 안 · 겹침 0** |
| 🔴 | **화면이 통째로 밀려 올라갔다** (보드가 위로 사라지고 아래에 빈 검은 띠) | `overscroll-behavior`는 스크롤 **연쇄**만 막는다 — 문서가 뷰포트보다 조금이라도 길면 페이지가 스크롤된다. **T0의 그 한 줄이 이것까지 막아준다고 읽고 있던 것이 오류** | **게임 화면에서만** `body.no-scroll` — 랜딩·대기실은 카드가 화면보다 길 수 있어 **제외**. 실측 `scrollHeight === innerHeight` · `scrollTo(0,2000)` 후에도 `scrollY 0` |
| 🟠 | **우측 컬럼 조작이 어려웠다** | 패널 4개가 세로로 쌓여 화면을 넘겼고, **그것이 위 문서 스크롤의 원인이기도 했다.** 닫기 버튼은 화면 한가운데 오른쪽에 떠 있어 무엇을 닫는지 안 읽힌다 | 컬럼 **안에서** 스크롤(`overscroll-behavior: contain`) · 닫기 버튼을 **컬럼 모서리**로(`.rp-on`) · 타깃 34×46 → **44×44** |

- 두 번째 항목이 §7.12 T0의 성격을 다시 정정한다. T0는 **핀치 확대와 스크롤 연쇄**를 막았을 뿐 **문서 스크롤 자체**는 손대지 않았다. "T0로 화면은 안 깨진다"는 컷 사유가 틀렸던 이유가 여기서도 반복된다.
- 세 번째는 **§7.9(탭 도크)의 최소 대체분**이 실질적으로 착지한 것이다 — 컬럼 자체 스크롤이 들어가 260px 폭 문제의 조작 측면은 해소됐다. 세그먼트 탭은 여전히 미착수.

#### ✅ 07-28 저녁 — "이 화면은 아름답지 못해" (커밋 `8dfa3e6`)
사용자 평. 기능은 됐지만 요소마다 크기·정렬·배경이 제각각이라 **조작부가 갈라져 보였다.** 정렬 축을 하나로 세웠다. **기능 변경 0**(`style(client)`)이고, 판단 축이 "되는가"에서 **"읽히는가"**로 한 칸 옮겨간 지점이다.

- **하단 바를 화면 폭 6등분 한 줄로**(`grid-template-columns: repeat(6, 1fr)`). 2줄로 접히는 덩어리는 우측 D-패드와 크기·정렬이 어긋나 **화면을 둘로 갈랐다.** 한 줄이 되면 앱 하단 탭바처럼 읽혀 **처음 보는 사람도 "여기가 조작부"임을 안다.**
- **비활성 버튼은 칸을 남기고 채도만** 뺀다(`.is-off`) — 배경을 지우면 6칸의 리듬이 끊긴다. 비활성은 "없는 것"이 아니라 **"지금은 못 쓰는 것"**이므로 자리가 남아야 옳다.
- **D-패드는 바와 같은 배경·라운딩을 얇게.** 완전히 지우면 화살표 4개가 흩어져 보이고, 두껍게 두면 바와 **두 덩어리**가 된다.
- **뷰 선택은 조작이 아니라 관람 설정**이다 → 게임 버튼과 같은 무게로 두지 않고 D-패드 반대쪽(좌하단) 칩으로. 하단이 좌우 대칭이 된다.
- 손패는 턴 배너 아래 **한 줄**, 폭은 `max-content`. 화면 폭을 다 먹으면 상단에 검은 띠가 두 개 생겨 **보드가 그만큼 사라진다.**
- 하단 데크가 두꺼워진 만큼 `#fxBanner` 하단 배치(§7.10 최소안)를 178px → **240px**로 올렸다 — 배너와 패드가 겹치면 §7.10이 상단에서 피한 3중 점유를 하단에서 재발시킨다.

## 7.13 🟡 접근성 기본기 — 30분짜리 두 건은 완성도 문제다
- **대비 실패** `#6b6355` on `#211c16` = **2.9:1**(AA 4.5 미달). 사용처 3곳이 전부 **랜딩 첫 화면**이고 그중 `.room-empty`("열린 공개방이 없어요")는 **심사자가 가장 먼저 보는 문장**이다. → `#8f8574`(4.6:1)로 일괄 치환. **10분.**
- **포커스 표시 0건**(`:focus-visible`/`outline` 선언 없음) → 전역 1줄. **10분.**
- 시맨틱 0건(캐릭터 선택·증거노트 칩·세그가 전부 `<div onclick>`), 모달에 ESC·포커스 트랩 없음 → P2.
- **`prefers-reduced-motion` 0건** — 주사위 6프레임 깜빡임·타자기·1초 카메라 팬. **§2.5의 `view-consts.ts` 단일화 시점에 `reduced` 프로파일을 한 곳에서** 정의하면 4뷰 동시 대응된다.
- `TYPE_MS=0`(reduced)은 §1.4의 "TYPE_MS 불변" 결정과 표면적으로 충돌하나, reduced는 **사용자 OS 설정에 의한 분기**이고 촬영 환경은 기본 상태이므로 증거성 손실 0.

## 7.14 🔴 부수 — 조사 플레이스홀더가 화면에 그대로 출력된다 (P0 · 30분)
실측 로그: **"호랑이 대감`가` 대청마루`(으)로` 불려왔습니다"**, **"떡시루`이` 사라졌던걸"**.
`clue-room.ts`의 소환 로그와 `narrator.ts` 폴백 템플릿에 조사 처리 헬퍼가 **0건**이다. 소환은 §2.2 FX의 주인공이고 콘티 10-22s 구간이라 **영상에 확실히 찍힌다.** 종성 판정 헬퍼 15줄로 해결.

## 7.15 🟡 인프라 — 서버 주소가 리터럴 IP로 하드코딩
`vercel.json`에 `VITE_SERVER_URL=wss://141-147-157-219.sslip.io`. OCI 공인 IP가 예약 IP가 아니면 재부팅·회수 시 바뀌고, 그 순간 Vercel 빌드·Caddy 인증서·문서 4곳을 동시에 고쳐야 한다. TLS는 `notAfter Oct 18 2026`으로 심사 기간은 커버되나 갱신이 sslip.io DNS 해석에 의존.
- 조치: OCI 예약 공인 IP 전환(무료) + `VITE_SERVER_URL`을 Vercel 환경변수로 이전(커밋 없이 교체) + 서버 연결 실패 시 랜딩에 명시 배너.

## 7.16 🟡 상류 근거와의 논리 단절
- **"load-bearing AI" 용어가 뒤집혀 있다.** 상류 정의는 "**AI 없으면 게임이 성립 안 되는** 설계여야 감점 안 됨(껍데기 AI ❌)"인데, ④는 "**load-bearing AI = 표현 전용**"이라 쓰고 "LLM 장애와 무관하게 게임은 정상"이라고 **자기 선언**한다. 7.1에서 **실제로 제거된 채 라이브 중임이 실측**됐으므로 심사자가 5초 만에 검증할 수 있다.
  → 제목을 **"이중 계층: 결정론적 진실 계층 + LLM 표현 계층"**으로 바꾸고 "왜 판정에서 LLM을 배제했는가"를 **설계 판단으로 논증**(환각·재현성·무료티어·치트 방지). 상류가 요구한 프레이밍은 버리는 게 아니라 **이동**시킨다 — "AI를 어떻게 게임에 넣었나" → **"AI를 어떻게 지휘·검수했나"**(§4가 이미 옳게 짚은 방향, 본행사 산출물 요구와 직결).
- **07-18 확정 차별점(자유 심문·거짓말/뇌물 축)이 미구현**이다. D-14에 신규 구현은 §6.2와 같은 이유로 기각하되, **④에서 "본행사 로드맵"으로 명시적으로 계승**할 것: "자유 심문은 진실값 경계를 LLM에 넘기므로, 사전과제에서 결정론 경계를 먼저 확립하고 본행사에서 검증 프로토콜과 함께 얹는다." 아무 말도 안 하면 **"차별점을 약속하고 안 만든 팀"**과 구분되지 않는다.
- ③에 "왜 이 게임인가" 6줄 추가(장르=소규모 퍼즐 추리, NHN 웹보드/캐주얼 포트폴리오 정합성, 십이지 테마 접근성). 현재 ③은 규칙설명서일 뿐 기획서가 아니다.
- ⚠️ **"심사축 1위(AI)"·"그래픽은 채점 항목이 아니다"는 저장소에 출처가 없다.** 공고 원문이 repo에 없다 → `docs/planning/`에 **공고 원문을 넣고**(30분), 그 전까지 이 우선순위는 **잠정**으로 표기.

---

## 7.99 정정 후 실행 순서 (D-14, §5를 대체)

| 일자 | 항목 |
|---|---|
| **07-27 즉시(2h)** | 🔴 라이브 LLM 키 확인(7.1) · 조사 헬퍼(7.14) · 대비·포커스(7.13) · 터치 T0(7.12) · `hudInset` 가드(7.8) · PLACEHOLDER 배지 제거(§2.4) |
| **07-27~28** | 🔴 **⑤ 제출 형태 확인 요청 + 초안 작성**(7.2) · `@media print` 11파일 + PDF 실검증(7.3) · 문서 노출 차단(7.4) |
| 07-28 | 오버레이 큐 + 상단 슬롯 정리 + silent 첫 전환(7.10) — §2.1·2.2 착수 **전** |
| 07-29 | 즉시고발권(7.5.1) · `revealed` 분리(§1.1, 상한 60) · `ZODIAC_COLOR` 12색(7.11) — §2.3 착수 **전** |
| 07-30 | 재진입 규칙 + 손패 지목 허용(7.5.2) · 페이싱 재조정(7.5.4) |
| 07-31 | 솔로 10판 실측 → 공통단서 장수 확정(2장 유지 권장) |
| 08-01 | **안전 테이크 녹화·업로드**(컷 편집 콘티, 7.6) |
| 08-02~04 | AI 계측 패널(기본 노출, 7.7) · 탭 도크(7.9) · 제안 기록표 |
| 08-05~07 | ④ 문서 승격(§4 + 7.16) · `eval-narrator.mjs` · 터치 T1 |
| 08-08 | 본 테이크 재촬영 · 뷰4 도트 · 문서 정합성 · PDF |

---

# 8. 멀티플레이·재접속 (3차 검수)

> §1~7은 **1인 플레이(사람1+봇5)** 기준이었다. 여러 사람이 함께 하면 판이 깨진다.

## 8.1 🔴 이탈자의 손패가 증발해 "유령 카드"를 만든다 — 진실값이 깨진다 (P0 · 1.5h)
`removePlayer()`는 `players`와 `hands`에서 지우지만 **`turnOrder`에서는 지우지 않는다.** 반증 루프는 `turnOrder`를 돌며 `hands.get(id) ?? []`로 접근하므로 이탈자를 **조용히 건너뛴다.**
→ 이탈자가 들고 있던 **카드 2~4장이 영구히 반증 불가능**해지고, 그 카드를 포함한 제안이 `"❗ 아무도 반증하지 못함 — 정답 후보!"`로 방송된다. 정답이 아닌데 정답 후보다.
**연쇄**: 봇은 "아무도 반증 못 함 + 내 패에도 없음"이면 **즉시 확신 고발**한다 → 유령 카드 하나가 봇 여러 명을 연쇄 오답·탈락시키고, `turnOrder`가 1명이 되면 "최후 생존 승리"로 끝난다.
**즉 사람 1명이 판 도중 브라우저를 닫으면 2~3라운드 안에 봇들이 집단 자살하고 판이 사고사한다.**
게다가 UI가 이를 유도한다 — 탈락 배너의 **유일한 버튼이 [메인으로]**다. **판을 가장 확실하게 망가뜨리는 버튼이 탈락자에게 제공되는 유일한 버튼이다.**
- 조치: `removePlayer` 대신 **`handoverToBot`** — `isBot=true; awayBot=true; connected=false` + `initBotKnowledge`(기존 손패로 노트 재구성). **`hands`·`turnOrder`를 절대 건드리지 않는다.** 대기실에서만 완전 삭제. 재접속 시 좌석 반환.
- `awayBot`인 봇은 `revealed` 전역 공유 예외 처리(§1.1과 겹쳐 과하게 강해지는 것 방지).
- 4뷰: `displayName(p)` 헬퍼를 `shared`에 만들고 3개 렌더러의 이름 라벨을 치환(1줄 × 3) → `🤖`(순수 NPC) / `💤`(대리 중인 사람 자리).

## 8.2 🔴 턴 타이머가 코드에 존재하지 않는다 — AFK 1명이 판 전체를 무한 정지 (P0 · 2h)
`clock.setTimeout`은 봇 경로 3곳뿐이고 **사람 턴 타임아웃은 0개다.** `handleEndTurn`은 당사자만 호출 가능 → **AFK 1명 = 무기한 정지.** 이탈 시엔 `allowReconnection(60)` 동안 `currentTurn`이 이탈자에게 고정돼 **최대 60초 완전 정지**이고, `connected` 필드는 스키마에 있으나 **클라이언트 어디에서도 읽지 않는다**(grep 0건).
- 조치: `turnEndsAt`을 스키마에 노출 + `armTurnClock()`(사람 45초 / `connected=false` 8초). **§7.5.1의 "제안 후 고발 30초 자동 종료"와 같은 프리미티브를 공유**하도록 인자화(중복 구현 방지). **사람이 1명뿐이면 클럭을 끈다** — 솔로 심사 경험은 그대로.
- 이탈 시 `allowReconnection`을 120초로 늘리되 **20초 시점에 `handoverToBot` 선행** → 60초 무정지가 20초 유예 + 봇 대행으로 바뀐다.
- **다인 페이싱**: `npcDelay()`가 `avgHumanTurnMs/2`를 **사람 수와 무관하게** 쓴다. 사람 4+봇 2, 평균 20초면 1라운드 ≈ **107초**. 게다가 `avg`가 단일 EMA라 **가장 느린 한 명이 모두의 봇 딜레이를 끌어올린다.** → `/(2 × humans)` + `humans≥2`면 `BOT_ACT_GAP`/`SPEAK_HOLD`를 700/1200으로. 규약("사용자 평균의 절반")은 **1인당 해석**으로 유지되므로 위반이 아니다(문서에 해석 명시).
- 4뷰: `turnEndsAt`은 DOM HUD에서만 소비 → 뷰 독립. 진행 바 width만 rAF로 갱신.

## 8.3 🔴 재접속이 "들어와지긴 하는데 아무것도 못 하는 상태"로 복구된다 (P0 · 1h)
- **(a) 손패가 돌아오지 않는다.** `hand` 메시지는 `startGame()` **단 한 곳**에서만 전송된다. 재접속 분기는 `connected=true`와 로그만 처리한다. → 새로고침하면 서버는 손패를 갖고 있는데 클라 `myCards`는 빈 Set → 손패 HUD가 "(잔치 시작 후 표시)"로 남고, 증거노트가 **내 패를 정답 후보로 표시**하며, **자기 카드로 고발해 자멸할 수 있다.**
- **(b) 추리 기록 전체가 소실된다.** `#log`는 순수 DOM이다. 증거 노트는 `localStorage`로 살아남지만 **"누가 무엇으로 반증했는가"는 어디에도 저장되지 않는다.** 재접속해도 사실상 처음부터 다시 하는 셈.
- **(c) 60초를 넘기면 영원히 못 돌아온다.** `handleStart`가 `this.lock()`을 호출하고 **어디에서도 `unlock()`하지 않는다.** Colyseus의 `joinById`는 잠긴 방을 **거부**하므로 초대 링크 재입장 불가이고, **`onJoin`의 "이미 진행 중인 게임입니다 (관전)" 분기는 도달 불가능한 죽은 코드**다(관전 입장은 구현되어 있지 않다).
- 조치: `sendHand(id)` 헬퍼로 재접속 시 재전송(6줄) · `publicLog`(최근 200) + `privateLog`(sessionId 키) → 재접속·관전 입장 시 `replay` 전송 · **`lock()` → `setPrivate(true)`**(목록에서만 숨기고 코드 참가는 허용, 이미 비공개방에 쓰는 API) + 관전 좌석.
- ✅ **07-28 보완 — 공개 상태 복귀(`1f55b28`).** `setPrivate(true)`는 "숨기는 시점"만 정하고 **되돌리는 시점을 규정하지 않아** 판이 끝나도 방이 목록에서 사라진 채였다. 조치 2건:
  - **판 종료 3경로 전부에서 `setListed(true)`** — 고발 성공·최후 생존·상한 도달 무승부. 종료 경로가 하나가 아니므로 한 곳만 걸면 나머지 두 경로에서 방이 영구 실종된다.
  - **숨김을 `handleStart` → `startGame`으로 이동.** 리매치는 `handleStart`를 타지 않고 `startGame`을 직접 다시 부른다 → 안 옮기면 **리매치 판이 진행 중인데 공개 목록에 뜬 채로 돈다**(들어온 사람은 이미 시작된 판을 만난다). 숨김·복귀를 같은 함수(`setListed`)의 양끝으로 둬서 짝이 깨지지 않게 했다.
- ⚠️ 관전자는 `freeCellIn` 점유 집합·`handleMove` 충돌·소환 대상 탐색에서 **반드시 제외**(안 하면 유령 몸통이 문을 막는 봉쇄 버그). 각 1줄.
- ⚠️ `privateLog`는 반드시 `client.send`로만 — CLAUDE.md의 "비밀 정보는 대상에게만 개별 전송" 규약.

## 8.4 🟠 대기실이 여러 사람을 전제로 만들어져 있지 않다 (P1 · 2h)
- **대기실 단계의 모든 서버 로그가 보이지 않는다.** `addLog`는 `#log`에 쓰는데 `#log`는 **`#gameScreen` 안쪽**이고 대기실 화면은 `#lobby`다. → "입장·퇴장·캐릭터 중복·방장 전용" 피드백이 **전부 무음으로 사라진다.** 두 사람이 동시에 같은 캐릭터를 누르면 진 쪽은 **클릭했는데 아무 일도 안 일어난다**(데이터는 서버가 권위 있게 지킨다).
- **사람을 구분할 이름이 없다.** `Player.name`은 `label(suspect)`뿐 — **닉네임 필드가 스키마에 없다.** 4명이 하면 목록이 "🐭/🐮/🐯/🐰"뿐이라 누가 내 친구인지 알 수 없고, 게임 중 로그도 사람/NPC 구분이 안 된다.
- **공개방 메타가 낡는다** — 대기실에서 캐릭터를 바꿔도 `syncMeta()`가 호출되지 않는다.
- **시작 경합** — 방장이 시작하면 즉시 `lock()`이라, 친구가 `joinById` 왕복 중이면 "없는 방" 오류를 본다.
- 조치: 대기실 전용 로그 라인 + 캐릭터 셀 낙관적 `.pending` · **닉네임 8자**(서버 새니타이즈 + `displayName()` 재사용) · `syncMeta` 호출 2곳 추가 · 시작 3초 카운트다운 + 버튼 라벨 `"잔치 시작 (사람 3 + NPC 3)"`.
- ⚠️ 닉네임은 사용자 입력이다. `renderTurnStrip`·`openTurnCircle`·`showCharInfo`가 **`innerHTML`을 쓰므로** 서버 새니타이즈에 더해 클라 이스케이프 필수(XSS).

## 8.5 🟠 리매치가 "같은 판 한 번 더"일 뿐이다 (P1 · 2h)
- **아무나 혼자서 리매치를 강행한다** — 방장 검사·동의·카운트다운이 전부 없다. 4명이 결과 화면에서 정답을 보는 중에 한 명이 누르면 나머지는 보드로 끌려간다.
- **리매치는 NPC를 재충원하지 않는다**(충원 루프가 `handleStart`에만 있다) → 3명이 나간 뒤 리매치하면 **3인 판**이 되고 덱 구성·공통단서 규칙이 어긋난다.
- **선공이 영구 고정** — `ids[0]` = 입장 순서라 **방장이 모든 판에서 선공**이다. 이미 있는 주사위 연출 자산이 놀고 있다.
- **판 사이에 아무것도 누적되지 않는다** — 승수·시리즈 필드가 스키마에 없다. **한 판 더 할 이유가 게임 안에 없다.**
- 조치: 리매치 **과반 합의**(사람 1명이면 `need=1`이라 솔로 경험 동일) · 충원 루프를 `startGame`으로 이동 · **선공 셔플 + 기존 주사위 오버레이 재사용**(단 §7.10의 오버레이 큐에 `dice 80` 우선순위로 편입할 것) · `roundNo`+`wins` 스키마로 **시리즈 스코어보드**("제3판 · 통산 2:1").
- **탈락자·반증자 정보 결손**: 반증이 성립해도 **반증한 당사자에게는 어떤 메시지도 가지 않는다** — 자기가 어떤 카드를 누구에게 노출했는지 모른다. 정보 추적이 전부인 장르에서 사람끼리 할 때 치명적이다(봇끼리는 서버가 다 알아서 드러나지 않았다). → `send("shown", {to, card})` 12줄.

## 8.6 실행 순서 (멀티)
| 순 | 항목 | 누적 | 없으면 |
|---|---|---|---|
| 1 | 8.1 이탈자 봇 인계 | 1.5h | 사람 1명 이탈 = 판 사고사 |
| 2 | 8.2 턴 클럭 + 다인 페이싱 | 3.5h | AFK 1명 = 무한 정지 |
| 3 | 8.3(a)(b) 손패 재전송 + 로그 리플레이 | 4.5h | "재접속 지원"이 새로고침 한 번에 거짓말이 됨 |
| 4 | 8.5 리매치 합의·충원·선공 | 5.5h | 남이 리매치 강행, 방장 영구 선공 |
| 5 | 8.4 대기실 로그 + 닉네임 | 7.5h | 다인 대기실 피드백 전무 |
| 6 | 8.5 시리즈 전적 · 반증 통지 | 9h | 2판째를 할 이유 없음 |

**1~3(4.5h)까지가 "여러 사람이 함께 해도 판이 깨지지 않는다"의 최소선**이다. 지금 상태로 4명이 접속해 한 명이 브라우저를 닫으면 그 판은 규칙적으로 오염되고 대개 봇들의 연쇄 오답으로 사고사한다 — **멀티 시연에서 재현될 확률이 매우 높은 시나리오다.**

---

# 9. 런타임 건전성 (3차 검수)

## 9.1 🔴 첫 3초 — 번들 2,130 kB 단일 청크 (P0 · 2~3h)
빌드 실측: `index-*.js` **2,130.36 kB (gzip 512 kB)**, 단일 청크. 분해하면 phaser 1,479 / three 527 / colyseus 76 / 앱 44 kB.
`main.ts`가 3개 렌더러를 **정적 import**하므로, 랜딩이 인터랙티브해지기 전에 Phaser + Three를 전부 받고 파싱한다. **심사자가 뷰1만 보고 나가면 Three 527 kB는 100% 낭비**이고, 공개방 목록이 뜨기까지 2.1 MB 파싱을 기다린다.
- 조치: 동적 `import()`로 Phaser는 `enterGame()`, Three는 three 분기 최초 진입 시. **선행 조건**: `pixel-scene`이 `game-scene`에서 상수를 가져오는 **순환 의존을 shared 이관으로 먼저 끊어야** pixel 청크가 phaser를 통째로 끌고 오지 않는다(§3 이행 목록과 동일 작업). 랜딩 진입 시 `void import(...)`로 선로딩, 뷰 드롭다운 `pointerenter`로 prefetch.
- **효과: 크리티컬 JS 2,130 → 약 120 kB (−94%).**

#### ✅ 07-28 구현 완료 (`63c46be`) — 목표 도달
직전 실측은 **2,172 kB 단일 청크**였다(§9.1 작성 시점 2,130 kB에서 기능 추가분만큼 늘었다). 결과:

| 항목 | 전 | 후 |
|---|---|---|
| 초기 로드 JS | 2,172 kB (단일) | **115.66 kB** |
| 초기 로드 합계(html 포함) | — | **158 kB · gzip 49 kB** |
| 청크 수 | 1 | **8** (초기 1 / 지연 7) |
| 최대 지연 청크 | — | phaser 1,478 kB · iso-view 551 kB |

- 진입점은 `setStage`다 — **렌더러를 동적 `import()`로 불러온다.** 항상 뷰1로 시작하므로 Three는 첫 화면에 필요 없고, 뷰2·3을 안 누르는 심사자는 551 kB를 아예 받지 않는다.
- **순환 의존 제거가 선행 조건이었고, `view-consts` 이관(§2.5) 때 이미 해소돼 있었다** — 실행계획 §7.2 ③이 지적한 숨은 의존성이 순서대로 맞아떨어진 사례다.
- **로딩 중 빈 화면 금지**: 청크를 받는 동안 오버레이 버스의 `interstitial`(우선순위 60) 슬롯이 전면을 채운다(§7.10 큐 그대로 재사용 — 새 레이어를 만들지 않았다).
- **`STAGES`에 `load` 자리를 비워뒀다**(§9.5 최소 방어 · 실행계획 §7.2 ④). 생명주기 계약이 본행사에 들어올 때 같은 지점을 다시 열지 않는다.
- 회귀 방지는 번들 예산 게이트가 맡는다(§9.6) — 기준선 115.66 kB **+3% 래칫**.

## 9.2 🔴 보이지 않는 뷰가 계속 돈다 — 루프 2개·컨텍스트 2개·프레임당 강제 레이아웃 2회 (P0 · 1.5h)
Phaser `SceneManager.update()`는 **`visible`을 검사하지 않는다** — `setVisible(false)`는 **그리기만 멈추고 `update()`는 계속 돈다.** `PixelScene.update()`에는 자기 방어 가드가 있는데 **`GameScene.update()`에는 없다** → 뷰2·3에서 매 프레임 `getBoundingClientRect()`가 실행되고, 동시에 `IsoView.loop()`가 자기 RAF로 돌며 **또** 호출한다.
→ **뷰2·3에서 RAF 2개 / 라이브 WebGL 컨텍스트 2개 / 프레임당 강제 동기 레이아웃 2회.** 144Hz면 초당 288 콜백·288 리플로우.
- 조치: `setStage`에서 `three ? game.loop.sleep() : game.loop.wake()`. **`GameScene.update()`에 `isVisible()` 가드를 넣으면 안 된다** — 뷰4에서 GameScene은 invisible이지만 카메라를 돌려야 하고 PixelScene이 그걸 미러링한다. 정확한 축은 "three 뷰인가"다.
- 인셋을 프레임마다 읽지 말고 `ResizeObserver` + 드래그 핸들러에서만 갱신하는 캐시로. **§7.8은 가드의 정확성을 고치지만 프레임당 리플로우는 그대로 남는다** — 같은 함수를 손대는 김에 함께.
- `IsoView.setActive(false)`가 **말풍선 타이머를 정리하지 않는다** → 숨겨진 레이어에서 타자기가 계속 돌고 DOM이 남는다.
- ✅ **07-28 구현 완료** — `main.ts`가 `three ? game.loop.sleep() : (!running && wake() + scale.refresh())`로 분기하고, `IsoView.setActive(false)`가 `clearBubbles()`(타자기·홀드 타이머 전량) + `clearFx()` + `releaseHudInset()`을 수행한다. 실측 **뷰1·4 = 60fps / 뷰2·3 = 0fps.** ⚠️ **뷰4는 재우면 안 된다** — `mirrorCamera()`가 invisible한 `GameScene`의 카메라를 매 프레임 베끼므로 뷰1 루프가 뷰4를 굴린다(위 "축은 three 뷰인가"의 실제 이유).
  - 📌 **체크리스트·실행계획이 이 항목을 "유일한 미착수 P0 잔여분"으로 계속 적고 있었다** — 07-28 저녁 검수에서 코드 확인으로 정정. 문서가 코드보다 뒤처진 오늘 네 번째 사례다.
- 📌 **§7.8 정정**: null 가드가 아니라 **zero-rect** 문제다. `if (!el) return 0`은 이미 있고, `display:none`일 때 `getBoundingClientRect()`가 0을 반환해 `innerWidth - 0`이 되는 것이 원인이다(조치 문구는 그대로 유효).

## 9.3 🔴 `dispose()` 0건 — 뷰 왕복마다 GPU 자원이 쌓인다 (P0 · 2h)
`grep dispose apps/client/src` → **0건.** Three는 geometry/material/texture를 GC하지 않는다.
`setAssets()`는 `scene.remove` + `clear()`만 하고 다음 프레임의 `syncState()`가 전부 새로 만든다. **왕복 1회당 geometry 18+ / material 36+ / texture 28+ 유출**(룸 텍스처 교체도 이전 텍스처를 dispose 없이 덮는다). `THREE.Cache.enabled`는 기본 `false`라 뷰3 재진입마다 같은 SVG 10장을 **재디코드 + GPU 재업로드**한다.
**이 경로는 로드맵의 간판 기능(뷰 인터스티셜·연표)이 정확히 유도하는 동선이다.** 10회 왕복이면 geometry 180 / texture 280이 남는다.
- 조치: `disposeObject()` 재귀 해제 + 공유 지오메트리 상수화(초기 geometry 49→24) + `applyTexture`에 `map?.dispose()` 선행 + `THREE.Cache.enabled = true`.
- ⚠️ **`THREE.Sprite`는 모듈 전역 공유 지오메트리를 쓴다 — geometry를 dispose하면 화면의 모든 스프라이트가 함께 죽는다.** 순진한 구현이 정확히 여기서 깨진다.
- **회귀 게이트**: 뷰1→2→3→4→1 **10회 순회 후 `renderer.info.memory`가 초기값 ±5 이내**면 통과. 지금 돌리면 우상향 직선이 나온다.
- ✅ **07-28 구현 완료** — `disposeObject()` 재귀 해제 + 공유 지오메트리 상수화 + `applyTexture`의 `map?.dispose()` 선행 + `THREE.Cache.enabled = true`(`apps/client/src/scenes/three-res.ts`). `THREE.Sprite`의 모듈 전역 지오메트리는 `isSharedTexture`/공유 판정으로 예외 처리했다. 정적 계측은 `pnpm gate:gpu`가 상시 감시하고, **런타임 ±5 게이트는 브라우저가 필요해 CLI에서 항상 SKIP**으로 명시 출력된다(§9.6).
- 뷰1 부수: **타자기가 55ms마다 `setText()`를 호출해 캔버스 재렌더 + 텍스처 재업로드**를 시킨다(30자 = 30회, 6인 동시 발화면 초당 ~109회). 전체 텍스트를 1회 그리고 `setCrop`으로 마스킹할 것. **뷰4에 타자기를 추가할 때 같은 결함을 복제하지 말 것.**

## 9.4 🟠 저사양·고주사율 (P1 · 2h)
- `antialias: true` + `dpr 2` → Retina에서 **7.7 Mpx를 MSAA로** 매 프레임, 같은 크기 Phaser 캔버스가 하나 더 살아 있다.
- 드로우콜 ≈ 120 / 텍스처 바인드 ≈ 50. 스프라이트가 전부 `depthTest:false`라 **깊이 거부 없이 투명 패스에서 개별 드로우콜**. 통합그래픽에서는 필레이트보다 이 CPU 오버헤드가 먼저 막힌다.
- **`loop()`가 RAF 타임스탬프를 인자로 받지도 않는다** → `lerp` 계수가 프레임 수 기준이라 144Hz에서 60Hz 대비 2.4× 빨리 수렴한다. **§3(view-contract-spec)이 요구한 `k = 1 - exp(-dt/τ)`는 dt를 만들 클럭이 없어 현 구조로는 구현 불가**다 → `loop(t)` 시그니처 변경이 **선행 조건**.
- `webglcontextlost` 핸들러 **0건** — 컨텍스트 2개 + 텍스처 누수 + 메모리 압박 = 검은 화면 영구화.
- 조치: dt 클럭 · `antialias:false` + dpr 1.5 + p95 기반 적응 해상도(히스테리시스) · 컨텍스트 로스 핸들러 · (P2) 정적 스프라이트 아틀라스·기둥 InstancedMesh.
- ⚠️ **적응 해상도를 뷰4에 적용하면 안 된다**(도트 그리드가 깨진다) — 스케일러는 three 뷰 전용임을 계약에 명시.

## 9.5 🟠 `STAGES.push`만으로 뷰5 추가는 성립하지 않는다 (P1 · 3h)
push만으로 자동 편입되는 것은 **드롭다운 목록뿐**이고, 런타임은 `main.ts` **8곳**(kind 유니온 · say 라우팅 3분기 · `scene:[...]` 정적 배열 · three/pixel 분기 · IsoView 싱글턴 · `scene.run("pixel")` 리터럴 · setVisible 개별 호출 · 키보드 게이팅)을 손봐야 한다. FX 계약이 들어오면 9번째가 붙는다.
뷰5가 3D라면 IsoView 재사용 시 **§9.3의 미해제 재생성이 3배**가 되거나, 별도 렌더러면 **컨텍스트 3개**가 되어 브라우저가 가장 오래된 것을 회수하는데 핸들러가 없어 **뷰1이 영구히 검게 남는다.**
- 조치: `ViewContract`에 **직교하는 생명주기 축** 추가 — `mount()`(동적 import) / `setActive()` / **`tick(dt)`(드라이버가 dt를 준다 — 뷰가 자기 RAF를 갖지 않는다)** / `dispose()` / `contextCost`. `STAGES`를 `{ load: () => Promise<ViewLifecycle> }`로 → `kind` 유니온과 분기 8곳이 소멸하고 §9.1의 동적 import가 그대로 들어간다.
- **프레임 드라이버 1개가 활성 뷰의 `tick(dt)`만 호출** → **§9.2(루프 2개)와 §9.4(dt 부재)가 같은 변경으로 함께 해결된다.**
- **성공 신호: 뷰5 추가 PR의 diff가 `STAGES` 항목 1개 + 새 파일 1개.**
- ⚠️ 뷰2·3은 **인스턴스를 2개로 쪼개지 말 것**(컨텍스트가 늘어난다) — `contextCost: 1`을 두 stage가 공유한다고 계약에 명시.

## 9.6 단일 수용 게이트
개발 HUD에 `renderer.info.memory.{geometries,textures}` + `render.calls` + `game.loop.actualFps`를 띄우고,
**뷰1→2→3→4→1 10회 순회 후 메모리 카운터가 초기값 ±5 이내이며 뷰2·3에서 Phaser `actualFps ≈ 0`**이면 §9.1~9.4가 전부 통과한 것이다.

### ✅ 07-28 신설 — `pnpm verify` 통합 회귀 게이트 (`4244e5c`)
검증이 여러 스크립트로 흩어져 있어 **커밋 전에 무엇을 돌려야 하는지 사람이 기억해야 했다.** 마감 직전 회귀는 되돌릴 시간이 없다.

| 명령 | 용도 |
|---|---|
| `pnpm verify:quick` | **커밋 전 3.4초.** 타입 · 문서·코드 상수 정합 · GPU 정적 계측 · 규칙 밸런스 회귀 · LLM 환각 검증(36케이스) |
| `pnpm verify` | **푸시·촬영 전 전체.** 위 5종 + 빌드 + 번들 예산, 환각 검증은 전수 스윕 |

- **어떤 항목도 조용히 건너뛰지 않는다.** SKIP은 반드시 **사유와 함께** 출력된다 — 은폐된 미측정이 가장 위험하다. 위 **§9.6 런타임 게이트는 브라우저 WebGL 컨텍스트가 필요해 CLI로 측정 불가**이므로 항상 SKIP + 수동 절차 안내(`node scripts/gate-gpu-baseline.mjs --print-runtime-gate`)로 남는다.
- **번들 예산**(`gate-bundle-budget.mjs`) — `dist/index.html`이 **즉시 받는 것**만 집계한다. Vite가 정적 import 그래프만 `modulepreload`로 박으므로 그 집합이 곧 "첫 3초"이고 동적 청크는 자동 제외된다. 기준선 **+3% 래칫**: 라이브러리 하나를 삼키면 반드시 걸리고 코드 몇 줄로는 안 닿는 폭이다.
- **밸런스 회귀**(`gate-sim-regression.mjs`) — 시드 고정이라 같은 코드면 **결과가 비트 단위로 같다.** 따라서 밴드는 잡음 흡수용이 아니라 **"이 정도 움직이면 의도된 밸런스 변경이다"의 선**이다.
- **문서·코드 상수 정합**(`gate-docs-consistency.mjs`) — 이 저장소에서 반복 발생한 "코드만 고치고 문서를 안 고침"을 상시 감시한다(현재 4건). 특히 시뮬의 `SUGGEST_CAP`은 서버 규칙의 **이식본**이라, 한쪽만 바뀌면 회귀 게이트가 배포되는 규칙과 다른 것을 재면서 **조용히 거짓말을 한다.**
- **기준선 갱신은 `--reason` 없이는 거부한다** — "왜 이 값이 바뀌는 것이 의도된 변경인가"를 적지 않으면 래칫이 그냥 풀린다.
- 판정 기준은 `scripts/gate.config.mjs` 한 곳, 현재 값은 `scripts/gate.baseline.json`에 분리 기록한다.

#### 🔴 07-28 저녁 — 게이트가 **자기 근거 파일을 지우고 있었다** (해소 · `2fb83ef`)
`eval-narrator.mjs`는 실행마다 `docs/design/eval-narrator-report.md`를 갱신한다(§7.7 후속에서 "근거 파일이 실제로 없다"를 고치며 넣은 동작). 그런데 `verify --quick`은 **폴백 전수 스윕을 생략**하면서 리포트는 그대로 덮어썼다 → **커밋 전 게이트를 돌릴 때마다** ④ §3.4의 최대 정량 근거인 전수 수치가 **`112,752 → 0`으로 퇴화**했다.

- 조치: 빠른 모드에 `--no-report`를 함께 넘긴다(`RUNNERS.narrator.quickArgs`). **리포트는 전체 모드만 갱신한다.**
- **"빠른 모드"는 검사를 줄이라는 뜻이지 기록을 줄이라는 뜻이 아니었다.** 축약 모드가 산출물을 축약본으로 갈아치우면, 게이트를 성실히 돌린 사람일수록 근거를 더 많이 지운다 — **성실함이 벌점이 되는 구조**였다.
- 규약으로 승격: **측정 도구는 기본값이 읽기 전용이어야 한다.** 산출물을 쓰는 모드는 명시적으로만(전체 모드 = 전수 측정 = 전수 기록).
- 발견 경위: 다른 결함(§9.7)을 고치고 커밋하려다 `git status`에 `eval-narrator-report.md`가 매번 함께 올라오는 것을 보고 잡았다. **게이트 자신은 이것을 검출하지 못한다** — 문서·코드 상수 정합 게이트는 상수를 보지 리포트 본문을 보지 않는다.

## 9.7 🔴 뷰4에서 말이 얼어 있었다 — **폴링 렌더러가 매 프레임 트윈을 죽이고 있었다** (✅ 07-28 · `2fb83ef`)
심사자가 뷰4에서 조작하면 **"이동이 안 된다"로 읽는** 결함이다. §7.12에서 폰 조작을 막 열어 준 직후라 파급이 더 컸다.

- **정상이던 것부터**: 루프·가시성 모두 참(`loopRunning true · pixelVisible true`), 서버 좌표도 정확히 들어왔다. 즉 **데이터도 루프도 범인이 아니었다.**
- **원인**: Phaser 트윈은 **두 번째 스텝부터 움직인다** — 새 트윈의 첫 `update`는 `delta = 0`으로 리셋된다("start progress from zero"). 뷰4는 `update()`마다 서버 상태를 다시 읽는 **폴링** 렌더러인데, 트윈 재시작 조건이 **`그리는 중인 좌표 ≠ 목표`**였다. 도착 전까지 이 조건은 **매 프레임 참**이라 60Hz로 `kill → add`를 반복했고, **모든 트윈이 첫 스텝만 맞고 죽어** `elapsed`가 0에 못 박혔다.
- 🔴 **뷰1은 `onStateChange` 이벤트 구동이라 같은 비교를 써도 안전했다.** 같은 한 줄이 **한쪽에서만** 깨진 것이고 **그 구조 차이가 곧 원인**이다 — 두 파일을 나란히 놓고 읽으면 그 줄은 똑같아 보인다. 코드 리뷰로 잡을 수 없는 종류의 결함이었다.
- **결정적 증거**: 폴링만 멈추자(`setVisible(false)`) 토큰이 **스스로 목표에 도착했다.** 렌더러를 끄면 낫는 버그는 렌더러가 범인이다.
- **조치**: 재시작 조건을 **"목표 좌표가 바뀌었는가"**로 — 토큰·장물이 그리는 중인 좌표와 별도로 **목표 `tx/ty`를 보관**한다. `warp()`는 순간이동 후 `tx/ty`도 함께 옮긴다(안 옮기면 다음 폴링이 "아직 안 갔다"고 오해해 방금 워프한 말을 트윈으로 도로 끈다). **뷰2·3이 같은 폴링을 `target` + dt 보간으로 푸는 것과 같은 규약**이라 4뷰가 다시 한 규약 위에 선다.
- **같은 원인으로 장물도 얼어 있었다.** 계략 NPC는 **별개 결함** — 생성 후 위치를 한 번도 갱신하지 않아 재대국에서 유령이 남았다. 이쪽은 이동이 아니라 **재배치**이므로 트윈이 아닌 **스냅**(`setPosition`)으로 고쳤다. 셋 다 해소.
- ⚠️ **§2.5 `ViewContract`가 이것을 막지 못하는 이유**: 계약은 *무엇을 표시하는가*를 강제하고 *어떻게 갱신하는가*(이벤트 구동/폴링)는 자유로 뒀다 — "계약은 정보만 강제하고 표현은 자유로 둔다"는 §2.5의 선택 그대로다. 그 자유가 이번 결함이 산 자리다. **계약을 넓히는 것이 답은 아니고**, 대신 규약 한 줄을 코드 주석으로 못 박았다: **폴링 뷰의 트윈 재시작 조건은 언제나 "목표의 변화"여야 한다.**

### 9.7.1 왜 헤드리스 검증이 §7.12·§9.7을 전부 통과시켰는가
§4.4 절차는 **390×844로 재고 있었고 4뷰 전부에서 말을 움직여 확인**까지 했는데도 액션 바 가림·문서 스크롤·말 얼어붙음을 셋 다 놓쳤다. 원인은 도구가 아니라 **질문**이다.

| 놓친 것 | 헤드리스가 실제로 물은 것 | 왜 통과했나 |
|---|---|---|
| 액션 바 가림 | "6개 버튼이 DOM에 있고 뷰포트 안인가" | **가림은 요소의 속성이 아니라 요소 *쌍*의 관계다.** 두 요소 각각의 `getBoundingClientRect()`는 둘 다 정상이고, 가려진 요소도 폭·높이·`visibility`를 그대로 보고한다. **아무도 둘을 비교하지 않았다** |
| 문서 스크롤 | "`scrollWidth vs innerWidth`"(가로) · `touch-action` | 세로를 재지 않았다. 게다가 **전체 페이지 캡처는 문서 전체를 렌더**하므로 "페이지가 밀려 올라간 화면"이 스크린샷에서는 **정상으로 보인다** — 증상 자체가 캡처에서 사라진다 |
| 뷰4 말 얼어붙음 | "패드가 보낸 프레임이 키보드와 바이트 동일한가" · "말이 움직였는가" | **네트워크 계층에서 참이고 좌표도 갱신됐다.** 그리고 뷰를 전환하면 폴링이 멈춰 마지막 트윈이 살아나 말이 목표로 튄다 — **검증 사이에 뷰를 오간 순간 화면이 스스로 정상으로 복구**된다 |

- **공통 형태**: 우리는 매번 **"고친 그것이 됐는가"**를 물었다. 실기 스크린샷은 아무것도 묻지 않고 **한 시점의 화면 전체**를 준다.
- 📌 §4.4가 이미 옳게 적어 둔 문장이 있다 — **"스크린샷은 근거이지 측정이 아니다."** 이번에 드러난 것은 그 역도 참이라는 것이다: **측정은 자기가 물은 것만 답한다.** 둘 다 있어야 하고, 우리에게 없던 쪽은 **"묻지 않은 눈"**이었다.
- 실행 가능한 최소 보강(제안 · 미착수): ① 폰 뷰에서 **`.hud-*` 쌍별 사각형 교차 판정** 1회(가림은 산술로 잡힌다) ② `document.scrollingElement.scrollHeight === innerHeight` **세로 확인** ③ 뷰 전환 없이 **같은 뷰에 머문 채** N초 뒤 토큰 좌표 재측정(§9.7의 "튀는" 증상이 여기서 드러난다).

## 변경 이력
- **2026-07-28(6차): 저녁 세 커밋 반영.**
  - **§9.7 신설** — 뷰4에서 말·장물이 얼어 있던 원인은 **폴링 렌더러가 60Hz로 트윈을 죽인 것**이었다(`2fb83ef`). **뷰1은 이벤트 구동이라 같은 비교로도 안전했다** — 같은 코드가 한쪽에서만 깨졌고 그 구조 차이가 원인이다. 재시작 조건을 "목표의 변화"로. 계략 NPC 위치 미갱신(재대국 유령)은 별개 결함으로 함께 해소.
  - **§9.7.1 신설** — 헤드리스 390×844 검증이 이 셋을 전부 통과시킨 이유. **측정은 자기가 물은 것만 답한다**(가림은 요소 *쌍*의 관계라 단일 rect로는 안 보이고, 전체 페이지 캡처는 문서 스크롤 증상 자체를 지운다).
  - **§9.6 추가** — `verify --quick`이 eval 리포트를 덮어써 전수 수치를 **`112,752 → 0`으로 퇴화**시키고 있었다(해소). **게이트가 자기 근거 파일을 지우고 있었고, 성실히 돌린 사람일수록 더 많이 지웠다.** 규약: 측정 도구의 기본값은 읽기 전용.
  - **§7.12 후속 2건** — 실기 iPhone 스크린샷이 잡은 **액션 바 가림·문서 스크롤·우측 컬럼 조작**(`1e23362`)과 **조작부 시각 정리**(`8dfa3e6`, 하단 6등분 한 줄 · 비활성은 채도만 · 뷰 선택은 좌하단 칩). `overscroll-behavior`는 스크롤 **연쇄**만 막는다는 것을 T0 이후 처음 실기에서 확인.
  - **§9.2 완료 표기** — `loop.sleep/wake` + `IsoView` 말풍선 타이머 정리가 이미 코드에 있었다. 체크리스트·실행계획이 "유일한 미착수 P0"로 계속 적고 있던 것을 정정.
- **2026-07-28(5차): 마지막 세 라운드 반영.**
  - **§7.1.1 신설** — 폴백률 **22% → 실측 0%**(`6d3a8a9`). 목표 길이 인하(상한 40 불변) + 3단 절단. 원인은 «1순위 절단이 원리적으로 발동 불가»였고 **계측이 답을 이미 갖고 있었다**(`drop 9` / `truncate 0`). 1차 시도가 ④ C4를 6/8로 떨어뜨린 것을 실측이 잡았고 8/8 복구. 캐시 0%는 설계 예측값(키 공간 15,552 대 발화 30)이라 **의도적으로 올리지 않음.** eval **P6** 신설(0/16 → 16/16, 단 코퍼스는 실측 표본 아님).
  - **§7.12 정정** — **T1 D-패드 컷 결정을 뒤집었다**(`55297c0`). 폰에서 말을 못 움직여 제출물 ① 요건의 절반이 성립하지 않았다. **화면이 안 깨지는 것과 플레이가 되는 것은 다르다.** 탭-투-무브(T2)는 클라 판정을 부르므로 폐기.
  - **§7.7 완료·확장** — AI 패널을 판 시작과 동시에 노출(`0·0·0`은 그리지 않고 `—` 관례) · `✨♻⚙` 범례 · 턴 HUD 겹침 해소(`55297c0`). 랜딩 AI 신호가 **첫 45초에 0**이던 것을 경계 고지 + 실존 경로 링크로 해소, `eval-narrator-report.md`를 실제로 생성(`c90c723`).
  - §1.3 구현 시 정정 2건(게이팅 폐기 · 말풍선 배지 미부착) 병기. §7.1 남은 조치 3건 전건 완료 표기.
- 2026-07-27: 최초 작성. 심사축·플레이경험·4뷰디자인 3개 관점 실코드 검수 결과 통합.
- 2026-07-27(2차): §7 추가. 라이브 실접속·시뮬레이션 1만 판·렌더 재검수. **§6.3·§5 반증**, §1.1 수치 정정, §1.2 재분류.
- **2026-07-28: §7.1 원인 규명·해결.** 추정(키 미설정/쿼터 소진)이 아니라 `thinkingBudget: 0`이 400으로 거부된 것이었다. 코드 수정 후 라이브 재검증 12/12 LLM.
- **2026-07-28(4차): 구현 완료 반영.** §7.5.3 봇 도박 고발(`1f55b28`) — **§7.5.3이 적어둔 "탈락 알파 버그는 솔로에서 발현되지 않는다"를 정정**(봇이 7.0%의 판에서 죽는다). §9.1 코드 스플리팅(`63c46be`, 2,172 kB → 115.66 kB). §9.6에 `pnpm verify` 통합 회귀 게이트 신설(`4244e5c`). §2.5에 비밀 통로 표기 상수 4종(`41c7a52`). §8.3 방 공개 상태 복귀. §9.3 `dispose()` 완료 표기.
- 2026-07-27(3차): **§8 멀티플레이·재접속 · §9 런타임 건전성 추가.** 이탈자 유령 카드로 판이 사고사하는 P0, 턴 타이머 부재, 재접속 손패 미복구 + 방 영구 잠금, 번들 2,130 kB 단일 청크, `dispose()` 0건, `STAGES.push` 확장성 반증. §7.8을 **zero-rect 문제로 정정.** 라이브 실접속·시뮬레이션 1만 판·렌더 재검수. **§6.3(⑤ 개인 참여 확정)과 §5(계략 밸런스 경고) 반증**, §1.1 상한 수치 정정, §1.2 재분류, §2.1·2.2 착수 전 선행 조건 발견.
