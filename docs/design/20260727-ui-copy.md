# UI 문안(카피) 명세 — 호랑이 생신 잔치

> 작성 2026-07-27 · **개정 2026-07-28**(§8.2 코드 정합 + 미확정 4건 확정) · 사람용 `.html` 동봉 · 근거: `apps/client/index.html` · `apps/client/src/main.ts` · `apps/server/src/rooms/clue-room.ts` · `apps/server/src/ai/narrator.ts` 실측
> 범위: **화면에 출력되는 한국어 문장 전부**. 진단·우선순위는 `20260727-improvement-roadmap.md` 소관(중복 금지), 이 문서는 **붙여넣을 문장**만 확정한다.
> 표기: `[있음]` 현재 DOM/코드에 있음 · `[선행]` **선행 조건**(요소·필드가 아직 없음, 문안은 확정하되 구현 전제 명시) · `[주의]` · `[조사]` 조사 오류 · `[톤]` 어휘·톤 불일치 · `[갈라짐]` 코드↔문서 불일치
> (상태는 이모지가 아니라 대괄호 표기로 적는다 — 사람용 `.html`은 같은 뜻의 CSS 배지를 쓴다. 이모지는 **확정 문안·게임 아이콘 전용**이다.)

---

## 0. 이 문서의 사용법
1. 새 문장을 쓰기 전에 §1 톤 규칙과 §2 어휘표를 먼저 본다.
2. 문장은 §3~§9에서 **그대로 복사**한다. 변형이 필요하면 이 문서를 먼저 고친다.
3. 적용 위치는 §10 표에서 파일·함수 단위로 찾는다.

---

## 1. 톤 규칙

### 1.1 두 개의 목소리 — 절대 섞지 않는다
| 층위 | 누가 말하는가 | 문체 | 화면 위치 | 예 |
|---|---|---|---|---|
| **캐릭터 대사** | 십이지 NPC | **사극체** (하게체·하오체, 종결 `-네/-군/-는가/-렷다`) | 말풍선, 로그의 `💬 <이름>:` 줄 | `쯧쯧, 서재 쪽을 살피니 「떡시루」, 그게 사라졌던걸` |
| **UI 안내** | 시스템(게임) | **현대 표준어 해요체** | 버튼 툴팁·모달·오버레이·랜딩·대기실 | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` |
| **시스템 로그** | 시스템(중립 기록) | **중립 서술** — `-습니다` 또는 체언 종결 | 우측 `📜 기록 / 알림` | `🚪 진입 — 호랑이 대감 → 대청마루` |

- **경계선은 접두 기호다.** `💬` 로 시작하는 줄만 사극체. 그 외 모든 줄은 사극체 어휘(`~하렷다`, `자네`, `아뢰오`)를 쓰지 않는다.
- UI 안내에 사극체를 섞으면 "분위기"가 아니라 **조작 불능**이 된다 — 안내문은 규칙을 알려주는 유일한 통로다.
- 로그와 UI 안내의 문체가 다른 것은 **의도**다. 로그=사실 통보(기록물), UI 안내=사용자 대화. 이 두 계열을 서로 맞추려 하지 말 것.

### 1.2 조사(은/는·이/가·을/를·(으)로) — **카피 차원에서 회피한다**
실측 결함(`호랑이 대감가 대청마루(으)로 불려왔습니다`, `떡시루이 사라졌던걸`)은 **변수 뒤에 조사를 붙였기 때문**이다. 종성 판정 헬퍼를 넣는 것보다 **문장 구조를 바꾸는 쪽이 항상 싸고 안전하다.**

| # | 규칙 | 금지 | 확정 |
|---|---|---|---|
| **R1** | **변수 뒤에 조사를 붙이지 않는다.** 꼭 필요하면 받침이 고정된 접미어(`님`·`편`·`쪽`·`측`·`것`·`방`)를 사이에 끼운다 | `${name}이` | `${name} 님이` |
| **R2** | 주격/목적격 → **콜론 라벨** | `${w}을 훔쳤다` | `훔친 것: ${w}` |
| **R3** | 이동·변화 → **화살표 `→`** | `${s}가 ${r}(으)로 불려왔습니다` | `소환 — ${s} → ${r}` |
| **R4** | 인용·강조 → **낫표 「」 + 체언 종결** | `${w}이 사라졌다` | `사라진 것 — 「${w}」` |
| **R5** | 변수 뒤에 허용되는 조사는 **받침 무관한 것만**: `에 · 에서 · 의 · 도 · 만 · 까지 · 부터` | `${r}(으)로` | `${r}에서` |
| **R6** | 종성 판정 헬퍼(`packages/shared/src/josa.ts`)는 **NPC 대사 전용**. UI·로그는 R1~R5로 해결한다 | — | — |

> R6의 근거: NPC 대사는 자연스러운 사극 문장이어야 하므로 조사를 못 피한다. 반면 UI·로그는 라벨형 문장이 오히려 **읽기 빠르다** — 회피가 타협이 아니라 개선이다.

### 1.3 문장 규칙
- **1문장 = 1행동.** 안내문에 두 가지를 시키지 않는다.
- **금지 상태에는 반드시 "그래서 뭘 하면 되는지"를 붙인다.** `방 안에서만 가능해요.`(금지) → `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.`(확정)
- 느낌표는 **판 전체에서 3종만** 허용: 승리·최후 생존·미반증(정답 후보). 그 외는 마침표.
- 이모지는 **줄 앞 1개**. 연속 배치 금지(`🎉🎉`, `❌ ⚠`).
- 사용자 지칭은 `내`(소유)·`나`(주체). "유저"·"플레이어"는 **UI에 쓰지 않는다**(문서 전용).
- 오류 문구에 `에러`·`실패`를 단독으로 쓰지 않는다 → `~하지 못했어요` + 다음 행동.

### 1.4 진실값 경계 (CLAUDE.md 규약의 카피 측면)
- UI 문안은 **정답·타인 손패를 암시하지 않는다.** `정답 후보`는 규칙엔진이 계산한 공개 결과이므로 허용, `정답인 듯해요` 같은 추측형은 금지.
- 나만 보는 정보에는 **반드시 `(나만 봄)` 표기**를 붙인다: 반증 카드, 증거 노트. (계략 엿보기는 07-30 제거됨)
- NPC 대사 문안은 **LLM 출력이므로 이 문서가 확정하지 않는다.** 이 문서가 정하는 것은 폴백 템플릿(§8.2)과 접두 기호뿐.
- `[주의]` **폴백 템플릿에 대한 이 문서의 확정 권한은 "길이 예산 안에서"다.** 예산은 제출물 ④ §2.3이 정하고 코드 상수(`LINE_BUDGET_SUGGEST` 25 · `LINE_MAX` 40)가 강제한다 — 예산이 문안보다 상위다. 07-27 확정본이 이 예산을 어겨 07-28에 템플릿을 교체했다(§8.2 정정 노트).
- **대사 경로 표시(✨/♻/⚙)는 진실값이 아니라 운영 메타데이터다**(④ §4). 게임 상태를 말하지 않으므로 `(나만 봄)` 대상도 아니고, 숨기지도 않는다 — 문안은 §4.4.

### 1.5 길이 제약
| 대상 | 상한 | 근거 |
|---|---|---|
| 버튼 라벨 | **8자** | `.hud-ctrl` 5개 버튼이 한 줄에 들어가야 함 |
| 툴팁(`title`) | **40자** | 40자 초과 시 OS 툴팁이 줄바꿈되어 커서를 덮음 |
| 로그 1줄 | **48자** | `.rp-col` 기본 260px 기준 2행 이내 |
| 턴 배너 상태줄 | **28자** | `.hud-turn`이 `white-space: nowrap` |
| 결과 제목 | **16자** | `.end-title` 26px |
| 결과 부제 | **44자** | `.end-sub` 1~2행 |
| 목표 카드 1줄 | **32자**(권장) | **4줄** 카드가 380px 안에. ⚠️ ③은 44자로 이미 초과 상태이며 폰에서 2줄로 접힌다 — 교정 대상(§3.1) |
| 랜딩 `.msg` | **40자** | `min-height:16px` 1행 |
| **NPC 대사 — 제안** | **25자** | 코드 `LINE_BUDGET_SUGGEST`. 봇 1턴 = `npcDelay`+`BOT_ACT_GAP`+`bubbleLifeMs(len)` ≤ 4s 역산(④ §2.3) |
| **NPC 대사 — 고발·계략** | **40자** | 코드 `LINE_MAX`. 홀드 타이머가 없어 클라이맥스 대사의 캐릭터색을 남긴다 |
| AI 로그 배지 | **8자** | `.log-badge` 1칸. 폴백 사유 한글 표기(§4.4)가 이 상한에 맞춰져 있다 |

---

## 2. 어휘 통일표 — 1개념 = 1표기

| 개념 | **확정 표기** | 쓰지 않는 표기 (현재 코드에 혼재) |
|---|---|---|
| 3요소 ①누가 | **용의자** (지목 대상), **도둑**(정답 확정 후) | `범인`, 로그의 `도둑:`(제안 단계에선 `용의자:`로) |
| 3요소 ②무엇을 | **훔친 것** | `장물`, `무기`, `weapon` 노출 |
| 3요소 ③어디서 | **장소** | `방`(칸 개념과 충돌), `현장` |
| 내 카드 묶음 | **내 손패** | `내 단서 패`, `핸드` |
| 전체 공개 카드 | **공통 단서** | `공개 카드`, `공용 카드` |
| 방 진입점 | **입구(🚪)** | `문`, `도어` |
| 게임 시작 | **잔치 시작** | `게임 시작`(서버 로그가 이 표기) |
| 한 판 | **판** | `게임`, `라운드`(라운드는 턴 한 바퀴) |
| 방 참가 동작 | **참가** | `참여`(공개방 목록 버튼이 이 표기) |
| 고정 NPC 능력 | **계략** | `보너스`, `bonus` |
| 관전 상태 | **관전** | `구경`, `스펙테이터` |
| AI 플레이어 | **NPC** | `봇`, `AI 플레이어`(UI 한정. 문서·코드는 봇 허용) |
| 대리 NPC | **대리 NPC** `[선행]` | `임시 봇`, `AFK 봇` |
| 승자 없는 종료 | **무승부** | `비김`, `드로우`, `유찰` |
| 대사 생성 경로 3종 | **LLM · 캐시 · 폴백** | `AI`, `fallback`, `hit`, `규칙 대사` |
| 폴백 사유 | **한글 표기 6종(§4.4)** | 영문 토큰(`timeout`·`http`…) 화면 노출 |

**액션 버튼 라벨 5종 (07-30 교체 · 동사형 통일)** — 버튼에 찍히는 글자와, 문장 안에서 그 버튼을 가리키는 대괄호 표기는 **글자 그대로 같아야 한다.**

| 버튼 | **확정 라벨** | 개념어(그대로 유지) |
|---|---|---|
| `#suggest` | **추리하기** | 개념·로그·기록표는 **제안** — `🔍 제안`, `제안 기록표`, `제안 상한 75`는 바꾸지 않는다 |
| `#accuse` | **신고하기** | 개념은 **고발** — 모달 제목 `고발 — 진범을 지목하라`(§6.2)와 `❌ 고발 실패`는 유지 |
| `#passage` | **지름길로 가기** | 개념은 **비밀 통로** |
| `#bonus` | **다가가기** | 개념은 **계략**(위 표 「고정 NPC 능력」 행) · 07-30 엿보기 효과 제거·이동 보너스만 — 로그 `🃏 계략 — …에게 다가가 이동 +N` |
| `#endTurn` | **내 턴 종료** | 개념은 **턴 종료** |

> ⚠️ **라벨만 바꿨고 툴팁·안내 문안의 동사는 아직 개념어다** — 예: `#suggest`가 「추리하기」인데 툴팁은 `방 안에서만 제안할 수 있어요`(§5.1). 교체 스펙이 버튼 라벨 5종으로 범위를 한정했기 때문이며, 「같은 것을 두 이름으로 배우게 하지 않는다」(§8.1)에 걸리는 지점이라 **기획 판단이 필요하다.** 판단 전까지는 라벨만 새 이름이다.

**표기 접두 기호(고정)**
`🎲` 주사위 · `🔍` 제안 · `🛡` 반증 · `❗` 미반증 · `❌` 실패·탈락 · `🎉` 승리 · `🏅` 최후 생존 · `🏳` 무승부 · `🚪` 방 진입/통로 · `🃏` 계략·손패 · `💬` 캐릭터 대사 · `📢` 공통 단서 · `⏳` 턴/시간 · `💤` 대리 중 · `🤖` NPC · `📍` 장소 · `✨` LLM 대사 · `♻` 캐시 대사 · `⚙` 폴백 대사

> 접두 기호는 **이형 선택자(VS16 `U+FE0F`) 없이** 쓴다. 저장소 전체가 그렇고(`🛡` `U+1F6E1` 단독), 한 곳만 붙이면 폭이 달라져 로그 좌측 정렬이 어긋난다.

> ~~`[주의]` 증거 노트 `장소` 그룹 칩의 아이콘 칸이 전부 비어 있다.~~ **07-29 해소 확인.** `EMOJI` 맵에 장소 9종이 없는 것은 그대로지만(카드에 이모지를 추가하지 않는다 — 방 9곳에 자연스러운 이모지가 없다), `main.ts` `cardIcon()`이 장소를 **고정 문자 `📍`**로 채운다 — 증거 노트·제안 모달·손패가 같은 경로를 탄다.

---

## 3. 첫 진입 목표 카드 `[선행]`

**선행 조건**: 오버레이 DOM(`#goalCard`), 1회 표시 플래그(`localStorage: zc_seen_goal`), 재열람 버튼(`.hud-ctrl` 끝 `[?]`), `?demo=1` 스킵 파라미터. 현재 저장소에 튜토리얼·도움말 DOM은 0건.

### 3.1 전문
```
🐯 60초 안내

① 목표 — 도둑 1명 · 훔친 것 1개 · 장소 1곳을 맞히면 승리.
② 출발점 — 내 손패 3장은 정답이 아닙니다. 여기서부터 지워 나가세요.
③ 방법 — 방향키로 이동해 방에서 [추리하기]. 아무도 반증 못 한 조합이 정답 후보예요.
④ 끝내기 — 확신할 때만 [신고하기]. 틀리면 즉시 탈락해요.

              [ 알겠어요, 시작할게요 ]

좌측 상단 [?]를 누르면 다시 볼 수 있어요.
```

- 제목 `🐯 60초 안내` (8자) — "튜토리얼"이라 쓰지 않는다(길고, 학습 부담을 예고한다).
- ①②③④ 각 줄 ≤32자(권장). **② 가 이 카드의 존재 이유다** — 순서를 바꾸지 말 것.
- **④ 신설(2026-08-26).** 이전 3줄은 목표·출발점·방법을 가르치면서 **«어떻게 이기는가»가 없었다** —
  ①이 "맞히면 승리"라고만 하고 그 선언을 어느 버튼으로 하는지도, **틀리면 즉시 탈락**한다는 것도
  말하지 않았다. 신규 유입이 ①을 읽고 [신고하기]를 누르는 경로가 그대로 열려 있었다.
  라벨을 `끝내기`로 잡아 ③ `방법`(반복하는 행동)과 **역할로** 대비시킨다 — 버튼 이름 둘을 나란히
  놓고 «다르다»를 설명하는 것보다 각자의 자리를 주는 편이 짧다.
  **문안 근거**: 「탈락」에서 끊지 않고 **「반증만 남아요」까지** 말한다. 오답 고발의 실제 대가는
  «쫓겨남»이 아니라 **«남은 판 내내 앉아서 반증만 하는 것»**이다 — `doSuggestion`은 `turnOrder`
  전체를 돌며 eliminated를 거르지 않는다(`clue-room.ts`). 저장소의 다른 세 곳이 이미 그렇게 말한다:
  §5.2 `남은 역할은 반증입니다` · §7.2 `내 손패로 반증은 계속돼요` · 서버 로그 `탈락 · 반증만 가능`.
  ④만 「탈락」에서 끊으면 카드가 그 대가를 감춘다.
  같은 사고를 **시각**으로 막은 것이 액션 바 3단 위계다(`docs/plans/done/22-action-bar-hierarchy.md`) —
  이 줄은 그 **문안 쪽 절반**이고, 시각이 말할 수 없는 «왜 다른지»를 맡는다.
- 버튼 라벨 `알겠어요, 시작할게요` — `확인`·`닫기`는 쓰지 않는다(무엇이 확인됐는지 알 수 없음).
- 재열람 안내는 **본문보다 작게, 카드 하단**. 12자 이상 줄이지 말 것 — 이 문장이 없으면 사용자가 카드를 닫기를 주저한다.

### 3.2 재열람 버튼 `[?]`
- 라벨 `?` · 툴팁 `게임 목표와 조작을 다시 봅니다.` (18자)

---

## 4. 손패 라벨 · 공통 단서

### 4.1 손패 HUD (`.hud-hand` / `#hand`)
| 상태 | 현재 `[있음]` | **교체안** |
|---|---|---|
| 잔치 시작 전 | `내 단서 패: (잔치 시작 후 표시)` | `🃏 내 손패 — 잔치가 시작되면 3장이 배부돼요.` |
| 배부 후 | `내 단서 패: 잡채, 서재, 뱀 무녀` | 아래 2행 블록 |
| 재접속 직후 `[선행]` | (복구 안 됨) | `🃏 내 손패 — 불러오는 중…` |

**배부 후 블록 (2행)**
```
🃏 내 손패 · 이 3장은 정답이 아님
🍜 잡채    📍 서재    🐍 뱀 무녀
```
- 1행이 **라벨이자 규칙 설명**이다. `이 3장은 정답이 아님`을 괄호로 감싸지 말 것 — 괄호는 부가정보로 읽혀 건너뛴다.
- 2행은 카테고리 접두 이모지 + 라벨. 쉼표 대신 **넉넉한 공백**으로 카드처럼 분리.
- `.hud-hand` 툴팁: `내 손패는 정답 봉투에 없어요. 후보에서 먼저 지우고 시작하세요.` (34자)

### 4.2 공통 단서
- 서버 로그(§8): `📢 공통 단서 — 모두 공개 · 정답 아님: 젓가락, 행랑채`
- 손패 HUD 3행(있을 때만): `📢 공통 단서 · 모두 공개: 🥢 젓가락  📍 행랑채`
- 증거 노트 칩 툴팁: `공통 단서 — 모두 공개 · 정답 아님 (자동 제외)` (26자)

### 4.3 증거 노트 (`#evidence`)
| 대상 | 문안 |
|---|---|
| 패널 제목 `[있음]` | `🔍 증거 노트` + 부제 `(나만 봄)` — **유지** |
| 내 패 칩 툴팁 | `내 손패 — 정답 아님 (자동 제외)` (20자) |
| 공통 단서 칩 툴팁 | `공통 단서 — 모두 공개 · 정답 아님 (자동 제외)` |
| 일반 칩 툴팁 `[있음]` | `클릭: 없음(제외) → 의심 → 초기화` — **유지** |
| 그룹 라벨 | `용의자` / `훔친 것` / `장소` (§2 표기와 일치) |
| 빈 상태 `[선행]` | `아직 지운 후보가 없어요. 칩을 눌러 표시하세요.` |

> ~~`[갈라짐]` 코드가 갈라져 있다(07-28 확인).~~ **07-29 해소 — 코드가 확정본과 같다**(`main.ts` `buildEvidence()`의 잠금 칩 `chip.title`). 갈라졌던 이유의 기록은 남긴다: ① `내 패`는 §2 어휘표 위반(확정 표기는 `내 손패`, `내 패`는 셀렉트 옵션 접미 §6.1·§6.2 전용 축약형이다) ② 잠금 칩은 **클릭해도 반응하지 않는 유일한 칩**인데 옛 코드 문안에는 그 이유가 없었다 — `(자동 제외)`가 §1.3 "금지 상태에는 왜 그런지를 붙인다"를 만족시키는 부분이다 ③ 길이는 각각 20자·26자로 툴팁 40자 상한 안이다.

### 4.4 AI 카운터 칩 · 대사 경로 배지 (`#aiPanel` · `.log-badge`) `[있음]`
근거: 제출물 ④ §4(관측 가능성) 요구사항 3·5. **칩 본문과 경고 배지는 ④가 지정한 문자열이며 이 문서는 그것을 그대로 인용한다.** 여기서 새로 확정하는 것은 ① 폴백 사유 6종의 한글 표기 ② 상세 패널 행 라벨이다.

| 요소 | 문안 |
|---|---|
| 패널 제목 `[있음]` | `🤖 AI 대사` — **유지** |
| 칩 본문 | `✨LLM 12 · ♻캐시 3 · ⚙폴백 0` — 세 경로를 **항상 함께** 보여준다(폴백이 전부라도 숫자를 가리지 않는다) |
| 칩 툴팁 | 칩 본문과 **같은 문자열** |
| 상세 · 평균 지연 | 라벨 `평균` · 값 `1.2s` (계측 전이면 `—`) |
| 상세 · 모델 | 라벨 `모델` · 값 `gemini-flash-lite-latest` (계측 전이면 `—`) |
| 상세 · 사유별 건수 | 라벨 `⚙ 응답 지연` · 값 `3` |
| 전건 폴백 경고 | `AI 대사 일시 폴백` (④ §4 요구사항 5 지정본) |
| 로그 배지 · LLM/캐시 | `✨ 1.2s` / `♻ 0.0s` |
| 로그 배지 · 폴백 | `⚙ 응답 지연` |
| 로그 배지 툴팁 | `llm · 1183ms · gemini-flash-lite-latest` — **진단용 원값이라 영문 토큰을 허용하는 유일한 자리** |

**폴백 사유 6종 — 한글 표기 확정** (코드는 현재 영문 토큰을 그대로 찍는다 → 교체 필요)

| 코드 토큰 | **확정 표기** | 실제로 있었던 일 |
|---|---|---|
| `timeout` | **응답 지연** | 4000ms 초과(`AbortError`) |
| `http` | **응답 오류** | 2xx 아님(429·5xx) 또는 왕복 실패 |
| `empty` | **빈 응답** | `candidates` 텍스트 없음 |
| `toolong` | **길이 초과** | 정규화로도 예산에 못 맞춰 폐기 |
| `nokey` | **키 없음** | `GEMINI_API_KEY` 미설정 |
| `disabled` | **LLM 꺼짐** | `AI_NARRATE=off` |

- 전부 **5자 이내 체언 종결** — 배지 8자(§1.5)와 로그 48자 상한을 동시에 만족한다. 조사 없음(R1).
- `timeout`을 `시간 초과`로 옮기지 않는다 — 그 문안은 §9.5 **턴 만료**(`⌛ 시간 초과`)가 이미 쓴다. 서로 다른 사건에 같은 말을 주면 로그에서 구분되지 않는다.
- `실패`를 단독으로 쓰지 않는 것은 §1.3 규칙이다. 다만 이 배지는 **관측 표시**라 "다음 행동"을 붙이지 않는다 — 사용자가 할 수 있는 일이 없고 게임은 정상 진행된다. 대신 사용자 대상 문장은 `AI 대사 일시 폴백` 하나로 끝낸다.
- 칩의 `LLM`은 캐릭터가 아니라 **문장이 온 경로**의 이름이므로, "UI에서 AI 플레이어는 NPC로 부른다"는 §2 규칙과 충돌하지 않는다.
- 칩은 **첫 계측이 도착하기 전에는 표시하지 않는다**(코드 `aiSeen`). `0 · 0 · 0`을 먼저 띄우면 "대사가 없었다"는 없는 사실을 주장하게 된다 — **문안 없음이 확정본**이다.

---

## 5. 액션 버튼 4종 — 상태별 문안

### 5.0 구현 전제 (문안보다 먼저 고쳐야 하는 것)
- **`disabled` 속성이 붙은 버튼은 `title` 툴팁이 뜨지 않고 클릭 이벤트도 발생하지 않는다.** 현재 비활성 사유 안내(`addLog("방 안에서만…")`)가 실행되지 않는 것은 이 때문이다.
- 따라서 비활성은 `disabled` 대신 **`aria-disabled="true"` + `.is-off` 클래스**로 표현하고, 클릭 핸들러는 살려 둔 채 첫 줄에서 사유를 안내하고 `return` 한다.
- **같은 문장을 툴팁과 클릭 피드백에 공용한다.** 문안을 두 벌 만들지 않는다.
- 클릭 피드백 출력처는 **로그가 아니라 턴 배너 부제(`.ti-sub`)** `[선행]` — 사용자의 시선은 버튼 근처에 있고 로그는 우측 끝이다. 로그에도 남기려면 동일 문장을 그대로.

### 5.1 [추리하기] `#suggest`
| 우선순위 | 조건 (판정식) | 문안 (툴팁 = 클릭 피드백) |
|---|---|---|
| 1 | `state.phase !== "playing"` | `잔치가 시작되면 추리할 수 있어요.` |
| 2 | `me.eliminated` | `탈락한 뒤에는 추리할 수 없어요. 반증만 이어집니다.` |
| 3 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 4 | `!me.room` | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` |
| 5 `[선행]` | 같은 방 재제안 차단 도입 시 | `이 방에서는 이미 추리했어요. 다른 방으로 옮기세요.` |
| — | **활성** | `이 방에서 용의자와 훔친 것을 지목해요. 장소는 현재 방으로 고정.` |

> 판정 순서를 지킬 것. `탈락 + 내 턴 아님`이 동시 성립할 때 `내 차례가 아니에요`를 보여주면 사용자는 차례를 기다린다.

### 5.2 [신고하기] `#accuse`
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` | `잔치가 시작되면 신고할 수 있어요.` |
| 2 | `me.eliminated` | `이미 신고에 실패했어요. 남은 역할은 반증입니다.` |
| 3 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| — | **활성** | `정답 3장을 지목해요. 틀리면 즉시 탈락합니다.` |
| — | 추리 직후 활성 `[선행]` | `지금 신고할 수 있어요. 넘기려면 [내 턴 종료].` |

### 5.3 [지름길로 가기] `#passage`
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` 또는 `me.eliminated` | **행위 중립 공용 문안** — `잔치가 시작되면 쓸 수 있어요.` / `탈락한 뒤에는 쓸 수 없어요. 반증만 이어집니다.` |
| 2 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 3 | `!me.room` | `지름길은 방 안에서만 쓸 수 있어요.` |
| 4 | `!passageOf(me.room)` | `이 방에는 지름길이 없어요. (지름길은 3쌍)` |
| — | **활성** | `지름길 — 서재 → 정지(부엌). 주사위 없이 즉시.` |

- 활성 툴팁은 **목적지 이름을 넣어 동적 생성**한다: `` `지름길 — ${label(me.room)} → ${label(dest)}. 주사위 없이 즉시.` `` — R3(화살표)로 조사 없음.

### 5.4 [다가가기] `#bonus`
> 07-30 기획 다이어트(다희): «엿보기»(정보열람) 효과 제거, **이동 보너스만** 남김. 라벨도 `[엿보기]→[다가가기]`.
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` 또는 `me.eliminated` | **행위 중립 공용 문안** — `잔치가 시작되면 쓸 수 있어요.` / `탈락한 뒤에는 쓸 수 없어요. 반증만 이어집니다.` |
| 2 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 3 | 인접 헬퍼 있으나 `used` `[선행]` | `이 NPC에겐 이미 다가갔어요. 다른 NPC를 찾아보세요.` |
| 4 | 인접 헬퍼 없음 | `다가갈 이가 근처에 없어요. 보드 가장자리의 NPC 곁으로.` |
| — | **활성** | `다가가기 — 근처 NPC에게 다가가 이동 보너스. NPC마다 1회.` |

> `[주의]` 현재 코드의 활성 판정은 `!h.used && 인접`을 한 번에 검사하므로 **3번과 4번을 구분하지 못한다**(이미 쓴 NPC 옆에 서면 "근처에 없어요"가 뜬다). 두 조건을 분리해야 이 문안이 성립한다.
> `[주의]` `CLAUDE.md`는 계략을 **"판당 1회"**로 규정하지만 코드는 `helper.used`로 **토큰(NPC)마다 1회**다. 문안은 코드 실동작(`NPC마다 1회`)을 따르고, 규약 문구 정정은 로드맵 소관.

### 5.5 [내 턴 종료] `#endTurn` (4종 외 · 참고)
- 활성 툴팁: `이번 차례를 마치고 다음 사람에게 넘겨요.`
- 이동 남았을 때 `[선행]`: `아직 ${n}칸 남았어요. 그래도 넘길까요?` → 확인 없이 즉시 종료(되돌릴 수 없으므로 툴팁으로만 경고)

---

## 6. 제안 모달 / 고발 모달

공통 규칙
- **제목은 무엇을 고르는지 말한다.** `제안`(금지 — 명사만) → `제안 — 이 방에서 누가, 무엇을?`
- **확인 버튼 라벨에 동사를 넣는다.** `확인`(금지) → `이 조합으로 제안` / `고발한다`.
- 취소 버튼은 `취소` `[있음]` 유지.
- 셀렉트 옵션 라벨: `이모지 + 라벨` (`🐍 뱀 무녀`). 장소는 `📍` 접두.

### 6.1 제안 모달
```
제안 — 이 방에서 누가, 무엇을?
──────────────────────────────
 장소     📍 대청마루   현재 방 · 고정
 용의자   [ 🐍 뱀 무녀            ▾ ]
 훔친 것  [ 🍶 술동이             ▾ ]
──────────────────────────────
 지목한 인물과 물건이 이 방으로 소환돼요.

                  [ 취소 ]  [ 이 조합으로 제안 ]
```
| 요소 | 문안 |
|---|---|
| 제목 | `제안 — 이 방에서 누가, 무엇을?` |
| **장소 고정행** `[선행]` | 라벨 `장소` / 값 `📍 ${label(me.room)}` / 배지 `현재 방 · 고정` |
| 장소 행 툴팁 | `제안 장소는 지금 서 있는 방으로 정해져요.` (22자) |
| 안내 줄 | `지목한 인물과 물건이 이 방으로 소환돼요.` (21자) |
| 확인 버튼 | `이 조합으로 제안` (8자) |
| 내 패 옵션 접미 `[선행]` | `— 내 패` (선택 **가능**. 반증자를 통제하는 정석 전술이므로 제안에서는 막지 않는다) |

> 현재 `openPicker(..., needRoom=false)`라 장소 행이 아예 없다. 고정행은 **읽기 전용 표시**이며 서버로 보내는 값(`room: ""`)은 바꾸지 않는다.

### 6.2 고발 모달
```
고발 — 진범을 지목하라
──────────────────────────────
 ⚠ 틀리면 즉시 탈락합니다. 그 뒤로는 반증만 할 수 있어요.
──────────────────────────────
 용의자   [ 🐍 뱀 무녀            ▾ ]
 훔친 것  [ 🍶 술동이             ▾ ]
 장소     [ 📍 서재               ▾ ]
──────────────────────────────
 이 3장이 정답 봉투와 모두 같아야 승리해요.

                  [ 취소 ]  [ 고발한다 ]
```
| 요소 | 문안 |
|---|---|
| 제목 | `고발 — 진범을 지목하라` (사극 어감이지만 **명령형 관용구**라 허용. 본문은 해요체) |
| 위험 경고 (상단 고정) | `⚠ 틀리면 즉시 탈락합니다. 그 뒤로는 반증만 할 수 있어요.` (33자) |
| 하단 안내 | `이 3장이 정답 봉투와 모두 같아야 승리해요.` (23자) |
| 확인 버튼 | `고발한다` (4자) — 위험 스타일(`--red` 계열 배경) |
| 내 패 옵션 접미 `[있음]` | `— 내 패 (정답 아님)` · `disabled` **유지** (자멸 방지) |

- 경고는 **모달을 열자마자 보이는 위치**에 둔다. 확인 버튼 옆 작은 글씨는 이미 결정한 뒤라 읽지 않는다.
- 2차 확인 다이얼로그는 만들지 않는다 — 경고 + 동사 버튼으로 충분하고, 턴 시간이 유한하다.

---

## 7. 결과 화면 · 관전 배너

### 7.0 선행 조건
- 현재 `updateEndState()`는 **승자 이름만 보고 한 문장**을 쓴다(`사건이 종결되었습니다. 정답은 기록(우측)을 확인하세요.`). 6종 분기를 위해 `winner === 나 / 나의 eliminated / 승리 사유(고발 성공 vs 최후 생존) / winner === ""(무승부)`가 필요하다.
- **정답 봉투 문자열이 클라이언트에 없다.** 07-28 기준 로그로 브로드캐스트되는 경로는 **고발 성공**과 **무승부**(`endInDraw()`) 둘뿐이고, **최후 생존 종료에서는 정답이 어디에도 공개되지 않는다.** `[선행]` `state.solutionText`(종료 시에만 채움) 또는 `broadcast("solution", {...})` 필요 — 오버레이 6종이 전부 정답 봉투를 화면에 실으려면 이 전달이 선행이다.
- 정답 표기(공용): `` `정답 봉투 — ${emoji(s)} ${label(s)} · ${emoji(w)} ${label(w)} · 📍 ${label(r)}` `` → R2/R4로 조사 없음.

### 7.1 결과 오버레이 6종

> **2026-07-28 구현 정정 — 4번(고발 실패)의 우선순위.**
> 이 절은 «내가 탈락 상태의 무승부 → 4번 우선»만 적었으나, 문자 그대로 구현하면
> 실측 발생률 **0.0%**인 무승부에서만 뜨는 **죽은 문안**이 된다.
> 같은 절이 «4번의 대조는 패배 화면의 유일한 정보 가치 — 생략하지 말 것»이라 못박고 있으므로,
> 코드는 4번을 **탈락 상태 전반의 최상위**로 올렸다. 그 해석을 여기 확정한다.
> 최종 판정 순서: **4 고발 실패 → 5 무승부 → 3/3′ 최후 생존 → 1/2 고발 성공.**
> 3′·2는 **좌석 없는 관전자**가 보는 화면이다.
>
> 또 최후 생존 종료 경로에 `📜 정답 봉투 — …` 로그 1줄이 추가됐다(§8.1 표에 반영 필요 시 같은 문자열).

| # | 조건 | 제목 (`#endTitle`) | 부제 (`#endSub`) | 버튼 |
|---|---|---|---|---|
| 1 | 내가 고발 성공 | `🎉 사건 해결!` | `정답 봉투 — 🐍 뱀 무녀 · 🍶 술동이 · 📍 서재` | `[다시 하기]` `[메인으로]` |
| 2 | 남이 먼저 해결 | `🔍 사건 종결` | `🐭 생쥐 서생 님이 먼저 맞혔어요. 정답 봉투 — 🐍 뱀 무녀 · 🍶 술동이 · 📍 서재` | `[다시 하기]` `[메인으로]` |
| 3 | 내가 최후 생존 | `🏅 최후의 1인` | `모두 오답으로 탈락했어요. 정답 봉투 — 🐍 뱀 무녀 · 🍶 술동이 · 📍 서재` | `[다시 하기]` `[메인으로]` |
| 3′ | 남이 최후 생존 | `🏅 판 종료` | `🐭 생쥐 서생 님만 남았어요. 정답 봉투 — …` | 동일 |
| 4 | 내가 오답 탈락 후 종료 | `❌ 고발 실패` | `내 지목 — 🐰 토끼 낭자 · 🥢 젓가락 · 📍 안방 / 정답 봉투 — 🐍 뱀 무녀 · 🍶 술동이 · 📍 서재` | 동일 |
| 5 | **무승부**(제안 상한 도달 · `winner === ""`) | `🏳 무승부` | `제안 75회에 이르도록 아무도 해결하지 못했어요. 정답 봉투 — 🐍 뱀 무녀 · 🍶 술동이 · 📍 서재` | 동일 |

**5(무승부) 확정 근거**
- 판정식은 `phase === "ended" && winner === ""` — 서버 `endInDraw()`가 만드는 유일한 상태다(`SUGGEST_CAP` 75회 도달). 실측 발생률 0.0%지만 **코드에 살아 있는 분기이므로 문안 없이 두지 않는다**(문안이 없으면 승자 칸이 빈 채로 `게임 종료`가 뜬다).
- 제목에 느낌표를 붙이지 않는다 — 허용된 느낌표 3종(승리·최후 생존·미반증)에 무승부는 없다. 제목 5자(상한 16자).
- 부제 1문장은 **왜 끝났는지**를 말한다. `75`는 서버 상수(`SUGGEST_CAP`)를 넣어 만들며 `회에`는 받침 무관(R5)이라 조사 문제가 없다. 2행 허용.
- **내가 이미 탈락한 상태에서 무승부로 끝나면 4번이 우선한다.** 나에게 일어난 일(고발 실패)이 판 전체의 결과보다 구체적이고, 4번만이 `내 지목 / 정답 봉투` 대조를 보여준다. 이때 부제 뒤에 `· 무승부로 종료`를 덧붙이지 않는다 — 3행이 된다.
- 승자가 없으므로 **승리 연출(`setOutcome`)은 호출하지 않는다** — 현재 코드가 이미 그렇다(문안 없음이 확정).

- **부제 44자 상한을 넘는 2·3·4·5는 2행 허용**(`.end-sub`가 `margin` 기준 2행까지 안정). 3행이 되면 정답 봉투만 남기고 앞 문장을 잘라낼 것.
- 4번의 `내 지목 / 정답 봉투` 대조는 **패배 화면의 유일한 정보 가치**다. 생략하지 말 것.
- `사건이 종결되었습니다. 정답은 기록(우측)을 확인하세요.` 는 **폐기.** 클라이맥스에서 사용자를 로그로 보내는 문장이다.
- `[선행]` 리매치 합의 도입 시 버튼 라벨은 §9.4로 대체.

### 7.2 관전 배너 (`#spectateBar`) — 도달 상태 **2종**

> **정정 (2026-07-29) — 이 절은 `#spectateBar`를 «진행 중 탈락» 전용으로만 규정했다.**
> 실제로는 **좌석 없는 관전자**(진행 중 입장 · 재접속 창을 넘겨 초대 코드로 재참가)도 **같은 배너**에 도달한다.
> 그런데 표시 판정이 `players.get(나)?.eliminated` 하나였고 관전자에게는 그 값이 `undefined`라
> **배너가 한 번도 뜨지 않았다.** 손패도 턴도 없는 화면을 아무 설명 없이 보게 되어 **"판을 잃었다"로 읽힌다**
> (커밋 `40f0b6e` — 판정을 `탈락 || 좌석 없음`으로 넓혔다).
> **문안은 새로 짓지 않았다** — §8.1 행 268의 서버 안내와 **글자 그대로 같은 문자열**을 재사용한다.
> §8.1 표 말미의 규약(«같은 사유를 두 문장으로 배우게 하지 않는다»)을 **서버 로그 ↔ HUD 배너**로 확장한 것이다.

| 요소 | 현재 `[있음]` | **교체안 / 확정** |
|---|---|---|
| 본문 ① **진행 중 탈락** | `❌ 탈락 · 관전 중` | `❌ 고발 실패 · 관전 중 — 내 손패로 반증은 계속돼요.` (28자) |
| 본문 ② **좌석 없는 관전** `[있음]` | (표시 자체가 없었다) | `이미 진행 중인 판이에요. 관전으로 들어갑니다.` — **§8.1 행 268과 같은 문자열** |
| 버튼 | `[메인으로]` | `[나가기]` (같은 위치, **확인 절차를 거친다**) |

- **DOM은 배너 하나 · 문장만 갈아 끼운다**(`#specText`). 상태가 둘이라고 배너를 둘로 늘리면 문안 소스가 갈라진다 — 소스는 이 표 하나다.
- ②는 서버가 입장 순간 보내는 **로그 1줄과 같은 문장**이다. 로그는 곧 위로 밀려 올라가므로 배너가 그 문장을 **상시로 붙들어 둔다** — 중복이 아니라 **지속**이다.
- 아래 [나가기] 확인 문안 2종은 **좌석이 있는 사람(①)** 기준이다. ②는 좌석도 손패도 없어(`state.players`에 아예 없다) 나가도 판이 오염되지 않으므로 **그 사유가 성립하지 않는다.**

**[나가기] 확인 문안** `[선행]` (선행: 확인 다이얼로그 DOM)

- 대리 NPC 인계(8.1) **도입 전** — 이탈이 실제로 판을 오염시키는 현재 상태:
  ```
  지금 나가면 내 손패가 판에서 빠져요.
  남은 사람들이 반증할 수 없는 카드가 생겨 추리가 어긋납니다.
                       [ 계속 관전 ]  [ 그래도 나가기 ]
  ```
- 대리 NPC 인계 **도입 후**:
  ```
  지금 나가면 내 자리는 대리 NPC(💤)가 이어받아요.
  같은 탭으로 돌아오면 자리를 돌려받습니다.
                       [ 계속 관전 ]  [ 나가기 ]
  ```
- 기본 포커스는 **`[계속 관전]`**. 파괴적 동작이 기본값이 되어서는 안 된다.

---

## 8. 서버 로그 문안 교체표

### 8.1 `clue-room.ts`
`[조사]` = 조사 오류가 실제로 출력되는 문장 · `[톤]` = 어휘·톤 불일치

| 줄 | 현재 | **교체안** | 사유 |
|---|---|---|---|
| 697 `[조사]` | `${label(suspect)}가 ${label(room)}(으)로 불려왔습니다.` | `🔔 소환 — ${label(suspect)} → ${label(room)}` | R1·R3. `(으)로`가 화면에 그대로 출력 |
| 406 `[조사]` | `${label(value)}는 이미 선택되었습니다.` | `이미 선택된 캐릭터예요 — ${label(value)}` | R1. 변수를 문미로 |
| 383 | `${player.name} 님이 ${label(nextRoom)}에 들어갔습니다.` | `🚪 진입 — ${player.name} → ${label(nextRoom)}` | R3. 48자 상한·스캔 속도 |
| 165 | `🚪 ${player.name} 님이 비밀 통로로 ${label(dest)}에 이동! (제안 또는 턴 종료)` | `🚪 비밀 통로 — ${player.name} → ${label(dest)} · 제안 또는 턴 종료` | 58자 → 42자 |
| 214 | `🃏 ${player.name} 님이 ${label(helper.value)}의 계략(엿보기 ${n}·이동 +${refund}) 사용` | `🃏 계략 — ${player.name} · ${label(helper.value)}에게서 엿보기 ${n} · 이동 +${refund}` | `에게서`는 받침 무관(R5) |
| 585 `[톤]` | `게임 시작! 정답 봉투 봉인. NPC ${botCount}명 합류. ${first?.name} 님의 턴.` | `🎊 잔치 시작 — 정답 봉투 봉인 · NPC ${botCount}명 합류` + 별도 줄 `⏳ ${first?.name} 님의 턴` | §2 `잔치 시작`. 한 줄에 3정보는 스캔 불가 |
| 681 `[톤]` | `🔍 [제안] ${name} — 도둑: … · 훔친 것: … · 장소: …` | `🔍 제안 — ${name} · 용의자: … · 훔친 것: … · 장소: …` | §2: 제안 단계는 `용의자`. 대괄호 제거 |
| 724 | `🛡 ${other?.name} 님이 반증했습니다.` | `🛡 반증 — ${other?.name} 님` | 표기 일관·짧게 |
| 733 | `❗ 아무도 반증하지 못함 — 정답 후보!` | **유지** (허용된 느낌표 3종 중 하나) | — |
| 784 | `🎉 ${player.name} 사건 해결! 정답: … · … · …` | `🎉 사건 해결 — ${player.name} 님` + 별도 줄 `📜 정답 봉투 — … · … · 📍 …` | 결과 오버레이(§7)와 같은 문자열 재사용 |
| 792 | `❌ [고발 실패] ${player.name} 탈락(반증만 가능).` | `❌ 고발 실패 — ${player.name} 님 탈락 · 반증만 가능` | 대괄호 제거 |
| 1003 | `🎉 ${w?.name} 최후 생존 — 승리!` | `🎉 최후 생존 — ${w?.name} 님 승리!` | 변수 뒤 조사 없음 유지, 어순만 |
| `endInDraw()` `[있음]` | `🏳 무승부 — 제안 ${SUGGEST_CAP}회 도달 · 판을 종료합니다` | **확정 — 그대로 유지** (07-28 임시 문구를 검토 후 확정) | 접두 1개(§2 `🏳`) · 26자 · 조사 없음(`회` 고정 접미) · 승자 줄과 같은 `kind:"win"` |
| `endInDraw()` `[있음]` | `📜 정답 봉투 — … · … · 📍 …` | **유지** | 784와 동일한 2줄 구성(결과 + 정답 봉투). 오버레이 §7.1 5번과 같은 문자열 |
| 1014 | `${np?.name} 님의 턴입니다.` | `⏳ ${np?.name} 님의 턴` | 접두 통일 |
| 547 | `📢 공통 단서 공개(정답 아님): …` | `📢 공통 단서 — 모두 공개 · 정답 아님: …` | §4.2 |
| 295 | `${player.name} 입장.` | `🎎 입장 — ${player.name} 님` | §9.1(대기실 라인으로도 재사용) |
| 323 | `${player.name} 퇴장.` | `🚪 퇴장 — ${player.name} 님` | 동상 |
| 306 | `${player?.name ?? "누군가"} 연결 끊김 — 재접속 대기…` | `📡 연결 끊김 — ${name} 님 · 재접속 대기` | — |
| 312 | `${back?.name ?? "플레이어"} 재접속!` | `🙋 재접속 — ${name} 님` | 느낌표 3종 규칙 |
| 434 | `🔄 ${p?.name} 님이 다시 하기 — 새 판을 시작합니다.` | `🔄 다시 하기 — ${p?.name} 님 · 새 판을 시작합니다` | — |
| 151·176·745·803 | `당신의 턴이 아닙니다.` | `지금은 내 차례가 아니에요.` | §5와 **같은 문장**(당신→내) |
| 156 | `이 방엔 비밀 통로가 없습니다.` | `이 방에는 지름길이 없어요. (지름길은 3쌍)` | §5.3과 동일 |
| 189 | `가까이에 계략을 줄 이가 없습니다.` | `엿볼 수 있는 이가 근처에 없어요. 보드 가장자리의 NPC 곁으로.` | §5.4와 동일 |
| 749 | `방 안에서만 제안할 수 있습니다.` | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` | §5.1과 동일 |
| 417 | `방장만 잔치를 시작할 수 있습니다.` | `방장만 잔치를 시작할 수 있어요.` | 해요체(사용자 대상 안내) |
| 268 | `이미 진행 중인 게임입니다 (관전).` | `이미 진행 중인 판이에요. 관전으로 들어갑니다.` **확정·적용됨** | ~~관전 좌석 미구현 = 도달 불가 죽은 코드~~ → **07-28 관전 좌석 구현으로 도달 가능**(`lock()` → `setPrivate/setListed`). **07-29부터 §7.2 관전 배너가 이 문자열을 그대로 재사용**한다 — 문안을 고치려면 **두 곳이 같이 움직인다** |

> **`client.send("log")` 로 가는 개인 안내(151·156·176·189·745·749·803·417)는 §5의 버튼 문안과 글자 그대로 같아야 한다.** 같은 사유를 두 문장으로 배우게 하지 않는다.

### 8.2 `narrator.ts` 폴백 템플릿 (사극체 — R6 예외지만 회피가 가능한 곳은 회피)

**확정본 = 코드 `fallbackLine()` 현행**(2026-07-28 교체). 아래 표가 단일 소스이며, 문안을 바꾸려면 이 표를 먼저 고친다.

| 액션 | 예산 | 템플릿(랜덤 1택) |
|---|---|---|
| **제안** | 25자 | `${room}의 ${suspect}, 「${weapon}」` · `${suspect}, ${room}의 「${weapon}」` · `${room}에서 ${suspect}, ${weapon}` · `${suspect}, 자네 ${room}엔 왜 갔는가` |
| **제안(반증당함)** | 25자 | `${suspect}, 「${weapon}」… 아니군` · `${room}의 「${weapon}」${josa(weapon,"이","가")} 아니라니` · `${suspect}도 ${weapon}도 아니군` |
| **고발** | 40자 | `도둑은 ${suspect}! ${room}의 「${weapon}」` · `${suspect}, 「${weapon}」, ${room} — 끝일세` · `더 볼 것 없다. ${suspect}, ${weapon}, ${room}` |
| **계략(귓속말)** | 40자 | `쉿… ${hint}, 자네만 알게` · `가까이 오게. ${hint} — 못 들은 걸로` · `은밀히 이르네. ${hint}` · `${hint}… 내 입에서 난 말은 아닐세` |

**추임새 부착 규칙 — `fit()`**: 추임새(`cards.ts` `VOICE`의 `intro`/`outro`, 예 `쯧쯧, ` / ` 마땅히 그러하렷다.`)는 **인물을 식별하는 유일한 표식**이라 "길면 통째로 버린다"가 아니라 **예산 안에서 들어가는 만큼** 붙인다. 순서는 `intro+본문+outro` → `본문+outro` → `intro+본문` → `본문`. 마지막 폴백에서도 `LINE_MAX`(40)는 지킨다.
- outro를 intro보다 먼저 살리는 것은 **인물 고유 문구라 색이 더 진하기** 때문이다.
- 그래서 본문 템플릿은 **짧게 쓸수록 캐릭터색이 더 붙는다.** 새 템플릿을 추가할 때 이 방향을 뒤집지 말 것.

**조사 처리**
- 회피가 원칙이다: `의`·`에서`·`엔`·`도`는 받침 무관(R5), 낫표 「」로 목적격을 대체(R4), 나열은 쉼표.
- 단 한 곳 `${room}의 「${weapon}」${josa(weapon,"이","가")} 아니라니`만 **종성 헬퍼를 쓴다** — NPC 대사는 R6 예외이고, 여기서 조사를 빼면 문장이 성립하지 않는다.
- `${weapon}이라니`류(받침 없는 말 뒤에도 허용되는 이형)는 현행 템플릿에서 사라졌다.

> **정정 노트 (2026-07-28) — 왜 07-27 확정본을 버렸나**
> ① 07-27 확정본(`${room} 쪽을 살피니 「${weapon}」, 그게 사라졌던걸`, `이건 필시 ${suspect}의 소행! ${room}에서 훔친 것은 「${weapon}」` 등)은 **조사 오류는 고쳤지만 길이를 보지 않았다.** 실측 폴백 제안 대사 **평균 47.1자(최대 71)** — 제출물 ④ §2.3 길이 규약을 **71~79% 위반**(로드맵 §7.5 2차 실측).
> ② 길이는 미관 문제가 아니라 **페이싱**이다. `bubbleLifeMs = len×TYPE_MS(55) + BUBBLE_HOLD_MS`가 대사 길이에 비례하므로, 47자 대사는 봇 1턴을 그대로 늘려 **판 길이를 지배**했다.
> ③ 그래서 **규약 쪽을 살리고 문안을 교체**했다. 코드가 예산을 상수로 강제하고(`LINE_BUDGET_SUGGEST` 25 · `LINE_MAX` 40) 이 문서가 그 예산 안의 문안을 확정한다 — §1.4의 확정 권한은 그대로다.
> ④ 교체 후 실측(`scripts/eval-narrator.mjs` 폴백 **전수 112,752문장**): 제안 평균 21.6자(최대 25) · 제안(반증) 21.0 · 고발 37.1 · 계략 36.5 — **위반 0건.** 추임새 부착률 84.5~100%(캐릭터색은 유지됐다).
> ⑤ 반증당한 뒤 템플릿에서 3요소를 다시 읊지 않는 것은 길이 레버다 — 제안 내용은 이미 로그 한 줄에 다 있고, 여기서 필요한 것은 "지웠다"는 반응이다(8~10자 절약).

### 8.3 `main.ts` 클라이언트 로그
| 현재 | **교체안** | 사유 |
|---|---|---|
| `🔎 ${by} 님이 "${label(card)}" 단서로 반증 (나만 봄)` | `🛡 나에게 반증 — ${by} 님 · 「${label(card)}」 (나만 봄)` | 접두 `🛡` 통일, 「」 |
| `🔎 아무도 반증하지 못함 — 정답 후보!` | **삭제** | 서버 733이 이미 전체 브로드캐스트 → **제안자만 같은 줄을 2번 본다** |
| `🎉 ${player} 정답!` / `❌ ${player} 오답` | **삭제** | 서버 784/792와 중복 |
| `잔치 시작! 이동: 방향키, 방에 들어가 [제안]` | `🎮 이동 — 방향키 · 방에 들어가 [제안]` | 서버 585 `잔치 시작`과 중복 |
| `💬 ${from}: ${text}` | **유지** | §1.1 사극체 경계 표식 |
| `🃏 ${from}의 계략 — 엿본 카드: … (정답 아님·나만 봄)` | `🃏 엿본 카드 — ${from}에게서: … (정답 아님 · 나만 봄)` | `에게서`(R5) |
| `에러(${code}): ${message}` | `문제가 생겼어요 (${code}). 새로고침하면 이어서 할 수 있어요.` | §1.3 |
| `서버(ws://localhost:2567)에 연결하지 못했습니다. 서버가 켜져 있나요? → \`pnpm dev\`` | 사용자용: `서버에 연결하지 못했어요. 잠시 뒤 다시 시도해 주세요.` / 개발 힌트는 `console.warn`으로 분리 | **배포 빌드에서 localhost 안내가 심사자에게 노출**된다 |

### 8.4 랜딩 문안 (대비 수정 대상 포함)
| 위치 | 현재 | **교체안** |
|---|---|---|
| `.room-empty` | `열린 공개방이 없어요. 방을 만들어보세요.` | `열린 공개방이 없어요. [방 만들기]로 첫 판을 열어보세요.` |
| `.room-empty` (로딩) | `불러오는 중…` | `공개방을 찾는 중…` |
| `.room-empty` (실패) | `목록을 불러오지 못했어요.` | `목록을 불러오지 못했어요. [↻ 새로고침]을 눌러보세요.` |
| 공개방 참여 버튼 | `참여` / `만석` | `참가` / `만석` (§2 통일) |
| `.hint-sm` | `캐릭터는 대기실에서 고릅니다. 공개방은 목록에 노출됩니다.` | `캐릭터는 대기실에서 골라요. 공개방은 목록에 보여집니다.` |
| 참가 실패 | `없는 방이거나 참가할 수 없어요. 코드를 확인하거나 새 방을 만드세요. (…)` | `없는 방이거나 이미 시작한 판이에요. 코드를 확인해 주세요.` (원문 오류는 콘솔로) |
| 목록 항목 제목 `[조사]` | `${방장}님의 방` · 이름이 없으면 `대기 중님의 방` | 이름 있음 → `${방장} 님의 방` (R1: `님` 앞에 한 칸) · **이름 없음 → `방장 대기 중`** |
| 공개방 참가 진행 `[있음]` | `참여하는 중…` | `참가하는 중…` (§2 통일 — 초대 코드 참가와 **글자 그대로 같은 문자열**) |
| 공개방 참가 실패 `[있음]` | `참여 실패: (원문)` | 위 «참가 실패» 행과 **같은 문장**. 경로가 둘이라고 문장을 둘로 만들지 않는다 |
| 초대 진입 | `초대 링크로 들어왔어요. [참가] 후 대기실에서 캐릭터를 고르세요.` | **유지** |

> `[조사]` **«대기 중님의 방»은 실제로 화면에 나갔다**(07-29 캡처 확인). 폴백 값 `"대기 중"`이 이름 자리에 들어간 뒤 `님의 방`이 그대로 이어 붙었기 때문이다.
> **이름 자리에 상태를 넣지 않는다** — 이름이 없으면 문장 구조를 바꾼다(R2의 취지). 방장 이름은 `label(suspect)`이라 좌석이 하나라도 차면 항상 존재하고, 빈 값은 «좌석 예약만 잡히고 아무도 앉지 않은 순간»에만 나온다.

> `.room-empty`·`.hint-sm`·`.divider`의 색 `#6b6355`(대비 2.9:1)는 **문안이 아니라 색 문제**다 — 로드맵 소관이나, 이 문서의 문장은 그 색이 고쳐진 뒤에야 읽힌다. (07-28 `#8f8574`로 교체 완료 · 07-29 화면 게이트 S6이 회귀를 막는다.)

### 8.5 공개방 목록 항목 — 상태 배지 `[완료]`

**왜 필요한가.** 판이 끝난 방은 **목록에 되돌아온다**(`setListed(true)` — 설계대로다).
그런데 거기 들어가면 리매치 전까지 **관전**이다. 버튼이 `참가`라고만 적혀 있으면
**«참가한다»고 읽고 들어가 관전 화면을 본다.** 심사자가 두 명이면 실제로 밟는 동선이다.

**선행 조건은 풀렸다**(07-29). `clue-room.ts`의 `syncMeta()`가 `{hostName, count, humans, isPublic, phase}`를
싣고, 클라 `PublicRoomMeta`가 그 필드와 1:1로 맞춰졌다. 아래 문안은 `loadPublicRooms()`에 **적용 완료**다.
⚠️ `phase`가 없으면 배지를 **아예 그리지 않는다** — 없는 값을 기본값으로 메우면 추정이 된다.
⚠️ **클라가 추정하지 않는다.** `count > clients`(NPC 충원 흔적) 같은 간접 신호로 상태를 만들어내면
서버가 말한 적 없는 것을 화면이 단언하게 된다 — §1.4 진실값 경계 위반이다.
목록에 «진행 중»은 **원리적으로 나타나지 않는다**(판이 도는 동안 목록에서 빠진다). 그래서 상태는 **2종**이다.

| 방 상태(서버 `metadata.phase`) | 배지 | 버튼 | 부가 |
|---|---|---|---|
| `lobby` — 아직 시작 전 | `대기 중` | `참가` | `${clients}/${maxClients}인` (현행 유지) |
| `ended` — 판이 끝나 목록에 돌아옴 | `종료됨` | `관전` | `다시 하기를 기다리는 중` |
| ~~(정원 초과)~~ | — | ~~`만석`(비활성)~~ | — | **도달 불가 — 아래** |

⚠️ **`만석`은 목록에 원리적으로 나타나지 않는다**(07-29 실측, 화면 게이트 S7). Colyseus
`getAvailableRooms()`는 `{locked:false, private:false}`인 방만 돌려주는데, 좌석이 차는 순간
`_incrementClientCount()`가 방을 **잠근다**. 즉 정원이 찬 방은 **목록에서 사라진다** —
목록 안에서 «만석»으로 보이는 상태가 존재하지 않는다. 클라의 `full` 분기는 **도달 불가 코드**다.
목록이 비어 있는 것이 곧 이 사실의 실측이라, 게이트는 이 행을 **FAIL이 아니라 SKIP**으로 사유와 함께 인쇄한다.

- 배지는 **텍스트 + CSS 뱃지**다. 이모지를 늘리지 않는다(§2 접두 기호는 확정 목록이 전부다).
- `관전`은 §2 «관전 상태» 표기와 같은 낱말이다. 들어간 뒤 보게 되는 배너(§7.2 본문 ②)와 낱말이 이어진다.
- 버튼 라벨은 **들어가면 무엇이 되는지**를 말한다(상태의 되풀이가 아니다) — 배지가 «지금», 버튼이 «다음»이다.
- 조사 회피(R2): 부가 문구는 `다시 하기를 기다리는 중`처럼 **변수 없는 고정 문장**으로 둔다.

---

## 9. 멀티플레이 문안 `[선행]`

### 9.1 대기실 서버 피드백
**선행 조건**: `#lobby` 안에 피드백 라인(`#lobbyMsg`)이 없다. 현재 `addLog`는 `#gameScreen` 안쪽 `#log`에 쓰므로 **대기실 단계 서버 메시지가 전부 무음으로 사라진다.**

| 이벤트 | 문안 | 표시 |
|---|---|---|
| 입장 | `🎎 토끼 낭자 님 입장 (3/6)` | `#lobbyMsg` 3초 |
| 퇴장 | `🚪 토끼 낭자 님 퇴장 (2/6)` | 3초 |
| 캐릭터 중복 거부 | `이미 선택된 캐릭터예요 — 🐰 토끼 낭자` | 3초 · 해당 셀 흔들림 |
| 방장 전용 거부 | `방장만 잔치를 시작할 수 있어요.` | 3초 |
| 방장 위임 | `👑 방장이 바뀌었어요 — 🐭 생쥐 서생 님` | 3초 |
| 연결 끊김 | `📡 연결 끊김 — 토끼 낭자 님 · 재접속 대기` | 유지 |
| 재접속 | `🙋 재접속 — 토끼 낭자 님` | 3초 |

- 시작 버튼 동적 라벨: `잔치 시작 (사람 3 + NPC 3)` — 빈자리 충원 결과를 **누르기 전에** 보여준다.
- 방장 힌트 `[있음]` `빈 자리는 NPC로 채워집니다 (최대 6인). 바로 시작할 수 있어요.` → `빈 자리는 NPC로 채워요 (최대 6인). 혼자서도 바로 시작할 수 있어요.`
- 비방장 힌트 `[있음]` `방장이 시작하기를 기다리는 중…` → **유지**
- 시작 카운트다운: `잔치 시작 3…` → `2…` → `1…` (버튼 라벨 교체, 3자)

### 9.2 닉네임 입력
**선행 조건**: `Player.name`은 `label(suspect)`뿐 — **닉네임 필드가 스키마에 없다.**

| 요소 | 문안 |
|---|---|
| 필드 라벨 | `닉네임` |
| placeholder | `8자까지 · 비워도 괜찮아요` |
| 비움 안내 | `이름 없이 시작하면 캐릭터 이름으로 표시돼요.` |
| 길이 초과 | `닉네임은 8자까지예요.` |
| 중복 | `같은 이름이 있어 뒤에 숫자를 붙였어요 — 안벨로퍼2` |
| 표시 형식 | `안벨로퍼 (🐰 토끼 낭자)` — 목록·턴 원형 / 로그·말풍선은 `🐰 토끼 낭자` 유지 |

> `[주의]` 닉네임은 사용자 입력이고 `renderTurnStrip`·`openTurnCircle`·`showCharInfo`가 `innerHTML`을 쓴다 → **서버 새니타이즈 + 클라 이스케이프 필수**(XSS). 문안 적용 전 선행.

### 9.3 이탈 · 대리 NPC 표기
| 상황 | 문안 |
|---|---|
| 이탈 → 대리 인계 | `💤 대리 시작 — 토끼 낭자 님 이탈 · NPC가 자리를 이어받았어요 (손패 유지)` |
| 복귀 | `🙋 복귀 — 토끼 낭자 님이 자리를 돌려받았어요` |
| 목록·턴 원형 배지 | `💤 대리 중` / `🤖 NPC` |
| 범례 (턴 원형 하단) | `🤖 NPC · 💤 자리를 비운 사람` |

- **`손패 유지`가 핵심 정보다.** 이 표기가 없으면 남은 사람은 "저 카드는 이제 반증 안 되나?"를 알 수 없다.

### 9.4 리매치 합의
| 상태 | 버튼 라벨 | 로그 |
|---|---|---|
| 미동의 (사람 2명 이상) | `다시 하기 (1/3)` | — |
| 내가 누른 뒤 | `동의함 (2/3) · 대기 중` (버튼 비활성) | `🔄 다시 하기에 동의 — 토끼 낭자 님 (2/3)` |
| 성립 | — | `🔄 과반 동의 — 새 판을 시작합니다` |
| 사람 1명(솔로) | `다시 하기` (카운트 숨김) | 기존 434 문안 |
| 시리즈 표기 `[선행]` | 결과 부제 앞에 `제3판 · 통산 2:1` |

### 9.5 턴 타이머 경고
| 시점 | 문안 | 위치 |
|---|---|---|
| 내 턴 진행 중 | `⏳ 남은 시간 32초` | 턴 배너 부제 `.ti-sub` |
| 10초 이하 | `⏳ 10초 — 시간이 지나면 자동으로 넘어가요` | 부제 (강조색) |
| 만료 | `⌛ 시간 초과 — 다음 사람에게 넘어갔어요` | 로그 |
| 이탈자 대기 | `⏳ 접속 대기 8초 — 토끼 낭자 님` | 로그 |
| 제안 후 고발 대기 `[선행]` | `⏳ 지금 신고할 수 있어요 (30초). 넘기려면 [내 턴 종료]` | 부제 |
| 솔로(사람 1명) | **타이머 문안 전부 표시하지 않음** | — |

---

## 10. 적용 위치 표

| § | 문안 묶음 | 파일 | 함수 / 요소 |
|---|---|---|---|
| 3 | 목표 카드 `[선행]` | `apps/client/index.html` · `apps/client/src/main.ts` | 신규 `#goalCard` 오버레이 · `enterGame()` 말미, `.hud-ctrl` 끝 `[?]` |
| 4.1 | 손패 라벨 | `apps/client/src/main.ts` | `r.onMessage("hand", …)` (≈212) · `index.html` `#hand` 초기 텍스트 |
| 4.2 | 공통 단서 | `apps/server/src/rooms/clue-room.ts` (≈546) · `main.ts` | `startGame()` · `buildEvidence()` |
| 4.3 | 증거 노트 툴팁 `[있음]` | `apps/client/src/main.ts` | `buildEvidence()` — 잠금 칩 `chip.title` — **07-29 정합 확인** |
| 4.4 | AI 칩 · 대사 경로 배지 | `apps/client/src/main.ts` | `renderAi()` (≈216) · `addLog()`의 배지 분기 (≈170) — 폴백 사유 한글화 |
| 5.0 | `disabled` → `aria-disabled` 전환 | `apps/client/src/main.ts` · `index.html` | `updateTurnInfo()` (≈436) · `button:disabled` CSS 옆에 `.is-off` 추가 |
| 5.1–5.5 | 버튼 4종 + 턴 종료 문안 | `apps/client/src/main.ts` | `updateTurnInfo()` 에서 `el.title` 설정 · 각 `onclick` 첫 분기 |
| 5.0 | 클릭 피드백 출력처 `[선행]` | `apps/client/index.html` · `main.ts` | `.hud-turn` 안에 `.ti-sub` 추가 · `updateTurnInfo()` |
| 6.1 | 제안 모달 (장소 고정행) | `apps/client/src/main.ts` | `openPicker()` (≈102) — 읽기전용 행 분기 추가 |
| 6.2 | 고발 모달 (경고) | `apps/client/src/main.ts` | `openPicker()` · `#accuse` `onclick` |
| 7.0 | 정답 봉투 전달 `[선행]` | `apps/server/src/rooms/clue-room.ts` | `doAccusation()` (≈768) · `advanceTurn()` 최후 생존 분기 (≈998) |
| 7.1 | 결과 6종(무승부 포함) | `apps/client/src/main.ts` | `updateEndState()` (≈1377) |
| 7.2 | 관전 배너 본문 2종 `[있음]` | `apps/client/index.html` · `apps/client/src/main.ts` | `#spectateBar` 안 `#specText` · `updateEndState()` (표시 판정 `탈락 \|\| 좌석 없음`) |
| 7.2 | 나가기 확인 `[선행]` | `apps/client/index.html` · `main.ts` | `#specHome` `onclick` (현재 `exitToMain` 직결) |
| 8.1 | 서버 로그 25항목 + 무승부 2줄 | `apps/server/src/rooms/clue-room.ts` | 표의 줄 번호 · `endInDraw()` (≈1272) |
| 8.2 | 폴백 대사 `[있음]` | `apps/server/src/ai/narrator.ts` | `fallbackLine()` · `fit()` — **코드가 확정본과 일치** |
| 8.3 | 클라 로그 · 중복 제거 | `apps/client/src/main.ts` | `wireRoom()` 의 `onMessage` 5개 · `errMsg()` · `enterGame()` 말미 |
| 8.4 | 랜딩 `[있음]` | `apps/client/index.html` · `main.ts` | `.hint-sm`·`.room-empty` · `loadPublicRooms()` · `init()` 의 `joinBtn` — **07-29 적용 완료** |
| 8.5 | 공개방 상태 배지 `[완료]` | `apps/server/src/rooms/clue-room.ts` · `network.ts` · `main.ts` · `index.html` | `syncMeta()` 의 `phase` → `PublicRoomMeta` → `loadPublicRooms()` 의 `li` 조립 · `.ri-badge` — **07-29 적용 완료** |
| 9.1 | 대기실 피드백 `[선행]` | `apps/client/index.html` · `main.ts` · `clue-room.ts` | 신규 `#lobbyMsg` · `renderLobby()` · `onJoin`/`removePlayer`/`handleChooseCharacter` |
| 9.2 | 닉네임 `[선행]` | `packages/shared` · `clue-room.ts` · `index.html` | `Player.name` 분리 · 대기실 `.field` 추가 |
| 9.3 | 대리 NPC 표기 `[선행]` | `packages/shared` · 3개 렌더러 | `displayName(p)` 헬퍼 · `renderLobby`/`renderTurnStrip`/`openTurnCircle` |
| 9.4 | 리매치 합의 `[선행]` | `clue-room.ts` · `main.ts` | `handleRematch()` (≈430) · `#endRematch` |
| 9.5 | 턴 타이머 `[선행]` | `clue-room.ts` · `main.ts` | `armTurnClock()` 신설 · `updateTurnInfo()` |

---

## 11. 검수 체크리스트
- [ ] 화면 문장 중 **변수 바로 뒤에 조사**가 오는 곳이 0건인가 (`grep -nE '\$\{[^}]+\}(은|는|이|가|을|를|와|과|\(으\)로|으로|로)' apps/`)
- [ ] 같은 사유가 **툴팁과 클릭 피드백에서 글자 그대로 동일**한가
- [ ] `💬` 없는 줄에 사극체 어휘가 섞이지 않았는가
- [ ] 로그에 **같은 사건이 두 줄**로 뜨지 않는가 (§8.3의 중복 4건)
- [ ] §2 어휘표 밖의 동의어(`장물`·`단서 패`·`참여`·`게임 시작`)가 남아 있지 않은가
- [ ] 버튼 라벨 8자 · 툴팁 40자 상한을 넘긴 문장이 없는가
- [ ] 결과 화면 6종이 **정답 봉투를 화면에서** 보여주는가 (로그로 보내지 않는가)
- [ ] NPC 대사가 **예산(제안 25 · 고발/계략 40)** 안에 있는가 — `node scripts/eval-narrator.mjs` (폴백 전수, 위반 0이어야 함)
- [ ] 화면에 **영문 토큰**이 노출되지 않는가 (`timeout`·`http`·`empty`·`toolong`·`nokey`·`disabled` → §4.4 한글 표기. 예외는 배지 툴팁 1곳)
- [ ] §8.2 표의 템플릿과 `fallbackLine()`이 **문자 그대로 같은가** (문서가 코드를 뒤따라가면 §1.4의 확정 권한이 무효가 된다)
- [ ] 관전 배너 본문 ②(§7.2)와 서버 안내(§8.1 행 268)가 **문자 그대로 같은가** — 한쪽만 고치면 같은 사유가 두 문장이 된다
- [ ] **상태는 있는데 문안이 없는 화면**이 없는가 — 좌석 없는 관전자가 07-28까지 그 상태였다(배너 조건이 `undefined`에 걸리지 않았다)

## 변경 이력
- **2026-08-26**: **§3.1 목표 카드 3줄 → 4줄(④ 끝내기 신설).** 카드가 목표·출발점·방법을 가르치면서
  **«어떻게 이기는가»가 없었다** — ①이 "맞히면 승리"라고만 하고 그 선언을 어느 버튼으로 하는지도,
  틀리면 어떻게 되는지도 말하지 않아, 신규 유입이 ①을 읽고 [신고하기]를 누르는 경로가 열려 있었다.
  같은 사고를 **시각**으로 막은 것이 액션 바 3단 위계(`plans/done/22`)이고 ④는 그 **문안 쪽 절반**이다.
  함께: 한도표 「32자」를 **실측 27자**로 정정(폰 390 가용폭 306px 기준 · ②③은 이미 초과) ·
  §3.1 재열람 문안을 구현과 일치시킴(`좌측 상단 [?]` → `[?] 버튼을` — 폰에서 액션 바는 **하단**이라
  「좌측 상단」은 거짓이었다) · 화면 게이트 `#goalCard .goal-lines div` 가드를 `min: 3 → 4`
  (min이라 3으로 두면 ④가 가려져도 통과한다).
- **2026-07-29 (4)**: **§8.5 3행 `만석`을 «도달 불가»로 정정.** 화면 게이트에 S7(공개방 목록 배지)을
  넣으면서 실측으로 드러났다 — Colyseus가 정원이 찬 방을 잠그고 `getAvailableRooms()`가 잠긴 방을
  거르므로, **목록 안에 «만석»인 방이 존재할 수 없다.** 문안을 확정해두고 코드에 붙여둔 채
  «있다»고 믿고 있었다. **게이트를 만들지 않았으면 계속 몰랐을 종류의 오류**다.
  `대기 중`·`종료됨` 2행은 이제 기계가 매 커밋 잰다(S7, 기본 티어 +6.4s).
- **2026-07-29 (3)**: **§8.5 `[선행]` 해제 → `[완료]`.** 서버 `syncMeta()`가 `phase`를 싣게 되면서 선행 조건이 풀렸고,
  클라 `PublicRoomMeta`가 서버 필드와 1:1로 맞춰져 확정 문안 3행이 그대로 붙었다.
  **`phase`가 없으면 배지를 그리지 않는다**는 규칙을 코드와 문안 양쪽에 명시 — 없는 값을 기본값으로 메우는 순간 §1.4의 추정 금지를 어긴다.
  촬영 확인: `대기 중`+`참가`는 실제 화면에서 확인했고, **`종료됨`+`관전`은 아직 화면으로 본 적이 없다**(서버가 `phase:"ended"`로 목록에 복귀하는 것만 e2e로 확인).
- **2026-07-29 (2)**: **§8.4 적용 완료 + §8.5 신설.** §8.4 확정본 6행이 07-27 확정 이후 코드에 붙지 않고 있었다 → `index.html`·`main.ts`에 적용(`참여`→`참가` 포함). 함께 «공개방 참가 진행·실패» 2행을 §8.4에 추가(경로가 둘이라고 문장을 둘로 만들지 않는다). **§8.5는 «종료된 방의 버튼이 실제로는 관전»을 화면에서 말하기 위한 문안**인데, 서버가 `metadata`에 `phase`를 싣지 않아 **클라가 상태를 알 수 없다** → 문안만 확정하고 `[선행]`으로 둔다(추정하지 않는다 — §1.4).
- **2026-07-29**: **§7.2 정정 — 관전 행 추가.** `#spectateBar`를 «진행 중 탈락» 전용으로 규정했으나 **좌석 없는 관전자도 같은 배너에 도달**한다(커밋 `40f0b6e`). 문안은 새로 짓지 않고 **§8.1 행 268의 서버 안내와 글자 그대로 같은 문자열**을 재사용 — 같은 사유를 두 문장으로 배우게 하지 않는다. 함께 §8.1 행 268의 «도달 불가 죽은 코드» 표기를 정정(07-28 관전 좌석 구현으로 도달 가능), §10에 `#specText` 적용 위치 분리, §11에 정합 체크 2건 추가.
- 2026-07-27: 최초 작성. 실코드 실측 기준으로 톤 규칙·조사 회피 6규칙·어휘 통일표 확정, 버튼 4종 상태별 문안·모달 2종·결과 5종·서버 로그 25항목 교체표·멀티 문안 확정.
- **2026-07-28**: ① §8.2 폴백 템플릿을 **현행 코드 기준으로 전면 갱신** + 07-27 확정본을 버린 이유(길이 규약 위반 71~79%)를 정정 노트로 기록. ② 미확정 4건 확정 — 무승부 종료 로그(§8.1)·결과 오버레이 무승부(§7.1 5번)·AI 카운터 칩과 대사 경로 배지(§4.4 신설)·폴백 사유 6종 한글 표기(§4.4). ③ §2에 어휘 3항목·접두 기호 4종(`🏳`·`✨`·`♻`·`⚙`) 추가, §1.5에 대사 길이 예산 3행 추가. ④ §4.3 잠금 칩 툴팁이 코드와 갈라진 사실을 기록(**문서가 옳다** — 코드 수정 대상).
