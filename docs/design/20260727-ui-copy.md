# UI 문안(카피) 명세 — 호랑이 생신 잔치

> 작성 2026-07-27 · **개정 2026-07-28**(§8.2 코드 정합 + 미확정 4건 확정) · 사람용 `.html` 동봉 · 근거: `apps/client/index.html` · `apps/client/src/main.ts` · `apps/server/src/rooms/clue-room.ts` · `apps/server/src/ai/narrator.ts` 실측
> 범위: **화면에 출력되는 한국어 문장 전부**. 진단·우선순위는 `20260727-improvement-roadmap.md` 소관(중복 금지), 이 문서는 **붙여넣을 문장**만 확정한다.
> 표기: `[있음]` 현재 DOM/코드에 있음 · `[선행]` **선행 조건**(요소·필드가 아직 없음, 문안은 확정하되 구현 전제 명시) · `[주의]` · `[조사]` 조사 오류 · `[톤]` 어휘·톤 불일치 · `[갈라짐]` 코드↔문서 불일치
> (상태는 이모지가 아니라 대괄호 표기로 적는다 — 사람용 `.html`은 같은 뜻의 CSS 배지를 쓴다. 이모지는 **확정 문안·게임 아이콘 전용**이다.)

---

## 0. 이 문서의 사용법
1. 새 문장을 쓰기 전에 §1 톤 규칙과 §2 어휘표를 먼저 본다.
2. 문장은 §3~§9에서 **그대로 복사**한다. 변형이 필요하면 이 문서를 먼저 고친다.
3. 적용 위치는 §10 표에서 파일·함수 단위로 찾는다.

---

## 1. 톤 규칙

### 1.1 두 개의 목소리 — 절대 섞지 않는다
| 층위 | 누가 말하는가 | 문체 | 화면 위치 | 예 |
|---|---|---|---|---|
| **캐릭터 대사** | 십이지 NPC | **사극체** (하게체·하오체, 종결 `-네/-군/-는가/-렷다`) | 말풍선, 로그의 `💬 <이름>:` 줄 | `쯧쯧, 서재 쪽을 살피니 「떡시루」, 그게 사라졌던걸` |
| **UI 안내** | 시스템(게임) | **현대 표준어 해요체** | 버튼 툴팁·모달·오버레이·랜딩·대기실 | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` |
| **시스템 로그** | 시스템(중립 기록) | **중립 서술** — `-습니다` 또는 체언 종결 | 우측 `📜 기록 / 알림` | `🚪 진입 — 호랑이 대감 → 대청마루` |

- **경계선은 접두 기호다.** `💬` 로 시작하는 줄만 사극체. 그 외 모든 줄은 사극체 어휘(`~하렷다`, `자네`, `아뢰오`)를 쓰지 않는다.
- UI 안내에 사극체를 섞으면 "분위기"가 아니라 **조작 불능**이 된다 — 안내문은 규칙을 알려주는 유일한 통로다.
- 로그와 UI 안내의 문체가 다른 것은 **의도**다. 로그=사실 통보(기록물), UI 안내=사용자 대화. 이 두 계열을 서로 맞추려 하지 말 것.

### 1.2 조사(은/는·이/가·을/를·(으)로) — **카피 차원에서 회피한다**
실측 결함(`호랑이 대감가 대청마루(으)로 불려왔습니다`, `떡시루이 사라졌던걸`)은 **변수 뒤에 조사를 붙였기 때문**이다. 종성 판정 헬퍼를 넣는 것보다 **문장 구조를 바꾸는 쪽이 항상 싸고 안전하다.**

| # | 규칙 | 금지 | 확정 |
|---|---|---|---|
| **R1** | **변수 뒤에 조사를 붙이지 않는다.** 꼭 필요하면 받침이 고정된 접미어(`님`·`편`·`쪽`·`측`·`것`·`방`)를 사이에 끼운다 | `${name}이` | `${name} 님이` |
| **R2** | 주격/목적격 → **콜론 라벨** | `${w}을 훔쳤다` | `훔친 것: ${w}` |
| **R3** | 이동·변화 → **화살표 `→`** | `${s}가 ${r}(으)로 불려왔습니다` | `소환 — ${s} → ${r}` |
| **R4** | 인용·강조 → **낫표 「」 + 체언 종결** | `${w}이 사라졌다` | `사라진 것 — 「${w}」` |
| **R5** | 변수 뒤에 허용되는 조사는 **받침 무관한 것만**: `에 · 에서 · 의 · 도 · 만 · 까지 · 부터` | `${r}(으)로` | `${r}에서` |
| **R6** | 종성 판정 헬퍼(`packages/shared/src/josa.ts`)는 **NPC 대사 전용**. UI·로그는 R1~R5로 해결한다 | — | — |

> R6의 근거: NPC 대사는 자연스러운 사극 문장이어야 하므로 조사를 못 피한다. 반면 UI·로그는 라벨형 문장이 오히려 **읽기 빠르다** — 회피가 타협이 아니라 개선이다.

### 1.3 문장 규칙
- **1문장 = 1행동.** 안내문에 두 가지를 시키지 않는다.
- **금지 상태에는 반드시 "그래서 뭘 하면 되는지"를 붙인다.** `방 안에서만 가능해요.`(금지) → `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.`(확정)
- 느낌표는 **판 전체에서 3종만** 허용: 승리·최후 생존·미반증(정답 후보). 그 외는 마침표.
- 이모지는 **줄 앞 1개**. 연속 배치 금지(`🎉🎉`, `❌ ⚠`).
- 사용자 지칭은 `내`(소유)·`나`(주체). "유저"·"플레이어"는 **UI에 쓰지 않는다**(문서 전용).
- 오류 문구에 `에러`·`실패`를 단독으로 쓰지 않는다 → `~하지 못했어요` + 다음 행동.

### 1.4 진실값 경계 (CLAUDE.md 규약의 카피 측면)
- UI 문안은 **정답·타인 손패를 암시하지 않는다.** `정답인 듯해요` 같은 추측형은 금지.
  ⚠️ **2026-08-26 정정** — 이 줄은 원래 «`정답 후보`는 규칙엔진이 계산한 공개 결과이므로 허용»이었다.
  **틀렸다.** 「정답 후보」는 규칙엔진의 계산 결과가 아니라 그 위에 얹은 **추론**이고, 참·거짓이
  **보는 사람의 지식**에 달렸다(자기 손패 지목·공통 단서에서 거짓). 이 조항이 거짓 문안을 승인했다.
  로그는 일어난 일만 말하고 **추론은 각자의 증거 노트에 맡긴다.**
- 나만 보는 정보에는 **반드시 `(나만 봄)` 표기**를 붙인다: 반증 카드, 증거 노트. (계략 엿보기는 07-30 제거됨)
- NPC 대사 문안은 **LLM 출력이므로 이 문서가 확정하지 않는다.** 이 문서가 정하는 것은 폴백 템플릿(§8.2)과 접두 기호뿐.
- `[주의]` **폴백 템플릿에 대한 이 문서의 확정 권한은 "길이 예산 안에서"다.** 예산은 제출물 ④ §2.3이 정하고 코드 상수(`LINE_BUDGET_SUGGEST` 25 · `LINE_MAX` 40)가 강제한다 — 예산이 문안보다 상위다. 07-27 확정본이 이 예산을 어겨 07-28에 템플릿을 교체했다(§8.2 정정 노트).
- **대사 경로 표시(✨/♻/⚙)는 진실값이 아니라 운영 메타데이터다**(④ §4). 게임 상태를 말하지 않으므로 `(나만 봄)` 대상도 아니고, 숨기지도 않는다 — 문안은 §4.4.

### 1.5 키보드 포커스 가시성 (전역) `[2026-08-27]`
| 면 | 링 색 | 대비 |
|---|---|---|
| 밝은 면 전부(카드·모달·HUD 패널·D패드) | **`--wds-primary-strong`** `#8A4128` | 카드 **6.65** · 모달 6.95 · HUD 5.67 · D패드 5.41 |
| **어두운 면 2종** — **카드 화면의** 배경음 컨트롤 · **도감 오버레이** | `--wds-accent` `#E3B873` | 배경음 **7.34** · 도감 **7.88** |
| **게임 화면의 배경음 컨트롤** `[2026-08-28]` | `--wds-primary-strong` + **`outline-offset: -2px`** | 크림 면 **안쪽** 최악 **5.84** |

⚠️ **어두운 면은 하나가 아니다.** 초안이 «배경음 컨트롤 하나뿐»이라고 단정했다가
**도감 오버레이**(`.codex-overlay` — 어두운 배경 이미지 + 0.5 스크림)를 놓쳤고, 그 안 닫기 버튼
(`#codexClose`)의 링이 **7.88 → 1.99:1**(이미지 밝은 지점에선 **1.01:1**)로 떨어졌다 —
**고치려던 것과 정확히 같은 결함을 옆 화면에 새로 만들 뻔했다**(검수 실측).
`#game` 캔버스(`#1c1712`)·토스트도 어두우나 **그 위에 포커스 대상이 없다**(오늘 기준) — 잠재 축이다.

- 🔴 **이전에는 전 화면이 `--wds-accent` 하나였다.** 이 테마의 면은 전부 밝아서
  카드 위 **1.68:1** · 모달 위 1.75:1 — **WCAG 2.4.11/1.4.11(비텍스트 ≥3.0) 미달**이다.
  즉 **키보드 사용자에게 포커스가 어디에도 안 보였다.**
- 🖼 **`:hover`는 이제 기계가 잡는다 `[2026-08-28 · S9]`.** 25회차에 `button:hover`가 커스텀
  버튼으로 새어 대비가 **1.16:1**이 됐는데 게이트가 원리적으로 못 봤다 — 그 구멍을 메웠다.
  S9는 조작 요소에 **진짜 마우스를 올려**(CDP `Input.dispatchMouseEvent`) 색이 **바뀐 것만**
  다시 재고, 하한은 S6와 **같은 함수**를 쓴다. 음성 테스트로 «hover에서만 깨지는 결함»을
  주입해 잡히는 것을 확인한다.
  ⚠️ **`:active`·`:focus`는 여전히 사람 몫이다.** 그리고 S9는 **마우스 뷰포트에서만** 돈다 —
  손가락 화면은 `(hover: hover)`가 거짓이라 SKIP한다(실기 폰에도 hover가 없다).
  즉 **포커스 링(위 표)의 수치는 지금도 직접 계산해야 한다.**
- 어두운 면에서는 두 색의 우열이 **뒤집힌다**(`primary-strong` 1.85~2.18 · `accent` 7.34~8.64).
  그래서 전역은 `primary-strong`, `.bgm-ctrl`·`.codex-overlay` 안만 `accent`로 되돌린다.
- `[주의]` **배경음 컨트롤의 기본 배경은 완전 투명**(`rgba(28,20,11,0)`)이고 0.18s 트랜지션이 있다 —
  포커스 직후 그 시간 동안은 링이 **밝은 씬 위**에 그려진다. 위 7.34는 **펼쳐진 뒤**의 값이다.
  🔴 **게임 화면에서는 이 예외가 뒤집혔다 `[2026-08-28]`.** 23회차가 컨트롤을 보드 위로 옮기며
  **크림 면**(HUD 크롬)을 입혔다 — 거기서 `accent`는 최악 **1.47:1**로 진다.
  그리고 기본 `outline-offset: 2px`는 링을 **컨트롤 밖(=보드 위)**에 그리는데, 보드는 어두운 벽부터
  노란 돗자리까지라 **어느 색도 3.0을 못 맞춘다**(`accent` 1.10 · `primary-strong` 1.09).
  → 게임 화면에서는 링을 **면 안쪽**으로 넣는다(`offset: -2px` · `primary-strong` **5.84**).
  **«면이 어두운가»가 아니라 «링이 어디에 그려지는가»가 먼저다** — 오프셋이 양수면 링의 배경은
  컨트롤이 아니라 **그 뒤 화면**이고, 뒤가 캔버스면 색으로는 못 푼다.

### 1.5.0 타깃 크기 `[2026-08-29]`
| 포인터 | 하한 | 근거 |
|---|---|---|
| 손가락 | **44px** | **Apple HIG 44pt** |
| 마우스 | **24px** | WCAG 2.2 **§2.5.8 (AA)** |

⚠️ **«2.5.8만 포인터 무관»이 아니다.** 2.5.5(44)도 포인터를 안 가린다 — **등급이 AAA일 뿐**이다.
44를 **손가락에만** 요구하는 근거는 HIG 하나다. (초안 표가 없는 대비를 만들어 놨다.)

- 🔴 **27회차까지 마우스 화면은 통째로 무계측이었다.** 「44를 마우스에 요구하지 않는다」는
  옳지만 그 결론이 **「아무것도 안 잰다」**가 됐다. 넓히자마자 **43건**이 나왔다 —
  `game/desktop` **42건**(`.evi-hit` **83×20** ×21 · `.evi-memo-btn` **11.5×12** ×21) +
  **랜딩 1건**(`[↻ 새로고침]` **70×21**). 42는 한 화면의 수이고 새로고침은 **다른 화면**이다.
- 🔴 **간격 예외를 못 쓰는 이유를 정확히 적는다.** 규범문은 «원이 **다른 타깃**과 교차하지
  않을 것»인데 두 **중심**은 약 51px 떨어져 **원끼리는 안 겹친다.** 못 쓰는 진짜 이유는
  메모 버튼의 24px 원이 `.evi-hit`의 **바운딩 박스**를 약 2px 파고들기 때문이다.
  «원이 겹친다»로 적으면 다음 사람이 원↔원 거리로 계산해 **틀린 면제를 발급한다.**
- 하한을 `.evi-memo-btn`에 주면 행이 그 높이가 되고 `.evi-hit`가 `align-self: stretch`로
  **따라온다** — 한 줄로 둘이 풀린다.
  ⚠️ **폭 보상을 잊지 마라.** 버튼이 커지면 이름이 그만큼 좁아진다 — 데스크톱에서
  `.evi-hit` 83 → **72px**이 되며 「호랑이 대감」이 처음으로 잘렸다.
  `margin-right: -7px`(칩 오른쪽 여백 위로 겹침)로 되돌린다 → 74.5×24 · **잘림 0**(실측).
  이 보상은 24회차가 **폰에서 이미 한 번** 넣은 것인데 데스크톱에 안 넣어 재발했다.

### 1.5.0.1 크기 조절 손잡이 `[2026-08-29]`
| 자리 | 문안 | 역할 |
|---|---|---|
| 높이 손잡이 ×3 | `title` **`드래그·방향키로 높이 조절`** · `aria-label` **`${패널 이름} 높이 조절`** | `role="separator"` |
| 너비 손잡이 | `title` **`드래그·방향키로 너비 조절`** · `aria-label` **`우측 컬럼 너비 조절`** | `role="separator"` |

- 🔴 **전에는 «드래그로 …»였고, 그것이 사실의 전부였다** — `pointerdown`만 듣고 있어
  **키보드로는 기능이 0**이었다. 문안이 옳았던 게 아니라 **기능이 없었던 것**이다.
  ARIA **window splitter**(`tabindex="0"` + `aria-valuenow`)로 만들고 문안을 사실에 맞췄다.
- **`aria-hidden="true"`를 걷어냈다** — 보조기술 사용자는 「패널 크기를 바꿀 수 있다」는
  사실 자체를 몰랐다.
- 방향키 1스텝 = **16px** · `PageUp/Down` 4스텝 · `Home`/`End` 양끝.
  ⚠️ **왼쪽 방향키가 «넓히기»다** — 컬럼이 오른쪽에 붙어 있어 드래그와 같은 방향이다.
  반대로 하면 손과 키보드가 서로 다른 말을 한다.

### 1.5.1 길이 제약
> ⚠️ **아래 상한은 별도 표기가 없으면 «폰 390» 기준이다** `[2026-08-29]`.
> 상한이 전부 **렌더 폭**에서 나오는데 폭이 뷰포트마다 다르기 때문이다 — 데스크톱 콘텐츠 폭은
> **하나가 아니다**(실측): `.card` **454** · `.modal` **454** · `.goal-card` **452**
> (520 − padding 64 − **테두리**; `.goal-card`는 테두리가 2px라 2px 더 좁다). 폰은 308.
> ⚠️ 「456」처럼 테두리를 빠뜨린 값을 쓰면 다음 사람이 상한을 잘못 계산한다.
> 폰 상한을 데스크톱 조판에 그대로 적용하지 마라.

| 대상 | 상한 | 근거 |
|---|---|---|
| 버튼 라벨 | **8자** | `.hud-ctrl` 5개 버튼이 한 줄에 들어가야 함 |
| 툴팁(`title`) | **40자** | 40자 초과 시 OS 툴팁이 줄바꿈되어 커서를 덮음 |
| 로그 1줄 | **48자** | `.rp-col` 기본 260px 기준 2행 이내 |
| 턴 배너 상태줄 | **28자** | `.hud-turn`이 `white-space: nowrap` |
| 결과 제목 | **16자** | `.end-title` 26px |
| 결과 부제 | **44자** | `.end-sub` 1~2행 |
| 목표 카드 1줄 | **순한글 ≈33자** (마커 제외) | **상한은 글자 수가 아니라 렌더 폭이다**(폰 390 가용폭 306px). ①은 **37자인데 1행**(숫자·`·`·공백이 좁다), ⑤는 **36자인데 2행**(순한글 비중). 2026-08-26 실측 — 1행: ①37 · ④33 / 2행: ②39 · ③52 · ⑤36. ⚠️ ③(52자)이 최장. **글자 수로 판정하지 말고 `node scripts/gate-screen.mjs --keep` 캡처로 확정하라.** ~~«32자 권장 · ③은 44자»(md)와 «27자 · ③은 44자»(html)는 서로 달랐고 둘 다 실측과 불일치 — 2026-08-26 폐기~~ |
| 랜딩 `.msg` | **40자** | `min-height:16px` 1행. ⚠️ **폰 기준이다** — 데스크톱은 카드가 넓다(아래) |
| **NPC 대사 — 제안** | **25자** | 코드 `LINE_BUDGET_SUGGEST`. 봇 1턴 = `npcDelay`+`BOT_ACT_GAP`+`bubbleLifeMs(len)` ≤ 4s 역산(④ §2.3) |
| **NPC 대사 — 고발·계략** | **40자** | 코드 `LINE_MAX`. 홀드 타이머가 없어 클라이맥스 대사의 캐릭터색을 남긴다 |
| AI 로그 배지 | **8자** | `.log-badge` 1칸. 폴백 사유 한글 표기(§4.4)가 이 상한에 맞춰져 있다 |

---

### 1.6 모달 포커스 관리 `[2026-08-27]`
전면 오버레이는 **열 때 포커스를 안으로 넣고, 닫을 때 연 자리로 되돌린다.**

- **버튼으로 바꾸는 것만으로는 아무것도 나아지지 않는다** — 포커스가 뒤 페이지에 남아 있으면
  **오버레이가 덮어서 보이지도 않는 컨트롤들을** Tab으로 지나야 모달 안에 닿는다.
  도감은 실제로 그 상태였다(열어도 `activeElement`가 트리거 버튼).
- **Tab을 안에서 순환시킨다.** `aria-modal="true"`는 **보조 기술에만** 말할 뿐
  시퀀셜 포커스를 막지 않는다 — 순환은 코드가 해야 한다.
  ⚠️ **리스너를 오버레이에 걸면 구멍이 난다** — 모달 안의 **포커스 불가 영역**을 클릭하면
  `activeElement`가 `<body>`가 되고 keydown이 오버레이를 **거치지 않는다** → 덮인 배경 컨트롤로
  그대로 빠져나간다(검수 실측: 배경 focusable 10개). `document`에 걸고
  «모달 밖이면 안으로 끌어온다» 분기를 둬야 한다.
- Esc로 닫힌다(도감은 원래 있었다).
- **선택 상태를 색으로만 말하지 않는다** — 썸네일에 `aria-pressed`,
  바뀌는 상세 패널(`#cxInfo`)에 `aria-live="polite"`. 없으면 키보드 사용자는
  «이름 15개를 넘길 수 있을 뿐» 내용을 못 받는다.
- 실측(도감): focusable **1 → 16개** · 열면 포커스가 **기본 선택(호랑이 대감)**으로 ·
  Tab이 캐러셀을 따라 이동하고 링이 **어두운 면 위에서 보인다**(§1.5의 `accent` 예외).
- ✅ **오버레이 버스도 해소(2026-08-27).** `showOverlay`가 띄우는 것 중 **포커스 대상이 있는 것만**
  «모달»로 다룬다 — 목표 카드·결과 화면은 포커스를 가져가고, **주사위·로딩 인터스티셜은 가져가지 않는다**
  (포커스 대상이 **0개**라 옮기면 Tab이 죽는다).
  ~~«`pointer-events: none`이라»~~ — **틀렸다.** 그건 `.dice-overlay`뿐이고
  `#viewLoad`(`.vl-overlay`)는 **클릭을 전부 삼키는 전면 스크림**이다. 근거는 «포커스 대상 0개» 하나다.
- **초기 포커스는 «가장 덜 파괴적인 곳»**(APG Dialog). 결과 화면의 첫 버튼 `[다시 하기]`는
  **누르는 즉시 재대국을 보내므로** 거기 두면 Enter 한 번이 새 판이다 →
  마크업이 `[data-initial-focus]`로 자리를 선언하고, 결과 화면은 **제목**(`tabindex="-1"`)에 둔다.
  `aria-describedby="endSub endCards"`로 **정답 봉투 3장까지** 읽히게 한다(제목만 읽히던 상태였다).
- Tab 순환·복원 코드는 도감과 **같은 헬퍼**(`cycleTab`/`focusablesIn`)를 쓴다.
  ⚠️ **리스너는 두 벌이라 `defaultPrevented`로 서로 물러난다** — 없으면 «버스가 옮긴 포커스를
  도감이 다시 뺏는» 무한 리셋이 된다.
- **되돌릴 자리는 «보이는가»로 판정한다.** `isConnected`만으로는 부족하다 — 이 앱은 화면을
  제거하지 않고 `display:none`으로 끄고, 증거 노트 메모 `<input>`은 `onblur`가
  **자기 자신을 DOM에서 떼어낸다**.
- 실측(솔로 진입, 실제 앱): 진입 직후 `activeElement` = **`#goalOk`(카드 안)** ·
  Tab·Shift+Tab이 **안에 머문다** · 카드를 닫으면 **주사위 오버레이가 떠도 포커스를 안 가져간다**(`inDice: false`).
- `[주의]` **`#turnCircle`(턴 순서)은 아직 남아 있다** — `inset:0` · `z-index:45` · 스크림까지 있는
  **완전한 전면 모달**인데 버스를 안 타고, `role="dialog"`도 없고, 포커스를 넣지도·가두지도·되돌리지도
  않으며 **Esc로도 안 닫힌다**(버튼·스크림 클릭뿐).
- 🔴 **모달 밖 부유 컨트롤은 «포인터로도» 못 닿아야 한다 `[2026-08-27 (4차)]`.**
  `cycleTab`이 키보드는 모달 안에 가둬 놨는데 **마우스·터치는 나갈 수 있었다** —
  배경음 컨트롤이 목표 카드·결과 화면 **위에** 뜨고 눌렸다(실측: 스크림이 안 먹은 밝은
  스피커 + `elementFromPoint` → `#bgmToggle`, 폰·데스크톱 양쪽).
  ⚠️ **z-index로 올릴 길이 없다** — ① `#gameScreen`이 `position: fixed`라 **스택 컨텍스트**를
  만들어 그 안의 모달(z:35·40·45)이 `<body>` 직계 컨트롤을 못 이긴다(4든 40이든 같다) ·
  ② 데스크톱에선 스택 컨텍스트와 무관하게도 진다 — 제안·신고 모달 `.overlay`는 **z:10**인데
  컨트롤 기본값이 **z:40**이다. 그래서 «어느 z를 줄까»가 아니라 **«뜨면 끈다»**가 답이다.
  «폰에서 안 났다»가 «없다»가 아니었다 — 이 결함은 **데스크톱이 더 넓다.**
  ⚠️ **`display: none`이라야 한다.** `visibility:hidden`·`opacity:0`도 눈에는 같지만
  `opacity:0`은 **히트 타깃이 남는다** — 「안 보이는데 눌린다」(§1.7).
  판정은 **열려 있는 모달 DOM에서 파생**시킨다(`syncModalOn`). 불리언 토글은 소유자가
  넷이라(버스·제안/신고 모달·턴 순서·도감) «A를 닫으면 B 위에서 되살아나는» 순서 버그가 난다.
- `[주의]` **`#turnCircle`은 배경음만 해소됐다** — 여전히 버스를 안 타므로 `role="dialog"`도,
  포커스 트랩도, Esc도 없다. 버스에 올리는 것이 남은 일이다.
- `[주의]` **`#viewLoad`는 마우스만 막고 키보드는 안 막는다** — 청크 로딩 동안 Tab으로 아래
  컨트롤에 닿는다. 정답은 `inert`다.
- `[예외]` **결과 화면은 Esc로 닫히지 않는다** — 의도된 예외다(판이 끝난 화면에서 «닫기»가
  무엇을 뜻하는지 정의되지 않는다. 나가는 길은 두 버튼뿐).

---

### 1.7 부유 컨트롤 배치 (전역) `[2026-08-27]`
**화면에 고정(`position: fixed`)된 컨트롤은 «내용이 지나가는 띠»에 두지 않는다.**

- 랜딩·대기실 카드는 `.center`가 **세로 중앙 정렬 + 내용 의존 높이**다. 카드가 자라면
  상단이 **위로 움직여** 오른쪽 정렬 컨트롤이 화면 중턱의 고정 좌표를 **가로지른다.**
- 🔴 **같은 사고가 두 번 났다** — 배경음 버튼(`top:150px`) 밑으로 초대 링크 `[복사]`가
  들어가는 S1 FAIL이 **13회차(잠금 상태 행 예약 +36px)와 17회차(요약 한 줄 +40px)에**
  났다. **원인이 매번 다른데 결과가 같으면 원인은 내용이 아니다.**
  두 번 다 «내용을 줄여» 피했고, 그 처방이 이 문서에 **문안 길이 제약으로** 굳어져 있었다.
- 🔴 **`top`을 화면 맨 위로 되돌리는 것만으로는 부족하다** — 초안이 «카드 상단은 `.center`
  padding으로 y≥16px에 고정되니 상한이 내용과 무관하다»라고 적었는데 **거짓이다.**
  카드 화면은 게임 화면과 달리 **문서가 스크롤한다**(`body.no-scroll`이 없다) — `fixed`면
  카드가 컨트롤 밑으로 **흘러 들어간다.** 그 상한은 **`scrollTop = 0`에서만** 성립했고,
  화면 게이트 S1도 거기서만 잰다(검수 실측 → 재확인: `#joinBtn` scrollY 373 ·
  `#refreshRooms` scrollY 433에서 `elementFromPoint`가 배경음 컨트롤을 잡는다).
- **처방은 «스크롤과 함께 움직인다»** — 카드 화면의 부유 컨트롤은 `position: absolute`.
  문서 좌표에서 컨트롤(y 12~62)과 카드 컨트롤(y ≥ 110)이 갈리므로 **어떤 스크롤 위치에서도**
  겹치지 않는다. 대가는 «긴 카드에서 같이 스크롤돼 나간다»이고, 그 편이 «보이는데 안 눌린다»보다 낫다.
- **중턱 좌표는 «그 값을 필요하게 만든 화면에서만»** — 배경음 `150px`의 지배적 사유는
  상시 전폭인 **턴 배너**(`.hud-turn` y10~74)다(`#rpToggle`은 패널이 열렸을 때만 있는 부차 사유).
  둘 다 `#gameScreen` 안이라 카드 화면엔 없다 → `body.no-scroll`로 한정.
- ⚠️ **게이트가 잡아 준다고 배치가 맞는 것은 아니다.** S1은 «이 캡처, 이 스크롤 위치에서
  겹쳤는가»만 말한다. 겹치기 **직전**도, **스크롤한 뒤**도 통과다 — 문안 한 줄이 다음 회차에 뒤집는다.
- **카드 폭은 `#bgmCtrl`과의 «가로» 여유가 정한다** `[2026-08-29 실측]`.
  §1.7이 관리해 온 불변량은 「카드가 자라면 위로 움직여 고정 좌표를 가로지른다」였는데,
  데스크톱의 실제 안전 근거는 **세로가 아니라 가로**다 —
  최악은 데스크톱 최소폭 **761**: 카드 오른쪽 `640.5` vs `#bgmCtrl` 왼쪽 `699` → **여유 58.5px**.
  카드 폭을 657px 이상으로 올리거나 분기를 761 아래로 내리면 **그 순간 되살아난다.**
  ⚠️ 그리고 **게이트 S1 PASS는 이 쌍의 안전 근거가 못 된다** — 1440에서 둘은 **378px 떨어져**
  있어 y가 어떻게 움직여도 원리적으로 FAIL이 날 수 없다. 바로 위 「게이트가 잡아 준다고
  배치가 맞는 것은 아니다」의 실례다.
- **데스크톱 카드 조판** `[2026-08-29 · 35회차]` — `@media (min-width: 761px) and (min-height: 660px)`에서
  `.card` 380 → **520**(= `.end-card` 상한) · `h1` 20 → **26**(= `.end-title`).
  값을 저장소 안의 축에서 가져온 이유는 §1.5.1 위 주석과 같다 — 새 폭을 만들면 축이 하나 더 는다.
  ⚠️ **높이 조건이 필수다.** 폭만 걸면 짧은 데스크톱(높이 655px 미만)에서
  「잔치 시작」이 **첫 화면 밖**으로 나간다(실측).
  ~~⚠️ 남은 어긋남: `.goal-card` 380 · `.modal` 300~~ → **36회차에 CSS 상수를 통일했다.**
  ⚠️ **«520 하나로 정렬»은 렌더 기준으로는 거짓이다** — `.end-card`는 `max-width`(상한)라
  내용에 맞게 줄어들어 **실측 476.5px**이고 나머지 셋만 정확히 520이다.
  그리고 이 절이 인용한 원칙의 원문은 **실제 렌더 폭** 이야기다(폰에서 358·359 vs 300).
  즉 지금 상태는 「같은 상수를 쓴다」이지 「같은 폭으로 보인다」가 아니다.
  근거: 신고 모달은 **판을 끝내는 결정**(틀리면 즉시 탈락)을 받는 자리인데 1440px에서 300px였고,
  60초 안내는 **처음 읽는 글**인데 5줄 중 **4줄이 2행**으로 접혔다(이후 ③만 2행).
  ⚠️ 이 둘에는 `#bgmCtrl` 축이 없다 — `body.modal-on .bgm-ctrl { display: none }`이 먼저 끈다.
  ⚠️ **«우측 컬럼에 안 닿는다»를 불변량으로 적지 마라** — 1440에서만 참이다.
  실측 1024×768에서 모달이 `.rp-col`을 **24px 침범**한다(변경 전 46px 여유). 스크림이 전면을
  덮으므로 시각 사고는 아니지만, 게이트도 이 쌍을 `pairs`에 안 넣어 **영원히 침묵**한다.
  ⚠️ 높이 가드 **660은 랜딩에서 빌려온 값**이다. 그쪽 근거(「첫 화면 밖으로 나간다」)는
  문서를 스크롤하는 카드의 사정이고, 이 둘은 뷰포트 중앙 오버레이라 성립하지 않는다 —
  실측상 **높이 560에서도** 잘림·스크롤이 없다. 501~659가 근거 없이 옛 크기로 남는다.
  ⚠️ **681~760px 띠는 어느 분기도 안 걸린다**(폰은 `max-width: 680px` · 데스크톱은 `min-width: 761px`).
  실측(720×900): 우측 컬럼은 이미 데스크톱 배치인데 **카드만 380·13px**로 남는다.
  분기를 681로 내리면 덮이지만 `.card`의 `#bgmCtrl` 가로 여유가 58.5 → **18.5px**로 준다.
- `[미확인]` **coarse 태블릿(681px~)의 `150px`은 근거가 확인되지 않았다** — 그 구간엔
  `#rpToggle`이 없고(`display:flex`는 `max-width:680px`에서만) 배너도 전폭이 아니다.
  게이트 뷰포트가 **폰·데스크톱 둘뿐**이라 잴 수단이 없어 값을 그대로 뒀다. 재는 것이 먼저다.
  ⚠️ **미측정 구간이 넓어졌다** `[2026-08-29]` — 761~1279px에 이제 «520px 카드»라는 새 상태가 있고,
  그 구간에서 대기실 전신 일러스트(`#lobby-char`)가 카드에 **83%까지 가린다**(761 실측).
  겹침 자체는 이 회차 이전부터 있었고(0이 되는 폭은 W > 1655였다) 이번에 **W > 1774**로 넓어졌다.

## 2. 어휘 통일표 — 1개념 = 1표기

| 개념 | **확정 표기** | 쓰지 않는 표기 (현재 코드에 혼재) |
|---|---|---|
| 3요소 ①누가 | **용의자** (지목 대상), **도둑**(정답 확정 후) | `범인`, 로그의 `도둑:`(제안 단계에선 `용의자:`로) |
| 3요소 ②무엇을 | **훔친 것** | `장물`, `무기`, `weapon` 노출 |
| 3요소 ③어디서 | **장소** | `방`(칸 개념과 충돌), `현장` |
| 승리 선언 행동 | **신고** | `고발` — **2026-08-26 확정**(쿼카 결정 · 오너). 내부 식별자(`accuse`·`doAccusation`·`bot-accuse.ts`)와 코드 주석·설계 서술 산문에서만 유지 |
| 내 카드 묶음 | **내 손패** | `내 단서 패`, `핸드` |
| 전체 공개 카드 | **공통 단서** | `공개 카드`, `공용 카드` |
| 방 진입점 | **입구(🚪)** | `문`, `도어` |
| 게임 시작 | **잔치 시작** | `게임 시작`(서버 로그가 이 표기) |
| 한 판 | **판** | `게임`, `라운드`(라운드는 턴 한 바퀴) |
| 방 참가 동작 | **참가** | `참여`(공개방 목록 버튼이 이 표기) |
| 고정 NPC 능력 | **계략** | `보너스`, `bonus` |
| 관전 상태 | **관전** | `구경`, `스펙테이터` |
| AI 플레이어 | **NPC** | `봇`, `AI 플레이어`(UI 한정. 문서·코드는 봇 허용) |
| 대리 NPC | **대리 NPC** `[선행]` | `임시 봇`, `AFK 봇` |
| 승자 없는 종료 | **무승부** | `비김`, `드로우`, `유찰` |
| 대사 생성 경로 3종 | **LLM · 캐시 · 폴백** | `AI`, `fallback`, `hit`, `규칙 대사` |
| 폴백 사유 | **한글 표기 6종(§4.4)** | 영문 토큰(`timeout`·`http`…) 화면 노출 |

**액션 버튼 라벨 5종 (07-30 교체 · 동사형 통일)** — 버튼에 찍히는 글자와, 문장 안에서 그 버튼을 가리키는 대괄호 표기는 **글자 그대로 같아야 한다.**

| 버튼 | **확정 라벨** | 개념어(그대로 유지) |
|---|---|---|
| `#suggest` | **추리하기** | 개념·로그·기록표는 **제안** — `🔍 제안`, `제안 기록표`, `제안 상한 75`는 바꾸지 않는다 |
| `#accuse` | **신고하기** | ✅ 모달·확인·결과 **전부 «신고»** — §2 본표 «승리 선언 행동» 행 참조. ~~«개념은 고발 — 유지»~~ **철회**(2026-08-26) |
| `#passage` | **지름길로 가기** | 개념은 **비밀 통로** |
| `#bonus` | **다가가기** | 개념은 **계략**(위 표 「고정 NPC 능력」 행) · 07-30 엿보기 효과 제거·이동 보너스만 — 로그 `🃏 계략 — …에게 다가가 이동 +N` |
| `#endTurn` | **내 턴 종료** | 개념은 **턴 종료** |

> ⚠️ **라벨만 바꿨고 툴팁·안내 문안의 동사는 아직 개념어다** — 예: `#suggest`가 「추리하기」인데 툴팁은 `방 안에서만 제안할 수 있어요`(§5.1). 교체 스펙이 버튼 라벨 5종으로 범위를 한정했기 때문이며, **1개념 1표기**(§2 제목)에 걸리는 지점이라 **기획 판단이 필요하다.** 판단 전까지는 라벨만 새 이름이다.
> `[정정 2026-08-26]` 이 자리에 원래 「같은 것을 **두 이름**으로 배우게 하지 않는다」(§8.1)라고 적혀 있었다 — **§8.1에 그런 문장은 없다.** 실제 조항은 「같은 **사유**를 두 **문장**으로 배우게 하지 않는다」(§8.1 표 말미 · §7.2)로 **다른 명제**다. 1개념 1표기의 근거는 **§2 제목 자체**다. 이 오인용이 플랜 21·26과 `SKILL.md`로 6회차 동안 전파됐다.
> 또한 `#accuse`에 대해서는 이 노트가 이제 거짓이다 — 툴팁 4종(`main.ts`)이 이미 전부 «신고»다.

**표기 접두 기호(고정)**
`🎲` 주사위 · `🔍` 제안 · `🛡` 반증 · `❗` 미반증 · `❌` 실패·탈락 · `🎉` 승리 · `🏅` 최후 생존 · `🏳` 무승부 · `🚪` 방 진입/통로 · `🃏` 계략·손패 · `💬` 캐릭터 대사 · `📢` 공통 단서 · `⏳` 턴/시간 · `💤` 대리 중 · `🤖` NPC · `📍` 장소 · `✨` LLM 대사 · `♻` 캐시 대사 · `⚙` 폴백 대사

**UI 표식 기호(문장 접두가 아님 · 서로 뜻이 겹치면 안 된다)**

| 기호 | 뜻 | 자리 |
|---|---|---|
| `▸` / `▾` | **접힘 / 펼침** | `<summary>` 왼쪽(`.rp-fold`·`.hud`) |

> 🔴 **«아래로 더 있음»에는 기호를 쓰지 않는다 `[2026-08-28]`.** 20회차가 `↓` 표식을
> 만들었다가 **폐기**했다 — ① `▾`를 쓰면 접힘 어휘와 충돌하고(같은 머리글에 **나란히** 붙는다),
> ② `↓`로 피해도 `content` 글리프는 **접근성 이름에 그대로 들어간다**
> (`opacity: 0`은 접근성 트리에서 요소를 지우지 않는다 — 넘치지도 않는 패널의 표식까지 낭독된다).
> 스크롤은 **스크롤바가 말한다** — 기호는 «얼마나 남았는지»를 못 말한다는 점에서도 진다.
> `[미확인 · 2026-08-28]` 다만 **실기 폰에 이 신호가 닿는지는 엔진마다 다르다** —
> Android Chrome은 «`::-webkit-scrollbar`에 폭을 주면 오버레이가 클래식이 된다»는 Blink
> 동작 때문에 **닿을 가능성이 높고**, iOS Safari는 그 의사요소를 안 그려 **신호가 0일
> 가능성이 높다.** 둘 다 문헌 근거이고 **실기 측정이 아니다.**
> 헤드리스 게이트는 폰 에뮬레이션에서도 클래식 스크롤바를 그려 **이 차이를 못 가른다**
> (`gate.config.mjs` §사람 확인에 기록). **사람이 두 기기로 확인해야 한다.**

> 접두 기호는 **이형 선택자(VS16 `U+FE0F`) 없이** 쓴다. 저장소 전체가 그렇고(`🛡` `U+1F6E1` 단독), 한 곳만 붙이면 폭이 달라져 로그 좌측 정렬이 어긋난다.

> ~~`[주의]` 증거 노트 `장소` 그룹 칩의 아이콘 칸이 전부 비어 있다.~~ **07-29 해소 확인.** `EMOJI` 맵에 장소 9종이 없는 것은 그대로지만(카드에 이모지를 추가하지 않는다 — 방 9곳에 자연스러운 이모지가 없다), `main.ts` `cardIcon()`이 장소를 **고정 문자 `📍`**로 채운다 — 증거 노트·제안 모달·손패가 같은 경로를 탄다.

---

## 3. 첫 진입 목표 카드 `[선행]`

**선행 조건**: 오버레이 DOM(`#goalCard`), 1회 표시 플래그(`localStorage: zc_seen_goal`), 재열람 버튼(`.hud-ctrl` 끝 `[?]`), `?demo=1` 스킵 파라미터. 현재 저장소에 튜토리얼·도움말 DOM은 0건.

### 3.1 전문
```
🐯 60초 안내

① 목표 — 도둑 1명 · 훔친 것 1개 · 장소 1곳을 맞히면 승리.
② 출발점 — 내 손패 3장은 정답이 아닙니다. 여기서부터 지워 나가세요.
③ 방법 — 방향키로 이동해 방에서 [추리하기]. 내 제안이 반증되면 그 카드는 정답이 아니에요.
④ 확신 — 장소까지 안 지운 3장을 내가 냈는데 아무도 반증 못 하면 정답.
⑤ 끝내기 — 확신하면 [신고하기]. 틀리면 탈락하고 반증만 남아요.

              [ 알겠어요, 시작할게요 ]

[?] 버튼을 누르면 다시 볼 수 있어요.
```

- 제목 `🐯 60초 안내` (8자) — "튜토리얼"이라 쓰지 않는다(길고, 학습 부담을 예고한다).
- 줄 길이 상한은 **글자 수가 아니라 렌더 폭**이다 — §1.5.1 길이표를 따르고 **캡처로 확정**한다.
- **② 가 이 카드의 존재 이유다** — 순서를 바꾸지 말 것.
- **⑤ 신설(2026-08-26 · 1차 · 신설 당시 ④).** 이전 3줄은 목표·출발점·방법을 가르치면서 **«어떻게 이기는가»가 없었다** —
  ①이 "맞히면 승리"라고만 하고 그 선언을 어느 버튼으로 하는지도, **틀리면 즉시 탈락**한다는 것도
  말하지 않았다. 신규 유입이 ①을 읽고 [신고하기]를 누르는 경로가 그대로 열려 있었다.
  라벨을 `끝내기`로 잡아 ③ `방법`(반복하는 행동)과 **역할로** 대비시킨다 — 버튼 이름 둘을 나란히
  놓고 «다르다»를 설명하는 것보다 각자의 자리를 주는 편이 짧다.
  **문안 근거**: 「탈락」에서 끊지 않고 **「반증만 남아요」까지** 말한다. 오답 고발의 실제 대가는
  «쫓겨남»이 아니라 **«남은 판 내내 앉아서 반증만 하는 것»**이다 — `doSuggestion`은 `turnOrder`
  전체를 돌며 eliminated를 거르지 않는다(`clue-room.ts`). 저장소의 다른 세 곳이 이미 그렇게 말한다:
  §5.2 `남은 역할은 반증입니다` · §7.2 `내 손패로 반증은 계속돼요` · 서버 로그 `탈락 · 반증만 가능`.
  ⑤만 「탈락」에서 끊으면 카드가 그 대가를 감춘다.
  같은 사고를 **시각**으로 막은 것이 액션 바 3단 위계다(`docs/plans/done/22-action-bar-hierarchy.md`) —
  이 줄은 그 **문안 쪽 절반**이고, 시각이 말할 수 없는 «왜 다른지»를 맡는다.
- **④ 신설(2026-08-26 · 2차).** 3줄→4줄로 «어떻게 이기는가»를 채웠지만 **«어떻게 아는가»가 없었다** —
  ②③이 **둘 다 소거**(이건 정답이 아니다)이고, ⑤가 `확신하면`이라 말하면서 **확신이 어디서 오는지**는
  말하지 않았다. 카드 전체가 «지우는 법»만 가르치고 «찾는 법»은 가르치지 않은 것이다.
  **문안 근거**: 정확한 명제는 서버 규칙이 확정한다 —
  「**내가 낸** 제안의 3장을 **내가 «정답 아님»으로 본 적이 없고** 아무도 반증 못 하면 그게 정답」
  (`clue-room.ts` 미반증 즉시 고발 · `seenOf` 기준 · `docs/plans/done/25-bot-self-accuse-common-clue.md`).
  사용자 어휘로 «정답 아님으로 본 적 없는» = **«증거 노트에서 안 지운»**이다 —
  노트가 손패·공통 단서·받은 반증 카드를 **자동 제외**로 칠하기 때문이다
  (`main.ts` `buildEvidence()`의 `own` 판정 = `myCards ∪ commonSet ∪ definiteSet`).
  ② `여기서부터 지워 나가세요`가 이미 «지우다»를 심어 뒀으므로 새 어휘가 필요 없다.
  - 🔴 **`내가 냈는데`를 빼면 거짓이 된다.** 서버 반증 루프는 **제안자를 건너뛴다**
    (`clue-room.ts` `for (let i = 1; …)`)고, 자기 손패 지목은 **의도적으로 허용**된다.
    그래서 «아무도 반증 못 함»은 **제안자의 손패에 대해서만 무정보**다. 그런데 제안 3장도
    미반증 로그도 **전원에게 브로드캐스트**되므로, 남의 제안에 이 규칙을 적용하면 틀린다.
    ③이 `내 제안이 반증되면`으로 소유격을 명시하는 것과 **같은 이유**이며,
    이것은 문체가 아니라 **참·거짓** 문제다 — 2026-08-26 1차가 로그에서 걷어낸 그 추론
    (`정답 후보`)을 온보딩 카드로 되살리는 셈이 된다.
  - **`장소까지`가 두 번째 핵심어.** 제안의 장소는 **고르는 값이 아니라 서 있는 곳**이다
    (`openPicker({ needRoom: false, fixedRoom: me.room })` · 서버가 `player.room`에서 가져간다 —
    사용자는 **2장만 고른다**). 그래서 신규 유입이 **셋 중 장소 하나만 빠뜨린다.**
    같은 누락이 봇에서는 자멸 고발로 나타났다(플랜 25) — 이 줄은 그 **사람 쪽 절반**이다.
    ⚠️ 초안은 이 자리에 `방까지`라고 썼다 — **§2 어휘 통일표 위반**이다(3요소 ③의 확정 표기는
    **`장소`**이고 `방`은 «칸 개념과 충돌»로 금지). ③의 `방에서`는 **물리 공간** 용법이라 허용 범위지만,
    ④의 자리는 **정답 봉투의 한 장**을 가리키므로 `장소`여야 한다.
  - **소거 방향으로 안전하다**(거짓을 가르치지 않는다): «안 지운» = (자동 제외 ∪ 수동 제외)의 여집합이고
    이는 `seenOf`의 여집합에 **포함**된다. 즉 이 규칙이 발동하면 **반드시 참**이고, 수동 제외를
    많이 한 사용자는 기회를 놓칠 뿐 **틀린 결론에 도달하지 않는다**.
    덱 폐쇄성이 전제다 — `deal()`의 `deck`은 정답 3장을 뺀 전부이고 전량 딜된다
    (어떤 카드든 «손패 ∪ 공통 단서 ∪ 정답»).
    `[주의]` **알려진 예외 1건** — 판 중반 `revealCommonClue()`(제안 상한 도달 시)로 추가 공개되는
    공통 단서를 **증거 노트가 다시 그리지 않는다**(`buildEvidence` 호출처에 `commonCards` 리스너 없음).
    신고 모달은 `confirmedNotAnswer()`가 실시간이라 잠그므로 오답 신고까지 가지는 않지만,
    노트는 «안 지웠다»고 하고 모달은 «선택 불가»라고 하는 **모순이 남는다**. 발생률 0.09%(sim) — 후속 과제.
  - 라벨 `확신` — ⑤ `확신하면`이 이 줄을 **가리키게** 만든다. ③ `방법`(반복하는 행동) ·
    ④ `확신`(판단) · ⑤ `끝내기`(선언)로 **역할이 셋 다 다르다**(⑤ 신설 때 세운 «역할로 대비» 규칙).
- 버튼 라벨 `알겠어요, 시작할게요` — `확인`·`닫기`는 쓰지 않는다(무엇이 확인됐는지 알 수 없음).
- 재열람 안내는 **본문보다 작게, 카드 하단**. 12자 이상 줄이지 말 것 — 이 문장이 없으면 사용자가 카드를 닫기를 주저한다.

### 3.2 재열람 버튼 `[?]`
- 라벨 `?` · 툴팁 `게임 목표와 조작을 다시 봅니다.` (18자)

---

## 4. 손패 라벨 · 공통 단서

### 4.1 손패 HUD (`.hud-hand` / `#hand`)
| 상태 | 현재 `[있음]` | **교체안** |
|---|---|---|
| 잔치 시작 전 | `내 단서 패: (잔치 시작 후 표시)` | `🃏 내 손패 — 잔치가 시작되면 3장이 배부돼요.` |
| 배부 후 | `내 단서 패: 잡채, 서재, 뱀 무녀` | 아래 2행 블록 |
| 재접속 직후 `[선행]` | (복구 안 됨) | `🃏 내 손패 — 불러오는 중…` |

**배부 후 블록 (2행)**
```
🃏 내 손패 · 이 3장은 정답이 아님
🍜 잡채    📍 서재    🐍 뱀 무녀
```
- 1행이 **라벨이자 규칙 설명**이다. `이 3장은 정답이 아님`을 괄호로 감싸지 말 것 — 괄호는 부가정보로 읽혀 건너뛴다.
- 2행은 카테고리 접두 이모지 + 라벨. 쉼표 대신 **넉넉한 공백**으로 카드처럼 분리.
- `.hud-hand` 툴팁: `내 손패는 정답 봉투에 없어요. 후보에서 먼저 지우고 시작하세요.` (34자)

### 4.2 공통 단서
- 서버 로그(§8): `📢 공통 단서 — 모두 공개 · 정답 아님: 젓가락, 행랑채`
- 손패 HUD 3행(있을 때만): `📢 공통 단서 · 모두 공개: 🥢 젓가락  📍 행랑채`
- 증거 노트 칩 툴팁: `공통 단서 — 모두 공개 · 정답 아님 (자동 제외)` (26자)

### 4.3 증거 노트 (`#evidence`)
| 대상 | 문안 |
|---|---|
| 패널 제목 `[있음]` | `🔍 증거 노트` + 부제 `(나만 봄)` — **유지** |
| 내 패 칩 툴팁 | `내 손패 — 정답 아님 (자동 제외)` (20자) |
| 공통 단서 칩 툴팁 | `공통 단서 — 모두 공개 · 정답 아님 (자동 제외)` |
| 일반 칩 툴팁 | `눌러서 바꾸기 — 미정 → 제외 → 의심 → 미정` `[2026-08-28 교체]` |
| 그룹 라벨 | `용의자` / `훔친 것` / `장소` (§2 표기와 일치) |
| 빈 상태 `[선행]` | `아직 지운 후보가 없어요. 칩을 눌러 표시하세요.` |

- 🔴 **칩의 상태 순환은 «버튼»이다 `[2026-08-28]`.** 전에는 `.evi-row`(`<div>`)의 `onclick`뿐이라
  ① **키보드로 아예 못 썼고**(증거 노트는 이 게임의 추리 도구다) ② role이 없어 화면 게이트
  S3의 «조작 요소»에 안 잡혀 **44px 하한을 한 번도 안 쟀다**(버튼이 되자마자 `74.5×20` FAIL).
  13~15회차가 대기실 격자·도감 썸네일에 한 것과 같은 교정이다.
  ⚠️ 메모 버튼(`✎`) **안에 넣을 수 없다** — 버튼 중첩은 무효 HTML이라 **형제**로 둔다.
  잠금 칩은 `disabled`가 아니라 **`aria-disabled`** — 포커스에 남아야 «왜 안 바뀌는지»를 읽는다.
- 🔴 **상태를 `opacity`로 말하지 않는다 `[2026-08-28]`.** `.cleared`에 `opacity: 0.4`가 있었다 —
  칩 전체가 투명해져 **9.78 → 2.12:1**(AA 미달)이고 **S6는 합성 전 색을 읽어 못 본다.**
  §9.0이 대기실 격자에서 고친 **같은 함정**이 여기 남아 있었고, «제외»는 판에서 **가장 흔한
  상태**라 패널 대부분이 안 읽히는 상태였다.
  → 불투명한 색(`--wds-label-alternative` **5.72**) + **두께 2px 취소선**(`primary-normal` **4.04**).
  취소선은 색 단독이 아닌 **모양** 단서다(1.4.1). 자동 제외 칩의 `opacity: 0.5`(2.67)도 걷어냈다 —
  «못 누른다»는 **파선 테두리**가 말한다.
- 🔴 **그룹은 «툴바»다 — 탭 스톱을 그룹당 1개로 `[2026-08-28]`.** 칩을 버튼으로 만들면서
  탭 스톱이 **21 → 42**로 늘었다(칩 21 × 버튼 2). 증거 노트를 지나 제안 기록표·기록/알림에
  가려면 Tab을 **42번** 쳐야 했고, 데스크톱은 컬럼이 늘 열려 있어 피할 길이 없다.
  → APG **Toolbar**: `role="toolbar"` + roving tabindex. 스톱 **42 → 3**(그룹 3개).
  `←→` 버튼 1개씩 · `↑↓` **한 줄**(격자 열 수 × 2) · `Home`/`End`.
  열 수는 `repeat(auto-fill, …)`이라 뷰포트에 따라 변하므로 **누를 때 계산된 값에서 읽는다** —
  상수로 박으면 폰·데스크톱 중 한쪽이 틀린다.
  ⚠️ **다시 그리면 포커스가 사라진다** — `buildEvidence`는 손패 배부·반증·엿보기·리매치마다
  통째로 다시 만든다. 자리를 기억했다 되돌린다(§9.0이 대기실 격자에서 쓴 것과 같은 처방).
- **패널 머리글은 헤딩이다 `[2026-08-28]`** — `.rp-head`가 `<div>`라 보조기술 사용자가
  «헤딩 점프»로도 컬럼을 못 건너뛰었다. `<h2>`로 바꾸면 전역 제목 스타일이 붙어 레이아웃이
  흔들리므로 `role="heading" aria-level="2"`만 준다.
- **상태 낱말은 보조기술에만 준다** — 칩 폭이 좁아 화면에는 못 넣는다
  (실측: 이름 상자 60px · 최장 「호랑이 대감」 55.3px → 여유 **5px**).
  `aria-label = "${이름} — 미정|제외|의심"`. 잠금 칩은 이름을 **짧게**(`— 자동 제외`) 두고
  사유는 `title`에만 — 둘 다 넣으면 **같은 문장을 두 번 읽는다**.
- 🔴 **툴팁 문안을 교체했다 `[2026-08-28]`.** 옛 문안 `클릭: 없음(제외) → 의심 → 초기화`는
  ① **「클릭」이 마우스 전용 말**이고 — 키보드로 쓰라고 버튼으로 바꿨는데 안내가 클릭만 말했다 ·
  ② `title`은 **폰에 절대 안 뜬다**(hover가 없다) · ③ 상태 이름이 `aria-label`의
  `미정`/`제외`/`의심`과 **달라서**(「없음」/「초기화」) 순환 순서를 복원할 수 없었다.
- 🔴 **「의심」도 색 단독이었다 `[2026-08-28]`.** 「제외」만 고치면 1.4.1을 반만 지킨다 —
  `.suspect`의 배경은 칩 배경과 **1.15:1**(면 대비)이라 적색맹에겐 같은 면이다.
  → 「제외」와 **대칭**인 모양 단서(같은 2px 강조색 선, 위치만 아래 = 밑줄). 폭 비용 0.

> ~~`[갈라짐]` 코드가 갈라져 있다(07-28 확인).~~ **07-29 해소 — 코드가 확정본과 같다**(`main.ts` `buildEvidence()`의 잠금 칩 `chip.title`). 갈라졌던 이유의 기록은 남긴다: ① `내 패`는 §2 어휘표 위반(확정 표기는 `내 손패`, `내 패`는 셀렉트 옵션 접미 §6.1·§6.2 전용 축약형이다) ② 잠금 칩은 **클릭해도 반응하지 않는 유일한 칩**인데 옛 코드 문안에는 그 이유가 없었다 — `(자동 제외)`가 §1.3 "금지 상태에는 왜 그런지를 붙인다"를 만족시키는 부분이다 ③ 길이는 각각 20자·26자로 툴팁 40자 상한 안이다.

### 4.4 AI 카운터 칩 · 대사 경로 배지 (`#aiPanel` · `.log-badge`) `[있음]`
근거: 제출물 ④ §4(관측 가능성) 요구사항 3·5. **칩 본문과 경고 배지는 ④가 지정한 문자열이며 이 문서는 그것을 그대로 인용한다.** 여기서 새로 확정하는 것은 ① 폴백 사유 6종의 한글 표기 ② 상세 패널 행 라벨이다.

| 요소 | 문안 |
|---|---|
| 패널 제목 `[있음]` | `🤖 AI 대사` — **유지** |
| 칩 본문 | `✨LLM 12 · ♻캐시 3 · ⚙폴백 0` — 세 경로를 **항상 함께** 보여준다(폴백이 전부라도 숫자를 가리지 않는다) |
| 칩 툴팁 | 칩 본문과 **같은 문자열** |
| 상세 · 평균 지연 | 라벨 `평균` · 값 `1.2s` (계측 전이면 `—`) |
| 상세 · 모델 | 라벨 `모델` · 값 `gemini-flash-lite-latest` (계측 전이면 `—`) |
| 상세 · 사유별 건수 | 라벨 `⚙ 응답 지연` · 값 `3` |
| 전건 폴백 경고 | `AI 대사 일시 폴백` (④ §4 요구사항 5 지정본) |
| 로그 배지 · LLM/캐시 | `✨ 1.2s` / `♻ 0.0s` |
| 로그 배지 · 폴백 | `⚙ 응답 지연` |
| 로그 배지 툴팁 | `llm · 1183ms · gemini-flash-lite-latest` — **진단용 원값이라 영문 토큰을 허용하는 유일한 자리** |

**폴백 사유 6종 — 한글 표기 확정** (코드는 현재 영문 토큰을 그대로 찍는다 → 교체 필요)

| 코드 토큰 | **확정 표기** | 실제로 있었던 일 |
|---|---|---|
| `timeout` | **응답 지연** | 4000ms 초과(`AbortError`) |
| `http` | **응답 오류** | 2xx 아님(429·5xx) 또는 왕복 실패 |
| `empty` | **빈 응답** | `candidates` 텍스트 없음 |
| `toolong` | **길이 초과** | 정규화로도 예산에 못 맞춰 폐기 |
| `nokey` | **키 없음** | `GEMINI_API_KEY` 미설정 |
| `disabled` | **LLM 꺼짐** | `AI_NARRATE=off` |

- 전부 **5자 이내 체언 종결** — 배지 8자(§1.5.1)와 로그 48자 상한을 동시에 만족한다. 조사 없음(R1).
- `timeout`을 `시간 초과`로 옮기지 않는다 — 그 문안은 §9.5 **턴 만료**(`⌛ 시간 초과`)가 이미 쓴다. 서로 다른 사건에 같은 말을 주면 로그에서 구분되지 않는다.
- `실패`를 단독으로 쓰지 않는 것은 §1.3 규칙이다. 다만 이 배지는 **관측 표시**라 "다음 행동"을 붙이지 않는다 — 사용자가 할 수 있는 일이 없고 게임은 정상 진행된다. 대신 사용자 대상 문장은 `AI 대사 일시 폴백` 하나로 끝낸다.
- 칩의 `LLM`은 캐릭터가 아니라 **문장이 온 경로**의 이름이므로, "UI에서 AI 플레이어는 NPC로 부른다"는 §2 규칙과 충돌하지 않는다.
- **칩의 «계측 전» 표기는 표면마다 다르다 `[2026-08-28 정정]`.** 어느 표면에서도
  `0 · 0 · 0`은 그리지 않는다 — "대사가 없었다"는 **없는 사실 주장**이기 때문이다.
  갈리는 것은 그 자리를 **무엇이 대신 말하는가**다.

  | 표면 | 계측 전 | 이유 |
  |---|---|---|
  | 패널 칩 `#aiChip` | **표시하지 않음**(`aiSeen`) | 같은 패널의 상세 행이 `평균 —` · `모델 —`로 말한다 |
  | 폰 HUD 칩 `#aiHudChip` | **`—`** | HUD엔 상세 행이 **없다** — 말할 자리가 칩뿐이다 |

  🔴 원래 이 자리는 「**문안 없음이 확정본**」 한 줄이었는데, 구현은 이미 HUD에 `—`를 찍고
  있었다(`index.html` 정적 마크업 + `main.ts` 주석이 §4.4의 「계측 전이면 `—`」로 정당화).
  **한 조항이 두 표면을 다르게 판정하던 것**을 표로 갈랐다 — 코드가 옳고 문서가 반쪽이었다.
- **패널은 기본 «접힘»이다(2026-08-26 확정).** 로드맵 §7.7이 원래 정한 값(«칩은 기본 노출, **상세만 접힘**»)이고
  구현이 `<details open>`으로 벗어나 있던 것을 되돌린 것이다.
  ④ §4가 요구하는 두 표면은 **접혀도 남는다**:
  | 요구 | 자리 | 접힘에도 보이는가 |
  |---|---|---|
  | 요구사항 3 · 칩 | `<summary>` 안 `#aiChip` | ✅ |
  | 요구사항 5 · `AI 대사 일시 폴백` | `<summary>` 안 **`#aiWarn`** | ✅ |
  접히는 것은 **평균·모델·사유별 건수**(진단 상세)와 기호 범례뿐이며 클릭 한 번으로 열린다.
  - ⚠️ **경고는 `#aiDetail`에 두면 안 된다.** 상세 안에 있으면 접힌 순간 사용자에게 도달하지 못하고,
    남는 신호는 칩 테두리 색 하나뿐이 된다 — §4.1이 「테두리 색**과** 문장 한 줄」로 정한 것을 반쪽만 지키는 것이다.
  - 🖼 **접힌 패널은 «머리글 + 빈 본문»으로 보이면 안 된다 `[2026-08-28]`.**
    예약된 높이는 **첫 계측 전에는 한 줄 summary**만 채운다 → `<summary>`의 구분선 아래에
    **빈 상자**가 남았다(실측 1440×900: panel 65 · summary **36** · 빈 공간 **27px**).
    접힌 동안에는 ① **구분선을 지우고** ② summary에 `flex: 1`을 줘 **예약된 높이를
    머리글이 통째로 쓰게** 한다 — 그러면 «눌러서 펼치는» 타깃이 36 → **64px**이 된다.
    ⚠️ `justify-content: center`로 하면 **글자만 옮기고 상자는 36px에 머물러** 타깃이
    줄고 자리까지 옮겨진다(초안이 그랬다 · 검수 실측).
    높이를 줄이지 **않는** 이유는 리플로우다 — 예약을 없애면 첫 계측 도착 시 컬럼이
    38 → 66으로 **28px** 흔들리고 그 아래가 📜 기록/알림(`flex:1 1 0`)이다.
  - ⚠️ **접힌 패널은 축소 대상에서 뺀다**(`.rp-ai:has(> details:not([open])) { flex: 0 0 auto }` + `min-height`).
    `.rp-panel`은 `min-height:0` + `overflow:hidden`이라, 컬럼이 빡빡해지면 접힌 패널이 **자기 summary보다
    작아지고 칩이 잘린다** — 관측 표면이 조용히 사라진다(검수 실측: 1024×560에서 칩 가시 **0%**). 펼친 기본값은 데스크톱 우측 컬럼의 **122px**를 «평균 —· 모델 —»(계측 전 기본값)로
  채웠고, 그 자리는 📜 기록/알림이 반증·계략·소환을 전달할 폭이었다 — 화면 게이트 S8이 그 통로의
  **보장 대역폭을 0줄**로 판정했다(`docs/plans/done/28-right-column-log-share.md`).
  «숨기지 않는다»(§1.4)는 **대사 경로 배지(✨/♻/⚙)를 로그에서 감추지 않는다**는 뜻이고, 배지는 그대로다.

---

### 4.5 제안 기록표 (`#sugPanel` / `.sg-row`) `[완료 2026-08-29]`
근거: 로드맵 §1.2는 이 패널을 **인지부하** 항목으로 분류한다 — 새 정보를 주지 않고,
로그에 흩어진 제안·반증을 한 행으로 모으는 것이 전부다. 그러면 **행 안에서 무엇이 먼저 읽히는가**가
이 패널의 전부이기도 하다.

🔴 **30회차까지 위계가 뒤집혀 있었다** (게이트 pageProbe 실측 · 이 화면 첫 계측):

| 조각 | 이전 | 이후 | 왜 |
|---|---|---|---|
| 조합(용의자·장물·장소) | 16px · **4.96:1** · 2줄째 | **15px 600** · **11.45:1** · **1줄째** | 추리가 실제로 쓰는 값 |
| 제안자 `🔍 이름` | 16px · **11.45:1** · 1줄째 | 12px · 8.49:1 · 2줄째 | 누가 물었나 = 메타데이터 |
| 반증자 `🛡 이름` | 12px · 8.49:1 | 그대로(2줄째) | 〃 |
| `❗ 미반증` | 12px 글자 · **4.08:1 (AA 미달)** | 채운 배지 · 흰 글자 · **7.32:1** | 판에서 가장 값진 사건 |

- **행 구조는 2줄이다.** 1줄 = `순번` + `조합`, 2줄 = `「나」` + `🔍 제안자` + `🛡 반증자`/`❗ 미반증`.
  DOM 순서가 곧 읽는 순서다 — 스크린리더도 **무엇을 물었나**를 먼저 듣는다.
- **「나」 배지는 제안자 이름 바로 앞**에 둔다. 예전에는 순번과 이름 사이에 떠 있어
  자리만으로는 무엇을 수식하는지 알 수 없었다.
- **채널을 둘로 가른다.** 왼쪽 3px 테두리 = **결과**(기본 `line-normal` 3.43 · 미반증
  `label-strong` 11.45) · 배경 + 「나」 배지 + inset 링(4.38) = **소유**.
  🔴 초안은 미반증을 `primary-strong`으로 뒀다 — 각각의 배경 대비는 통과하는데
  **두 상태의 상호 대비가 1.68:1**이라 «채널»이라 부를 수 없었다(회색조로 동일 ·
  둘 다 따뜻한 갈색이라 색각 이상에서 더 나쁘다). `label-strong`이면 **상호 3.33:1**.
  **각 색이 배경에 대해 통과한다는 것과 두 상태가 서로 구별된다는 것은 다른 명제다.**
  🔴 이전에는 `.sg-row`와 `.sg-row.sg-mine`의 `border-left-color`가 **같은 값**이라
  그 테두리가 아무 신호도 나르지 않았다(override가 no-op).
- **조합에는 아이콘이 붙는다** — 용의자는 얼굴(고유색 링) · 장물은 이모지 · 장소는 `📍`.
  §4.1의 «색 단독 금지»가 여기서도 성립하려면 이 아이콘이 2차 단서다.
  🔴 이 아이콘은 **처음부터 죽어 있었다**: 서버가 `label()`을 씌워 **한글 라벨**을 보냈고
  클라가 그 문자열에 `faceIc()`/`cardIcon()`을 걸었다 — `FACE_IDS.has("토끼 낭자")`는 늘 거짓이다.
  → 서버가 **카드 키**를 보내고 표시는 받는 쪽이 만든다(`SuggestEntry` 주석도 함께 고쳤다).
- **접힘 규칙**: 아이콘↔이름, 이름↔`·`는 `&nbsp;`로 묶고 `word-break: keep-all`.
  좁은 컬럼(220px까지 드래그된다)에서 «대/청마루»로 끊기던 것을 항목 단위 접힘으로 바꾼다.
  ⚠️ **이름 «안»의 공백도 NBSP로 묶어야 한다** — `keep-all`은 공백에서의 줄바꿈을 막지
  않으므로, 그것이 없으면 「고양이 / 밤손님」으로 쪼개진다(현 카드셋에서는 아직 발화하지
  않지만 이름이 한 글자만 길어져도 무너진다).
- 기호는 §2 고정본을 그대로 쓴다(`🔍` 제안 · `🛡` 반증 · `❗` 미반증 · `📍` 장소).
  🔴 **기호만으로는 역할이 안 읽힌다.** 스크린리더가 `🔍`/`🛡`을 「돋보기」/「방패」로 읽어
  한 행에서 **같은 이름이 «용의자 카드»와 «반증자»로 두 번** 나오는데 구분어가 없었다.
  → 결과 화면이 쓰는 `.ec-sr`(시각적으로만 감추는 낱말)로 `제안`·`반증`·`결과`를 싣는다.
- **행 높이 — 아이콘을 살리면 조합이 접힌다.** 16px에서는 기본 컬럼(조합 폭 218px)에서
  전형 조합도 2줄이 돼 행이 **55 → 74px**, 즉 «모아 보이는 양»이 약 28% 줄었다.
  이 패널의 존재 이유가 §1.2 «모아 보게 한다»이므로 그것은 회귀다.
  → **15px**로 내려 전형 조합을 한 줄에 넣었다: 행 **51px**(이 회차 이전 55px보다도 짧다).
  최장 조합(`고양이 밤손님 · 잔치 선물 · 대청마루`)은 여전히 2줄 **72px**.
- **입력 검증은 서버에서.** `handleSuggest`가 `msg.suspect`/`msg.weapon`의 **카드 집합 소속**을
  검사한다. 이전에는 클라이언트가 보낸 임의 문자열이 `suggestLog`에 저장돼 전원에게
  브로드캐스트됐다(재접속·관전 리플레이에도 남는다). 손패 검증이 아니라 **어휘 검증**이다 —
  §7.5.2의 «자기 손패도 지목 허용»은 그대로다.

**남은 미측정** — `❗ 미반증` 행은 **게이트에 한 번도 안 뜬다**. 화면 게이트의 좌석 사슬은
6건을 만들지만 그 6건이 전부 반증된다(실측 미반증 0행). AA 미달 4.08:1이 오래 살아남은 이유가
그것이다. `scripts/gate.config.mjs`의 `rightColumn.knownUnmeasured`에 사유를 적어 둔다.

---

## 5. 액션 버튼 4종 — 상태별 문안

### 5.0 구현 전제 (문안보다 먼저 고쳐야 하는 것)
- **`disabled` 속성이 붙은 버튼은 `title` 툴팁이 뜨지 않고 클릭 이벤트도 발생하지 않는다.** 현재 비활성 사유 안내(`addLog("방 안에서만…")`)가 실행되지 않는 것은 이 때문이다.
- 따라서 비활성은 `disabled` 대신 **`aria-disabled="true"` + `.is-off` 클래스**로 표현하고, 클릭 핸들러는 살려 둔 채 첫 줄에서 사유를 안내하고 `return` 한다.
- **같은 문장을 툴팁과 클릭 피드백에 공용한다.** 문안을 두 벌 만들지 않는다.
- 클릭 피드백 출력처는 **로그가 아니라 턴 배너 부제(`.ti-sub`)** `[선행]` — 사용자의 시선은 버튼 근처에 있고 로그는 우측 끝이다. 로그에도 남기려면 동일 문장을 그대로.

### 5.1 [추리하기] `#suggest`
| 우선순위 | 조건 (판정식) | 문안 (툴팁 = 클릭 피드백) |
|---|---|---|
| 1 | `state.phase !== "playing"` | `잔치가 시작되면 추리할 수 있어요.` |
| 2 | `me.eliminated` | `탈락한 뒤에는 추리할 수 없어요. 반증만 이어집니다.` |
| 3 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 4 | `!me.room` | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` |
| 5 `[선행]` | 같은 방 재제안 차단 도입 시 | `이 방에서는 이미 추리했어요. 다른 방으로 옮기세요.` |
| — | **활성** | `이 방에서 용의자와 훔친 것을 지목해요. 장소는 현재 방으로 고정.` |

> 판정 순서를 지킬 것. `탈락 + 내 턴 아님`이 동시 성립할 때 `내 차례가 아니에요`를 보여주면 사용자는 차례를 기다린다.

### 5.2 [신고하기] `#accuse`
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` | `잔치가 시작되면 신고할 수 있어요.` |
| 2 | `me.eliminated` | `이미 신고에 실패했어요. 남은 역할은 반증입니다.` |
| 3 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| — | **활성** | `정답 3장을 지목해요. 틀리면 즉시 탈락합니다.` |
| — | 추리 직후 활성 `[선행]` | `지금 신고할 수 있어요. 넘기려면 [내 턴 종료].` |

### 5.3 [지름길로 가기] `#passage`
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` 또는 `me.eliminated` | **행위 중립 공용 문안** — `잔치가 시작되면 쓸 수 있어요.` / `탈락한 뒤에는 쓸 수 없어요. 반증만 이어집니다.` |
| 2 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 3 | `!me.room` | `지름길은 방 안에서만 쓸 수 있어요.` |
| 4 | `!passageOf(me.room)` | `이 방에는 지름길이 없어요. (지름길은 3쌍)` |
| — | **활성** | `지름길 — 서재 → 정지(부엌). 주사위 없이 즉시.` |

- 활성 툴팁은 **목적지 이름을 넣어 동적 생성**한다: `` `지름길 — ${label(me.room)} → ${label(dest)}. 주사위 없이 즉시.` `` — R3(화살표)로 조사 없음.

### 5.4 [다가가기] `#bonus`
> 07-30 기획 다이어트(다희): «엿보기»(정보열람) 효과 제거, **이동 보너스만** 남김. 라벨도 `[엿보기]→[다가가기]`.
| 우선순위 | 조건 | 문안 |
|---|---|---|
| 1 | `phase !== "playing"` 또는 `me.eliminated` | **행위 중립 공용 문안** — `잔치가 시작되면 쓸 수 있어요.` / `탈락한 뒤에는 쓸 수 없어요. 반증만 이어집니다.` |
| 2 | `currentTurn !== me` | `지금은 내 차례가 아니에요. 차례가 오면 켜집니다.` |
| 3 | 인접 헬퍼 있으나 `used` `[선행]` | `이 NPC에겐 이미 다가갔어요. 다른 NPC를 찾아보세요.` |
| 4 | 인접 헬퍼 없음 | `다가갈 이가 근처에 없어요. 보드 가장자리의 NPC 곁으로.` |
| — | **활성** | `다가가기 — 근처 NPC에게 다가가 이동 보너스. NPC마다 1회.` |

> `[주의]` 현재 코드의 활성 판정은 `!h.used && 인접`을 한 번에 검사하므로 **3번과 4번을 구분하지 못한다**(이미 쓴 NPC 옆에 서면 "근처에 없어요"가 뜬다). 두 조건을 분리해야 이 문안이 성립한다.
> `[주의]` `CLAUDE.md`는 계략을 **"판당 1회"**로 규정하지만 코드는 `helper.used`로 **토큰(NPC)마다 1회**다. 문안은 코드 실동작(`NPC마다 1회`)을 따르고, 규약 문구 정정은 로드맵 소관.

### 5.5 [내 턴 종료] `#endTurn` (4종 외 · 참고)
- 활성 툴팁: `이번 차례를 마치고 다음 사람에게 넘겨요.`
- 이동 남았을 때 `[선행]`: `아직 ${n}칸 남았어요. 그래도 넘길까요?` → 확인 없이 즉시 종료(되돌릴 수 없으므로 툴팁으로만 경고)

---

## 6. 제안 모달 / 신고 모달

공통 규칙
- **제목은 무엇을 고르는지 말한다.** `제안`(금지 — 명사만) → `제안 — 이 방에서 누가, 무엇을?`
- **확인 버튼 라벨에 동사를 넣는다.** `확인`(금지) → `이 조합으로 제안` / `신고한다`.
- 취소 버튼은 `취소` `[있음]` 유지.
- 셀렉트 옵션 라벨: `이모지 + 라벨` (`🐍 뱀 무녀`). 장소는 `📍` 접두.

### 6.1 제안 모달
```
제안 — 이 방에서 누가, 무엇을?
──────────────────────────────
 장소     📍 대청마루   현재 방 · 고정
 용의자   [ 🐍 뱀 무녀            ▾ ]
 훔친 것  [ 🍶 술동이             ▾ ]
──────────────────────────────
 지목한 인물과 물건이 이 방으로 소환돼요.

                  [ 취소 ]  [ 이 조합으로 제안 ]
```
| 요소 | 문안 |
|---|---|
| 제목 | `제안 — 이 방에서 누가, 무엇을?` |
| **장소 고정행** `[선행]` | 라벨 `장소` / 값 `📍 ${label(me.room)}` / 배지 `현재 방 · 고정` |
| 장소 행 툴팁 | `제안 장소는 지금 서 있는 방으로 정해져요.` (22자) |
| 안내 줄 | `지목한 인물과 물건이 이 방으로 소환돼요.` (21자) |
| 확인 버튼 | `이 조합으로 제안` (8자) |
| 내 패 옵션 접미 `[선행]` | `— 내 패` (선택 **가능**. 반증자를 통제하는 정석 전술이므로 제안에서는 막지 않는다) |

> 현재 `openPicker(..., needRoom=false)`라 장소 행이 아예 없다. 고정행은 **읽기 전용 표시**이며 서버로 보내는 값(`room: ""`)은 바꾸지 않는다.

### 6.2 신고 모달
```
신고 — 진범을 지목하라
──────────────────────────────
 ⚠ 틀리면 즉시 탈락합니다. 그 뒤로는 반증만 할 수 있어요.
──────────────────────────────
 용의자   [ 🐍 뱀 무녀            ▾ ]
 훔친 것  [ 🍶 술동이             ▾ ]
 장소     [ 📍 서재               ▾ ]
──────────────────────────────
 이 3장이 정답 봉투와 모두 같아야 승리해요.

                  [ 취소 ]  [ 신고한다 ]
```
| 요소 | 문안 |
|---|---|
| 제목 | `신고 — 진범을 지목하라` (사극 어감이지만 **명령형 관용구**라 허용. 본문은 해요체) |
| 위험 경고 (상단 고정) | `⚠ 틀리면 즉시 탈락합니다. 그 뒤로는 반증만 할 수 있어요.` (33자) |
| 하단 안내 | `이 3장이 정답 봉투와 모두 같아야 승리해요.` (23자) |
| 확인 버튼 | `신고한다` (4자) — 위험 스타일(`--red` 계열 배경) |
| 내 패 옵션 접미 `[있음]` | `— 내 패 (정답 아님)` · `disabled` **유지** (자멸 방지) |

- 경고는 **모달을 열자마자 보이는 위치**에 둔다. 확인 버튼 옆 작은 글씨는 이미 결정한 뒤라 읽지 않는다.
- 2차 확인 다이얼로그는 만들지 않는다 — 경고 + 동사 버튼으로 충분하고, 턴 시간이 유한하다.

---

## 7. 결과 화면 · 관전 배너

### 7.0 선행 조건
- 현재 `updateEndState()`는 **승자 이름만 보고 한 문장**을 쓴다(`사건이 종결되었습니다. 정답은 기록(우측)을 확인하세요.`). 6종 분기를 위해 `winner === 나 / 나의 eliminated / 승리 사유(고발 성공 vs 최후 생존) / winner === ""(무승부)`가 필요하다.
- **정답 봉투 문자열이 클라이언트에 없다.** 07-28 기준 로그로 브로드캐스트되는 경로는 **고발 성공**과 **무승부**(`endInDraw()`) 둘뿐이고, **최후 생존 종료에서는 정답이 어디에도 공개되지 않는다.** `[선행]` `state.solutionText`(종료 시에만 채움) 또는 `broadcast("solution", {...})` 필요 — 오버레이 6종이 전부 정답 봉투를 화면에 실으려면 이 전달이 선행이다.
- 정답 표기(공용): `` `정답 봉투 — ${emoji(s)} ${label(s)} · ${emoji(w)} ${label(w)} · 📍 ${label(r)}` `` → R2/R4로 조사 없음.
  `[2026-08-26]` 이 **한 줄 형식을 쓰는 곳은 이제 서버 로그뿐**이다(`clue-room.ts`). 결과 화면은 **3장 조판**으로 간다 → §7.1.1.

### 7.1 결과 오버레이 6종

> **2026-07-28 구현 정정 — 4번(신고 실패)의 우선순위.**
> 이 절은 «내가 탈락 상태의 무승부 → 4번 우선»만 적었으나, 문자 그대로 구현하면
> 실측 발생률 **0.0%**인 무승부에서만 뜨는 **죽은 문안**이 된다.
> 같은 절이 «4번의 대조는 패배 화면의 유일한 정보 가치 — 생략하지 말 것»이라 못박고 있으므로,
> 코드는 4번을 **탈락 상태 전반의 최상위**로 올렸다. 그 해석을 여기 확정한다.
> 최종 판정 순서: **4 신고 실패 → 5 무승부 → 3/3′ 최후 생존 → 1/2 신고 성공.**
> 3′·2는 **좌석 없는 관전자**가 보는 화면이다.
>
> 또 최후 생존 종료 경로에 `📜 정답 봉투 — …` 로그 1줄이 추가됐다(§8.1 표에 반영 필요 시 같은 문자열).

> **2026-08-26 구조 변경 — 봉투·내 지목은 «부제»가 아니라 «3장 조판»이다(§7.1.1).**
> 아래 «부제» 칸은 이제 **산문만** 담는다. 3장은 `#endCards`가 렌더한다.

| # | 조건 | 제목 (`#endTitle`) | 부제 (`#endSub`) — 산문만 | 3장 조판 (`#endCards`) | 버튼 |
|---|---|---|---|---|---|
| 1 | 내가 신고 성공 | `🎉 사건 해결!` | *(없음 — `:empty`로 자리도 사라진다)* | 정답 봉투 | `[다시 하기]` `[메인으로]` |
| 2 | 남이 먼저 해결 | `🔍 사건 종결` | `🐭 생쥐 서생 님이 먼저 맞혔어요.` | 정답 봉투 | 동일 |
| 3 | 내가 최후 생존 | `🏅 최후의 1인` | `모두 오답으로 탈락했어요.` | 정답 봉투 | 동일 |
| 3′ | 남이 최후 생존 | `🏅 판 종료` | `🐭 생쥐 서생 님만 남았어요.` | 정답 봉투 | 동일 |
| 4 | 내가 오답 탈락 후 종료 | `❌ 신고 실패` | *(없음)* | **내 지목**(대조 표시) + 정답 봉투 | 동일 |
| 5 | **무승부**(제안 상한 도달 · `winner === ""`) | `🏳 무승부` | `제안 75회에 이르도록 아무도 해결하지 못했어요.` | 정답 봉투 | 동일 |

### 7.1.1 3장 조판 (`#endCards`) `[2026-08-26 신설]`
```
              내 지목
   [🦎 게코 도령] [🍜 잡채] [📍 사랑채]      ← 틀린 장: 붉은 틴트 + 취소선
              정답 봉투
   [🐰 토끼 낭자] [💰 금고] [📍 정지(부엌)]   ← 중립 틴트
```
**장 상태는 셋이다**: `중립`(봉투 줄 — 대조 대상이 없다) · `맞음`(초록 틴트) · `틀림`(붉은 틴트 + 취소선).
«내 지목» 줄만 맞음/틀림을 갖는다 — 3장 다 맞으면 애초에 이 화면(4번 «신고 실패»)이 아니다.
- **왜 바꿨나.** §7.1이 4번의 «내 지목 ↔ 정답 봉투» 대조를 **«패배 화면의 유일한 정보 가치…
  생략하지 말 것»**이라고 못 박았는데, 한 줄 산문이라 **어느 장이 틀렸는지가 안 보였다.**
  폰 실측에서 **3행으로 접히고 구분자 `/`가 줄머리에 떨어져** 오타처럼 읽혔다.
  게임의 전부가 3장인데 그 3장이 쉼표로 이어진 글이었다.
- **맞음/틀림은 색만으로 말하지 않는다** — 틀린 장에 **취소선**을 함께 긋는다(CVD).
- **대조는 진실값 생성이 아니다**(§1.4) — 서버가 준 두 값(`endInfo.mine` ↔ `endInfo`)의
  **동일성 비교**일 뿐이고 승패는 이미 서버가 정했다.
- 봉투가 아직 안 왔으면 조판이 통째로 비고 `.end-cards:empty`가 자리를 지운다 — **지어내지 않는다.**
- 대비(실측): 중립 카드 글자 **11.58** · 맞음 **11.82** · 틀림 **11.17** ·
  테두리 vs 모달 4.14 / 5.15 / 5.98 · 라벨 5.97. 전부 새 색 없이 기존 토큰 조합이다.

**5(무승부) 확정 근거**
- 판정식은 `phase === "ended" && winner === ""` — 서버 `endInDraw()`가 만드는 유일한 상태다(`SUGGEST_CAP` 75회 도달). 실측 발생률 0.0%지만 **코드에 살아 있는 분기이므로 문안 없이 두지 않는다**(문안이 없으면 승자 칸이 빈 채로 `게임 종료`가 뜬다).
- 제목에 느낌표를 붙이지 않는다 — 허용된 느낌표 2종(구 3종 — 미반증에서 `!`가 빠졌다)(승리·최후 생존·미반증)에 무승부는 없다. 제목 5자(상한 16자).
- 부제 1문장은 **왜 끝났는지**를 말한다. `75`는 서버 상수(`SUGGEST_CAP`)를 넣어 만들며 `회에`는 받침 무관(R5)이라 조사 문제가 없다. 2행 허용.
- **내가 이미 탈락한 상태에서 무승부로 끝나면 4번이 우선한다.** 나에게 일어난 일(신고 실패)이 판 전체의 결과보다 구체적이고, 4번만이 `내 지목 / 정답 봉투` 대조를 보여준다. 이때 부제 뒤에 `· 무승부로 종료`를 덧붙이지 않는다 — 3행이 된다.
- 승자가 없으므로 **승리 연출(`setOutcome`)은 호출하지 않는다** — 현재 코드가 이미 그렇다(문안 없음이 확정).

- **부제 44자 상한을 넘는 2·3·4·5는 2행 허용**(`.end-sub`가 `margin` 기준 2행까지 안정). 3행이 되면 정답 봉투만 남기고 앞 문장을 잘라낼 것.
- 4번의 `내 지목 / 정답 봉투` 대조는 **패배 화면의 유일한 정보 가치**다. 생략하지 말 것.
- `사건이 종결되었습니다. 정답은 기록(우측)을 확인하세요.` 는 **폐기.** 클라이맥스에서 사용자를 로그로 보내는 문장이다.
- `[선행]` 리매치 합의 도입 시 버튼 라벨은 §9.4로 대체.

### 7.2 관전 배너 (`#spectateBar`) — 도달 상태 **2종**

> **정정 (2026-07-29) — 이 절은 `#spectateBar`를 «진행 중 탈락» 전용으로만 규정했다.**
> 실제로는 **좌석 없는 관전자**(진행 중 입장 · 재접속 창을 넘겨 초대 코드로 재참가)도 **같은 배너**에 도달한다.
> 그런데 표시 판정이 `players.get(나)?.eliminated` 하나였고 관전자에게는 그 값이 `undefined`라
> **배너가 한 번도 뜨지 않았다.** 손패도 턴도 없는 화면을 아무 설명 없이 보게 되어 **"판을 잃었다"로 읽힌다**
> (커밋 `40f0b6e` — 판정을 `탈락 || 좌석 없음`으로 넓혔다).
> **문안은 새로 짓지 않았다** — §8.1 행 268의 서버 안내와 **글자 그대로 같은 문자열**을 재사용한다.
> §8.1 표 말미의 규약(«같은 사유를 두 문장으로 배우게 하지 않는다»)을 **서버 로그 ↔ HUD 배너**로 확장한 것이다.

| 요소 | 현재 `[있음]` | **교체안 / 확정** |
|---|---|---|
| 본문 ① **진행 중 탈락** | `❌ 탈락 · 관전 중` | `❌ 신고 실패 · 관전 중 — 내 손패로 반증은 계속돼요.` (28자) |
| 본문 ② **좌석 없는 관전** `[있음]` | (표시 자체가 없었다) | `이미 진행 중인 판이에요. 관전으로 들어갑니다.` — **§8.1 행 268과 같은 문자열** |
| 버튼 | `[메인으로]` | `[나가기]` (같은 위치, **확인 절차를 거친다**) |

- **DOM은 배너 하나 · 문장만 갈아 끼운다**(`#specText`). 상태가 둘이라고 배너를 둘로 늘리면 문안 소스가 갈라진다 — 소스는 이 표 하나다.
- ②는 서버가 입장 순간 보내는 **로그 1줄과 같은 문장**이다. 로그는 곧 위로 밀려 올라가므로 배너가 그 문장을 **상시로 붙들어 둔다** — 중복이 아니라 **지속**이다.
- 아래 [나가기] 확인 문안 2종은 **좌석이 있는 사람(①)** 기준이다. ②는 좌석도 손패도 없어(`state.players`에 아예 없다) 나가도 판이 오염되지 않으므로 **그 사유가 성립하지 않는다.**

**[나가기] 확인 문안** `[선행]` (선행: 확인 다이얼로그 DOM)

- 대리 NPC 인계(8.1) **도입 전** — 이탈이 실제로 판을 오염시키는 현재 상태:
  ```
  지금 나가면 내 손패가 판에서 빠져요.
  남은 사람들이 반증할 수 없는 카드가 생겨 추리가 어긋납니다.
                       [ 계속 관전 ]  [ 그래도 나가기 ]
  ```
- 대리 NPC 인계 **도입 후**:
  ```
  지금 나가면 내 자리는 대리 NPC(💤)가 이어받아요.
  같은 탭으로 돌아오면 자리를 돌려받습니다.
                       [ 계속 관전 ]  [ 나가기 ]
  ```
- 기본 포커스는 **`[계속 관전]`**. 파괴적 동작이 기본값이 되어서는 안 된다.

---

## 8. 서버 로그 문안 교체표

### 8.1 `clue-room.ts`
`[조사]` = 조사 오류가 실제로 출력되는 문장 · `[톤]` = 어휘·톤 불일치

| 줄 | 현재 | **교체안** | 사유 |
|---|---|---|---|
| 697 `[조사]` | `${label(suspect)}가 ${label(room)}(으)로 불려왔습니다.` | `🔔 소환 — ${label(suspect)} → ${label(room)}` | R1·R3. `(으)로`가 화면에 그대로 출력 |
| 406 `[조사]` | `${label(value)}는 이미 선택되었습니다.` | `이미 선택된 캐릭터예요 — ${label(value)}` | R1. 변수를 문미로 |
| 383 | `${player.name} 님이 ${label(nextRoom)}에 들어갔습니다.` | `🚪 진입 — ${player.name} → ${label(nextRoom)}` | R3. 48자 상한·스캔 속도 |
| 165 | `🚪 ${player.name} 님이 비밀 통로로 ${label(dest)}에 이동! (제안 또는 턴 종료)` | `🚪 비밀 통로 — ${player.name} → ${label(dest)} · 제안 또는 턴 종료` | 58자 → 42자 |
| 214 | `🃏 ${player.name} 님이 ${label(helper.value)}의 계략(엿보기 ${n}·이동 +${refund}) 사용` | `🃏 계략 — ${player.name} · ${label(helper.value)}에게서 엿보기 ${n} · 이동 +${refund}` | `에게서`는 받침 무관(R5) |
| 585 `[톤]` | `게임 시작! 정답 봉투 봉인. NPC ${botCount}명 합류. ${first?.name} 님의 턴.` | `🎊 잔치 시작 — 정답 봉투 봉인 · NPC ${botCount}명 합류` + 별도 줄 `⏳ ${first?.name} 님의 턴` | §2 `잔치 시작`. 한 줄에 3정보는 스캔 불가 |
| 681 `[톤]` | `🔍 [제안] ${name} — 도둑: … · 훔친 것: … · 장소: …` | `🔍 제안 — ${name} · 용의자: … · 훔친 것: … · 장소: …` | §2: 제안 단계는 `용의자`. 대괄호 제거 |
| 724 | `🛡 ${other?.name} 님이 반증했습니다.` | `🛡 반증 — ${other?.name} 님` | 표기 일관·짧게 |
| 733 | `❗ 아무도 반증하지 못함 — 정답 후보!` | **`❗ 아무도 반증하지 못함 — 다른 손패엔 이 3장이 한 장도 없음`** (2026-08-26 개정) | 「정답 후보」는 **추론**이고 참·거짓이 보는 사람의 지식에 달렸다 — 자기 손패 지목·공통 단서에서 거짓. 전원 브로드캐스트에 추론을 실을 수 없다(§문체 «로그=사실 통보») |
| 784 | `🎉 ${player.name} 사건 해결! 정답: … · … · …` | `🎉 사건 해결 — ${player.name} 님` + 별도 줄 `📜 정답 봉투 — … · … · 📍 …` | 결과 오버레이(§7)와 같은 문자열 재사용 |
| 792 | `❌ [고발 실패] ${player.name} 탈락(반증만 가능).` | `❌ 신고 실패 — ${player.name} 님 탈락 · 반증만 가능` | 대괄호 제거 |
| 1003 | `🎉 ${w?.name} 최후 생존 — 승리!` | `🎉 최후 생존 — ${w?.name} 님 승리!` | 변수 뒤 조사 없음 유지, 어순만 |
| `endInDraw()` `[있음]` | `🏳 무승부 — 제안 ${SUGGEST_CAP}회 도달 · 판을 종료합니다` | **확정 — 그대로 유지** (07-28 임시 문구를 검토 후 확정) | 접두 1개(§2 `🏳`) · 26자 · 조사 없음(`회` 고정 접미) · 승자 줄과 같은 `kind:"win"` |
| `endInDraw()` `[있음]` | `📜 정답 봉투 — … · … · 📍 …` | **유지** | 784와 동일한 2줄 구성(결과 + 정답 봉투). 오버레이 §7.1 5번과 같은 문자열 |
| 1014 | `${np?.name} 님의 턴입니다.` | `⏳ ${np?.name} 님의 턴` | 접두 통일 |
| 547 | `📢 공통 단서 공개(정답 아님): …` | `📢 공통 단서 — 모두 공개 · 정답 아님: …` | §4.2 |
| 295 | `${player.name} 입장.` | `🎎 입장 — ${player.name} 님` | §9.1(대기실 라인으로도 재사용) |
| 323 | `${player.name} 퇴장.` | `🚪 퇴장 — ${player.name} 님` | 동상 |
| 306 | `${player?.name ?? "누군가"} 연결 끊김 — 재접속 대기…` | `📡 연결 끊김 — ${name} 님 · 재접속 대기` | — |
| 312 | `${back?.name ?? "플레이어"} 재접속!` | `🙋 재접속 — ${name} 님` | 느낌표 3종 규칙 |
| 434 | `🔄 ${p?.name} 님이 다시 하기 — 새 판을 시작합니다.` | `🔄 다시 하기 — ${p?.name} 님 · 새 판을 시작합니다` | — |
| 151·176·745·803 | `당신의 턴이 아닙니다.` | `지금은 내 차례가 아니에요.` | §5와 **같은 문장**(당신→내) |
| 156 | `이 방엔 비밀 통로가 없습니다.` | `이 방에는 지름길이 없어요. (지름길은 3쌍)` | §5.3과 동일 |
| 189 | `가까이에 계략을 줄 이가 없습니다.` | `엿볼 수 있는 이가 근처에 없어요. 보드 가장자리의 NPC 곁으로.` | §5.4와 동일 |
| 749 | `방 안에서만 제안할 수 있습니다.` | `방 안에서만 추리할 수 있어요. 입구(🚪)로 들어가세요.` | §5.1과 동일 |
| 417 | `방장만 잔치를 시작할 수 있습니다.` | `방장만 잔치를 시작할 수 있어요.` | 해요체(사용자 대상 안내) |
| 268 | `이미 진행 중인 게임입니다 (관전).` | `이미 진행 중인 판이에요. 관전으로 들어갑니다.` **확정·적용됨** | ~~관전 좌석 미구현 = 도달 불가 죽은 코드~~ → **07-28 관전 좌석 구현으로 도달 가능**(`lock()` → `setPrivate/setListed`). **07-29부터 §7.2 관전 배너가 이 문자열을 그대로 재사용**한다 — 문안을 고치려면 **두 곳이 같이 움직인다** |

> **`client.send("log")` 로 가는 개인 안내(151·156·176·189·745·749·803·417)는 §5의 버튼 문안과 글자 그대로 같아야 한다.** 같은 사유를 두 문장으로 배우게 하지 않는다.

### 8.2 `narrator.ts` 폴백 템플릿 (사극체 — R6 예외지만 회피가 가능한 곳은 회피)

**확정본 = 코드 `fallbackLine()` 현행**(2026-07-28 교체). 아래 표가 단일 소스이며, 문안을 바꾸려면 이 표를 먼저 고친다.

| 액션 | 예산 | 템플릿(랜덤 1택) |
|---|---|---|
| **제안** | 25자 | `${room}의 ${suspect}, 「${weapon}」` · `${suspect}, ${room}의 「${weapon}」` · `${room}에서 ${suspect}, ${weapon}` · `${suspect}, 자네 ${room}엔 왜 갔는가` |
| **제안(반증당함)** | 25자 | `${suspect}, 「${weapon}」… 아니군` · `${room}의 「${weapon}」${josa(weapon,"이","가")} 아니라니` · `${suspect}도 ${weapon}도 아니군` |
| **고발** | 40자 | `도둑은 ${suspect}! ${room}의 「${weapon}」` · `${suspect}, 「${weapon}」, ${room} — 끝일세` · `더 볼 것 없다. ${suspect}, ${weapon}, ${room}` |
| **계략(귓속말)** | 40자 | `쉿… ${hint}, 자네만 알게` · `가까이 오게. ${hint} — 못 들은 걸로` · `은밀히 이르네. ${hint}` · `${hint}… 내 입에서 난 말은 아닐세` |

**추임새 부착 규칙 — `fit()`**: 추임새(`cards.ts` `VOICE`의 `intro`/`outro`, 예 `쯧쯧, ` / ` 마땅히 그러하렷다.`)는 **인물을 식별하는 유일한 표식**이라 "길면 통째로 버린다"가 아니라 **예산 안에서 들어가는 만큼** 붙인다. 순서는 `intro+본문+outro` → `본문+outro` → `intro+본문` → `본문`. 마지막 폴백에서도 `LINE_MAX`(40)는 지킨다.
- outro를 intro보다 먼저 살리는 것은 **인물 고유 문구라 색이 더 진하기** 때문이다.
- 그래서 본문 템플릿은 **짧게 쓸수록 캐릭터색이 더 붙는다.** 새 템플릿을 추가할 때 이 방향을 뒤집지 말 것.

**조사 처리**
- 회피가 원칙이다: `의`·`에서`·`엔`·`도`는 받침 무관(R5), 낫표 「」로 목적격을 대체(R4), 나열은 쉼표.
- 단 한 곳 `${room}의 「${weapon}」${josa(weapon,"이","가")} 아니라니`만 **종성 헬퍼를 쓴다** — NPC 대사는 R6 예외이고, 여기서 조사를 빼면 문장이 성립하지 않는다.
- `${weapon}이라니`류(받침 없는 말 뒤에도 허용되는 이형)는 현행 템플릿에서 사라졌다.

> **정정 노트 (2026-07-28) — 왜 07-27 확정본을 버렸나**
> ① 07-27 확정본(`${room} 쪽을 살피니 「${weapon}」, 그게 사라졌던걸`, `이건 필시 ${suspect}의 소행! ${room}에서 훔친 것은 「${weapon}」` 등)은 **조사 오류는 고쳤지만 길이를 보지 않았다.** 실측 폴백 제안 대사 **평균 47.1자(최대 71)** — 제출물 ④ §2.3 길이 규약을 **71~79% 위반**(로드맵 §7.5 2차 실측).
> ② 길이는 미관 문제가 아니라 **페이싱**이다. `bubbleLifeMs = len×TYPE_MS(55) + BUBBLE_HOLD_MS`가 대사 길이에 비례하므로, 47자 대사는 봇 1턴을 그대로 늘려 **판 길이를 지배**했다.
> ③ 그래서 **규약 쪽을 살리고 문안을 교체**했다. 코드가 예산을 상수로 강제하고(`LINE_BUDGET_SUGGEST` 25 · `LINE_MAX` 40) 이 문서가 그 예산 안의 문안을 확정한다 — §1.4의 확정 권한은 그대로다.
> ④ 교체 후 실측(`scripts/eval-narrator.mjs` 폴백 **전수 112,752문장**): 제안 평균 21.6자(최대 25) · 제안(반증) 21.0 · 고발 37.1 · 계략 36.5 — **위반 0건.** 추임새 부착률 84.5~100%(캐릭터색은 유지됐다).
> ⑤ 반증당한 뒤 템플릿에서 3요소를 다시 읊지 않는 것은 길이 레버다 — 제안 내용은 이미 로그 한 줄에 다 있고, 여기서 필요한 것은 "지웠다"는 반응이다(8~10자 절약).

### 8.3 `main.ts` 클라이언트 로그
| 현재 | **교체안** | 사유 |
|---|---|---|
| `🔎 ${by} 님이 "${label(card)}" 단서로 반증 (나만 봄)` | `🛡 나에게 반증 — ${by} 님 · 「${label(card)}」 (나만 봄)` | 접두 `🛡` 통일, 「」 |
| `🔎 아무도 반증하지 못함 — 정답 후보!` | **삭제 ✅(2026-08-26 반영)** | 서버 733이 이미 전체 브로드캐스트 → **제안자만 같은 줄을 2번 본다**. 확정 후에도 `main.ts`에 남아 있었다 |
| `🎉 ${player} 정답!` / `❌ ${player} 오답` | **삭제** | 서버 784/792와 중복 |
| `잔치 시작! 이동: 방향키, 방에 들어가 [제안]` | `🎮 이동 — 방향키 · 방에 들어가 [제안]` | 서버 585 `잔치 시작`과 중복 |
| `💬 ${from}: ${text}` | **유지** | §1.1 사극체 경계 표식 |
| `🃏 ${from}의 계략 — 엿본 카드: … (정답 아님·나만 봄)` | `🃏 엿본 카드 — ${from}에게서: … (정답 아님 · 나만 봄)` | `에게서`(R5) |
| `에러(${code}): ${message}` | `문제가 생겼어요 (${code}). 새로고침하면 이어서 할 수 있어요.` | §1.3 |
| `서버(ws://localhost:2567)에 연결하지 못했습니다. 서버가 켜져 있나요? → \`pnpm dev\`` | 사용자용: `서버에 연결하지 못했어요. 잠시 뒤 다시 시도해 주세요.` / 개발 힌트는 `console.warn`으로 분리 | **배포 빌드에서 localhost 안내가 심사자에게 노출**된다 |

### 8.4 랜딩 문안 (대비 수정 대상 포함)

> **2026-08-27 구조 — 게임이 하나뿐이면 선택 UI를 접는다.**
> 랜딩 첫 화면이 신규 유입의 첫인상인데, **선택지가 하나인 셀렉터**가 ~180px를 쓰면서
> 바로 위 `<h1>`과 **제목·이모지를 글자 그대로 반복**하고 있었다(캡처 실측).
> 고를 것이 없는 자리를 «고르는 UI»로 그리면 제품이 아니라 **설정 폼**으로 읽힌다.
> - 마크업은 **지우지 않는다** — 콘텐츠 교체가 이 저장소의 설계 목표다(`CLAUDE.md`).
>   `#gameSelect > .game-item`이 **하나일 때만** 접는다(`:has(:only-child)` · JS 0바이트 —
>   초기 로드 JS는 §9.1 B1 래칫 예산이고 지금 red다).
>   ⚠️ 되살아나는 것은 **선택 UI의 «껍데기»뿐이다** — `.game-item`엔 `role`·핸들러가 없고
>   방 생성 프로토콜에 게임 식별자 필드 자체가 없다. **동작은 게임 #2를 넣을 때 붙여야 한다.**
> - **자식 결합자(`>`)여야 한다** — 자손이면 항목이 래퍼에 감싸이는 순간 각각이 `:only-child`가 되어
>   **게임이 둘인데도 접힌 채**로 남는다(검수 실측).
> - 접힌 동안 소개 한 줄은 `#gameMeta`가 진다. 문안은 `.gi-sub`와 **글자 그대로 같다** —
>   같은 사실을 두 문장으로 만들지 않는다(§8.1 규약과 같은 취지).
> - `:has()` 미지원 브라우저는 **둘 다 보인다** — 중복이지 정보 손실이 아니다.
>   이것이 성립하는 이유는 **`:not()`이 forgiving이 아니어서** 두 규칙이 **함께** 죽기 때문이다.
>   `:is(:has(…))`로 썼다면 폴백이 뒤집혀 `#gameMeta`가 영구히 숨는다.

| 위치 | 확정 문안 |
|---|---|
| `<h1>` | `🐯 호랑이 생신 잔치` |
| `.subtitle` | `대감집 손님들 중 잔치 선물을 훔친 자를 추리하라` |
| `#gameMeta` / `.gi-sub` | `클루형 추리 · 추천 2~6인 · 1인도 가능(빈자리는 NPC)` — **두 자리가 같은 문자열** |

| 위치 | 현재 | **교체안** |
|---|---|---|
| `.room-empty` | `열린 공개방이 없어요. 방을 만들어보세요.` | `열린 공개방이 없어요. [방 만들기]로 첫 판을 열어보세요.` |
| `.room-empty` (로딩) | `불러오는 중…` | `공개방을 찾는 중…` |
| `.room-empty` (실패) | `목록을 불러오지 못했어요.` | `목록을 불러오지 못했어요. [↻ 새로고침]을 눌러보세요.` |
| 공개방 참여 버튼 | `참여` / `만석` | `참가` / `만석` (§2 통일) |
| `.hint-sm` | `캐릭터는 대기실에서 고릅니다. 공개방은 목록에 노출됩니다.` | `캐릭터는 대기실에서 골라요. 공개방은 목록에 보여집니다.` |
| 참가 실패 | `없는 방이거나 참가할 수 없어요. 코드를 확인하거나 새 방을 만드세요. (…)` | `없는 방이거나 이미 시작한 판이에요. 코드를 확인해 주세요.` (원문 오류는 콘솔로) |
| 목록 항목 제목 `[조사]` | `${방장}님의 방` · 이름이 없으면 `대기 중님의 방` | 이름 있음 → `${방장} 님의 방` (R1: `님` 앞에 한 칸) · **이름 없음 → `방장 대기 중`** |
| 공개방 참가 진행 `[있음]` | `참여하는 중…` | `참가하는 중…` (§2 통일 — 초대 코드 참가와 **글자 그대로 같은 문자열**) |
| 공개방 참가 실패 `[있음]` | `참여 실패: (원문)` | 위 «참가 실패» 행과 **같은 문장**. 경로가 둘이라고 문장을 둘로 만들지 않는다 |
| 초대 진입 | `초대 링크로 들어왔어요. [참가] 후 대기실에서 캐릭터를 고르세요.` | **유지** |

> `[조사]` **«대기 중님의 방»은 실제로 화면에 나갔다**(07-29 캡처 확인). 폴백 값 `"대기 중"`이 이름 자리에 들어간 뒤 `님의 방`이 그대로 이어 붙었기 때문이다.
> **이름 자리에 상태를 넣지 않는다** — 이름이 없으면 문장 구조를 바꾼다(R2의 취지). 방장 이름은 `label(suspect)`이라 좌석이 하나라도 차면 항상 존재하고, 빈 값은 «좌석 예약만 잡히고 아무도 앉지 않은 순간»에만 나온다.

> `.room-empty`·`.hint-sm`·`.divider`의 색 `#6b6355`(대비 2.9:1)는 **문안이 아니라 색 문제**다 — 로드맵 소관이나, 이 문서의 문장은 그 색이 고쳐진 뒤에야 읽힌다. (07-28 `#8f8574`로 교체 완료 · 07-29 화면 게이트 S6이 회귀를 막는다.)

### 8.5 공개방 목록 항목 — 상태 배지 `[완료]`

**왜 필요한가.** 판이 끝난 방은 **목록에 되돌아온다**(`setListed(true)` — 설계대로다).
그런데 거기 들어가면 리매치 전까지 **관전**이다. 버튼이 `참가`라고만 적혀 있으면
**«참가한다»고 읽고 들어가 관전 화면을 본다.** 심사자가 두 명이면 실제로 밟는 동선이다.

**선행 조건은 풀렸다**(07-29). `clue-room.ts`의 `syncMeta()`가 `{hostName, count, humans, isPublic, phase}`를
싣고, 클라 `PublicRoomMeta`가 그 필드와 1:1로 맞춰졌다. 아래 문안은 `loadPublicRooms()`에 **적용 완료**다.
⚠️ `phase`가 없으면 배지를 **아예 그리지 않는다** — 없는 값을 기본값으로 메우면 추정이 된다.
⚠️ **클라가 추정하지 않는다.** `count > clients`(NPC 충원 흔적) 같은 간접 신호로 상태를 만들어내면
서버가 말한 적 없는 것을 화면이 단언하게 된다 — §1.4 진실값 경계 위반이다.
목록에 «진행 중»은 **원리적으로 나타나지 않는다**(판이 도는 동안 목록에서 빠진다). 그래서 상태는 **2종**이다.

| 방 상태(서버 `metadata.phase`) | 배지 | 버튼 | 부가 |
|---|---|---|---|
| `lobby` — 아직 시작 전 | `대기 중` | `참가` | `${clients}/${maxClients}인` (현행 유지) |
| `ended` — 판이 끝나 목록에 돌아옴 | `종료됨` | `관전` | `다시 하기를 기다리는 중` |
| ~~(정원 초과)~~ | — | ~~`만석`(비활성)~~ | — | **도달 불가 — 아래** |

⚠️ **`만석`은 목록에 원리적으로 나타나지 않는다**(07-29 실측, 화면 게이트 S7). Colyseus
`getAvailableRooms()`는 `{locked:false, private:false}`인 방만 돌려주는데, 좌석이 차는 순간
`_incrementClientCount()`가 방을 **잠근다**. 즉 정원이 찬 방은 **목록에서 사라진다** —
목록 안에서 «만석»으로 보이는 상태가 존재하지 않는다. 클라의 `full` 분기는 **도달 불가 코드**다.
목록이 비어 있는 것이 곧 이 사실의 실측이라, 게이트는 이 행을 **FAIL이 아니라 SKIP**으로 사유와 함께 인쇄한다.

- 배지는 **텍스트 + CSS 뱃지**다. 이모지를 늘리지 않는다(§2 접두 기호는 확정 목록이 전부다).
- `관전`은 §2 «관전 상태» 표기와 같은 낱말이다. 들어간 뒤 보게 되는 배너(§7.2 본문 ②)와 낱말이 이어진다.
- 버튼 라벨은 **들어가면 무엇이 되는지**를 말한다(상태의 되풀이가 아니다) — 배지가 «지금», 버튼이 «다음»이다.
- 조사 회피(R2): 부가 문구는 `다시 하기를 기다리는 중`처럼 **변수 없는 고정 문장**으로 둔다.

---

## 9. 멀티플레이 문안 (§9.0은 **적용 완료** · 나머지 `[선행]`)

### 9.0 대기실 캐릭터 그리드 — 카드가 자기 상태를 말한다 `[완료 2026-08-27]`
| 상태 | 카드 안 낱말 | 테두리 | 면 | 이름 대비 | 낱말 대비 |
|---|---|---|---|---|---|
| 선택 가능 | *(줄 없음)* | 실선 `line-normal` | `bg-normal` | 9.78:1 | — |
| **선택됨** | **`내 캐릭터`** (`primary-strong`) | 실선 `primary-normal` | `bg-alternative` | **11.58:1** | **5.84:1** |
| **사용중** | **`사용중`** (`label-alternative`) | **점선** | `bg-normal` | 9.78:1 | **5.72:1** |

- **범례가 없다.** 카드가 스스로 말하므로 사용자가 배울 기호가 **0개**다.
- 🔴 **이전 범례 `(선택됨 🟡 / 사용중 🔒)`는 화면에 없는 기호를 설명했다** — 그리드는
  `.selected`/`.locked` **클래스만** 붙였고 두 기호를 **한 번도 그리지 않았다.**
- 🔴 **`opacity: 0.35`는 화면 게이트가 원리적으로 못 보는 대비 저하였다.**
  S6는 `getComputedStyle` 기반이라 «`#4A3B2A` on `#FBF3E7` = **9.78:1**»을 읽는다 —
  실제 합성 렌더는 **1.87:1**이다(직접 계산). ⚠️ **다만 S6가 이 칸을 실제로 잰 적은 없다** —
  게이트 대기실 캡처는 참가자 1명이라 `.locked` 칸이 **생기지 않는다**. 「쟀다면 통과시켰을 값」이다.
- **기호 배지를 쓰지 않은 이유**: 모서리 배지는 **360·320px에서 초상을 덮는다**
  (실측 중심거리 16.0 / 12.5px < 초상 반지름 17px). 390px에서도 여유가 2px뿐이라 설계가 아니라 우연이다.
- **색 단독 신호를 쓰지 않은 이유**: 그것이 바로 이전 «선택됨»의 상태였다(면 대비 **1.14:1**) — 1.4.1 위반.
- **`grayscale`을 쓰지 않은 이유**: 이 격자는 «게임 전에 십이지 색을 학습시키는 자리»인데
  (`renderLobbyChars` 주석) **잠긴 칸이 곧 같이 플레이할 사람**이라 색이 가장 필요하다.
  게다가 `filter`도 S6가 못 보는 축이다 — **투명도 함정을 지우면서 같은 계열을 새로 만들지 않는다.**
- **낱말은 게이트가 잰다** — S5(글자 크기 10px 하한) · S6(대비). 사각지대를 만들지 않는다.
- ⚠️ **빈 줄을 예약하지 않는다.** 12칸 전부에 두면 카드가 **36px 자란다.** 격자는
  **행 단위로** 높이를 맞추므로 상태가 있는 행만 그만큼 자란다.
  (당시 이유는 «자란 카드가 배경음 버튼과 겹친다»였다 — 그 제약은 **§1.7에서 해소됐다.**
  남은 이유는 «쓸 데 없이 긴 카드»뿐이고, 그것만으로도 예약하지 않는 편이 맞다.)
- 보조 기술: 잠긴 칸에 `aria-disabled="true"`. 상태는 **카드 안 텍스트**로 이미 있으므로
  `title`에 중복해 넣지 않는다(넣었더니 이름이 두 번 읽혔다).
- ✅ **도감 캐러셀도 해소(2026-08-27 · §1.6).** `.cx-thumb` 15칸이 `<button>`이 됐고
  도감 focusable이 **1 → 16개**가 됐다.
- ✅ **키보드로 고를 수 있다(2026-08-27 해소).** `.char`는 이제 실제 **`<button type="button">`**이다 —
  포커스·Enter/Space·역할이 전부 따라온다(전엔 `onclick`만 있는 `div`라 **focusable 0개**, WCAG 2.1.1 A 위반).
  잠긴 칸은 `disabled`가 아니라 **`aria-disabled`**라 **포커스에 남는다** — 그래야 «이미 나갔다»를 읽을 수 있다.
  선택된 칸은 `aria-current="true"`. 호버 미리보기의 **키보드 짝**으로 `focus`에서도 직업 풀이가 뜬다.

### 9.0.1 참가자 수 · 빈 좌석 `[완료 2026-08-27]`
**수와 설명을 청중으로 가른다** — 수는 모두에게, 설명은 **시작을 결정하는 방장에게만**.

| 자리 | 확정 문안 | 대상 |
|---|---|---|
| 라벨 | `참가자 (${count}/${MAX_PLAYERS})` + 빈 자리가 있으면 ` · 빈 자리 ${n}` | 전원 |
| 안내(방장) | `빈 자리는 NPC가 대신해요. 바로 시작할 수 있어요.` | 방장 |
| 안내(그 외) | `방장이 시작하기를 기다리는 중…` — **유지** | 비방장 |

- **라벨은 수만 말한다.** R1대로 변수 뒤에 조사가 없고, 짧아서 **320px에서도 안 접힌다**
  (실측 최악 143.5px < 가용 238px). 설명까지 넣으면 한글 1.0em 서체에서 **최악 260px > 238px**로
  2줄이 된다. ~~«2줄이면 카드가 자라 `[복사]`가 배경음 버튼과 겹친다»~~ — **그 제약은 §1.7에서
  해소됐다.** 라벨을 한 줄로 두는 이유는 이제 **읽기**다(수를 세는 줄이 접히면 훑기가 안 된다).
- 🔴 **`곳`은 좌석의 단위가 아니다** — 초안이 R1의 «받침 고정 접미어»를 기계적으로 만족시키려고
  `빈 자리 ${n}곳은`이라고 썼는데, `곳`은 **장소**를 세는 말이라 `자리`와 **의미가 겹친다**
  («빈 자리 다섯 장소»). R1 예시 목록(`님·편·쪽·측·것·방`)에도 없다.
  → **조사를 아예 없앴다**(R1의 첫 처방). 설명이 다른 문장으로 갔으므로 가능해졌다.
- 🔴 **`최대 6인`도 하드코딩이었다** — 마크업의 `6`만 걷어내고 이 문장의 `6`은 남겨 두면
  `MAX_PLAYERS`를 바꿀 때 **라벨만 따라가고 안내는 안 따라간다.** 문구에서 총원을 뺐다.
- 🔴 **같은 사실을 두 문장으로 말하고 있었다** — 라벨의 «NPC가 대신해요»와 방장 안내의
  «NPC로 채워집니다»가 한 화면에 같이 떴다(§8.1 규약 위반). 청중으로 가르면서 해소.
- 라벨에 `aria-live="polite"` — 사람이 들어와 수가 바뀌면 **읽힌다**(§9.1이 지적한 «대기실 무음»의 일부).
- 라벨에 `word-break: keep-all` — 한국어는 아무 음절에서나 끊긴다.
- ⚠️ **목록에 «빈 좌석 행»을 그리지 않는다.** 두 안을 캡처로 보고 **둘 다 버렸다**:
  ① 좌석마다 한 줄 → **같은 문장 5줄**로 카드가 훌쩍 길어졌다(정보가 아니라 소음) ·
  ② 요약 한 줄 → 라벨의 `· 빈 자리 5`와 **같은 사실을 두 문장으로** 말한다(§8.1).
  ~~«카드가 ~40px 자라 S1 FAIL»~~ — 그 사유는 **§1.7에서 해소됐다.** ①의 사유는 그대로 산다.

### 9.1 대기실 서버 피드백
**선행 조건**: `#lobby` 안에 피드백 라인(`#lobbyMsg`)이 없다. 현재 `addLog`는 `#gameScreen` 안쪽 `#log`에 쓰므로 **대기실 단계 서버 메시지가 전부 무음으로 사라진다.**

| 이벤트 | 문안 | 표시 |
|---|---|---|
| 입장 | `🎎 토끼 낭자 님 입장 (3/6)` | `#lobbyMsg` 3초 |
| 퇴장 | `🚪 토끼 낭자 님 퇴장 (2/6)` | 3초 |
| 캐릭터 중복 거부 | `이미 선택된 캐릭터예요 — 🐰 토끼 낭자` | 3초 · 해당 셀 흔들림 |
| 방장 전용 거부 | `방장만 잔치를 시작할 수 있어요.` | 3초 |
| 방장 위임 | `👑 방장이 바뀌었어요 — 🐭 생쥐 서생 님` | 3초 |
| 연결 끊김 | `📡 연결 끊김 — 토끼 낭자 님 · 재접속 대기` | 유지 |
| 재접속 | `🙋 재접속 — 토끼 낭자 님` | 3초 |

- 시작 버튼 동적 라벨: `잔치 시작 (사람 3 + NPC 3)` — 빈자리 충원 결과를 **누르기 전에** 보여준다.
- 방장 힌트 `[있음]` `빈 자리는 NPC로 채워집니다 (최대 6인). 바로 시작할 수 있어요.` → `빈 자리는 NPC로 채워요 (최대 6인). 혼자서도 바로 시작할 수 있어요.`
- 비방장 힌트 `[있음]` `방장이 시작하기를 기다리는 중…` → **유지**
- 시작 카운트다운: `잔치 시작 3…` → `2…` → `1…` (버튼 라벨 교체, 3자)

### 9.2 닉네임 입력
**선행 조건**: `Player.name`은 `label(suspect)`뿐 — **닉네임 필드가 스키마에 없다.**

| 요소 | 문안 |
|---|---|
| 필드 라벨 | `닉네임` |
| placeholder | `8자까지 · 비워도 괜찮아요` |
| 비움 안내 | `이름 없이 시작하면 캐릭터 이름으로 표시돼요.` |
| 길이 초과 | `닉네임은 8자까지예요.` |
| 중복 | `같은 이름이 있어 뒤에 숫자를 붙였어요 — 안벨로퍼2` |
| 표시 형식 | `안벨로퍼 (🐰 토끼 낭자)` — 목록·턴 원형 / 로그·말풍선은 `🐰 토끼 낭자` 유지 |

> `[주의]` 닉네임은 사용자 입력이고 `renderTurnStrip`·`openTurnCircle`·`showCharInfo`가 `innerHTML`을 쓴다 → **서버 새니타이즈 + 클라 이스케이프 필수**(XSS). 문안 적용 전 선행.

### 9.3 이탈 · 대리 NPC 표기
| 상황 | 문안 |
|---|---|
| 이탈 → 대리 인계 | `💤 대리 시작 — 토끼 낭자 님 이탈 · NPC가 자리를 이어받았어요 (손패 유지)` |
| 복귀 | `🙋 복귀 — 토끼 낭자 님이 자리를 돌려받았어요` |
| 목록·턴 원형 배지 | `💤 대리 중` / `🤖 NPC` |
| 범례 (턴 원형 하단) | `🤖 NPC · 💤 자리를 비운 사람` |

- **`손패 유지`가 핵심 정보다.** 이 표기가 없으면 남은 사람은 "저 카드는 이제 반증 안 되나?"를 알 수 없다.

### 9.4 리매치 합의
| 상태 | 버튼 라벨 | 로그 |
|---|---|---|
| 미동의 (사람 2명 이상) | `다시 하기 (1/3)` | — |
| 내가 누른 뒤 | `동의함 (2/3) · 대기 중` (버튼 비활성) | `🔄 다시 하기에 동의 — 토끼 낭자 님 (2/3)` |
| 성립 | — | `🔄 과반 동의 — 새 판을 시작합니다` |
| 사람 1명(솔로) | `다시 하기` (카운트 숨김) | 기존 434 문안 |
| 시리즈 표기 `[선행]` | 결과 부제 앞에 `제3판 · 통산 2:1` |

### 9.5 턴 타이머 경고
| 시점 | 문안 | 위치 |
|---|---|---|
| 내 턴 진행 중 | `⏳ 남은 시간 32초` | 턴 배너 부제 `.ti-sub` |
| 10초 이하 | `⏳ 10초 — 시간이 지나면 자동으로 넘어가요` | 부제 (강조색) |
| 만료 | `⌛ 시간 초과 — 다음 사람에게 넘어갔어요` | 로그 |
| 이탈자 대기 | `⏳ 접속 대기 8초 — 토끼 낭자 님` | 로그 |
| 제안 후 신고 대기 `[선행]` | `⏳ 지금 신고할 수 있어요 (30초). 넘기려면 [내 턴 종료]` | 부제 |
| 솔로(사람 1명) | **타이머 문안 전부 표시하지 않음** | — |

---

## 10. 적용 위치 표

| § | 문안 묶음 | 파일 | 함수 / 요소 |
|---|---|---|---|
| 1.7 | 부유 컨트롤 배치 `[완료]` | `apps/client/index.html` | `.bgm-ctrl` — 폰·`pointer:coarse` 두 블록의 `top`을 `body.no-scroll`로 한정 |
| 3 | 목표 카드 `[선행]` | `apps/client/index.html` · `apps/client/src/main.ts` | 신규 `#goalCard` 오버레이 · `enterGame()` 말미, `.hud-ctrl` 끝 `[?]` |
| 4.1 | 손패 라벨 | `apps/client/src/main.ts` | `r.onMessage("hand", …)` (≈212) · `index.html` `#hand` 초기 텍스트 |
| 4.2 | 공통 단서 | `apps/server/src/rooms/clue-room.ts` (≈546) · `main.ts` | `startGame()` · `buildEvidence()` |
| 4.3 | 증거 노트 툴팁 `[있음]` | `apps/client/src/main.ts` | `buildEvidence()` — 잠금 칩 `chip.title` — **07-29 정합 확인** |
| 4.4 | AI 칩 · 대사 경로 배지 | `apps/client/src/main.ts` | `renderAi()` (≈216) · `addLog()`의 배지 분기 (≈170) — 폴백 사유 한글화 |
| 4.5 | 제안 기록표 행 위계 `[완료]` | `apps/client/index.html` · `main.ts` · `apps/server/src/rooms/clue-room.ts` · `packages/shared/.../types.ts` | `.sg-head`/`.sg-meta`/`.sg-res.none`/`.sg-unrefuted` · `sugRow()` · `recordSuggestEntry()` — 라벨 대신 **카드 키** 전송 · `handleSuggest()` 어휘 검증 |
| 5.0 | `disabled` → `aria-disabled` 전환 | `apps/client/src/main.ts` · `index.html` | `updateTurnInfo()` (≈436) · `button:disabled` CSS 옆에 `.is-off` 추가 |
| 5.1–5.5 | 버튼 4종 + 턴 종료 문안 | `apps/client/src/main.ts` | `updateTurnInfo()` 에서 `el.title` 설정 · 각 `onclick` 첫 분기 |
| 5.0 | 클릭 피드백 출력처 `[선행]` | `apps/client/index.html` · `main.ts` | `.hud-turn` 안에 `.ti-sub` 추가 · `updateTurnInfo()` |
| 6.1 | 제안 모달 (장소 고정행) | `apps/client/src/main.ts` | `openPicker()` (≈102) — 읽기전용 행 분기 추가 |
| 6.2 | 신고 모달 (경고) | `apps/client/src/main.ts` | `openPicker()` · `#accuse` `onclick` |
| 7.0 | 정답 봉투 전달 `[선행]` | `apps/server/src/rooms/clue-room.ts` | `doAccusation()` (≈768) · `advanceTurn()` 최후 생존 분기 (≈998) |
| 7.1 | 결과 6종(무승부 포함) | `apps/client/src/main.ts` | `updateEndState()` (≈1377) |
| 7.2 | 관전 배너 본문 2종 `[있음]` | `apps/client/index.html` · `apps/client/src/main.ts` | `#spectateBar` 안 `#specText` · `updateEndState()` (표시 판정 `탈락 \|\| 좌석 없음`) |
| 7.2 | 나가기 확인 `[선행]` | `apps/client/index.html` · `main.ts` | `#specHome` `onclick` (현재 `exitToMain` 직결) |
| 8.1 | 서버 로그 25항목 + 무승부 2줄 | `apps/server/src/rooms/clue-room.ts` | 표의 줄 번호 · `endInDraw()` (≈1272) |
| 8.2 | 폴백 대사 `[있음]` | `apps/server/src/ai/narrator.ts` | `fallbackLine()` · `fit()` — **코드가 확정본과 일치** |
| 8.3 | 클라 로그 · 중복 제거 | `apps/client/src/main.ts` | `wireRoom()` 의 `onMessage` 5개 · `errMsg()` · `enterGame()` 말미 |
| 8.4 | 랜딩 `[있음]` | `apps/client/index.html` · `main.ts` | `.hint-sm`·`.room-empty` · `loadPublicRooms()` · `init()` 의 `joinBtn` — **07-29 적용 완료** |
| 8.5 | 공개방 상태 배지 `[완료]` | `apps/server/src/rooms/clue-room.ts` · `network.ts` · `main.ts` · `index.html` | `syncMeta()` 의 `phase` → `PublicRoomMeta` → `loadPublicRooms()` 의 `li` 조립 · `.ri-badge` — **07-29 적용 완료** |
| 9.0 | 대기실 캐릭터 그리드 `[완료]` | `apps/client/index.html` · `main.ts` | `.char.selected`/`.locked`·`.char-state` · `renderLobbyChars()` |
| 9.0.1 | 참가자 수 · 빈 좌석 `[완료]` | `apps/client/index.html` · `main.ts` | `#playerCount`/`#playerMax`/`#seatHint` · `renderLobby()` |
| 9.1 | 대기실 피드백 `[선행]` | `apps/client/index.html` · `main.ts` · `clue-room.ts` | 신규 `#lobbyMsg` · `renderLobby()` · `onJoin`/`removePlayer`/`handleChooseCharacter` |
| 9.2 | 닉네임 `[선행]` | `packages/shared` · `clue-room.ts` · `index.html` | `Player.name` 분리 · 대기실 `.field` 추가 |
| 9.3 | 대리 NPC 표기 `[선행]` | `packages/shared` · 3개 렌더러 | `displayName(p)` 헬퍼 · `renderLobby`/`renderTurnStrip`/`openTurnCircle` |
| 9.4 | 리매치 합의 `[선행]` | `clue-room.ts` · `main.ts` | `handleRematch()` (≈430) · `#endRematch` |
| 9.5 | 턴 타이머 `[선행]` | `clue-room.ts` · `main.ts` | `armTurnClock()` 신설 · `updateTurnInfo()` |

---

## 11. 검수 체크리스트
- [ ] 화면 문장 중 **변수 바로 뒤에 조사**가 오는 곳이 0건인가 (`grep -nE '\$\{[^}]+\}(은|는|이|가|을|를|와|과|\(으\)로|으로|로)' apps/`)
- [ ] 같은 사유가 **툴팁과 클릭 피드백에서 글자 그대로 동일**한가
- [ ] `💬` 없는 줄에 사극체 어휘가 섞이지 않았는가
- [ ] 로그에 **같은 사건이 두 줄**로 뜨지 않는가 (§8.3의 중복 4건)
- [ ] §2 어휘표 밖의 동의어(`장물`·`단서 패`·`참여`·`게임 시작`)가 남아 있지 않은가
- [ ] 버튼 라벨 8자 · 툴팁 40자 상한을 넘긴 문장이 없는가
- [ ] 결과 화면 6종이 **정답 봉투를 화면에서** 보여주는가 (로그로 보내지 않는가)
- [ ] NPC 대사가 **예산(제안 25 · 고발/계략 40)** 안에 있는가 — `node scripts/eval-narrator.mjs` (폴백 전수, 위반 0이어야 함)
- [ ] 화면에 **영문 토큰**이 노출되지 않는가 (`timeout`·`http`·`empty`·`toolong`·`nokey`·`disabled` → §4.4 한글 표기. 예외는 배지 툴팁 1곳)
- [ ] §8.2 표의 템플릿과 `fallbackLine()`이 **문자 그대로 같은가** (문서가 코드를 뒤따라가면 §1.4의 확정 권한이 무효가 된다)
- [ ] 관전 배너 본문 ②(§7.2)와 서버 안내(§8.1 행 268)가 **문자 그대로 같은가** — 한쪽만 고치면 같은 사유가 두 문장이 된다
- [ ] **상태는 있는데 문안이 없는 화면**이 없는가 — 좌석 없는 관전자가 07-28까지 그 상태였다(배너 조건이 `undefined`에 걸리지 않았다)

## 변경 이력
- **2026-08-29**: **§4.5 제안 기록표 신설 — 행의 위계를 정보 가치 순으로 뒤집었다.**
  게이트가 이 화면을 **처음 계측**하고(그 전까지 S8 하나만 돌았다) 나온 실측이 근거다:
  «누가 물었나»가 16px·11.45:1, «무엇을 물었나»가 같은 16px에 4.96:1이었다.
  같은 계측에서 `❗ 미반증`이 **4.08:1로 AA 미달**인 것도 드러났다 — 다만 그 행은
  게이트 시나리오에 한 번도 안 떠서(6건 전부 반증) **여전히 무계측**이다.
  아이콘 3종은 서버가 라벨을 보내는 바람에 **처음부터 죽어 있었고**, 서버가 카드 키를
  보내도록 고쳤다. `border-left-color`의 `.sg-mine` override는 **no-op**이었다.
- **2026-08-28**: **§2에 UI 표식 기호표 신설 — 스크롤 신호에는 기호를 쓰지 않는다.**
  9회차가 우측 컬럼에 깐 «더 있음» 어두운 오버레이가 **잘리는 줄의 AA를 깨고 있었다**
  (합성 실측 **3.44:1**(α=0.30) / **4.10:1**(α=0.15)). 마스크를 대비 근거로 기각해 놓고
  **같은 함정에 다른 문으로** 들어간 것이다 — `::after`는 글자 **위**라 배경과 글자가 같이
  어두워진다. **S6는 합성 전 색을 읽으므로 원리적으로 못 본다.**
  대안으로 머리글 `↓` 표식을 만들었다가 **폐기**했다 — `content` 글리프가 접근성 이름에
  들어가고(`opacity:0`은 접근성 트리에서 안 지운다), `▾`는 접힘 어휘와 충돌한다.
  **결론은 진짜 스크롤바다** — 9회차의 「네이티브는 못 쓴다」가 플랫폼 한계가 아니라
  저장소 자신의 `* { scrollbar-width: thin }` 때문이었다(실측으로 확인).
- **2026-08-27 (4차)**: **§1.6에 «포인터 쪽» 규약 추가 — 모달이 뜨면 밖의 부유 컨트롤을 끈다.**
  `cycleTab`이 키보드는 가둬 놨는데 마우스·터치는 나갈 수 있었다 — 배경음 스피커가
  결과 화면·목표 카드 **위에** 스크림도 안 먹은 채 뜨고 **눌렸다.**
  ⚠️ **z-index로는 못 막는 자리였다** — `#gameScreen`이 `position: fixed`라 스택 컨텍스트를
  만들어 그 안의 z:35·40이 `<body>` 직계 컨트롤을 이길 수 없다. 「몇으로 줄까」가 아니라
  「뜨면 끈다」가 답이다. 판정은 버스의 «포커스 대상 > 0»을 그대로 쓴다.
  회귀는 화면 게이트 쌍 검사가 잡는다(음성 테스트로 FAIL 재현 확인).
  `#turnCircle`은 버스를 안 타 **미해소**로 남았다 — §1.6에 `[주의]`로 명시.
- **2026-08-27 (3차)**: **§1.7 신설 — 부유 컨트롤은 «내용이 지나가는 띠»에 두지 않는다.**
  배경음 버튼 밑으로 `[복사]`가 들어가는 S1 FAIL이 **13·17회차에 각각 다른 이유로** 났고,
  두 번 다 «내용을 줄여» 피한 결과가 이 문서에 **문안 길이 제약**으로 굳어 있었다.
  원인이 매번 다른데 결과가 같으면 원인은 내용이 아니다 — 배치를 고쳤다
  (`top:150px`은 그 값을 필요하게 만든 **턴 배너**가 사는 게임 화면에만 · 카드 화면은
  `position: absolute`로 **문서와 함께** 움직인다).
  🔴 **초안은 «카드 상단이 y≥16px에 고정되니 상한이 내용과 무관»이라 적었다 — 거짓이었다.**
  카드 화면은 문서가 스크롤해서 `fixed` 컨트롤 밑으로 흘러 들어간다(검수가 잡았고 실측으로
  재확인 — `#joinBtn`·`#refreshRooms`가 실제로 안 눌렸다). 그 상한은 **게이트가 재는
  `scrollTop=0`에서만** 참이었다.
  **§9.0·§9.0.1의 「카드가 자라면 겹친다」 사유 3곳을 해제**했다. 남은 사유(소음·읽기·§8.1
  중복)는 그대로 살려 두 절의 결론은 바뀌지 않는다 — **사유만 고쳐 적었다.**
  §10 적용 위치 표에 §1.7 행 추가.
- **2026-08-26 (2)**: **제안 결과 문안을 사실로 되돌렸다.** 「아무도 반증 못 한 조합 = 정답 후보」가
  **두 경우에 거짓**이다 — ① **자기 손패 지목**(반증 루프가 제안자를 건너뛰고, 서버는 이 지목을
  «클루 최강 전술»이라며 의도적으로 허용한다) ② **공통 단서**(솔로 2장 공개 · `deck.shift()`로 딜 전에
  빼므로 누구 손패에도 없다). **솔로가 곧 온보딩 경로**라, ②를 방금 읽은 사람이 첫 제안에서 자기모순에
  부딪쳤다. 로그는 **일어난 일**만 말하고 추론은 각자의 증거 노트에 맡긴다(§문체 «로그=사실 통보»).
  카드 ③도 거짓 추론을 빼고 **소거 규칙**을 가르치게 바꿨다 — ②(내 손패를 지운다) → ③(반증받은 카드를
  지운다)로 카드가 하나의 소거 수업이 된다. 「어떻게 이기는가」는 ⑤(신설 당시 ④)가 맡는다.
  함께: §10 «삭제» 확정분(`🔎 아무도 반증하지 못함`)이 `main.ts`에 남아 제안자가 같은 줄을 두 번 보던
  것을 실제로 지웠다. 로그 배지 `정답후보`(`main.ts`)도 같은 사유로 `미반증`으로 바꿨다 —
  §4.4에 규격이 없던 미명세 문안이었다.
  ⚠️ **초안은 「코드는 규칙을 알고 있었고 틀린 것은 문안뿐이다」라고 적었다 — 거짓이다.**
  같은 거짓 추론이 **봇 고발 판정에 실제로 구현돼 있다**(`clue-room.ts` `holdsAny`가 자기 손패만 보고
  `seenOf`의 공통 단서를 안 본다). 별건으로 큐에 올렸다.
- **2026-08-27 (2차)**: **§9.0.1 신설 — 참가자 수 옆에 «빈 자리 n».**
  수는 전원에게(라벨), 설명은 **방장에게만**(안내) — 같은 사실을 두 문장으로 말하던 것을 청중으로 갈랐다.
  초안의 `빈 자리 n곳은`은 **`곳`이 장소의 단위**라 «자리»와 겹쳐 폐기(R1을 기계적으로만 만족시킨 결과).
  방장 안내의 «최대 6인» 하드코딩도 제거 — `MAX_PLAYERS`가 바뀌면 라벨만 따라갔다.
  §10 적용 위치 표도 함께 갱신.
- **2026-08-27**: **§9.0 신설 — 대기실 캐릭터 카드가 «자기 상태를 낱말로» 말한다.**
  범례 `(선택됨 🟡 / 사용중 🔒)`가 **화면에 없는 기호**를 설명하고 있었고,
  `.char.locked`의 `opacity: 0.35`는 **S6가 원리적으로 못 보는** 대비 저하였다(실렌더 1.87:1).
  기호 배지·색 단독·`grayscale` 모두 쓰지 않는다 — 각각 초상 가림(360px) · 1.4.1 위반 ·
  «색 학습» 목적 훼손 + 새 계측 사각지대라는 이유가 있다. 낱말은 S5·S6가 잰다.
  §9 제목의 «전부 선행»과 §10 적용 위치 표도 함께 갱신.
- **2026-08-26 (3차)**: **«고발» → «신고» 용어 통일 (오너 확정 · 쿼카가 정한 표기).**
  1~6회차 내내 버튼만 `신고하기`이고 모달 제목·확인 버튼·결과 화면은 `고발`이었다.
  §2가 「개념은 고발 — … 유지」로 **의도적 유지**를 못 박았으나 §2의 제목 자체가 **「1개념 = 1표기」**여서
  **표가 자기 제목과 어긋난 상태**였다. 그 조항을 **철회**한다.
  (1~6회차 보고가 근거로 든 「§8.1 — 같은 것을 두 이름으로…」는 **오인용**이었다. §8.1의 실제 조항은
  「같은 **사유**를 두 **문장**으로」이고 다른 명제다. 결론은 그대로지만 근거를 §2로 바로잡는다.)
  교체: 모달 제목 `신고 — 진범을 지목하라` · 확인 `신고한다` · 결과 `❌ 신고 실패` ·
  관전 배너 `❌ 신고 실패 · 관전 중 …` · 서버 로그 `❌ 신고 실패 — … 님 탈락 · 반증만 가능` ·
  NPC 대사 프롬프트 `행동: 신고 —`.
  **내부 식별자(`accuse`·`doAccusation`·`bot-accuse.ts`)와 코드 주석의 «고발»은 그대로 둔다** —
  사용자 문안과 코드 어휘는 다른 층이고, 식별자를 바꾸면 진단 가치 없는 대형 diff만 남는다.
  이 문서의 **설계 서술 산문**에 남은 «고발»(내부 `reason` 값 · 구 문자열 대조표 · 길이 예산 표 ·
  §8.2 폴백 템플릿표의 **액션 이름**)도 같은 이유로 유지한다.
- **2026-08-26 (2차)**: **§3.1 목표 카드 4줄 → 5줄(④ `확신` 신설 · 기존 ④ 끝내기 → ⑤).**
  카드가 **«지우는 법»만 가르치고 «찾는 법»을 안 가르쳤다** — ②③이 둘 다 소거이고 ⑤는 `확신하면`이라
  말하면서 확신의 출처를 말하지 않았다. 서버 규칙이 확정한 명제
  「내가 «정답 아님»으로 본 적 없는 3장을 아무도 반증 못 하면 그게 정답」(플랜 25)을
  사용자 어휘 «안 지운»으로 옮겼다. **`내가 냈는데`**가 필수어 — 반증 루프는 제안자를 건너뛰므로
  소유격 없이는 **남의 제안에 적용되어 거짓이 된다**(§3.1). **`장소까지`**가 둘째 —
  제안의 장소는 고르는 값이 아니라 **서 있는 곳**(`fixedRoom: me.room`)이라 신규 유입이 셋 중 장소만 빠뜨린다.
  초안의 `방까지`는 §2 어휘 통일표 위반(3요소 ③ = `장소`)이라 교체했다.
  §1.5.1 길이표의 «32자 권장 · ③은 44자»는 **실측과 달라 폐기**했다(실측 ③ = 52자 · 폰 1행 한계 ≈ 39자).
- **2026-08-26**: **§3.1 목표 카드 3줄 → 4줄(④ 끝내기 신설).** 카드가 목표·출발점·방법을 가르치면서
  **«어떻게 이기는가»가 없었다** — ①이 "맞히면 승리"라고만 하고 그 선언을 어느 버튼으로 하는지도,
  틀리면 어떻게 되는지도 말하지 않아, 신규 유입이 ①을 읽고 [신고하기]를 누르는 경로가 열려 있었다.
  같은 사고를 **시각**으로 막은 것이 액션 바 3단 위계(`plans/done/22`)이고 ④는 그 **문안 쪽 절반**이다.
  함께: 한도표 「32자」를 **실측 27자**로 정정(폰 390 가용폭 306px 기준 · ②③은 이미 초과) ·
  §3.1 재열람 문안을 구현과 일치시킴(`좌측 상단 [?]` → `[?] 버튼을` — 폰에서 액션 바는 **하단**이라
  「좌측 상단」은 거짓이었다) · 화면 게이트 `#goalCard .goal-lines div` 가드를 `min: 3 → 4`
  (min이라 3으로 두면 ④가 가려져도 통과한다).
- **2026-07-29 (4)**: **§8.5 3행 `만석`을 «도달 불가»로 정정.** 화면 게이트에 S7(공개방 목록 배지)을
  넣으면서 실측으로 드러났다 — Colyseus가 정원이 찬 방을 잠그고 `getAvailableRooms()`가 잠긴 방을
  거르므로, **목록 안에 «만석»인 방이 존재할 수 없다.** 문안을 확정해두고 코드에 붙여둔 채
  «있다»고 믿고 있었다. **게이트를 만들지 않았으면 계속 몰랐을 종류의 오류**다.
  `대기 중`·`종료됨` 2행은 이제 기계가 매 커밋 잰다(S7, 기본 티어 +6.4s).
- **2026-07-29 (3)**: **§8.5 `[선행]` 해제 → `[완료]`.** 서버 `syncMeta()`가 `phase`를 싣게 되면서 선행 조건이 풀렸고,
  클라 `PublicRoomMeta`가 서버 필드와 1:1로 맞춰져 확정 문안 3행이 그대로 붙었다.
  **`phase`가 없으면 배지를 그리지 않는다**는 규칙을 코드와 문안 양쪽에 명시 — 없는 값을 기본값으로 메우는 순간 §1.4의 추정 금지를 어긴다.
  촬영 확인: `대기 중`+`참가`는 실제 화면에서 확인했고, **`종료됨`+`관전`은 아직 화면으로 본 적이 없다**(서버가 `phase:"ended"`로 목록에 복귀하는 것만 e2e로 확인).
- **2026-07-29 (2)**: **§8.4 적용 완료 + §8.5 신설.** §8.4 확정본 6행이 07-27 확정 이후 코드에 붙지 않고 있었다 → `index.html`·`main.ts`에 적용(`참여`→`참가` 포함). 함께 «공개방 참가 진행·실패» 2행을 §8.4에 추가(경로가 둘이라고 문장을 둘로 만들지 않는다). **§8.5는 «종료된 방의 버튼이 실제로는 관전»을 화면에서 말하기 위한 문안**인데, 서버가 `metadata`에 `phase`를 싣지 않아 **클라가 상태를 알 수 없다** → 문안만 확정하고 `[선행]`으로 둔다(추정하지 않는다 — §1.4).
- **2026-07-29**: **§7.2 정정 — 관전 행 추가.** `#spectateBar`를 «진행 중 탈락» 전용으로 규정했으나 **좌석 없는 관전자도 같은 배너에 도달**한다(커밋 `40f0b6e`). 문안은 새로 짓지 않고 **§8.1 행 268의 서버 안내와 글자 그대로 같은 문자열**을 재사용 — 같은 사유를 두 문장으로 배우게 하지 않는다. 함께 §8.1 행 268의 «도달 불가 죽은 코드» 표기를 정정(07-28 관전 좌석 구현으로 도달 가능), §10에 `#specText` 적용 위치 분리, §11에 정합 체크 2건 추가.
- 2026-07-27: 최초 작성. 실코드 실측 기준으로 톤 규칙·조사 회피 6규칙·어휘 통일표 확정, 버튼 4종 상태별 문안·모달 2종·결과 5종·서버 로그 25항목 교체표·멀티 문안 확정.
- **2026-07-28**: ① §8.2 폴백 템플릿을 **현행 코드 기준으로 전면 갱신** + 07-27 확정본을 버린 이유(길이 규약 위반 71~79%)를 정정 노트로 기록. ② 미확정 4건 확정 — 무승부 종료 로그(§8.1)·결과 오버레이 무승부(§7.1 5번)·AI 카운터 칩과 대사 경로 배지(§4.4 신설)·폴백 사유 6종 한글 표기(§4.4). ③ §2에 어휘 3항목·접두 기호 4종(`🏳`·`✨`·`♻`·`⚙`) 추가, §1.5에 대사 길이 예산 3행 추가. ④ §4.3 잠금 칩 툴팁이 코드와 갈라진 사실을 기록(**문서가 옳다** — 코드 수정 대상).
