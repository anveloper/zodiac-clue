# plan: 게임 엔진 / 콘텐츠 분리

status: hold(보류) · created: 2026-07-20 · held: 2026-07-21 · **hold 재확인: 2026-07-29** ·
ref: `docs/design/20260720-engine-and-workflow-plan.md` §1.4 · `docs/design/20260729-mafia-content-design.md` §3·§9·§10.5

> **보류 사유 (2026-07-29 갱신 — 원래 사유는 더 이상 유효하지 않다)**
>
> ~~현재 주제(클루/십이지)로 제출·완성도에 집중. **주제 교체가 확정되면 재개.**~~
> → 🔴 **주제는 교체되지 않기로 사실상 결정됐다**(클루로 08-09 제출 · `20260727-execution-plan`).
> 원래 조건대로면 이 플랜은 **영원히 열리지 않는다.** 조건을 아래로 바꾼다.
>
> **지금의 보류 사유는 「주제가 안 정해져서」가 아니라 「순서」다** — 마피아 설계 §9와 같은 근거다:
> 1. 🔴 **안전망이 없다.** 핵심 과제인 「엔진 코어 추출」(= 설계 §3-⑥ `BaseGameRoom` = §9.2 S1)은
>    재접속·대리 NPC·관전 경로를 **전부 지나는데**, 이 경로들은 타입이 같은 채로 **동작만 틀릴 수 있어**
>    `typecheck`가 잡지 못한다. 그런데 `pnpm verify`의 sim 게이트가 도는 `scripts/sim-balance.mjs`는
>    **정적 import 0건의 완전 자립 미러**라(07-29 재확인) **서버 리팩터에 숫자 하나 반응하지 않는다.**
> 2. 🔒 **일정.** 승패 판정 분리(§9.2 S2·S3)는 **「진실값 변경은 08-01 이후 금지」**(실행계획 폴백 규칙 2)의
>    정면 대상이다. 08-01 전에 밀어 넣으면 촬영을 위협하고(폴백 규칙 1 위반), 후로 미루면 규칙 2에 걸린다.
> 3. **규모는 막는 이유가 아니다.** §9가 처음으로 쟀다 — 신규 ≈2,700~3,900줄 · 이동·수정 ≈700줄 · 4~8 작업일.
>    **막는 것은 크기가 아니라 순서다.**

## 재개 조건 (2026-07-29 신설 — 둘 다 충족돼야 한다)

- [ ] **① 08-08 코드 동결 해제** — 그전까지 진실값(승패·판정) 경로를 건드리지 않는다.
- [ ] **② 클루 서버 리팩터를 검증할 «통합 주행» 확보.** 후보는 둘뿐이다:
  - `scripts/gate-screen.mjs`의 **실판 픽스처**(6인 클루를 결과 화면까지 몰고 가는 `SCREEN.result`)를
    리팩터 **전후로** 돌려 대조 — 사실상 유일한 통합 주행이다(설계 §9.4 R1의 완화책).
  - 또는 `sim-balance.mjs`를 **실코드 공유**로 전환 — ⚠️ 그러면 `gate.baseline.json` 기준선이 무효화된다(설계 §3).
    (마피아 쪽은 §9.5(a)가 「처음부터 실코드 공유」로 뒤집었지만, **클루 미러에는 그 번복이 적용되지 않는다.**)

## goal
주제(클루)가 바뀌어도 재사용되도록 엔진 코어와 GameDefinition을 분리.

## tasks
- [ ] GameDefinition 인터페이스 정의: 구성물(카드/역할/맵)·승리조건·NPC정책(decideTurn)·초기배치·라벨/이모지
      ⚠️ **형태가 미확인이다.** 설계 §9 R3이 Colyseus **스키마 상속을 «회피»**했다(독립 `MafiaState`를 짜기로).
      「정의 하나로 전부 교체」가 **스키마 층에서도 성립하는지는 실험 전까지 모른다**(설계 §3-⑨ · 미확인 그대로).
- [ ] 엔진 코어 추출: 방/턴/상태동기화/NPC스케줄/재접속/딜레이 (게임 상수 0)
      → 대상 좌표는 설계 §3-⑥ · §9.2 S1에 `파일:줄`로 있다. **재개 조건 ② 없이 착수 금지**(위 사유 1).
      ⚠️ 함께 해야 하는 것: 종료 8단계 3벌 복붙 → `endGame()`(§3-⑦) · 행동 자격 술어 `canAct()`(§3-⑥).
      `canAct` 통일의 대상은 **한 모양이 아니라 두 모양**이다(`!== client.sessionId` 3곳 / `!player || … !== player.id` 3곳).
- [x] ~~클루를 GameDefinition 구현체로 이관 (shared cards + clue-room 리팩터)~~ → **절반만 완료**
      - [x] shared 층 분리 `3a132a8`(L0-③): `engine/`(ai·color·view-timing) ↔ `content/clue/`(cards·board·types·view-board)
            · 루트 배럴 3줄 재수출로 **import 경로 변경 0**. ⚠️ `view-consts.ts` **미분류 9상수**는 남았다.
      - [x] 룸 타입 파라미터화 `37a4c4a`(L0-①): `index.ts:58-61` `ROOM_TYPES` 테이블 · `network.ts:15-22` 인자화.
            ⚠️ **`RoomType` 원소 1개 · 인자를 넘기는 호출부 0곳** — 확장점만 열렸다.
      - [x] **미분류 층 소멸** `07-29 늦은 라운드`: `view-consts.ts`의 9상수를 판정해 6개는
            `engine/view-notation.ts`, 3개는 `content/clue/view-board.ts`로 옮기고 **파일을 지웠다.**
            루트 배럴이 **2수출**(`engine` · `content/clue`)이 되어 **「어느 층인지 모르는 파일」이 없어졌다.**
            판정 기준 4개(어휘·근거·값·방향)는 `engine/view-notation.ts` 헤더에 있다.
      - [ ] `clue-room.ts` 리팩터 — **미착수**(위 재개 조건에 걸린다)
      - [ ] `#gameSelect` 배선(`index.html:965`) — **미착수 · 여전히 죽은 훅**(전 저장소 참조 1건)
- [ ] 더미 GameDefinition으로 엔진만 구동되는 스모크 테스트
- [ ] 클라 렌더도 정의 주입식으로(보드/토큰/라벨을 GameDefinition에서)
      ⚠️ 설계 §3이 「`main.ts` 2735줄 분해보다 **새 엔트리를 짜는 게 싸다**」로 판단했다 — **판단이지 실측이 아니다**(비용 미측정).

## done-criteria
- 엔진 코어 어디에도 "클루/십이지" 상수 없음, 게임 교체가 GameDefinition 하나로 가능
- ⚠️ **07-29 실측 보정**: 이 기준은 **지금 0%가 아니라 «부분»**이다. 설계 §1.2가 지목한 오염 11건 중
  **완전 해소 0건 · 부분 해소 3건**(①룸 타입 · ③배럴 층 · ⑤뷰 상수) · 나머지 8건 그대로.
  ⑤는 07-29 늦은 라운드에 **「해소」에 가장 가까운 상태**가 됐다(미분류 층 소멸).
  「부분」인 이유는 셋 다 **자리를 만들었을 뿐 두 번째 주제가 들어오지 않았기** 때문이다.

## 참고 — 이 플랜이 검증하려던 상위 주장의 현재 상태
`CLAUDE.md`·상류 문서 §0의 「엔진은 그대로 재사용한다」는 **마피아 감사가 반증했다**(설계 §0: 「분리는 성립하지 않는다」).
상류 문서 `20260720-engine-and-workflow-plan.md`에 정정이 필요한 서술 **5건**이 설계 §10.4에 정리돼 있다 —
**그 문서 수정은 사람이 정한다**(이 플랜과 설계 문서는 보고만 했다).
