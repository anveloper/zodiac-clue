# plan: 배너를 상단 줄로 되돌린다 — 「측정 충돌」은 내 grep 오독이었다

status: done(2026-08-31) · created: 2026-08-31 · src: 개선 루프 50회차

## goal
플랜 69 큐 1번. 47회차가 「폭 임계값은 문안에 무너진다」며 배너를 **조건 없이** 한 줄 아래로
내렸고, 그 대가로 **겹침이 애초에 없는 넓은 화면(1440)에서도** 배너가 보드 위로 내려앉았다
(47회차 검수가 «회귀»라 불렀다).

## 🔴 49회차가 이 작업을 폐기한 근거는 **내 grep 오독**이었다
「폭 1280~1440 전부에서 교차가 131.3×40으로 동일 → 47회차 기록과 안 맞는다」고 적었는데,
그 131.3은 **1024폭 뷰포트 넷**(`band`·`guard`·`cardGuard`·`tierTop`)이 낸 같은 값이었다.
grep이 뷰포트 헤더와 위반 줄을 따로 찍는데 그것을 «폭 무관»으로 읽었다.

**48회차에 만든 S12로 좌표를 직접 읽어** 갈랐다:
`.hud-ctrl` 폭 **464.219**(오른끝 480.2) · `.hud-turn` 폭 **326.203** · `left` 1024→430 · 1440→638.
→ 겹침 = `480.2 − (W/2 − 163.1)` = 0 at **W = 1286.6** — 47회차의 「761~1286」과 **정확히 일치**.

## 설계 — 이제는 임계값을 써도 안전하다
47회차가 임계값을 안 쓴 이유는 「문안이 늘면 경계가 올라간다」였다. 그 사이
**S1 쌍 `#turnInfo`↔`.hud-ctrl`이 겹침을 직접 재게 됐다** — 경계가 틀려지면 그 쌍이 운다.

- 상한 **1359**(경계 1286.6 위 **73px** · 5.7%). 1360에서 여유 36.7px · 1440에서 76.7px.
- **경계를 양쪽으로** 재게 뷰포트 둘:
  · `bannerTop 1360×800` — 상한이 **올라가는** 회귀
  · `bannerBand 1240×800` — 상한이 **내려가는** 회귀. 처음엔 안 잡혔다(1201~1286에 뷰포트가
    없었다). **1280이면 겹침이 3.3px이라 `minOverlapEdgePx: 4`에 안 걸린다** — 1240은 23.3px.

## tasks
- [x] 좌표를 직접 읽어 「측정 충돌」을 가름
- [x] 상한 1359 · 경계 뷰포트 둘 · S12 규칙 조건 동기화
- [x] 양방향 되주입으로 래칫 확인
- [x] 캡처로 1440이 상단 줄로 돌아온 것을 눈 확인
- [x] 서브에이전트 검수 → 반영(**명시한 근거가 거짓이었고, 잡는다던 방향이 안 잡혔다**)
- [x] `ui-copy` (`.md` + `.html`) — 47회차의 「폭 임계값을 만들지 않는다」를 취소선으로 정정
- [x] 자기시험 `F22` — F를 안 붙인 것이 아래 2번을 못 본 이유였다

## 🔴 검수가 뒤집은 판단
1. **🔴 이번 변경의 «명시된» 근거가 거짓이었다.** 「그 사이 S1 쌍 `#turnInfo`↔`.hud-ctrl`이
   생겨서 안전해졌다」고 코드·설정 세 곳에 적었는데, 그 쌍은 **화면 게이트 최초 커밋**
   (`86efca0` · 07-29)부터 있었다 — 47회차보다 **한 달 앞선다**(검수가 `git log -S`로 잡았다).
   47회차는 **쌍을 가진 채로** 「임계값을 만들지 않는다」를 골랐다.
   → 진짜 하중 부재는 **«발화할 자리»**다: 그전에는 폭 1025~1439 × 높이 ≥501에
     뷰포트가 하나도 없었다(`short 1280×450`은 높이가 폰 분기).
2. **🔴 「상한이 올라가는 회귀를 잡는다」가 거짓이었다** — 상한을 1500으로 올려도 **전건 PASS**다.
   규칙이 경계를 따라가서 침묵한다(이 파일이 `cardGuard` 주석에 **스스로 못 박아 둔 함정**이다).
   → `same-row` 프리미티브(=`below`의 거울상)를 만들어 **「폭 ≥1360에서는 같은 줄에 선다」를
     따로 주장**하게 했다. 되주입해 잡는 것을 확인했다.
3. **🔴 또 F-id를 안 붙였다** — 49회차 검수가 바로 그 규약을 다시 세웠는데 반복했다.
   **F를 붙였으면 2번이 착수 중에 드러났다.** `F22` 추가(뷰포트를 `desktop`으로 골랐다면
   규칙이 안 걸려 `F19`와 같은 실수를 반복했을 것이다).
4. **🟠 「여유 73px · 5.7%」는 한글 3자다** — 바 버튼 1자당 경계가 22.5px 올라간다.
   px으로 적으면 여유가 커 보인다. 그리고 경계가 **1360~1368**이면 겹침이 0~4px이라
   `minOverlapEdgePx`에 안 걸리는 **죽은 띠**가 있다.
5. **🟠 「배너 «문장»의 렌더 폭이 정한다」가 틀렸다** — 배너 326.2의 지배자는
   턴 순서 스트립 `.ti-order`(**300.2**)이고 상태 문장은 234.7이다.
   감시 대상은 문안만이 아니라 `.ti-chip` 패딩·좌석 수·바 버튼 패딩까지다.
6. **🟠 「1201~1286에 뷰포트가 없다」가 틀렸다** — `short 1280×450`이 그 안에 있다.
   안 운 이유는 폭이 아니라 **높이 450이 폰 분기**라서다.
7. **🟠 배너↔컬럼 쌍이 없었다(비대칭)** — 바로 옆 `#bgmCtrl`↔`#rightPanel` 쌍은 사유에
   「컬럼 폭은 220~680px로 드래그된다」까지 적어 뒀는데 배너에는 없었다.
   그리고 **이번 변경이 그 교차를 97 → 179px로 키웠다.** 쌍을 걸었다.
8. **🟠 `game-column` 팔이 죽어 있었다** — 그 화면의 `onlyViewports` 셋이 규칙의 `when`을
   **원리적으로 못 만족한다**(커버리지 착시). 뺐다.
9. **🟠 주석 `W*/2`의 `*/`가 블록 주석을 조기에 닫아** 설정 파일이 깨졌다(구문 검사로 잡았다).

### 검수가 확인해 준 것
(A) grep 오독이 맞다(1024폭 넷이 131.3 · 1280/1440에는 그 수가 한 번도 안 나온다) ·
(B)(C) 좌표와 W=1286.6 재현 · **배너 폭은 상태에 안 변한다**(내 턴/남의 턴·카운트다운 유무 동일 —
`.ti-sub`는 높이만 늘린다) · (F)의 1240 선택은 옳다(1280이면 3.3px이라 안 세진다).

### 아직 안 고친 것
**배너에 hover하면** 이름이 펼쳐져 폭 611.9가 되어 액션 바를 106px 덮는다.
`main`에서는 줄이 달라 교차가 0이었으니 **이번 변경이 되살린 축**이다.
S1은 평상 상태만, S9는 색만 재므로 **사람 몫**이다 → 큐.

## done-criteria
- [x] 화면 게이트 **PASS 485 · FAIL 1**(알려진 `roomlist-ended` — 실행마다 갈린다)
- [x] 자기시험 PASS — `F22`
- [x] 경계 **양방향** 래칫(상향은 `same-row` 규칙 · 하향은 `bannerBand`)
- [x] `.md` ↔ `.html` 일치 · 뷰포트 **16개**

## 다음 회차 후보 (착수 전 **반드시 현재 화면·코드로 재확인**)
1. 🖼 **배너 hover 시 이름이 펼쳐져 액션 바를 106px 덮는다** — 이번 변경이 되살린 축이고
   S1(평상)·S9(색) 어느 쪽도 못 잰다.
2. 🔴 **게이트 도달이 «사람이 쓸 수 있는가»를 안 본다** — `#rpToggle`을 `display: none`으로
   만들어도 `game-column` 도달이 성공한다(`steps`의 `click`에 가시성 확인이 없다).
2. 🖼 **폰에 아직 규칙이 없는 의도들** — 배경음 감추기(저장소가 스스로 «무계측»이라 적어 둔 자리) ·
   `.hud-ai`/`.hud-hand`/`.rp-wresizer` · D-패드↔토글 여유 · 배경음↔명패 여유 ·
   **데크 균등분할이 폭 ≤380에서만**이라 390에는 없다.
3. 🟠 `roomlist-ended` S7 플레이크 — 이제 유일하게 남은 알려진 red(화면 쪽).
4. 🟠 `:focus-within`으로 커진 `#bgmCtrl`이 콘텐츠를 97px 덮는다 — S1은 평상 상태만 잰다.
5. 🖼 S11을 `select` 밖으로(`nowrap`+`overflow:hidden`은 `scrollWidth`로 싸게 잰다).
6. 🔴 `index.html` 주석이 빌드에서 안 걷힌다 — 59 kB가 그대로 나간다.
7. 🔴 결함 id 공간(`F11` 중복) · S11 SKIP 소음 · 폭 381~389 무계측.
8. 🔴 미반증 행 게이트 도달(플랜 50) · S3를 상태로 확장 · `tabindex`.
9. 🔴 번들 B1 다회차 분할(플랜 48) · 게임 화면 레벨1 헤딩 · S10을 다른 화면으로.
10. 🖼 실기 SE1(320×454) 조판 재설계 · 320에서 HUD가 캔버스의 31%.
11. 🟠 미사용 에셋 41 · 죽은 플랜 3건 · `clue-room.ts` 낡은 「명패행」 벌점.

## 형상관리 메모
`main`(`72b2773`)에서 `feat/r50`을 땄다.
