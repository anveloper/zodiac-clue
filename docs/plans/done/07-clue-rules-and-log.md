# plan: 클루 규칙 정교화 + 상호작용 + 로그 시스템 (라이브 피드백 2026-07-20)

status: done · created: 2026-07-20 · closed: 2026-07-20 · src: 라이브 테스트 9종 요청

## goal
정통 클루 이동/방 규칙을 정교화하고, 제안·반증을 명확히 표기하는 로그 시스템으로 개편.

## tasks
1. [x] 방 안 이동은 이동 수 소모 X (복도 출발 시만 소모)
2. [x] 방 **입구(door)** 1칸 — `canCross`로 경계는 입구로만 출입, 클라에 🚪 렌더
3. [x] **사람** 초기 위치 = 중앙 잔치상 주변(봇은 방 스폰 유지)
4. [x] 제안 시 지목된 **용의자 토큰을 그 방으로 소환**
5. [x] 턴 시작 시 **주사위(2d6) 굴림 애니메이션** UI
6. [x] 제안 항목 카테고리별(용의자/~~수법~~장물/장소) 명확 표기 + 콘솔 반영
   - (갱신 2026-07-21) 테마 변경으로 ~~수법(흉기)~~ → **장물(훔친 것)**. 제안 시 **장물 토큰도 방으로 소환**(초안엔 용의자 토큰만).
7. [x] 반증 결과 식별 + **원 제안 로그에 결과 배지**(sid 연결)
8. [x] 기록/알림 접힘 시 최신 한 줄 요약(summary)
9. [x] 로그 종류별 배경색 구분(제안/반증/고발/이동/승리)

## 후속(선택)
- 봇 `runBotTurn`이 매턴 임의 방으로 텔레포트 → 제안으로 소환된 위치를 무시함. 봇이 "현재 방"을 우선 쓰도록 개선 여지.
- 방 진입 시 이동 강제 정지(정통 클루) 미적용.

## 설계 메모
- shared: `RoomRegion.door{x,y}`, `canCross(ax,ay,bx,by)`(경계는 door로만), 이동 수는 복도 출발 시만 소모.
- 로그 메시지 확장: `log { text, kind, sid? }`. 제안=sid 부여 → 반증 결과가 같은 sid로 원 제안에 배지.
- 주사위: 서버 `stepsLeft`=2d6, 클라 배너에서 굴림 애니메이션.
- 제안 소환: doSuggestion에서 suspect 토큰 위치를 방 중심으로 이동.

## done-criteria
- 방은 입구로만 출입, 방 안 이동 무료, 사람은 중앙에서 시작
- 제안이 용의자를 방으로 소환, 주사위 UI 동작
- 로그에서 제안/반증/고발이 색·배지로 명확히 구분, 접으면 한 줄
