# 17 — 액션 버튼 라벨 교체 + 제출물 ③ 분량 재배분 (07-30)

## goal
기획 스펙 2건을 반영한다. 둘 다 «지시 → 실측 → 반영»이고, **실측이 스펙을 고친 곳이 있다.**
1. 액션 버튼 5종 라벨을 동사형으로 교체(§4·§5 전수) + 안내 문안 동사 정렬
2. 제출물 ③을 분량 재배분 — 60초 요약을 실제 60초로

## tasks
- [x] **③ 재배분** — 60초 요약 9행 → 5행 · AI 섹션 3 → 4항목 · 수치의 출처 `<details>` 부활
  - [x] 실측으로 방향 결정: 6,320자/12.6분 → 4,376자/8.8분, 요약 790자(95초) → **511자(61초)**
  - [x] 🔴 **닫힌 `<details>`가 인쇄에서 통째로 빠지는 것**을 발견 → `@media print` 강제 펼침.
        PDF 766.7 kB(표 없음 대조군과 동일) → **873.8 kB**(펼침 대조군과 동일)
  - [x] 스펙 표가 되살리려던 낡은 수치 정정(115.66 → 42.38 kB · 7종 → 8종) + 「판 전체 시드 재생」 행 신설
  - [x] `.md`는 평어 압축본 유지(규약) · 구조만 동기화 · 「톤·깊이는 다르고 수치·사실은 같다」 머리말 명시
- [x] **라벨 교체** — 제안→추리하기 · 고발→신고하기 · 통로→지름길로 가기 · 계략→엿보기 · 턴 종료→내 턴 종료
  - [x] 버튼 5 + 대괄호 참조 9(`index.html` 3·`main.ts` 5·`clue-room.ts` 1) + 주석 4
  - [x] `narrator.ts` 제외(§6) — LLM 프롬프트 내부 라벨이라 화면 미노출, 건드리면 폴백 112,752문장 재검증
  - [x] 스펙 줄번호가 실제와 어긋난 곳(1084 내용 상이 · 1085 부재 · 1203/1205→1202/1204) 내용으로 잡음
- [x] **문안 동사 정렬**(기획 「통일하거라」) — 툴팁·클릭 피드백을 라벨에 맞춤
  - [x] 🔴 **기존 결함 발견**: 공용 문안 `NOT_STARTED = "잔치가 시작되면 고발할 수 있어요."`가
        `#accuse`·`#passage`·`#bonus`·`#endTurn` 4종에 재사용 → **[지름길로 가기]가 「고발할 수 있어요」라고 답했다.**
        공용 문안을 **행위 중립**으로 바꾸고 `#accuse`만 자기 동사를 갖게 함
- [x] `ui-copy.{md,html}` 동기화 — `.md` 21곳 · `.html` 35곳 + §2 어휘 통일표(라벨↔개념어 경계) 양쪽 신설
- [x] 커밋 4덩이 (`3047b1d` `f10e566` `a029005` `54e7ee9`)

## 실측이 스펙을 반증한 것
| 스펙 §3의 우려 | 실측 결과 |
|---|---|
| 「5버튼이 한 줄에 안 들어갈 수 있다 → CSS 조정 필요」 | **기우.** 글자 잘림 0 · 화면 밖 넘침 0 · 가로 스크롤 0. `1fr`은 `minmax(auto,1fr)`이라 칸이 min-content 아래로 안 줄고 재분배된다 |
| — | 대신 **「지름길로 가기」가 67.6px를 요구해 나머지 5칸을 54.2 → 51.5px로 밀어낸다**(편차 16.1px, 6칸 균등 리듬 깨짐). 기능 결함 아님·게이트 대상 아님 |
| — | 데스크톱 막대 342 → 471px(+38%), 보드 가로의 23.8% → 32.7% 점유 |

## 미해결 (다음 작업으로 넘김)
- 🔴 **턴 배너 S4 회귀** — 라벨이 길어져 배너가 215 → 236px, 중앙 정렬이라 뷰 전환 시 좌우로 흔들려
  **화면 게이트 S4가 데스크톱에서 깨진다**(교체 전 0/4 FAIL → 후 5회 중 3회). 글자 잘림은 아님(상한 270px에 여유 28.5px).
  방향 2안: `#turnInfo` `min-width` 고정(흔들림 제거 / 보드 명패 더 덮음) · 배너 문안에서 「방에서」 제거(**207px** — 교체 전보다 좁아짐). **기획 판단 대기.**
- ⚠️ **기본 `verify`는 이 회귀를 못 잡는다** — 화면 게이트 `viewportsDefault: ["phone"]`이라 데스크톱은 `--full`에서만 돈다.
  게이트가 «자기가 물은 것만 답한다»는 사례가 또 나왔다(07-28 실기 폰 결함 3건과 같은 계열).
- 통일 범위 밖으로 남긴 3종: 모달 제목 `고발 — 진범을 지목하라`(스펙 §2가 근거를 적어 유지로 잡은 예외) ·
  결과 `❌ 고발 실패`(`gate.config.mjs:594` 기대값과 결합) · 로그·패널 개념어(`🔍 제안`·`제안 기록표`·`제안 상한 75`)
- 촬영 대본 34곳(`[제안]` 14·`[계략]` 11·`[턴 종료]` 7·`[고발]` 2)은 **일부러 미수정** — 콘티를 새로 쓰기로 결정됨

## done-criteria
- [x] 화면에 찍히는 라벨 5종이 새 이름 · 문장 안 대괄호 표기가 라벨과 글자 그대로 동일
- [x] 어떤 버튼도 남의 동사를 말하지 않음
- [x] `verify` 전체 PASS 8 · SKIP 1 (기본 모드)
- [ ] `--full`(데스크톱 포함) PASS — **S4 회귀로 미달.** 사유와 방향을 위에 남김
