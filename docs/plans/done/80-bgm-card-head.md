# plan: 배경음 버튼이 «카드의 머리» 위에 서 있었다 — 재는 쌍이 없다

status: done(2026-09-02) · created: 2026-09-02 · src: 개선 루프 60회차 (플랜 79 큐 4번)

## goal
플랜 79 큐 4번은 「폰에서 `#copyBtn` ↔ `#bgmCtrl`이 가로로 27~31px 겹친다」였다.
실측해 보니 **그 수는 맞았지만 대상이 틀렸다** — 겹치는 것은 `#copyBtn`이 아니라 **카드의 머리글**이다.
랜딩·대기실은 사람이 가장 먼저 보는 화면이고, 이 자리를 **재는 쌍이 하나도 없었다.**

## 🔍 실측 (임시 쌍 6개 · `--only=landing,lobby --full`)
- `n320 320×568`·`p553 375×553`의 **랜딩·대기실 넷 모두** — `#bgmCtrl`이 `h1`을 **31×29px**
- 랜딩은 `.subtitle`까지 **31×8px**
- `#copyBtn`·`#roomCode`·`#gameMeta` — **세로 겹침 0**. 큐의 「27~31px」은 **가로 투영값**이었다
  (CDP 실측: 랜딩 `phone`에서 `h1` ↔ 버튼 가로 겹침 **−27px** · 세로로만 비켜 있다)

## 🔴 폰이 «안전»한 게 아니라 «세로로 비켜 있을» 뿐이다
카드 위 여백은 `align-items: center`의 나머지 값이고 **카드 높이는 내용에 달렸다.**
대기실 `phone 390×844` 1인 실측: 카드 678 → 위 83 → `h1.top` 108 vs 버튼 바닥 62 = 여유 **46px**.
카드가 **166px** 더 자라면(사람이 차면 `#playerList`가 자란다 · 게이트는 1인으로 잰다) 넘쳐서
위 여백이 `.center` 패딩 **16에 고정**되고 `h1.top`은 **41** = 버튼 바닥보다 21px 위가 된다.
⚠️ `n320`·`p553`은 **이미 넘쳐 있다**(`.card.y` = 16 · `scrollH` 585 > 568).

## 🔴 세로로 비켜서는 못 막는다 — 되주입으로 확인
`.center { padding-top: 78px }` → `n320`·`p553` 대기실에서 `#startBtn` **35·32px 잘림** +
`#hostHint` **통째로 화면 밖**(S1 FAIL). 둘 다 계약이다. → **가로로 비켰다.**

## 설계 (최종)
`@media (max-width: 490px)` — `#landing h1, #landing .subtitle, #lobby h1 { margin-right: 39px }`
+ `.card .subtitle { word-break: keep-all }` (**조건 없이** — 언어의 성질이지 폭의 성질이 아니다)

- **식(CDP로 5개 폭 검증)**: 여유 `= (L − 68) − ((L + cardW)/2 − 1 − pad)`, `cardW = min(380, L − 32)`
  · `L ≤ 412` → `pad − 51` (= −31 / −27) · `L > 412` → `L/2 − 257 + pad`
  · 실측 대조 `n320` −31 · `p553` −31 · 랜딩 `phone` −27 · `L=474` **0.0** · `L=484` **5.0** — 전부 일치
- **경계 490** = «규칙 없이도 8px이 남는 폭»(`L/2 − 257 + pad ≥ 8` · pad 20)
- **여백 39** = 최악 겹침 31 + 8(게이트 관용치 4의 2배) · 실측 여유 8.0 / 8.0 / 12.0

## 🔴 검수가 뒤집은 판단 — 초안이 **연달아 두 번** 틀렸다
1. 🔴 **`#bgmCtrl` 폭을 56으로 셌다.** `.bgm-vol`은 `padding`·`border`만 0으로 지우고
   `<input type=range>`의 **UA `margin: 2px`을 안 지운다** → 실측 **60**.
   이 저장소가 `index.html:1007`과 `gate.config.mjs`에 **이미 60이라 적어 뒀는데** 그 위를 지나갔다.
2. 🔴 **경계를 «카드 바깥 상자»로 풀었다**(테두리 1 + 패딩 누락) → 508. 실제로 겹침이 0이 되는 폭은
   **474**이고, 474~507의 **34px 띠에서 겹침이 0인데 자리를 내주고 있었다** —
   `index.html:1013`이 43회차에 같은 지적을 받아 적어 둔 그 실수다.
   ⚠️ 두 오차가 **부분 상쇄**해서 결과는 안전했다 — 그러나 **유도문은 근거로 못 쓴다.**
3. 🔴 **36px은 「관용치의 2배를 밖에 둔다」를 달성하지 못했다** — 실측 여유 **5px**. → 39px.
4. 🔴 **「맨 위 줄이 언제나 먼저 맞는다」의 근거가 거짓이었다.** 「위 여백은 `align-items: center`의
   나머지 값」이라 적었는데, 결함을 실측한 `n320`·`p553`은 **카드가 이미 넘쳐 위쪽에 고정**돼 있다.
   근거를 «카드의 첫 줄»로 바꿨다.
5. 🔴 **「`phone` 여유 4px」이 틀렸다** — 캡처를 눈으로 읽으며 표시 배율을 빠뜨렸다.
   CDP 실측은 카드 678 / 위 83 / `h1` 여유 **46px**이다. 논지(사람이 차면 무너진다)는 살고 수만 틀렸다.
6. 🟠 **두 `h1` 쌍이 `why` 문장을 공유**해 자기시험이 서로를 구분 못 했다 → 문장을 갈라 `F31` 추가.
7. 🟡 **`keep-all`을 폭으로 가뒀다** — `edge 620×340`은 쿼리 밖인데 부제 상자가 235px뿐이다
   → 미디어 쿼리 밖(`.card .subtitle`)으로 옮겼다.
8. 🟡 규칙이 `.center`와 `.card` 사이에 끼어 다음 사람이 못 본다 → `.card .subtitle` 옆에 역참조 주석.

## 대가 — 적어 둔다 (CDP 전후 실측)
`n320`에서만 랜딩 부제가 두 줄이 되어(높이 16 → 32) 카드 553 → **569**,
`#landingMsg`(`role="status"`) 바닥 562 → **578**로 첫 화면(568) 밖(`scrollH` 585 → 601 · 스크롤로 닿는다).
`p553`·`phone`·대기실은 **높이 변화 0**.

## tasks
- [x] 임시 쌍 6개로 실측 → 결함이 `h1`·`.subtitle`임을 확인
- [x] 세로 처방 되주입 → **기각**(S1 FAIL 실증)
- [x] 가로 처방 + `keep-all`(조건 없이)
- [x] 쌍 3개를 계약으로 승격 + 자기시험 **`F30`**(랜딩·변경 전 코드) · **`F31`**(대기실·`padding` 오답)
- [x] 문서(`.md` + `.html`) — §1.7 · 요약표 · 변경 이력
- [x] 서브에이전트 검수 → **8건 반영**(위 목록 · 유도를 통째로 다시 풀었다)

## done-criteria
- [x] 새 쌍이 **변경 전 코드에서 FAIL**(실제로 그렇게 발견했다) · `F30`·`F31`로 상시 확인
- [x] `--only=landing,lobby` **PASS 240 / FAIL 0**
- [x] `--self-test` **PASS 47/47**
- [x] `pnpm -r typecheck` · `gate:docs` PASS
- [x] 전체 `node scripts/verify.mjs` — 새 FAIL 0. 알려진 red 둘뿐:
      **B1 번들 55.04 kB**(한도 43.65 · §9.1 목표 120까지 여유 64.96 — **HEAD와 동일, 이 회차 순증 0**)
      · `roomlist-ended` S7 플레이크

## 🔴 아직 못 재는 것 — 정직하게 남긴다
1. **`#bgmCtrl:focus-within`이 이 회차 처방을 통째로 무효화한다.** 열리면 폭 60 → 162가 되어
   `n320`에서 `h1`을 **97×29px**, **`#roomCode`를 39.3×8px** 덮는다(`p553` 여유 **15.7px** · `phone` 79px).
   `#roomCode`는 53회차가 `protect`에 올린 1급 값인데 **S14의 `vs` 목록은 52회차 것**이라 빠져 있다 —
   넣어서 재 봤고 **지금 바로 운다.** 이번에 안 넣은 이유: 고치려면 52회차가 «세로로 연다»를 좁힐 때
   `[복사]` 여유를 16 → −4px으로 만든 **그 축을 다시 열어야** 한다. **넣는 것과 고치는 것은 같은 회차에.**
2. **경계 490 양쪽에 계측 뷰포트가 없다.** 391~619가 통째로 무계측이고, §1.7이 「경계 양쪽에
   뷰포트를 둔다」로 스스로 규정한 형태를 못 지켰다. 식은 CDP 5개 폭으로 검증했으나 **상시 계측이 아니다.**
3. **`#landingMsg`가 `protect`에 없다** — 이 회차가 그 줄을 `n320` 첫 화면 밖으로 밀었는데 게이트가 조용하다.

## 다음 회차 후보 (착수 전 **반드시 현재 화면·코드로 재확인**)
1. 🔴 **`#roomCode`를 S14에 넣고 그 겹침을 고친다** — 위 «못 재는 것» 1번. 실측치가 이미 있다.
2. 🔴 **경계 490 양쪽에 뷰포트 둘**(`bannerBand`·`bannerTop` 선례) + `#landingMsg`를 `protect`에.
3. 🖼 **짧은 가로 컬럼의 고정 크롬이 47%다** — `#aiPanel` 66px + 손잡이 88px (플랜 79 큐 3번, 미착수).
4. 🔴 **로그의 «온전한 줄»을 컬럼 클립 기준으로 잰다** (플랜 79 큐 1번, 미착수).
5. 🔴 **`game-column`의 `onlyViewports`에 `short`** (플랜 79 큐 2번, 미착수).
6. 🟠 `roomlist-ended` S7 상시 red · `active/`에 죽은 플랜 3건.

## 형상관리 메모
`main`(`40e5d3a`)에서 `feat/r60`을 딴다.
