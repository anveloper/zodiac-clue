# 20260720 — 클루 규칙 정교화 · UI/버그 픽스 · 자동 루프

> 실배포 이후 라이브 피드백 반복 반영 + 무인 자동 루프 배치. (plans 06/07/08 대응)

---

## feat — 정통 클루 규칙/로그 개편 (plan 07)
- 이동: 방 안·잔치상 무료, 복도만 주사위 소모. 방 입구(door) 1칸으로만 출입(`canCross`), 클라 🚪 렌더.
- 사람 초기 위치=중앙 잔치상, 봇=방(빈 칸). 제안 시 지목 용의자 그 방으로 소환.
- 주사위 2d6 + 화면 중앙 굴림 오버레이. 용의자=참여자 6명으로 한정(경우의 수 축소).
- 로그 시스템: 종류(kind)별 색·배지(제안/반증/고발/이동/승리), 제안-반증 sid 연결, 우측 컬럼(증거노트+기록) 재구현(드래그 너비/높이, 스크롤바 테마).

## fix — 코어 버그 (plan 06 + 라이브)
- 이동 키 `e.code`(한글 IME ㅈㅁㄴㅇ 대응). NPC 방 스폰·칸 정렬·겹침 없음. 턴 게이팅.
- 잔치상/방 자유이동(한도 0이어도), 시작 시 즉시 렌더, 선물박스 글리프 패딩.
- NPC 턴 카메라 팬(전환 지연으로 반증 먼저 인지), 우측 패널 폭만큼 보드 중심 좌측 보정.
- 내 손패/공통 단서 제안·증거노트 자동 제외. 탈락 관전 배너·종료 결과 오버레이·[메인으로].

## feat — NPC 추리/표현
- 반증으로 드러난 카드 NPC 공유(revealed) → 사람 전멸 뒤 봇만 남아도 유한 시간 수렴(~40s).
- 봇 후보 방 우선 이동. LLM 대사 캐시(동일상황 LRU 200).

## chore — 배포 자동화
- 서버 GitHub Actions(SSH) + 클라 Vercel Git 연동 → main push 자동배포.

## feat — 자동 루프 배치 (plan 08, 무인 10분 간격)
1. LLM 대사 캐시 2. 방 진입 시 이동 종료(P2) 3. 문 봉쇄(P5) 4. 공통 단서 패(솔로 2장) 5. 비밀 통로(P1, 대각 3쌍) 6. 제출물 ④ AI 기술문서.

## 남은 것
- 리매치(Phaser 재구성), 도메인 구매 후 정적 공개, 02 엔진/콘텐츠 분리.
