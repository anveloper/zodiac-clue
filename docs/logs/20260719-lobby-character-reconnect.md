# 20260719 — 메인 로비 흐름 (캐릭터 선택·초대 링크·대기실·세션 유지)

> 그동안 이름을 `prompt()`로 받고 곧장 공용 방에 자동 입장하던 걸,
> **"캐릭터 선택 → 방 만들기 → 초대 링크 공유 → 링크로 참가 → 대기실 → 방장 시작"**
> 하는 실제 멀티플레이 진입 플로우로 교체.

---

## feat — 십이지신 캐릭터 선택 (`packages/shared`, client)

- 이름 입력 제거. 랜딩에서 **십이지신 12캐릭터를 이모지+호칭으로 선택**.
- **호랑이는 잔치 주최자라 잠금** → 플레이 가능한 용의자 = 나머지 **11지신 손님**. (이후 잠금 해제되어 호랑이 포함 12명 선택 가능)
- 선택한 캐릭터가 곧 정체성(이름=호칭, 토큰=이모지). 로비·게임 토큰에 이모지 표기.
- shared에 `ZODIAC`(12) · `SUSPECTS`(11) · `HOST` · `EMOJI` 추가.

## feat — 방 만들기 / 초대 링크 참가 (server, client)

- `client.create("clue")` → 방 생성(방장), `client.joinById(code)` → 코드로 참가.
- **초대 링크** `?room=<roomId>` 발급 + 복사 버튼. 링크로 들어오면 코드 자동 입력.
- **대기실(로비)**: 참가자 목록(이모지·방장 표시) + 인원수. 서버에 `host` 개념 추가 — **방장만 시작** 가능, 나머지는 대기 안내.
- 시작 시 `room.lock()`으로 난입 방지. `phase === "playing"` 되면 게임 화면으로 전환.
- 제안/고발 입력은 드롭다운 모달 유지.

## feat — 새로고침 세션 유지 (탭 기준) (server, client)

- 서버 `onLeave`: 비자발적 이탈이면 `allowReconnection(60s)`으로 좌석 유지 → 재접속 시 복구.
- 클라: 접속 시 `reconnectionToken`을 **sessionStorage**(탭 단위)에 저장, 로드 시 있으면 `client.reconnect()`로 자동 복귀. 실패 시 토큰 폐기 후 랜딩.

## fix — 접속 실패 메시지

- WebSocket 연결 실패(`ProgressEvent`)를 `[object ProgressEvent]` 대신 읽을 수 있는 안내로 표시.

---

## 검증 (agent-browser 실측, 2세션)

- ✅ 랜딩: 십이지신 12그리드, 호랑이 "주최자" 잠금, 미선택 시 버튼 비활성
- ✅ 방 만들기 → 대기실 초대링크 `?room=zEtqChZ2h`, 방장 `🐭 생쥐 서생 👑`
- ✅ 세션2가 초대 링크로 접속·황소 선택·참가 → 참가자 2/6 동기화, **비방장은 시작 비활성**
- ✅ 방장 시작 → 게임 전환 + 손패 private 분배 확인
- ✅ 새로고침 → 대기실로 자동 복귀(탭 세션 유지), 로그 "재접속!" 확인
- ✅ 3패키지 타입체크 통과

## 추가 개선 (같은 날 피드백 반영)

- **캐릭터 중복 방지**: 대기실에 캐릭터 그리드를 두어 **사용중(다른 사람)=잠금, 내 선택=하이라이트, 빈 것=클릭 변경**. 서버 `character` 메시지로 중복이면 거부. (실측: 두 사람이 같은 캐릭터 선택 시 자동 회피 + 대기실에서 변경 반영)
- **게임 UI 크기**: 좌상단에 작게 나오던 문제 → 보드 셀 20→24px, Phaser `Scale.FIT + CENTER_BOTH`, 화면 flex 중앙 정렬 + 보드 `min(80vmin, 600px)` 반응형.
- **초대 링크 경로형**: `?room=CODE` → **`/room/CODE`** 경로로 변경(주소창 `history.replaceState` 반영, 직접 접속 시 Vite SPA fallback + 코드 자동 입력). 구형 쿼리도 하위호환.
  - ⚠️ 배포 시: 정적 호스팅에서 `/room/*`가 index.html로 폴백되도록 설정 필요(GH Pages는 404.html 트릭 + base 처리).
- **호랑이 잠금 해제**: 랜딩/대기실에서 십이지 **12명 전부 선택 가능**(호랑이 포함). "주최자" 잠금 제거 → 호랑이도 일반 플레이 캐릭터.
- **없는 방 코드 처리**: `/room/<없는코드>`로 들어와 참가 실패 시 URL을 **메인(`/`)으로 복귀** + "없는 방이에요" 안내.

## 다음 할 일

- [ ] 캐릭터/맵 도트 아트
- [ ] 주사위/턴 정식화, 음성
- [ ] 배포 파이프라인(경로형 라우팅 폴백 포함)
