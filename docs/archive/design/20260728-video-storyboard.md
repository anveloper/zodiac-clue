# 제출물 ② 플레이 영상 콘티 (컷 편집판)

> 작성 2026-07-27 · 사람용 `.html` 동봉 · 대상: NHN NAN 2026 사전과제 제출물 ②
> 목표 길이 **55초**(요건 30~60초) · 구성 **실플레이 클립 6개의 컷 편집** · 원테이크 아님
> 전제 근거: `docs/design/20260727-improvement-roadmap.md` §7.5(고발 평균 7라운드)·§7.6(컷 편집 가능)

---

## 0′. 🔴 07-29 리허설 실측 정정 — **아래 컷 시트보다 이 절이 우선한다**

콘티는 07-27~28에 쓰였고 그 뒤 화면이 여러 번 바뀌었다. **07-29에 전 컷을 헤드리스로 실제 밟아
본 결과 13곳이 화면과 어긋난다.** 촬영일에 발견하면 그날이 통째로 날아가므로 여기 먼저 적는다.
근거 스크린샷은 세션 스크래치패드 `shots-video/`(저장소 미포함).

### 0′.1 촬영을 막을 수 있는 것 2건

| | 실측 | 대응 |
|---|---|---|
| 🔴 **Gemini 무료 쿼터 소진** | 07-29 기준 **HTTP 429 `RESOURCE_EXHAUSTED · limit: 500`**(일일). 2판 실측 **LLM 4 / 폴백 51**. 한 판이 **40~60콜** → **하루 8~12판이 한계** | ① 촬영 전 생존 확인(§4′ S0-2) ② **리허설은 반드시 `AI_NARRATE=off`** — 쿼터를 리허설에 쓰면 본 테이크가 폴백이 된다 ③ 429면 컷 C는 **대체 자막으로 확정**(판단을 촬영 뒤로 미루지 않는다) |
| 🔴 **판 중반 로그 패널 붕괴** | 우측 컬럼이 `AI 대사 → 증거 노트 → 제안 기록표 → 기록` 세로 스택인데 **제안 기록표가 자라 로그를 밀어낸다.** 900px에서 로그 몫 **72px · 가시 0줄** | **제안 기록표 제목 1회 클릭**(`<details>`)으로 로그 72 → **299px·6줄**. AI 대사까지 접으면 **458px·9줄**. 실제 마우스 클릭으로 검증 |

> ⚠️ **컷 순서가 바뀐다.** 콘티는 계략(D)을 4번째(0:29)에 두지만 **실제로는 제일 먼저 찍어야 한다** —
> 제안이 3~4건 쌓이면 로그 패널이 죽어 그 뒤로는 B·C·D를 못 찍는다.

### 0′.2 콘티가 화면과 어긋나는 곳 13건

| 콘티 | 콘티가 말하는 것 | **실제 화면** |
|---|---|---|
| §2.A 51 | 랜딩 **[혼자 바로 시작]** 버튼 | **그런 버튼 없다.** `?solo=1` 딥링크뿐(`main.ts:2701`). 로드맵 §1.6의 버튼화는 미착수 |
| §2.A 52 | (목표 카드 언급 없음) | 첫 진입에 **`🐯 60초 안내` 카드가 주사위 앞을 막는다.** 컷 길이에 반영 필요. `?demo=1`이면 안 뜬다 |
| §2.B 57 | 로그의 파란 `🔍 [제안]` 줄 · `반증됨` 배지 | **제안은 로그에 안 남고 「제안 기록표」 패널로 간다.** 로그엔 `🔎 … 단서로 반증 (나만 봄)` 한 줄. `반증됨` 배지는 **남의 제안**에만 붙는 경로라 본인 제안 컷에 안 나온다 |
| §2.C 63 | 칩 `✨LLM 2 · ⚙폴백 0` | **3항목**이다 — `✨LLM n · ♻캐시 n · ⚙폴백 n` |
| §2.C 64 | "✅ 선행 해소 — 라이브 LLM 동작(12/12 폴백 0)" | **07-29 현재 429.** 이 문장을 믿고 촬영하면 안 된다 |
| §2.D 69 | `🃏 … 계략(엿보기 2·이동 +5)` | `🃏 계략 — {이름} · {NPC}에게서 엿보기 2 · 이동 **+6**` (어순·구분자도 다름) |
| §2.D 70 | "모서리까지 거리가 있으므로 주사위 눈이 큰 턴" | **9 이상 한 번이면 1턴에 도달**(방 안 통과가 이동 무료). 실측 **4초** |
| §2.E 75 | "말들의 위치는 그대로인 채 표현만" | 좌표는 유지되나 **뷰2·3은 카메라가 크게 당겨진다** → 이 자막은 쓰지 말 것 |
| §2.E 76 | 🔶 선행 = PLACEHOLDER 배지 제거 | **이미 제거됨**(07-28). 선행 0건 |
| §2.F 81 | `🎉 승리!` · 로그 `🎉 사건 해결! 정답: …` | 제목은 **`🎉 사건 해결!`**, 부제 `정답 봉투 — …`, 로그 `🎉 사건 해결 — {이름} 님`. **자막을 «승리!»로 쓰면 화면과 어긋난다** |
| §2.B/D 58·70 | 🔶 선행 = 장소 고정행·소환 FX·즉시고발 | **전부 구현 완료.** 🔶 모두 해소 |
| §3 103 | "미구현(원클릭 진입·AI 칩·소환 FX)은 한 글자도 언급 말 것" | **AI 칩·소환 FX는 살아 있다.** 과도한 제약 |
| §1 32·33 | 우측 컬럼 320px · 증거 노트 300px 드래그 | 둘 다 되지만, 리사이저는 **실제로 잡히는 폭이 8px**이고(핸들 16px 중 오른쪽 절반을 패널이 덮음) **진짜 문제는 증거 노트가 아니라 제안 기록표**다 |

### 0′.3 확인된 것 (그대로 가도 되는 컷)

주사위 6프레임 + `이동 N칸` · 턴 배너 문안 **문자 그대로 일치** · 제안 모달의 `장소 · 현재 방 · 고정`
고정행 · 소환 워프 `🔔 소환 — X → Y` · NPC 말풍선·타자기 · 계략 귓속말 + 증거노트 자동 취소선 ·
뷰 4연속 전환(300~650ms, 로딩 없음, `PLACEHOLDER` 없음) · 턴 순서 원형 · 고발 모달 3열 ·
[다시 하기] 즉시 새 판 · 대기실 12캐릭터 도감 + 직업 풀이.

### 0′.4 못 잰 것 — **통과로 세지 않는다**

- **솔로에서 사람이 이기는 결말을 못 찍었다.** 실측 3판 모두 NPC가 먼저 맞혔다(`🔍 사건 종결`).
  승리 화면 자체는 화면 게이트와 같은 방식(사람 6좌석 → 손패 여집합)으로 도달해 확인했으나,
  **촬영 당일 솔로 판에서 사람이 이길 확률은 재지 못했다.** 대체 컷 ①을 실전 후보로 둘 것.
- **컷 C의 «말투가 다른 두 캐릭터가 연속 턴» 조건을 확인하지 못했다.** 쿼터 소진으로 LLM 대사
  표본이 4건뿐이라 말투 대비를 판단할 수 없었다.

### 0′.5 촬영과 무관하게 남은 결함 2건 (기록만)

- **뷰2·3에서 방 이름표가 우측 컬럼 위로 넘어온다**(「별당」 라벨 x≈1330). §5 재촬영 트리거에 없던 새 결함.
- **컬럼 폭 리사이저의 오른쪽 절반을 형제 패널이 덮는다** — 로직은 정상, 스택 순서 문제.

---

## 0. 요건 해석과 촬영 보조의 경계 — 이 절이 성실성 증거다

조항: **"실제 플레이 화면만 · AI합성/타인영상 금지"**

| | 허용 | 근거 |
|---|---|---|
| 컷 편집 | ✅ 클립 이어붙이기, 하드컷 | 금지 대상은 합성·타인 영상. 편집 자체는 금지어에 없다 |
| 자막 | ✅ 후처리 텍스트 오버레이 | 화면 픽셀을 대체하지 않음. **③ 문서와 영상 설명란에 "자막은 후처리"를 1줄 명시** |
| 크롭·확대 | ✅ 실제 녹화 픽셀 범위 내 | 없던 화면을 만들지 않음 |
| 표시 계층 조작 | ✅ 우측 컬럼 폭 드래그, 증거노트 높이 드래그, 뷰 드롭다운, 턴 순서 원형 열기, 브라우저 확대 | **전부 게임에 실제로 존재하는 사용자 조작** |
| 반복 촬영 | ✅ [다시 하기]로 여러 판 녹화 후 좋은 테이크 선택 | 각 테이크가 실제 플레이 |
| BGM·나레이션 | ⚠️ **넣지 않는다** | 무음 + 자막으로 확정. 판단 여지를 남기지 않음 |
| **주사위·정답·NPC 판정 개입** | ❌ **절대 금지** | 손대는 순간 "실제 플레이 화면"이 아니다 |
| DevTools 상태 주입 | ❌ | 위와 동일 |
| 가짜 HUD·목업 합성 | ❌ | 조항 정면 위반 |

> **판별 규칙 한 줄**: *"녹화를 멈추고 다시 켜도 같은 일이 일어나는가?"* — 예=허용(표시 계층), 아니오=금지(진실값 개입).
> **자막 정직성 규칙**: 화면에 실제로 찍힌 것만 주장한다. **LLM 배지가 안 찍혔으면 자막에 "LLM"을 쓰지 않는다**(→ 2.C 대체 자막).

## 1. 촬영 환경 고정값 (모든 클립 공통)

| 항목 | 값 | 이유 |
|---|---|---|
| 해상도 | 1920×1080 / 60fps | YouTube 기본. 업스케일 금지 |
| 브라우저 창 | **1600×900 고정**, 확대 100% | 680px 미만 반응형 분기에 걸리면 HUD 레이아웃이 달라져 클립 간 불일치 |
| 우측 컬럼 폭 | 드래그로 **320px** | 로그 문장 줄바꿈 방지(표시 계층 조작이라 허용) |
| 증거 노트 높이 | 드래그로 **300px** | 3그룹이 한 화면에 |
| 마우스 커서 | 표시 | 사람이 조작 중임의 증거 |
| 오디오 | 무음 | 0절 |
| 서버 | 로컬 `pnpm dev` 또는 라이브 중 **LLM이 실제 동작하는 쪽** | 로컬 실행도 실제 플레이. 단 **터미널·콘솔은 절대 프레임에 넣지 않는다** |
| 파일명 | `zc-<take>-<clip>-<n>.mov` | 원본 전량 보관(`~/zodiac-rec/`, 저장소 커밋 금지) |

## 2. 본 테이크 컷 시트 (선행 개선 완료 후 · 총 55초)

| # | 구간 | 길이 | 한 줄 |
|---|---|---|---|
| A | 0:00–0:10 | 10s | 링크 → 원클릭 솔로 진입 → 주사위 → 방 진입 |
| B | 0:10–0:20 | 10s | [제안] → 용의자·장물 소환 → 반증 |
| C | 0:20–0:29 | 9s | NPC 대사 2연 + AI 카운터 칩 |
| D | 0:29–0:37 | 8s | [계략] → 귓속말 → 증거노트 자동 제외 |
| E | 0:37–0:46 | 9s | 뷰1→2→3→4 연속 전환 |
| F | 0:46–0:55 | 9s | 고발 성공 → 결과 오버레이 |

### 클립 A — 진입 (10초)
- **화면**: 랜딩 카드 → **[혼자 바로 시작]** → `#diceOverlay` 주사위 2개 6프레임 굴림 → `이동 N칸` → 방향키 이동 → 🚪 입구로 방 진입 → 상단 `🎲 내 턴 · 남은 이동 N칸 · 방에서 [제안]`.
- **촬영 조건**: 🔶 선행 = 원클릭 솔로 진입(로드맵 §1.6). 미구현이면 3.A' 사용. 시작 위치 중앙 잔치상 → 최근접 방은 **대청마루(문 11,5)** 또는 **행랑채(문 11,18)**, 이동 2~4칸이면 도달. 주사위 오버레이는 확정 후 1.9초에 사라지므로 **굴림~방 진입을 끊지 말고 한 번에** 녹화.
- **자막**: `설치·로그인 없이 링크 한 번` → `빈 자리는 AI NPC 5명이 채웁니다`
- **대체 컷**: 주사위 눈이 낮아 방까지 못 가면 리매치로 재시작. 그래도 안 되면 [턴 종료] 후 다음 턴 주사위부터 — **주사위를 다시 굴리는 것은 게임 규칙 안의 행위**라 허용.

### 클립 B — 제안과 반증 (10초)
- **화면**: `[제안]` → 모달(용의자 / 훔친 것 / **장소: 대청마루 고정**) → 확인 → 용의자·장물 토큰이 소환 앵커로 **워프** → 로그에 파란 `🔍 [제안]` → 보라 `🛡 … 반증했습니다` + 원 제안 줄에 `반증됨` 배지.
- **촬영 조건**: 방 안이어야 버튼 활성. **반증이 뜨는 조합**을 고를 것(자기 손패에 없는 카드). 미반증(`❗ 정답 후보!`)이 뜨면 그 테이크는 **클립 F 소재로 따로 보관**하고 B는 재촬영. 🔶 선행 = 장소 고정행 + 소환 FX(§2.2).
- **자막**: `방에 들어가 제안하면` → `지목한 용의자와 장물이 그 방으로 소환됩니다` → `반증은 시계방향 첫 사람만`
- **대체 컷**: 소환 FX 미구현이면 **로그 패널 클로즈업**으로 전환(크롭만 사용).

### 클립 C — NPC 대사와 AI 계층 (9초)
- **화면**: 봇 턴 2연속, 말투가 다른 두 캐릭터(예 🐯 `어험— …감히 누구 앞이라고!` / 🐑 `저, 저기… …아니면 말고요.`)의 말풍선이 55ms 타자기로 찍힘 + AI 카운터 칩 `✨LLM 2 · ⚙폴백 0` 증가.
- **촬영 조건**: ✅ **선행 해소(2026-07-28)** — 라이브 LLM이 실제로 동작한다(재검증 12/12 · 폴백 0). 원인은 키가 아니라 `thinkingBudget:0` 400이었고 코드 수정으로 해결됐다. 다만 **촬영 직전 1판을 돌려 대사가 폴백 문투(`…아니라니 하나 지웠군` 등)가 아닌지 확인할 것** — 쿼터 소진 시 다시 폴백으로 떨어진다. **폴백 상태로 찍고 자막에 LLM이라 쓰면 허위 주장**이다. 🔶 AI 칩 기본 노출(§7.7). 말투 대비가 큰 2인이 연속 턴이 되도록 시작 전 턴 스트립 순서 확인. 봇 대사는 놓치면 다음 턴까지 대기 — 녹화는 계속 돌리고 편집에서 자른다.
- **자막**: `판정은 결정론적 규칙엔진` → `대사만 LLM이 생성합니다` → `✨ = LLM 응답, ⚙ = 규칙 폴백`
- **대체 자막(LLM 미동작 시)**: `NPC 12명 각자의 말투로 말합니다` — LLM 주장 제거.

### 클립 D — 계략 (8초)
- **화면**: 고정 NPC 인접 → `[계략]` → 귓속말 말풍선 + 로그 `🃏 … 계략(엿보기 2·이동 +5)` / `🃏 엿본 카드: 술동이 · 서재 (정답 아님·나만 봄)` → 증거 노트 칩 2개가 **취소선+흐림**으로 자동 전환.
- **촬영 조건**: **모서리 헬퍼 = 2장**이라 증거노트 변화가 크다. 인접은 체비셰프 ≤1(대각 포함). 중앙에서 모서리까지 거리가 있으므로 **주사위 눈이 큰 턴**을 고른다. 엿본 카드가 아직 체크 안 된 **판 초반**에 촬영.
- **자막**: `[계략] — 다른 손님의 패를 몰래 엿봅니다` → `엿본 단서는 증거 노트에서 자동으로 지워집니다`
- **대체 컷**: 노트 변화가 안 잡히면 로그 `(정답 아님·나만 봄)` 클로즈업.

### 클립 E — 뷰 진화 4연속 (9초)
- **화면**: 좌하단 `뷰1 · 2D ▲` → 드롭다운 → 뷰1→2→3→4를 2초 간격. **말들의 위치는 그대로**인 채 표현만 바뀐다.
- **촬영 조건**: 🔶 **유일한 필수 선행 = PLACEHOLDER 배지 제거**(§2.4). 안 하면 뷰3 전환이 "도장 찍히는" 역효과로 찍힌다. 보드에 말이 흩어진 **판 중반(3~4라운드)** 이후. 뷰4는 첫 진입에 한 프레임 지연이 있으니 **리허설로 한 번 켜둔 뒤** 본 녹화. **내 턴 중**(카메라 고정)에 전환.
- **자막**: `개발 진화 4단계를 게임 기능으로` → `2D → 2.5D → 에셋 → 절차적 도트`
- **대체 컷**: 카메라 팬이 겹치면 뷰1→뷰4 **2단계만**(6초로 축소, 남는 3초는 F로).

### 클립 F — 고발 성공 (9초)
- **화면**: `[고발]` → 모달(장소도 선택) → 확인 → 초록 로그 `🎉 사건 해결! 정답: …` → `#endOverlay` `🎉 승리!` + [다시 하기]/[메인으로].
- **촬영 조건**: ⚠️ **가장 확률이 낮은 클립**(개선 후에도 평균 7라운드째). **반드시 마지막에 몰아서** 촬영하고 [다시 하기]로 반복. 정답 확정 경로: 공통 단서 2장 + 계략 엿보기 2장 + 미반증 제안. 🔶 선행 = 제안 턴 고발 가능(§7.5.1). 없으면 두 조각(제안 미반증 / 다음 턴 고발)으로 찍어 컷으로 붙인다.
- **자막**: `누가 · 무엇을 · 어디서 — 셋 다 맞아야 승리` → `정답 고발 성공`
- **대체 컷(우선순위)**: ① **봇의 정답 고발** 종료 화면 + 자막 `NPC도 같은 규칙으로 추리해 사건을 해결합니다` ② **오답 고발 → 탈락** 관전바 + 자막 `틀리면 즉시 탈락, 관전으로` ③ 미반증 제안 로그 클로즈업으로 여운 종료.

## 3. 안전 테이크 컷 시트 (선행 개선 0건 · 현재 코드 그대로 · 총 52초)

> 목적: **08-01까지 요건 ②를 먼저 충족**해 심사 제외 리스크를 0으로. 이후 본 테이크로 교체 업로드.
> 원칙: 지금 확실히 찍히는 것만 담고, **미구현 기능을 자막으로 암시하지 않는다.**

| # | 구간 | 길이 | 내용 | 가능한 이유 |
|---|---|---|---|---|
| A' | 0:00–0:12 | 12s | 랜딩 → [방 만들기] → 🏮 대기실 → 캐릭터 12종을 훑으며 **직업 풀이 패널** 변화 → [잔치 시작] | 전부 구현됨. 4단계 진입을 **캐릭터 도감 쇼케이스**로 전환해 약점을 콘텐츠로 사용 |
| B' | 0:12–0:22 | 10s | 주사위 → 이동 → 방 진입 → [제안] → 로그 2줄 | 구현됨. **보드가 아니라 로그 패널 중심** |
| C' | 0:22–0:31 | 9s | 봇 대사 2연(말투 대비) | 로컬에서 키가 살아있을 때만 LLM 자막 사용 |
| D' | 0:31–0:40 | 9s | [계략] → 엿보기 로그 → 증거 노트 자동 취소선 | 구현됨 |
| E' | 0:40–0:52 | 12s | 뷰1→2→3→4 + 턴 배너 클릭 → **턴 순서 원형 오버레이** | 전부 구현됨. E'를 길게 잡아 F 부재를 메움 |

**안전 테이크 회피 목록**(표시 계층 선택만 사용)
- 소환 로그 `호랑이 대감가 대청마루(으)로` → B'에서 프레임에 오면 재촬영. **또는 조사 헬퍼(30분, §7.14)를 먼저 넣는 편이 훨씬 싸다 — 안전 테이크 전 유일하게 권장하는 선행 작업.**
- 뷰3 PLACEHOLDER 배지 → E'에서 뷰3 체류 1초 이하. (10분 제거 작업 권장)
- 승리 컷 → **처음부터 넣지 않는다.** 턴 순서 원형 오버레이로 마무리.
- 자막 총 6장. 미구현 기능(원클릭 진입·AI 칩·소환 FX)은 **한 글자도 언급하지 않는다.**

## 4. 촬영 순서 체크리스트

> 순서대로 따라 하면 소재가 전부 모인다. 세션 1회 ≈ 90분(안전) / 150분(본, F 반복 포함).

**S0. 준비(10분)**
> 🔴 **아래 S0~S7은 07-28판이다. 07-29 리허설로 순서와 조작이 바뀌었다 — §4′를 따를 것.**

- [ ] 창 1600×900, 확대 100%, 알림·확장 배너 끄기
- [ ] `pnpm dev` 기동 후 **터미널을 화면 밖으로**
- [ ] 리허설 1판으로 뷰4(`pixel`) 씬 예열
- [ ] 우측 컬럼 320px, 증거노트 300px로 드래그
- [ ] 60fps 확인, 테스트 5초 녹화 후 재생 확인

**S1. 진입(A/A')** — [ ] 페이지 로드 상태부터 녹화 시작 · [ ] 랜딩 1.5초 정지 · [ ] (안전) 캐릭터 4~5개 훑어 직업 풀이 변화 · [ ] 시작~주사위~방 진입 **끊지 않고**

**S2. 제안·반증(B/B')** — [ ] S1에 이어 같은 녹화로 [제안] → 로그 2줄까지 유지 · [ ] 미반증이면 파일을 `zc-*-Fcand-*.mov`로 보관 후 재시도

**S3. NPC 대사(C/C')** — [ ] 녹화 켠 채 [턴 종료] 반복으로 봇 턴 3~4회 · [ ] 말투 대비 큰 2인 연속 구간 타임코드 메모 · [ ] **AI 칩이 없으면 LLM 자막 금지**

**S4. 계략(D/D')** — [ ] 주사위 8 이상인 턴에 모서리 NPC로 이동 · [ ] 인접 후 [계략] · [ ] 말풍선·로그·증거노트가 한 프레임에 · [ ] 변화 후 2초 더 녹화

**S5. 뷰 전환(E/E')** — [ ] **내 턴에** 드롭다운 열고 2초 간격 전환 · [ ] (안전) 턴 배너 클릭 → 원형 3초 유지

**S6. 결말(F, 본 테이크 전용)** — [ ] 녹화 켠 채 끝까지 진행, 종료 시 즉시 [다시 하기] · [ ] **최대 20판 / 90분** 반복 · [ ] 안 나오면 2.F 대체 컷 1순위로 확정

**S7. 마감** — [ ] 원본 전량 `~/zodiac-rec/<날짜>/` 보관 · [ ] 채택 타임코드를 `takes.txt`에 · [ ] 편집 → 자막 → 무음 확인 → 길이 확인 → 업로드

## 4′. 촬영 당일 순서 — **07-29 리허설로 실측한 판. §4보다 이것을 따른다**

전부 실제 빌드에서 밟아 본 값이다. 「해보면 알겠지」를 남기지 않는다.

**S0. 준비 (15분)**
1. 창 **1600×900**, 확대 100%(실측 기준값). 알림·확장 배너 끄기. 터미널은 프레임 밖.
2. 🔴 **LLM 생존 확인 — 1회만 실행하고, 결과로 컷 C를 지금 확정한다.**
   ```
   node -e "process.loadEnvFile('apps/server/.env');fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent?key='+process.env.GEMINI_API_KEY,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:'ping'}]}]})}).then(r=>console.log(r.status))"
   ```
   **200 → LLM 자막 사용 가능 · 429 → 컷 C는 대체 자막으로 확정.** 이 판단을 촬영 뒤로 미루지 않는다.
3. 🔴 **리허설은 반드시 `AI_NARRATE=off`로 별도 기동.** 쿼터 500을 리허설에 쓰면 본 테이크가 폴백이 된다.
4. 리허설 1판으로 뷰4 예열 → **브라우저 프로필을 새로 만들지 말고 그대로** 본 테이크에 쓴다(목표 카드 재노출 방지).

**S1. 컷 A (10초) — 진입**
- 주소창에 `http://localhost:5173/?solo=1`을 넣은 상태에서 녹화 시작.
- ⚠️ 첫 진입이면 `🐯 60초 안내` 카드가 먼저 뜬다. **두 갈래 중 하나를 미리 정한다.**
  - **(권장)** 카드를 1.5초 보여주고 **[확인]** — "설치·로그인 없이" 자막과 잘 맞고, 닫으면 대기하던 주사위가 이어 굴러간다.
  - 카드를 안 넣으려면 `?solo=1&demo=1`.
- 진입 **약 3초 뒤 주사위 오버레이가 사라진다.** 그때부터 `↑` 6번(대청마루) — 실측 1.1초. **총 4.1초.**
- 필요한 눈 **3 이상**(중앙 잔치상 안은 이동 소모 0). 2가 나오면 [턴 종료] 후 다음 턴.

**S2. 컷 D (8초) — 계략 ※ 반드시 판 초반에, 제일 먼저**
- **첫 턴 주사위가 9 이상이면 그 자리에서 모서리 계략 NPC로 간다**(중앙→모서리 9칸, 방 안 통과가 무료라 실측 1턴·4초).
- 도착 → `[계략]` → 귓속말 말풍선 + 로그 2줄 + 증거노트 칩 2개 취소선이 **한 프레임에** 들어온다(판 초반에만).
- ⚠️ 봇이 내 캐릭터를 제안하면 내 말이 다른 방으로 **소환**된다. 소환당하면 그 테이크는 버리고 다시.

**S3. 컷 B (10초) — 제안·반증**
- `[제안]` → 모달의 `장소 · 현재 방 · 고정` 행 확인 → **내 패가 아닌 조합**으로 확인.
- 🔴 **자막을 «로그»에 걸지 말 것.** 반증은 우측 **「제안 기록표」** 행의 `🛡 {반증자}`에서 읽힌다.
  로그에는 `🔎 … 단서로 반증 (나만 봄)` 한 줄만 온다. **크롭 대상은 「제안 기록표」 패널.**

**S4. 컷 C (9초) — NPC 대사**
- 🔴 **먼저 「제안 기록표」 제목을 한 번 클릭해 접는다** → 로그가 6줄로 살아난다. AI 대사 패널까지 접으면 9줄.
- 녹화 켠 채 [턴 종료] 3~4회. 말투 대비가 큰 2인 연속 구간의 타임코드를 메모.
- ⚠️ **칩에 `⚙폴백`만 오르면 LLM 자막 금지.** 칩 문안은 `✨LLM n · ♻캐시 n · ⚙폴백 n` **3항목**이다.

**S5. 컷 E (9초) — 뷰 4연속**
- **내 턴에** 좌하단 드롭다운 → 뷰1→2→3→4를 2초 간격. 전환 300~650ms, 로딩 화면 없음.
- ⚠️ 뷰2·3에서 **「별당」 라벨이 우측 패널을 침범**한다. 그 프레임이 잡히면 카메라를 왼쪽으로 팬해 다시.
- ⚠️ 뷰2·3은 카메라가 크게 당겨진다 → **자막 "말 위치는 그대로"는 쓰지 않는다.**

**S6. 컷 F (9초) — 결말**
- 솔로 1판은 실측 **~90초 · NPC 15제안**이면 끝난다(사람이 [턴 종료]만 눌렀을 때). 반복은 [다시 하기].
- 사람이 이기면 오버레이는 **`🎉 사건 해결!`** + `정답 봉투 — …` + [다시 하기]/[메인으로].
  🔴 **자막을 «승리!»로 쓰면 화면과 어긋난다.**
- 못 뽑으면 대체 컷 ①(`🔍 사건 종결` — NPC가 먼저 맞힘)이 **실측으로 자주 나온다.**

**S7. 마감** — 원본 전량 보관 · 타임코드 정리 · **자막 문안을 §0′.2로 교정한 뒤** 편집.

## 5. 재촬영 트리거

| 클립 | 하나라도 해당하면 재촬영 |
|---|---|
| 공통 | 창 크기 불일치 / 알림·툴팁 잔상 / 프레임 드랍으로 타자기 끊김 / 터미널·에디터가 프레임에 |
| A·A' | 주사위 부족으로 방 미도달 / 주사위 오버레이가 확정 전 잘림 / 공개방 목록이 `불러오는 중…`에서 멈춤 |
| B·B' | 미반증이라 반증 배지 없음 / **조사 오류(`대감가`, `(으)로`)가 프레임에** / 모달에 장소 행이 없는데 자막이 "장소" 언급 |
| C·C' | 말투 대비가 안 읽힘 / 말풍선이 HUD에 가림 / **칩에 `⚙폴백`만 증가**(→ LLM 자막 사용 불가) |
| D·D' | `peek`(1장)만 떠서 변화가 1칸 / 엿본 카드가 이미 체크됨 / 귓속말 말풍선 미표시 |
| E·E' | 봇 턴 카메라 팬과 겹쳐 흔들림 / **뷰3에 `PLACEHOLDER` 보임** / 뷰4 첫 프레임이 빈 화면 |
| F | 고발 모달에서 장소 오선택으로 탈락 / 결과 오버레이가 정답 문장을 가림 |

## 6. 편집 · 업로드

- **자막**: 하단 중앙, 흰 글자 + 반투명 검정 띠, 1줄 20자 이내·최대 2줄, 클립당 1~3장, 노출 2초 이상.
- **컷 전환**: **하드컷만**(디졸브·와이프 금지 — "편집으로 꾸몄다"는 인상 최소화).
- **길이**: 최종 55초 ±3초. 60초 초과 시 E를 6초로 축소.
- **YouTube 메타**
  - 제목 `호랑이 생신 잔치 (zodiac-clue) — 실제 플레이 55초`
  - 공개 범위 **공개**(미등록은 심사자 접근 사고 위험)
  - 설명란 필수 3줄: ① 플레이 링크 ② 소스 링크 ③ **"실제 플레이 화면을 컷 편집한 영상입니다. 자막은 후처리 텍스트이며, 게임 화면·판정은 어떤 방식으로도 조작하지 않았습니다."**
- **제출 반영**: 업로드 후 `submission-checklist.md` ②와 `game-intro.md`의 플레이 영상 줄을 같은 커밋에서 갱신.

## 7. 폴백 결정 트리 (마감 역산)

- **08-01까지 선행 개선 미완** → 3절 안전 테이크로 촬영·업로드(요건 확보). 그 전에 조사 헬퍼 + PLACEHOLDER 배지 제거(합계 40분)만 처리.
- **08-08까지 LLM 미동작** → 본 테이크에서도 클립 C의 LLM 자막 제거. ④ 문서도 같은 날 문구 동기화(영상과 문서 주장이 어긋나지 않게).
- **08-08까지 사람 승리 컷 미확보** → 2.F 대체 컷 1순위로 확정. 길이가 부족하면 A를 12초로 연장.
- **어느 경우에도 하지 않는 것**: 마감 압박을 이유로 주사위·정답·봇 판정에 손대는 것. **요건 위반은 재촬영으로 되돌릴 수 없는 손실이다.**

## 변경 이력
- **2026-07-29**: **전 컷을 실제 빌드로 밟아 §0′ 신설 + §4′ 촬영 당일 순서 신설.**
  콘티가 화면과 어긋나는 곳 **13건**, 촬영을 막을 수 있는 것 **2건**(Gemini 일일 쿼터 429 소진 ·
  판 중반 로그 패널 72px 붕괴)을 실측했다. **컷 순서가 바뀐다** — 계략(D)을 제일 먼저 찍어야 한다.
  못 잰 것 2건(솔로에서 사람이 이기는 결말 · 말투 대비 2인 연속)도 §0′.4에 통과로 세지 않고 남긴다.
- 2026-07-27: 최초 작성(컷 편집 구성, 안전 테이크 + 본 테이크 이원화).
